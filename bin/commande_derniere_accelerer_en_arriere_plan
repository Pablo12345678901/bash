#!/usr/bin/env bash

# Syntaxe :
# - : pas d'argument à fournir
# Relance la dernière commande avec priorité max : nice -20

source fonctions_personnelles

DERNIERE_COMMANDE="$(commande_derniere_obtenir)"

# Retransmission sans modification de la commande à l'autre script (déjà entre guillemets simples)
commande_accelerer_en_arriere_plan "$DERNIERE_COMMANDE" -inc=-20