Bash :
- Liste des aliases, builtins, commands (=files), functions et keywords et informations.
- Les sections NAME et DESCRIPTION sont affichées pour chaque commande si celles-ci dépassent 3 mots.

"!" : 
	La commande "!" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help !", "info !" ou "!" pour essayer d'obtenir davantage d'informations.

"." : 
	DESCRIPTION :
		Exécute des commandes depuis un fichier dans le
		shell actuel. Lit et exécute des commandes depuis
		NOMFICHIER dans le shell actuel.  Les éléments dans
		$PATH sont utilisés pour trouver le répertoire
		contenant NOMFICHIER. Si des ARGUMENTS sont fournis,
		ils deviennent les paramètres de position lorsque
		NOMFICHIER est exécuté.

"2to3" : 
	La commande "2to3" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help 2to3", "info 2to3" ou "2to3" pour essayer d'obtenir davantage d'informations.

"2to3-3.11" : 
	La commande "2to3-3.11" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help 2to3-3.11", "info 2to3-3.11" ou "2to3-3.11" pour essayer d'obtenir davantage d'informations.

":" : 
	DESCRIPTION :
		Commande vide. Sans effet : la commande ne fait
		rien.

"aa" : 
	NAME :
		aa - Manipulate Apple Archives
	DESCRIPTION :
		aa creates and manipulates Apple Archives

"ab" : 
	NAME :
		ab - Apache HTTP server benchmarking tool

"ac" : 
	NAME :
		ac – connect time accounting
	DESCRIPTION :
		By default, a record of individual login and logout
		times are recorded by login(1) and init(8),
		respectively to syslogd(8).  The ac utility examines
		these records and writes the accumulated connect
		time (in hours) for all logins to the standard
		output.

"accton" : 
	NAME :
		accton – enable/disable system accounting
	DESCRIPTION :
		The accton utility is used for switching system
		accounting on or off.  If called with the argument
		acctfile, system accounting is enabled.  The acctfile
		specified must exist prior to starting system
		accounting, or accton will return an error.  A
		record of every process that is started by the
		execve(2) system call and then later exits the
		system is stored in acctfile.  The sa(8) command
		may be used to examine the accounting records.  If
		no arguments are given, system accounting is disabled.

"actool" : 
	La commande "actool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help actool", "info actool" ou "actool" pour essayer d'obtenir davantage d'informations.

"aea" : 
	NAME :
		aea - Manipulate Apple Encrypted Archives
	DESCRIPTION :
		aea creates and manipulates Apple Encrypted Archives
		(AEA)

"afclip" : 
	La commande "afclip" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help afclip", "info afclip" ou "afclip" pour essayer d'obtenir davantage d'informations.

"afconvert" : 
	NAME :
		afconvert – Audio File Convert
	DESCRIPTION :
		Audio File Convert will convert a source audio file
		to a new audio file with the specified file and
		data types

"afhash" : 
	NAME :
		afhash – Audio File Hash
	DESCRIPTION :
		Audio File Hash writes an SHA-1 hash to an audio
		file or prints (to stdout) the hash contained in
		an audio file

"afida" : 
	NAME :
		afida – Audio File Image Distortion Analyzer
	DESCRIPTION :
		afida compares a reference audio file with a distorted
		version and estimates the perceivable spatial image
		distortions in terms of image shift and width.

"afinfo" : 
	NAME :
		afinfo – Audio File Info
	DESCRIPTION :
		Audio File Info prints out information about an
		audio file to stdout

"afktool" : 
	NAME :
		afktool – AppleFirmwareKit debug utility
	DESCRIPTION :
		See afktool [help] for more information.

"afplay" : 
	NAME :
		afplay – Audio File Play
	DESCRIPTION :
		Audio File Play plays an audio file to the default
		audio output

"afscexpand" : 
	NAME :
		afscexpand – decompress files compressed with HFS+
		compression
	DESCRIPTION :
		The afscexpand command is used to decompress files
		compressed with HFS+ compression.

"agentxtrap" : 
	NAME :
		agentxtrap - send an AgentX NotifyPDU to an AgentX
		master agent
	DESCRIPTION :
		agentxtrap issues an AgentX NotifyPDU to a master
		agent. One or more object identifiers (OIDs) can
		be given as arguments on the command line.  A type
		and a value must accompany each object identifier.
		Each variable name is given in the format specified
		in variables(5).

"agvtool" : 
	La commande "agvtool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help agvtool", "info agvtool" ou "agvtool" pour essayer d'obtenir davantage d'informations.

"alias" : 
	DESCRIPTION :
		Définit ou affiche des alias. Sans argument, « alias
		» affiche la liste des alias dans le format
		réutilisable « alias NOM=VALEUR » sur la sortie
		standard.

"amdgpu-arch" : 
	La commande "amdgpu-arch" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help amdgpu-arch", "info amdgpu-arch" ou "amdgpu-arch" pour essayer d'obtenir davantage d'informations.

"amt" : 
	NAME :
		amt – Abstract Machine Test Utility
	DESCRIPTION :
		The amt utility is used to verify that the low level
		functions necessary to enforce requirements of the
		Controlled Access Protection Profile (CAPP) are
		working correctly.

"analyze-build" : 
	La commande "analyze-build" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help analyze-build", "info analyze-build" ou "analyze-build" pour essayer d'obtenir davantage d'informations.

"apachectl" : 
	NAME :
		apachectl - Apache HTTP Server Control Interface

"apfs_hfs_convert" : 
	NAME :
		apfs_hfs_convert – convert an existing HFS file
		system to APFS file system
	DESCRIPTION :
		The apfs_hfs_convert command converts an existing
		HFS file system to a new APFS file system.

"apfs_unlockfv" : 
	NAME :
		apfs_unlockfv – unlock an APFS FileVault data volume
	DESCRIPTION :
		The apfs_unlockfv utility unlocks an APFS-formatted
		FileVault-encrypted data volume.

"app-sso" : 
	NAME :
		app-sso – A tool used to control and get information
		about the Kerberos SSO extension.  -f, --force
	DESCRIPTION :
		app-sso is used to control and get information about
		the Kerberos Single Sign-on (SSO) extension via the
		command line. The Kerberos SSO extension simplifies
		using Kerberos authentication with an Active Directory
		based Kerberos realm. It also allows the user to
		use Active Directory specific functions such as
		password changes and password expiration notifications.

"appleh13camerad" : 
	La commande "appleh13camerad" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help appleh13camerad", "info appleh13camerad" ou "appleh13camerad" pour essayer d'obtenir davantage d'informations.

"applesingle" : 
	NAME :
		applesingle, binhex, macbinary – encode and decode
		files
	DESCRIPTION :
		applesingle, binhex, macbinary are implemented as
		a single tool with multiple names.  All invocations
		support the three verbs encode, decode, and probe.

"appletviewer" : 
	La commande "appletviewer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help appletviewer", "info appletviewer" ou "appletviewer" pour essayer d'obtenir davantage d'informations.

"apply" : 
	NAME :
		apply – apply a command to a set of arguments
	DESCRIPTION :
		The apply utility runs the named command on each
		argument argument in turn.

"appsleepd" : 
	NAME :
		appsleepd – app sleep daemon
	DESCRIPTION :
		appsleepd provides app sleep services.

"app_fermer" : 
	La commande "app_fermer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help app_fermer", "info app_fermer" ou "app_fermer" pour essayer d'obtenir davantage d'informations.

"app_fermer_brutal" : 
	La commande "app_fermer_brutal" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help app_fermer_brutal", "info app_fermer_brutal" ou "app_fermer_brutal" pour essayer d'obtenir davantage d'informations.

"app_fermer_normal" : 
	La commande "app_fermer_normal" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help app_fermer_normal", "info app_fermer_normal" ou "app_fermer_normal" pour essayer d'obtenir davantage d'informations.

"app_ouvrir" : 
	La commande "app_ouvrir" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help app_ouvrir", "info app_ouvrir" ou "app_ouvrir" pour essayer d'obtenir davantage d'informations.

"app_relancer" : 
	La commande "app_relancer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help app_relancer", "info app_relancer" ou "app_relancer" pour essayer d'obtenir davantage d'informations.

"apropos" : 
	NAME :
		man, apropos, whatis – display online manual
		documentation pages
	DESCRIPTION :
		The man utility finds and displays online manual
		documentation pages.  If mansect is provided, man
		restricts the search to the specific section of the
		manual.

"apt" : 
	La commande "apt" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help apt", "info apt" ou "apt" pour essayer d'obtenir davantage d'informations.

"ar" : 
	NAME :
		ar – create and maintain library archives
	DESCRIPTION :
		The ar utility creates and maintains groups of files
		combined into an archive.  Once an archive has been
		created, new files can be added and existing files
		can be extracted, deleted, or replaced.

"arch" : 
	NAME :
		arch – print architecture type or run selected
		architecture of a universal binary
	DESCRIPTION :
		The arch command with no arguments, displays the
		machine's architecture type.

"arp" : 
	NAME :
		arp – address resolution display and control
	DESCRIPTION :
		The arp utility displays and modifies the
		Internet-to-Ethernet address translation tables
		used by the address resolution protocol (arp(4)).
		With no flags, the program displays the current ARP
		entry for hostname. The host may be specified by
		name or by number, using Internet dot notation.

"as" : 
	NAME :
		as - Mac OS X Mach-O GNU-based assemblers
	DESCRIPTION :
		The as command translates assembly code in the named
		files to object code.  If no files are specified,
		as reads from stdin.  All undefined symbols in the
		assembly are treated as global.  The output of the
		assembly is left in the file a.out by default.

"asa" : 
	NAME :
		asa – interpret carriage-control characters.
	DESCRIPTION :
		The asa utility reads files sequentially, mapping
		FORTRAN carriage- control characters to line-printer
		control sequences, and writes them to the standard
		output.

"aslmanager" : 
	NAME :
		aslmanager – Apple System Log data life-cycle manager
	DESCRIPTION :
		aslmanager manages rotated files and ASL data written
		by the syslogd server.  It is started automatically
		at various times by syslogd.  It may also be invoked
		from the command line by the superuser (root).  It
		manages the life-cycle of data in the ASL database,
		rotated log files, and ASL directory data stores.
		Configuration information for aslmanager comes from
		the /etc/asl.conf configuration file, any ASL output
		module configuration files in the /etc/asl directory,
		and from command-line options that may be specified
		to override some settings found in the configuration
		files.  In normal operation, it first checks the
		ASL database in /var/log/asl, then it checks the
		files and directories

"asr" : 
	NAME :
		asr – Apple Software Restore; copy volumes (e.g.
		from disk images)
	DESCRIPTION :
		asr efficiently copies disk images onto volumes,
		either directly or via a multicast network stream.
		asr can also accurately clone volumes without the
		use of an intermediate disk image.

"AssetCacheLocatorUtil" : 
	NAME :
		AssetCacheLocatorUtil – Utility for reporting
		information about macOS Content Caches
	DESCRIPTION :
		AssetCacheLocatorUtil reports information related
		to macOS Content Caches running on the computer or
		on the local network.

"AssetCacheManagerUtil" : 
	NAME :
		AssetCacheManagerUtil – control the macOS Content
		Cache
	DESCRIPTION :
		AssetCacheManagerUtil controls the macOS Content
		Cache. AssetCacheManagerUtil must be run by root
		except as noted below.  The options are:

"AssetCacheTetheratorUtil" : 
	NAME :
		AssetCacheTetheratorUtil – control networking of
		tethered devices
	DESCRIPTION :
		iOS and tvOS devices connected to a computer with
		a USB cable can be "tethered," so that they route
		their Internet requests through the computer.
		AssetCacheTetheratorUtil enables a tethered network,
		disables it, or reports on its status.  Tethering
		requires Content Caching. AssetCacheTetheratorUtil
		must be run by root, except for the isEnabled and
		status commands.

"assetutil" : 
	NAME :
		assetutil process asset catalog .car files
	DESCRIPTION :
		assetutil processes a .car file generated from a
		image catalog removing requested scale factors,
		device idioms, subtypes, performance and memory
		classes. When thinning scale, idiom, subtype,
		performance, memory, and graphicsclassfallbacks can
		be given multiple times, the resulting file will
		contain all of the assets that match all of the
		parameters given. If scale, idiom, subtype and
		graphics class are given in one set, the same
		parameters must all be present in the subsequent
		set of parmeters. (IE the count must match)

"at" : 
	NAME :
		at, batch, atq, atrm – queue, examine, or delete
		jobs for later execution
	DESCRIPTION :
		The at and batch utilities read commands from
		standard input or a specified file.  The commands
		are executed at a later time, using sh(1).

"atos" : 
	NAME :
		atos – convert numeric addresses to symbols of
		binary images or processes
	DESCRIPTION :
		The atos command converts numeric addresses to their
		symbolic equivalents.  If full debug symbol information
		is available, for example in a .app.dSYM sitting
		beside a .app, then the output of atos will include
		file name and source line number information.

"atq" : 
	NAME :
		at, batch, atq, atrm – queue, examine, or delete
		jobs for later execution
	DESCRIPTION :
		The at and batch utilities read commands from
		standard input or a specified file.  The commands
		are executed at a later time, using sh(1).

"atrm" : 
	NAME :
		at, batch, atq, atrm – queue, examine, or delete
		jobs for later execution
	DESCRIPTION :
		The at and batch utilities read commands from
		standard input or a specified file.  The commands
		are executed at a later time, using sh(1).

"atsutil" : 
	NAME :
		atsutil – font registration system utility
	DESCRIPTION :
		atsutil tool controls some aspects of the font
		registration system.  It may be used to modify font
		auto activation settings, font protection settings,
		remove font registration databases and caches, or
		restart the font registration daemon.

"audit" : 
	NAME :
		audit – audit management utility
	DESCRIPTION :
		The audit utility controls the state of the audit
		system.  One of the following flags is required as
		an argument to audit:

"auditd" : 
	NAME :
		auditd – audit log management daemon
	DESCRIPTION :
		The auditd daemon responds to requests from the
		audit(8) utility and notifications from the kernel.
		It manages the resulting audit log files and specified
		log file locations.

"auditreduce" : 
	NAME :
		auditreduce – select records from audit trail files
	DESCRIPTION :
		The auditreduce utility selects records from the
		audit trail files based on the specified criteria.
		Matching audit records are printed to the standard
		output in their raw binary form.  If no file argument
		is specified, the standard input is used by default.
		Use the praudit(1) utility to print the selected
		audit records in human-readable form.

"autoconf" : 
	NAME :
		autoconf - Generate configuration scripts
	DESCRIPTION :
		Generate a configuration script from a TEMPLATE-FILE
		if given, or 'configure.ac' if present, or else
		'configure.in'.  Output is sent to the standard
		output if TEMPLATE-FILE is given, else into
		'configure'.

"autoheader" : 
	NAME :
		autoheader - Create a template header for configure
	DESCRIPTION :
		Create a template file of C '#define' statements
		for 'configure' to use.  To this end, scan
		TEMPLATE-FILE, or 'configure.ac' if present, or
		else 'configure.in'.

"autom4te" : 
	NAME :
		autom4te - Generate files and scripts thanks to M4
	DESCRIPTION :
		Run GNU M4 on the FILES, avoiding useless runs.
		Output the traces if tracing, the frozen file if
		freezing, otherwise the expansion of the FILES.

"automationmodetool" : 
	NAME :
		automationmodetool – Manage UI automation security
		preferences.
	DESCRIPTION :
		automationmodetool can be used to configure a device
		such that Automation Mode for UI testing can be
		enabled without user authentication.  This is useful
		for configuring machines in continuous integration
		(CI) environments as well as those in labs where
		the active users do not have administrator privileges.
		Running the tool for this purpose requires an
		administrator to authenticate in the shell.  If no
		argument is passed, the tool prints the current
		status of Automation Mode (enabled or disabled),
		the authentication configuration (required or not),
		and then exits.

"automator" : 
	NAME :
		automator – Runs Automator workflow.
	DESCRIPTION :
		automator runs the specified workflow.  To create
		or edit a workflow, use the Automator application.

"automount" : 
	NAME :
		automount – mount autofs on the appropriate mount
		points
	DESCRIPTION :
		automount reads the /etc/auto_master file, and any
		local or network maps it includes, and mounts autofs
		on the appropriate mount points to cause mounts to
		be triggered.  It will also attempt to unmount any
		top-level autofs mounts that correspond to maps no
		longer found.

"autopoint" : 
	NAME :
		autopoint - copies standard gettext infrastructure
	DESCRIPTION :
		Copies standard gettext infrastructure files into
		a source package.

"autoreconf" : 
	NAME :
		autoreconf - Update generated configuration files
	DESCRIPTION :
		Run 'autoconf' and, when needed, 'aclocal',
		'autoheader', 'automake', 'autopoint' (formerly
		'gettextize'), 'glibtoolize', 'intltoolize', and
		'gtkdocize' to regenerate the GNU Build System files
		in specified DIRECTORIES and their subdirectories
		(defaulting to '.').

"autoscan" : 
	NAME :
		autoscan - Generate a preliminary configure.ac
	DESCRIPTION :
		Examine source files in the directory tree rooted
		at SRCDIR, or the current directory if none is
		given.  Search the source files for common portability
		problems, check for incompleteness of 'configure.ac',
		and create a file 'configure.scan' which is a
		preliminary 'configure.ac' for that package.

"autoupdate" : 
	NAME :
		autoupdate - Update a configure.ac to a newer
		Autoconf
	DESCRIPTION :
		Update each TEMPLATE-FILE if given, or 'configure.ac'
		if present, or else 'configure.in', to the syntax
		of the current version of Autoconf. The original
		files are backed up.

"auval" : 
	NAME :
		auval – AudioUnit validation
	DESCRIPTION :
		AUValidation tests a specified AudioUnit for API
		and behavioural conformance.  returns: OK: 0,
		malformed execution: 1, unit not conformant: -1

"auvaltool" : 
	NAME :
		auval – AudioUnit validation
	DESCRIPTION :
		AUValidation tests a specified AudioUnit for API
		and behavioural conformance.  returns: OK: 0,
		malformed execution: 1, unit not conformant: -1

"avbdeviced" : 
	NAME :
		avbdeviced – peristent entity management for AVB.
	DESCRIPTION :
		The avbdeviced executable is used for the managment
		of persistent AVB entities which are advertised by
		the Mac.

"avbdiagnose" : 
	NAME :
		avbdiagnose – diagnostic tool for AVB.
	DESCRIPTION :
		The avbdiagnose tool is used to capture a snapshot
		of the current AVB system state and help diagnose
		common issues with AVB.

"avbutil" : 
	NAME :
		avbutil – manage AVB features and settings.
	DESCRIPTION :
		The avbutil executable is used for the managment
		AVB features and settings.

"avconvert" : 
	NAME :
		avconvert – movie conversion tool
	DESCRIPTION :
		avconvert is a tool that converts source media files
		to different file types for sharing on the web or
		loading onto devices.  The tool will not allow
		protected content to be converted.  Only one video
		and one audio track is preserved through the
		conversion, along with metadata tracks. The tool
		will never resize the video higher than the source
		dimensions. If the preset internal dimensions are
		larger than that of the source, the conversion will
		maintain the source dimensions.  The file extension
		provided for the output movie will determine the
		output file type.

"avmediainfo" : 
	NAME :
		avmediainfo – media analysis tool
	DESCRIPTION :
		avmediainfo is a tool that can be used to parse and
		analyze media files. It is capable of displaying
		generic information about the asset related to its
		tracks, metadata, format extensions, chunks, and
		samples. It also warns the user of any errors
		encountered while parsing the media file. <media_file>
		The media file to be analysed. <options> Use the
		options in order to display specific information
		regarding the asset.

"avmetareadwrite" : 
	La commande "avmetareadwrite" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help avmetareadwrite", "info avmetareadwrite" ou "avmetareadwrite" pour essayer d'obtenir davantage d'informations.

"awk" : 
	NAME :
		awk - pattern-directed scanning and processing
		language
	DESCRIPTION :
		Awk scans each input file for lines that match any
		of a set of patterns specified literally in prog
		or in one or more files specified as -f progfile.
		With each pattern there can be an associated action
		that will be performed when a line of a file matches
		the pattern.  Each line is matched against the
		pattern portion of every pattern-action statement;
		the associated action is performed for each matched
		pattern. The file name - means the standard input.
		Any file of the form var=value is treated as an
		assignment, not a filename, and is executed at the
		time it would have been opened if it were a filename.
		The option -v followed by var=value is an assignment
		to be done before prog

"banner" : 
	NAME :
		banner – print large banner on printer
	DESCRIPTION :
		Banner prints a large, high quality banner on the
		standard output.  If the message is omitted, it
		prompts for and reads one line of its standard
		input.

"base64" : 
	NAME :
		base64 – Encode and decode using Base64 representation
	DESCRIPTION :
		base64 encodes and decodes Base64 data, as specified
		in RFC 4648.  With no options, base64 reads raw
		data from stdin and writes encoded data as a
		continuous block to stdout.

"basename" : 
	NAME :
		 basename, dirname – return filename or directory
		 portion of pathname
	DESCRIPTION :
		The basename utility deletes any prefix ending with
		the last slash ‘/’ character present in string
		(after first stripping trailing slashes), and a
		suffix, if given.  The suffix is not stripped if
		it is identical to the remaining characters in
		string.  The resulting filename is written to the
		standard output.  A non-existent suffix is ignored.
		If -a is specified, then every argument is treated
		as a string as if basename were invoked with just
		one argument.  If -s is specified, then the suffix
		is taken as its argument, and all other arguments
		are treated as a string.

"bash" : 
	NAME :
		bash - GNU Bourne-Again SHell
	DESCRIPTION :
		Bash is an sh-compatible command language interpreter
		that executes commands read from the standard input
		or from a file.  Bash also incorporates useful
		features from the Korn and C shells (ksh and csh).

"bashbug" : 
	NAME :
		bashbug - report a bug in bash
	DESCRIPTION :
		bashbug is a shell script to help the user compose
		and mail bug reports concerning bash in a standard
		format.  bashbug invokes the editor specified by
		the environment variable EDITOR on a temporary copy
		of the bug report format outline. The user must
		fill in the appropriate fields and exit the editor.
		bashbug then mails the completed report to bug-
		bash@gnu.org, or email-address.  If the report
		cannot be mailed, it is saved in the file dead.bashbug
		in the invoking user's home directory.

"batch" : 
	NAME :
		at, batch, atq, atrm – queue, examine, or delete
		jobs for later execution
	DESCRIPTION :
		The at and batch utilities read commands from
		standard input or a specified file.  The commands
		are executed at a later time, using sh(1).

"bc" : 
	NAME :
		bc - arbitrary-precision decimal arithmetic language
		and calculator
	DESCRIPTION :
		bc(1) is an interactive processor for a language
		first standardized in 1991 by POSIX.  (The current
		standard is here
		(https://pubs.opengroup.org/onlinepubs/9699919799/utilities/bc.html).)
		The language provides unlimited precision decimal
		arithmetic and is somewhat C-like, but there are
		differences.  Such differences will be noted in
		this document.

"bg" : 
	DESCRIPTION :
		Déplace des tâches vers l'arrière plan. Place chaque
		JOB_SPEC en arrière plan comme s'il avait été démarré
		avec « & ». Si JOB_SPEC n'est pas fourni, le shell
		utilise sa propre notion de tâche actuelle.

"bind" : 
	DESCRIPTION :
		Définit les associations de touches et les variables
		de « Readline ». Associe une séquence de touches à
		une fonction « Readline » ou une macro, ou définit
		une variable « Readline ». La syntaxe des arguments
		non-options est équivalente à celle du fichier
		~/.inputrc, mais doivent être transmis comme arguments
		uniques : ex : bind '"\C-x\C-r" : re-read-init-file'.

"binhex" : 
	NAME :
		applesingle, binhex, macbinary – encode and decode
		files
	DESCRIPTION :
		applesingle, binhex, macbinary are implemented as
		a single tool with multiple names.  All invocations
		support the three verbs encode, decode, and probe.

"binhex.pl" : 
	NAME :
		binhex.pl - use Convert::BinHex to encode files as
		BinHex
	DESCRIPTION :
		Each file is converted to file.hqx.

"binhex5.30.pl" : 
	La commande "binhex5.30.pl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help binhex5.30.pl", "info binhex5.30.pl" ou "binhex5.30.pl" pour essayer d'obtenir davantage d'informations.

"bioutil" : 
	NAME :
		bioutil – tool for viewing/changing Touch ID
		configuration and listing/deleting enrolled
		fingerprints
	DESCRIPTION :
		bioutil provides the possibility of viewing and
		changing Touch ID configuration, both system-wide
		and user-specific. It also allows listing and
		deleting enrolled fingerprints.

"bison" : 
	NAME :
		bison - GNU Project parser generator (yacc replacement)
	DESCRIPTION :
		Bison is a parser generator in the style of yacc(1).
		It should be upwardly compatible with input files
		designed for yacc.

"bitesize.d" : 
	NAME :
		bitesize.d - analyse disk I/O size by process. Uses
		DTrace.
	DESCRIPTION :
		This produces a report for the size of disk events
		caused by processes. These are the disk events sent
		by the block I/O driver.

"bless" : 
	NAME :
		bless – set volume bootability and startup disk
		options
	DESCRIPTION :
		bless is used to modify the volume bootability
		characteristics of filesystems, as well as select
		the active boot device.  bless has 6 modes of
		execution: Folder Mode, Mount Mode, Device Mode,
		NetBoot Mode, Info Mode, and Unbless Mode.

"BlueTool" : 
	La commande "BlueTool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help BlueTool", "info BlueTool" ou "BlueTool" pour essayer d'obtenir davantage d'informations.

"bluetoothd" : 
	La commande "bluetoothd" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help bluetoothd", "info bluetoothd" ou "bluetoothd" pour essayer d'obtenir davantage d'informations.

"bluetooth_connexion_souris" : 
	La commande "bluetooth_connexion_souris" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help bluetooth_connexion_souris", "info bluetooth_connexion_souris" ou "bluetooth_connexion_souris" pour essayer d'obtenir davantage d'informations.

"bluetooth_eteindre" : 
	La commande "bluetooth_eteindre" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help bluetooth_eteindre", "info bluetooth_eteindre" ou "bluetooth_eteindre" pour essayer d'obtenir davantage d'informations.

"blueutil" : 
	La commande "blueutil" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help blueutil", "info blueutil" ou "blueutil" pour essayer d'obtenir davantage d'informations.

"BootCacheControl" : 
	NAME :
		BootCacheControl – provides control of caches used
		during startup.
	DESCRIPTION :
		Users should not run BootCacheControl manually.

"bputil" : 
	NAME :
		bputil – Utility to precisely modify the security
		settings on Apple Silicon Macs.
	DESCRIPTION :
		This utility is not meant for normal users or even
		sysadmins. It provides unabstracted access to
		capabilities which are normally handled for the
		user automatically when changing the security policy
		through GUIs such as the Startup Security Utility
		in macOS Recovery (“recoveryOS”). It is possible
		to make your system security much weaker and therefore
		easier to compromise using this tool. This tool is
		not to be used in production environments. It is
		possible to render your system unbootable with this
		tool. It should only be used to understand how the
		security of Apple Silicon Macs works. Use at your
		own risk!

"brctl" : 
	NAME :
		brctl – Manage the CloudDocs daemon
	DESCRIPTION :
		brctl understands the following commands:

"break" : 
	DESCRIPTION :
		Sort des boucles for, while, ou until. Sort d'une
		boucle FOR, WHILE ou UNTIL.  Si N est spécifié,
		sort de N boucles imbriquées.

"brew" : 
	NAME :
		brew - The Missing Package Manager for macOS (or
		Linux)
	DESCRIPTION :
		Homebrew is the easiest and most flexible way to
		install the UNIX tools Apple didn´t include with
		macOS. It can also install software not packaged
		for your Linux distribution without requiring sudo.

"brew_conserver_choses_installees" : 
	La commande "brew_conserver_choses_installees" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help brew_conserver_choses_installees", "info brew_conserver_choses_installees" ou "brew_conserver_choses_installees" pour essayer d'obtenir davantage d'informations.

"brew_liste_choses_installees.txt" : 
	La commande "brew_liste_choses_installees.txt" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help brew_liste_choses_installees.txt", "info brew_liste_choses_installees.txt" ou "brew_liste_choses_installees.txt" pour essayer d'obtenir davantage d'informations.

"brew_montrer_choses_installees" : 
	La commande "brew_montrer_choses_installees" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help brew_montrer_choses_installees", "info brew_montrer_choses_installees" ou "brew_montrer_choses_installees" pour essayer d'obtenir davantage d'informations.

"brew_reinstaller_formulaes_et_casks" : 
	La commande "brew_reinstaller_formulaes_et_casks" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help brew_reinstaller_formulaes_et_casks", "info brew_reinstaller_formulaes_et_casks" ou "brew_reinstaller_formulaes_et_casks" pour essayer d'obtenir davantage d'informations.

"bsdtar" : 
	NAME :
		tar – manipulate tape archives
	DESCRIPTION :
		tar creates and manipulates streaming archive files.
		This implementation can extract from tar, pax, cpio,
		zip, jar, ar, xar, rpm, 7-zip, and ISO 9660 cdrom
		images and can create tar, pax, cpio, ar, zip,
		7-zip, and shar archives.

"bspatch" : 
	NAME :
		bspatch – apply a patch built with bsdiff(1)
	DESCRIPTION :
		The bspatch utility generates newfile from oldfile
		and patchfile where patchfile is a binary patch
		built by bsdiff(1).

"BTLEServer" : 
	DESCRIPTION :
		BTLEServer is a process that handles Bluetooth LE
		features.

"BTLEServerAgent" : 
	DESCRIPTION :
		BTLEServerAgent is a process that handles Bluetooth
		LE features.

"bugpoint" : 
	La commande "bugpoint" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help bugpoint", "info bugpoint" ou "bugpoint" pour essayer d'obtenir davantage d'informations.

"builtin" : 
	DESCRIPTION :
		Exécute des commandes shell intégrées. Exécute
		SHELL-BUILTIN avec les arguments ARGs sans effectuer
		de recherche de commande.  Ceci est utile lorsque
		vous souhaitez remplacer une commande intégrée par
		une fonction shell, mais nécessite d'exécuter la
		commande intégrée dans la fonction.

"bundle" : 
	NAME :
		bundle - Ruby Dependency Management
	DESCRIPTION :
		Bundler manages an application´s dependencies through
		its entire life across many machines systematically
		and repeatably.

"bundler" : 
	La commande "bundler" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help bundler", "info bundler" ou "bundler" pour essayer d'obtenir davantage d'informations.

"bunzip2" : 
	NAME :
		bzip2, bunzip2 - a block-sorting file compressor,
		v1.0.8 bzcat - decompresses files to stdout
		bzip2recover - recovers data from damaged bzip2
		files
	DESCRIPTION :
		bzip2 compresses files using the Burrows-Wheeler
		block sorting text compression algorithm, and Huffman
		coding.  Compression is generally considerably
		better than that achieved by more conventional
		LZ77/LZ78-based compressors, and approaches the
		performance of the PPM family of statistical
		compressors.

"bzcat" : 
	NAME :
		bzip2, bunzip2 - a block-sorting file compressor,
		v1.0.8 bzcat - decompresses files to stdout
		bzip2recover - recovers data from damaged bzip2
		files
	DESCRIPTION :
		bzip2 compresses files using the Burrows-Wheeler
		block sorting text compression algorithm, and Huffman
		coding.  Compression is generally considerably
		better than that achieved by more conventional
		LZ77/LZ78-based compressors, and approaches the
		performance of the PPM family of statistical
		compressors.

"bzcmp" : 
	NAME :
		bzcmp, bzdiff - compare bzip2 compressed files
	DESCRIPTION :
		Bzcmp and bzdiff are used to invoke the cmp or the
		diff program on bzip2 compressed files.  All options
		specified are passed directly to cmp or diff.  If
		only 1 file is specified, then the files compared
		are file1 and an uncompressed file1.bz2.  If two
		files are specified, then they are uncompressed if
		necessary and fed to cmp or diff.  The exit status
		from cmp or diff is preserved.

"bzdiff" : 
	NAME :
		bzcmp, bzdiff - compare bzip2 compressed files
	DESCRIPTION :
		Bzcmp and bzdiff are used to invoke the cmp or the
		diff program on bzip2 compressed files.  All options
		specified are passed directly to cmp or diff.  If
		only 1 file is specified, then the files compared
		are file1 and an uncompressed file1.bz2.  If two
		files are specified, then they are uncompressed if
		necessary and fed to cmp or diff.  The exit status
		from cmp or diff is preserved.

"bzegrep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"bzfgrep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"bzgrep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"bzip2" : 
	NAME :
		bzip2, bunzip2 - a block-sorting file compressor,
		v1.0.8 bzcat - decompresses files to stdout
		bzip2recover - recovers data from damaged bzip2
		files
	DESCRIPTION :
		bzip2 compresses files using the Burrows-Wheeler
		block sorting text compression algorithm, and Huffman
		coding.  Compression is generally considerably
		better than that achieved by more conventional
		LZ77/LZ78-based compressors, and approaches the
		performance of the PPM family of statistical
		compressors.

"bzip2recover" : 
	NAME :
		bzip2, bunzip2 - a block-sorting file compressor,
		v1.0.8 bzcat - decompresses files to stdout
		bzip2recover - recovers data from damaged bzip2
		files
	DESCRIPTION :
		bzip2 compresses files using the Burrows-Wheeler
		block sorting text compression algorithm, and Huffman
		coding.  Compression is generally considerably
		better than that achieved by more conventional
		LZ77/LZ78-based compressors, and approaches the
		performance of the PPM family of statistical
		compressors.

"bzless" : 
	NAME :
		bzmore, bzless - file perusal filter for crt viewing
		of bzip2 compressed text
	DESCRIPTION :
		Bzmore is a filter which allows examination of
		compressed or plain text files one screenful at a
		time on a soft-copy terminal.  bzmore works on files
		compressed with bzip2 and also on uncompressed
		files.  If a file does not exist, bzmore looks for
		a file of the same name with the addition of a .bz2
		suffix.

"bzmore" : 
	NAME :
		bzmore, bzless - file perusal filter for crt viewing
		of bzip2 compressed text
	DESCRIPTION :
		Bzmore is a filter which allows examination of
		compressed or plain text files one screenful at a
		time on a soft-copy terminal.  bzmore works on files
		compressed with bzip2 and also on uncompressed
		files.  If a file does not exist, bzmore looks for
		a file of the same name with the addition of a .bz2
		suffix.

"c++" : 
	NAME :
		clang - the Clang C, C++, and Objective-C compiler
	DESCRIPTION :
		clang is a C, C++, and Objective-C compiler which
		encompasses preprocessing, parsing, optimization,
		code generation, assembly, and linking.  Depending
		on which high-level mode setting is passed, Clang
		will stop before doing a full link.  While Clang
		is highly integrated, it is important to understand
		the stages of compilation, to understand how to
		invoke it.  These stages are:

"c++filt" : 
	NAME :
		llvm-cxxfilt - LLVM symbol name demangler
	DESCRIPTION :
		llvm-cxxfilt is a symbol demangler that can be used
		as a replacement for the GNU c++filt tool. It takes
		a series of symbol names and prints their demangled
		form on the standard output stream. If a name cannot
		be demangled, it is simply printed as is.

"c-index-test" : 
	La commande "c-index-test" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help c-index-test", "info c-index-test" ou "c-index-test" pour essayer d'obtenir davantage d'informations.

"c89" : 
	NAME :
		c89 – standard C language compiler
	DESCRIPTION :
		This is the name of the C language compiler as
		required by the IEEE Std 1003.1-2001 (“POSIX.1”)
		standard.

"c99" : 
	NAME :
		c99 – standard C language compiler
	DESCRIPTION :
		This is the name of the C language compiler as
		required by the IEEE Std 1003.1-2001 (“POSIX.1”)
		standard.

"caffeinate" : 
	NAME :
		caffeinate – prevent the system from sleeping on
		behalf of a utility
	DESCRIPTION :
		caffeinate creates assertions to alter system sleep
		behavior.  If no assertion flags are specified,
		caffeinate creates an assertion to prevent idle
		sleep.  If a utility is specified, caffeinate creates
		the assertions on the utility's behalf, and those
		assertions will persist for the duration of the
		utility's execution. Otherwise, caffeinate creates
		the assertions directly, and those assertions will
		persist until caffeinate exits.

"cairo-sphinx" : 
	La commande "cairo-sphinx" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help cairo-sphinx", "info cairo-sphinx" ou "cairo-sphinx" pour essayer d'obtenir davantage d'informations.

"cairo-trace" : 
	La commande "cairo-trace" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help cairo-trace", "info cairo-trace" ou "cairo-trace" pour essayer d'obtenir davantage d'informations.

"cal" : 
	NAME :
		cal, ncal – displays a calendar and the date of
		Easter
	DESCRIPTION :
		The cal utility displays a simple calendar in
		traditional format and ncal offers an alternative
		layout, more options and the date of Easter.  The
		new format is a little cramped but it makes a year
		fit on a 25x80 terminal.  If arguments are not
		specified, the current month is displayed.

"calculatrice_appel_script" : 
	La commande "calculatrice_appel_script" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help calculatrice_appel_script", "info calculatrice_appel_script" ou "calculatrice_appel_script" pour essayer d'obtenir davantage d'informations.

"calculatrice_avec_ou_sans_affichage" : 
	La commande "calculatrice_avec_ou_sans_affichage" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help calculatrice_avec_ou_sans_affichage", "info calculatrice_avec_ou_sans_affichage" ou "calculatrice_avec_ou_sans_affichage" pour essayer d'obtenir davantage d'informations.

"calendar" : 
	NAME :
		calendar – reminder service
	DESCRIPTION :
		The calendar utility checks the current directory
		for a file named calendar and displays lines that
		begin with either today's date or tomorrow's.  On
		the day before a weekend (normally Friday), events
		for the next three days are displayed.

"caller" : 
	DESCRIPTION :
		Renvoie le contexte de l'appel de sous-routine
		actuel. Sans EXPR, renvoie « $ligne $nomfichier ».
		Avec EXPR, renvoie « $ligne $sousroutine $nomfichier
		»; ces informations supplémentaires peuvent être
		utilisées pour fournir une trace de la pile.

"cancel" : 
	NAME :
		cancel - cancel jobs
	DESCRIPTION :
		The cancel command cancels print jobs.  If no
		destination or id is specified, the currently
		printing job on the default destination is canceled.

"captoinfo" : 
	NAME :
		captoinfo - convert a termcap description into a
		terminfo description
	DESCRIPTION :
		captoinfo looks in file for termcap descriptions.
		For each one found, an equivalent terminfo description
		is written to standard output. Termcap tc capabilities
		are translated directly to terminfo use capabilities.

"cap_mkdb" : 
	NAME :
		cap_mkdb – create capability database
	DESCRIPTION :
		Cap_mkdb builds a hashed database out of the getcap(3)
		logical database constructed by the concatenation
		of the specified files .

"case" : 
	La commande "case" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help case", "info case" ou "case" pour essayer d'obtenir davantage d'informations.

"cat" : 
	NAME :
		cat – concatenate and print files
	DESCRIPTION :
		The cat utility reads files sequentially, writing
		them to the standard output.  The file operands are
		processed in command-line order.  If file is a
		single dash (‘-’) or absent, cat reads from the
		standard input.  If file is a UNIX domain socket,
		cat connects to it and then reads it until EOF.
		This complements the UNIX domain binding capability
		available in inetd(8).

"cc" : 
	NAME :
		clang - the Clang C, C++, and Objective-C compiler
	DESCRIPTION :
		clang is a C, C++, and Objective-C compiler which
		encompasses preprocessing, parsing, optimization,
		code generation, assembly, and linking.  Depending
		on which high-level mode setting is passed, Clang
		will stop before doing a full link.  While Clang
		is highly integrated, it is important to understand
		the stages of compilation, to understand how to
		invoke it.  These stages are:

"cd" : 
	DESCRIPTION :
		Change the shell working directory. Change the
		current directory to DIR.  The default DIR is the
		value of the HOME shell variable. If DIR is "-",
		it is converted to $OLDPWD.

"certtool" : 
	NAME :
		certtool - create key pairs, certificates and
		certificate signing requests for use with Keychains
	DESCRIPTION :
		Certtool is a UNIX command-line program which is
		used to create key pairs, certificates, and certificate
		signing requests; to import externally generated
		certificates and Certificate Revocation Lists (CRLs)
		into a Keychain, and to display the contents of
		certificates and CRLs.

"cfprefsd" : 
	NAME :
		cfprefsd – defaults server
	DESCRIPTION :
		cfprefsd provides preferences services for the
		CFPreferences and NSUserDefaults APIs.

"chat" : 
	NAME :
		chat - Automated conversational script with a modem
	DESCRIPTION :
		The chat program defines a conversational exchange
		between the computer and the modem. Its primary
		purpose is to establish the connection between the
		Point-to-Point Protocol Daemon (pppd) and the
		remote's pppd process.

"checkgid" : 
	NAME :
		checkgid – validate group identifiers
	DESCRIPTION :
		Given one or more group identifers on the command
		line (e.g., "httpd" or "#-1"), figure out whether
		they'll be valid for the server to use at run- time.

"checknr" : 
	NAME :
		checknr – check nroff/troff files
	DESCRIPTION :
		The checknr utility checks a list of nroff(1) or
		troff(1) input files for certain kinds of errors
		involving mismatched opening and closing delimiters
		and unknown commands.  If no files are specified,
		checknr checks the standard input.

"chflags" : 
	NAME :
		chflags – change file flags
	DESCRIPTION :
		The chflags utility modifies the file flags of the
		listed files as specified by the flags operand.

"chfn" : 
	NAME :
		chpass, chfn, chsh – add or change user database
		information
	DESCRIPTION :
		The chpass utility allows editing of the user
		database information associated with user or, by
		default, the current user.

"chgrp" : 
	NAME :
		chgrp – change group
	DESCRIPTION :
		The chgrp utility sets the group ID of the file
		named by each file operand to the group ID specified
		by the group operand.

"chmod" : 
	NAME :
		chmod – change file modes or Access Control Lists
	DESCRIPTION :
		The chmod utility modifies the file mode bits of
		the listed files as specified by the mode operand.
		It may also be used to modify the Access Control
		Lists (ACLs) associated with the listed files.

"chown" : 
	NAME :
		chown – change file owner and group
	DESCRIPTION :
		The chown utility changes the user ID and/or the
		group ID of the specified files.  Symbolic links
		named by arguments are silently left unchanged
		unless -h is used.

"chpass" : 
	NAME :
		chpass, chfn, chsh – add or change user database
		information
	DESCRIPTION :
		The chpass utility allows editing of the user
		database information associated with user or, by
		default, the current user.

"chroot" : 
	NAME :
		chroot – change root directory
	DESCRIPTION :
		The chroot utility changes its current and root
		directories to the supplied directory newroot and
		then exec's command with provided arguments, if
		supplied, or an interactive copy of the user's login
		shell.

"chsh" : 
	NAME :
		chpass, chfn, chsh – add or change user database
		information
	DESCRIPTION :
		The chpass utility allows editing of the user
		database information associated with user or, by
		default, the current user.

"cjpeg" : 
	NAME :
		cjpeg - compress an image file to a JPEG file
	DESCRIPTION :
		cjpeg compresses the named image file, or the
		standard input if no file is named, and produces a
		JPEG/JFIF file on the standard output.  The currently
		supported input file formats are: PPM (PBMPLUS color
		format), PGM (PBMPLUS grayscale format), BMP, GIF,
		and Targa.

"ckksctl" : 
	La commande "ckksctl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ckksctl", "info ckksctl" ou "ckksctl" pour essayer d'obtenir davantage d'informations.

"cksum" : 
	NAME :
		cksum, sum – display file checksums and block counts
	DESCRIPTION :
		The cksum utility writes to the standard output
		three whitespace separated fields for each input
		file.  These fields are a checksum CRC, the total
		number of octets in the file and the file name.  If
		no file name is specified, the standard input is
		used and no file name is written.

"clang" : 
	NAME :
		clang - the Clang C, C++, and Objective-C compiler
	DESCRIPTION :
		clang is a C, C++, and Objective-C compiler which
		encompasses preprocessing, parsing, optimization,
		code generation, assembly, and linking.  Depending
		on which high-level mode setting is passed, Clang
		will stop before doing a full link.  While Clang
		is highly integrated, it is important to understand
		the stages of compilation, to understand how to
		invoke it.  These stages are:

"clang++" : 
	NAME :
		clang - the Clang C, C++, and Objective-C compiler
	DESCRIPTION :
		clang is a C, C++, and Objective-C compiler which
		encompasses preprocessing, parsing, optimization,
		code generation, assembly, and linking.  Depending
		on which high-level mode setting is passed, Clang
		will stop before doing a full link.  While Clang
		is highly integrated, it is important to understand
		the stages of compilation, to understand how to
		invoke it.  These stages are:

"clang-16" : 
	La commande "clang-16" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-16", "info clang-16" ou "clang-16" pour essayer d'obtenir davantage d'informations.

"clang-apply-replacements" : 
	La commande "clang-apply-replacements" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-apply-replacements", "info clang-apply-replacements" ou "clang-apply-replacements" pour essayer d'obtenir davantage d'informations.

"clang-change-namespace" : 
	La commande "clang-change-namespace" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-change-namespace", "info clang-change-namespace" ou "clang-change-namespace" pour essayer d'obtenir davantage d'informations.

"clang-check" : 
	La commande "clang-check" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-check", "info clang-check" ou "clang-check" pour essayer d'obtenir davantage d'informations.

"clang-cl" : 
	La commande "clang-cl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-cl", "info clang-cl" ou "clang-cl" pour essayer d'obtenir davantage d'informations.

"clang-cpp" : 
	La commande "clang-cpp" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-cpp", "info clang-cpp" ou "clang-cpp" pour essayer d'obtenir davantage d'informations.

"clang-doc" : 
	La commande "clang-doc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-doc", "info clang-doc" ou "clang-doc" pour essayer d'obtenir davantage d'informations.

"clang-extdef-mapping" : 
	La commande "clang-extdef-mapping" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-extdef-mapping", "info clang-extdef-mapping" ou "clang-extdef-mapping" pour essayer d'obtenir davantage d'informations.

"clang-format" : 
	La commande "clang-format" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-format", "info clang-format" ou "clang-format" pour essayer d'obtenir davantage d'informations.

"clang-include-cleaner" : 
	La commande "clang-include-cleaner" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-include-cleaner", "info clang-include-cleaner" ou "clang-include-cleaner" pour essayer d'obtenir davantage d'informations.

"clang-include-fixer" : 
	La commande "clang-include-fixer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-include-fixer", "info clang-include-fixer" ou "clang-include-fixer" pour essayer d'obtenir davantage d'informations.

"clang-linker-wrapper" : 
	La commande "clang-linker-wrapper" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-linker-wrapper", "info clang-linker-wrapper" ou "clang-linker-wrapper" pour essayer d'obtenir davantage d'informations.

"clang-move" : 
	La commande "clang-move" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-move", "info clang-move" ou "clang-move" pour essayer d'obtenir davantage d'informations.

"clang-offload-bundler" : 
	La commande "clang-offload-bundler" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-offload-bundler", "info clang-offload-bundler" ou "clang-offload-bundler" pour essayer d'obtenir davantage d'informations.

"clang-offload-packager" : 
	La commande "clang-offload-packager" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-offload-packager", "info clang-offload-packager" ou "clang-offload-packager" pour essayer d'obtenir davantage d'informations.

"clang-pseudo" : 
	La commande "clang-pseudo" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-pseudo", "info clang-pseudo" ou "clang-pseudo" pour essayer d'obtenir davantage d'informations.

"clang-query" : 
	La commande "clang-query" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-query", "info clang-query" ou "clang-query" pour essayer d'obtenir davantage d'informations.

"clang-refactor" : 
	La commande "clang-refactor" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-refactor", "info clang-refactor" ou "clang-refactor" pour essayer d'obtenir davantage d'informations.

"clang-rename" : 
	La commande "clang-rename" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-rename", "info clang-rename" ou "clang-rename" pour essayer d'obtenir davantage d'informations.

"clang-reorder-fields" : 
	La commande "clang-reorder-fields" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-reorder-fields", "info clang-reorder-fields" ou "clang-reorder-fields" pour essayer d'obtenir davantage d'informations.

"clang-repl" : 
	La commande "clang-repl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-repl", "info clang-repl" ou "clang-repl" pour essayer d'obtenir davantage d'informations.

"clang-scan-deps" : 
	La commande "clang-scan-deps" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-scan-deps", "info clang-scan-deps" ou "clang-scan-deps" pour essayer d'obtenir davantage d'informations.

"clang-tblgen" : 
	La commande "clang-tblgen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-tblgen", "info clang-tblgen" ou "clang-tblgen" pour essayer d'obtenir davantage d'informations.

"clang-tidy" : 
	La commande "clang-tidy" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clang-tidy", "info clang-tidy" ou "clang-tidy" pour essayer d'obtenir davantage d'informations.

"clangd" : 
	La commande "clangd" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clangd", "info clangd" ou "clangd" pour essayer d'obtenir davantage d'informations.

"clangd-xpc-test-client" : 
	La commande "clangd-xpc-test-client" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help clangd-xpc-test-client", "info clangd-xpc-test-client" ou "clangd-xpc-test-client" pour essayer d'obtenir davantage d'informations.

"clear" : 
	NAME :
		clear - clear the terminal screen
	DESCRIPTION :
		clear clears your screen if this is possible.  It
		looks in the environment for the terminal type and
		then in the terminfo database to figure out how to
		clear the screen.

"cmp" : 
	NAME :
		asn1parse, ca, ciphers, cmp, cms, crl, crl2pkcs7,
		dgst, dhparam, dsa, dsaparam, ec, ecparam, enc,
		engine, errstr, gendsa, genpkey, genrsa, info, kdf,
		mac, nseq, ocsp, passwd, pkcs12, pkcs7, pkcs8, pkey,
		pkeyparam, pkeyutl, prime, rand, rehash, req, rsa,
		rsautl, s_client, s_server, s_time, sess_id, smime,
		speed, spkac, srp, storeutl, ts, verify, version,
		x509 - OpenSSL application commands
	DESCRIPTION :
		Every cmd listed above is a (sub-)command of the
		openssl(1) application.  It has its own detailed
		manual page at openssl-cmd(1). For example, to view
		the manual page for the openssl dgst command, type
		"man openssl-dgst".

"cmpdylib" : 
	NAME :
		cmpdylib - compare two dynamic shared libraries for
		compatibility
	DESCRIPTION :
		cmpdylib compares two versions of a dynamic shared
		library to see if they are compatible with each
		other. If the two versions are incompatible, the
		reason is printed to stdout, and the exit status
		is nonzero. If they are compatible, nothing is
		printed, and the exit status is zero.

"code" : 
	NAME :
		code - capture the namespace context for a code
		fragment
	DESCRIPTION :
		Creates a scoped value for the specified command
		and its associated arg arguments.  A scoped value
		is a list with three elements:  the "@scope" keyword,
		a namespace context, and a value string.  For
		example, the command namespace foo { code puts
		"Hello World!" } produces the scoped value: @scope
		::foo {puts {Hello World!}} Note that the code
		command captures the current namespace context.  If

"codecctl" : 
	La commande "codecctl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help codecctl", "info codecctl" ou "codecctl" pour essayer d'obtenir davantage d'informations.

"codesign" : 
	NAME :
		codesign – Create and manipulate code signatures
	DESCRIPTION :
		The codesign command is used to create, check, and
		display code signatures, as well as inquire into
		the dynamic status of signed code in the system.

"codesign_allocate" : 
	NAME :
		codesign_allocate -  add code signing data to a
		Mach-O file
	DESCRIPTION :
		codesign_allocate sets up a Mach-O file used by the
		dynamic linker so space for code signing data of
		the specified size for the specified architecture
		is embedded in the Mach-O file.  The program must
		be passed one -a argument or one -A argument for
		each architecture in a universal file, or exactly
		one -a or -A for a thin file.

"col" : 
	NAME :
		col – filter reverse line feeds from input
	DESCRIPTION :
		The col utility filters out reverse (and half
		reverse) line feeds so that the output is in the
		correct order with only forward and half forward
		line feeds, and replaces white-space characters
		with tabs where possible.

"colcrt" : 
	NAME :
		colcrt – filter nroff output for CRT previewing
	DESCRIPTION :
		The colcrt utility provides virtual half-line and
		reverse line feed sequences for terminals without
		such capability, and on which overstriking is
		destructive.  Half-line characters and underlining
		(changed to dashing `-') are placed on new lines
		in between the normal output lines.

"colldef" : 
	NAME :
		colldef – convert collation sequence source definition
	DESCRIPTION :
		The colldef utility converts a collation sequence
		source definition into a format usable by the
		strxfrm() and strcoll() functions.  It is used to
		define the many ways in which strings can be ordered
		and collated.  The strxfrm() function transforms
		its first argument and places the result in its
		second argument.  The transformed string is such
		that it can be correctly ordered with other transformed
		strings by using strcmp(), strncmp(), or memcmp().
		The strcoll() function transforms its arguments and
		does a comparison.

"colrm" : 
	NAME :
		colrm – remove columns from a file
	DESCRIPTION :
		The colrm utility removes selected columns from the
		lines of a file.  A column is defined as a single
		character in a line.  Input is read from the standard
		input.  Output is written to the standard output.

"column" : 
	NAME :
		column – columnate lists
	DESCRIPTION :
		The column utility formats its input into multiple
		columns.  Rows are filled before columns.  Input
		is taken from file operands, or, by default, from
		the standard input.  Empty lines are ignored.

"comm" : 
	NAME :
		comm – select or reject lines common to two files
	DESCRIPTION :
		The comm utility reads file1 and file2, which should
		be sorted lexically, and produces three text columns
		as output: lines only in file1; lines only in file2;
		and lines in both files.

"command" : 
	DESCRIPTION :
		Exécute une simple commande ou affiche des informations
		sur les commandes. Lance la COMMANDE avec des ARGS
		en court-circuitant la recherche de commande, ou
		affiche des informations sur les COMMANDEs spécifiées.
		Ceci peut être utilisé pour invoquer des commandes
		sur le disque lorsqu'il y a conflit avec une fonction
		portant le même nom.

"commandes_bash_obtenir_liste_et_info" : 
	La commande "commandes_bash_obtenir_liste_et_info" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help commandes_bash_obtenir_liste_et_info", "info commandes_bash_obtenir_liste_et_info" ou "commandes_bash_obtenir_liste_et_info" pour essayer d'obtenir davantage d'informations.

"commandes_perso_liste" : 
	La commande "commandes_perso_liste" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help commandes_perso_liste", "info commandes_perso_liste" ou "commandes_perso_liste" pour essayer d'obtenir davantage d'informations.

"common" : 
	La commande "common" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help common", "info common" ou "common" pour essayer d'obtenir davantage d'informations.

"compgen" : 
	DESCRIPTION :
		Affiche les possibilités de complètement dépendant
		des options. Ceci est destiné à être utilisé depuis
		une fonction de shell générant des auto-complètements
		possibles. Si le MOT optionnel est fourni, des
		correspondances avec « MOT » sont générées.

"complete" : 
	DESCRIPTION :
		Spécifie la façon dont Readline complète les
		arguments. Pour chaque NOM, la commande spécifie
		la façon dont les arguments sont complétés S'il n'y
		a pas d'option, le réglage actuel est affiché d'une
		manière réutilisable comme une entrée.

"compopt" : 
	DESCRIPTION :
		Modifie ou affiche les options d'auto-complètement.
		Modifie les options d'auto-complètement pour chaque
		NOM ou, si aucun NOM n'est fourni, pour l'auto-complètement
		actuellement exécuté.  Si aucune OPTION n'est donnée,
		affiche les options d'auto-complètement de chaque
		NOM ou le réglage actuel d'auto-complètement.

"compress" : 
	NAME :
		compress, uncompress – compress and expand data
	DESCRIPTION :
		The compress utility reduces the size of files using
		adaptive Lempel-Ziv coding.  Each file is renamed
		to the same name plus the extension .Z.  A file
		argument with a .Z extension will be ignored except
		it will cause an error exit after other arguments
		are processed.  If compression would not reduce the
		size of a file, the file is ignored.

"compression_tool" : 
	NAME :
		compression_tool - encode/decode files using the
		Compression library
	DESCRIPTION :
		compression_tool encodes (compresses), or decodes
		(uncompresses) files using the Compression library.

"comp_err" : 
	NAME :
		comp_err - compile MySQL error message file
	DESCRIPTION :
		comp_err creates the errmsg.sys file that is used
		by mysqld to determine the error messages to display
		for different error codes. comp_err normally is run
		automatically when MySQL is built. It compiles the
		errmsg.sys file from text-format error information
		in MySQL source distributions:

"config_data" : 
	NAME :
		config_data - Query or change configuration of Perl
		modules
	DESCRIPTION :
		The "config_data" tool provides a command-line
		interface to the configuration of Perl modules.  By
		"configuration", we mean something akin to "user
		preferences" or "local settings".  This is a
		formalization and abstraction of the systems that
		people like Andreas Koenig ("CPAN::Config"), Jon
		Swartz ("HTML::Mason::Config"), Andy Wardley
		("Template::Config"), and Larry Wall (perl's own
		Config.pm) have developed independently.

"config_data5.30" : 
	NAME :
		config_data - Query or change configuration of Perl
		modules
	DESCRIPTION :
		The "config_data" tool provides a command-line
		interface to the configuration of Perl modules.  By
		"configuration", we mean something akin to "user
		preferences" or "local settings".  This is a
		formalization and abstraction of the systems that
		people like Andreas Koenig ("CPAN::Config"), Jon
		Swartz ("HTML::Mason::Config"), Andy Wardley
		("Template::Config"), and Larry Wall (perl's own
		Config.pm) have developed independently.

"continue" : 
	DESCRIPTION :
		Reprend l'exécution des boucles for, while ou until.
		Reprend l'itération suivante de la boucle FOR, WHILE
		ou UNTIL de niveau supérieur. Si N est précisé,
		reprend à la N-ième boucle supérieure.

"coproc" : 
	La commande "coproc" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help coproc", "info coproc" ou "coproc" pour essayer d'obtenir davantage d'informations.

"corbeille_vider" : 
	La commande "corbeille_vider" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help corbeille_vider", "info corbeille_vider" ou "corbeille_vider" pour essayer d'obtenir davantage d'informations.

"coreaudiod" : 
	DESCRIPTION :
		coreaudiod is a daemon used for Core Audio related
		purposes.  coreaudiod was introduced with OSX version
		10.4.

"corelist" : 
	NAME :
		corelist - a commandline frontend to Module::CoreList
	DESCRIPTION :
		See Module::CoreList for one.

"corelist5.30" : 
	NAME :
		corelist - a commandline frontend to Module::CoreList
	DESCRIPTION :
		See Module::CoreList for one.

"corelist5.34" : 
	NAME :
		corelist - a commandline frontend to Module::CoreList
	DESCRIPTION :
		See Module::CoreList for one.

"count" : 
	La commande "count" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help count", "info count" ou "count" pour essayer d'obtenir davantage d'informations.

"cp" : 
	NAME :
		cp – copy files
	DESCRIPTION :
		In the first synopsis form, the cp utility copies
		the contents of the source_file to the target_file.
		In the second synopsis form, the contents of each
		named source_file is copied to the destination
		target_directory.  The names of the files themselves
		are not changed.  If cp detects an attempt to copy
		a file to itself, the copy will fail.

"cpan" : 
	NAME :
		cpan - easily interact with CPAN from the command
		line
	DESCRIPTION :
		This script provides a command interface (not a
		shell) to CPAN. At the moment it uses CPAN.pm to
		do the work, but it is not a one-shot command runner
		for CPAN.pm.

"cpan5.30" : 
	NAME :
		cpan - easily interact with CPAN from the command
		line
	DESCRIPTION :
		This script provides a command interface (not a
		shell) to CPAN. At the moment it uses CPAN.pm to
		do the work, but it is not a one-shot command runner
		for CPAN.pm.

"cpan5.34" : 
	NAME :
		cpan - easily interact with CPAN from the command
		line
	DESCRIPTION :
		This script provides a command interface (not a
		shell) to CPAN. At the moment it uses CPAN.pm to
		do the work, but it is not a one-shot command runner
		for CPAN.pm.

"cpio" : 
	NAME :
		cpio – copy files to and from archives
	DESCRIPTION :
		cpio copies files between archives and directories.
		This implementation can extract from tar, pax, cpio,
		zip, jar, ar, and ISO 9660 cdrom images and can
		create tar, pax, cpio, ar, and shar archives.

"cpp" : 
	La commande "cpp" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help cpp", "info cpp" ou "cpp" pour essayer d'obtenir davantage d'informations.

"cpuctl" : 
	NAME :
		cpuctl – program to control CPUs
	DESCRIPTION :
		The cpuctl command can be used to control and inspect
		the state of CPUs in the system.

"cpuwalk.d" : 
	NAME :
		cpuwalk.d - Measure which CPUs a process runs on.
		Uses DTrace.
	DESCRIPTION :
		This program is for multi-CPU servers, and can help
		identify if a process is running on multiple CPUs
		concurrently or not.

"cpu_profiler.d" : 
	La commande "cpu_profiler.d" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help cpu_profiler.d", "info cpu_profiler.d" ou "cpu_profiler.d" pour essayer d'obtenir davantage d'informations.

"crc32" : 
	NAME :
		crc32 - Perform a 32bit Cyclic Redundancy Check
	DESCRIPTION :
		This package provides a Tcl implementation of the
		CRC-32 algorithm based upon information provided
		at http://www.naaccr.org/standard/crc32/document.html
		If either the critcl package or the Trf package are
		available then a compiled version may be used
		internally to accelerate the checksum calculation.

"crc325.30" : 
	La commande "crc325.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help crc325.30", "info crc325.30" ou "crc325.30" pour essayer d'obtenir davantage d'informations.

"creatbyproc.d" : 
	NAME :
		creatbyproc.d - snoop creat()s by process name.
		Uses DTrace.
	DESCRIPTION :
		creatbyproc.d is a DTrace OneLiner to print file
		creations as it occurs, including the name of the
		process calling the open.

"createhomedir" : 
	NAME :
		createhomedir – create and populate home directories
		on the local computer.
	DESCRIPTION :
		createhomedir provides several options for creating
		and populating home directories.

"crlrefresh" : 
	NAME :
		crlrefresh - update and maintain system-wide CRL
		cache
	DESCRIPTION :
		Crlrefresh is a UNIX command-line program which is
		used to refresh and update the contents of the
		system-wide cache of Certificate Revocation Lists
		(CRLs). CRLs, which are optionally used as part of
		the procedure for verifying X.509 certificates, are
		typically fetched from the network using a URL which
		appears in (some) certificates. Caching CRLs is an
		optimization to avoid costs of network latency
		and/or unavailability. Each CRL has a finite validity
		time which is specified in the CRL itself. This
		validity time may be as short as one day, or it may
		be much longer. Crlrefresh examines the contents
		of the CRL cache and updates - via network fetch -
		all CRLs which are currently, or will

"cron" : 
	NAME :
		cron – daemon to execute scheduled commands (Vixie
		Cron)
	DESCRIPTION :
		The cron utility is launched by launchd(8) when it
		sees the existence of /etc/crontab or files in
		/usr/lib/cron/tabs.  There should be no need to
		start it manually.  See
		/System/Library/LaunchDaemons/com.vix.cron.plist
		for details.

"crontab" : 
	NAME :
		crontab – maintain crontab files for individual
		users (V3)
	DESCRIPTION :
		The crontab utility is the program used to install,
		deinstall or list the tables used to drive the
		cron(8) daemon in Vixie Cron.  Each user can have
		their own crontab, and they are not intended to be
		edited directly.

"csh" : 
	NAME :
		tcsh - C shell with file name completion and command
		line editing
	DESCRIPTION :
		tcsh is an enhanced but completely compatible version
		of the Berkeley UNIX C shell, csh(1).  It is a
		command language interpreter usable both as an
		interactive login shell and a shell script command
		processor.  It includes a command-line editor (see
		The command-line editor), programmable word completion
		(see Completion and listing), spelling correction
		(see Spelling correction), a history mechanism (see
		History substitution), job control (see Jobs) and
		a C-like syntax.  The NEW FEATURES section describes
		major enhancements of tcsh over csh(1). Throughout
		this manual, features of tcsh not found in most
		csh(1) implementations (specifically, the 4.4BSD
		csh) are labeled with `(+)',

"csplit" : 
	NAME :
		csplit – split files based on context
	DESCRIPTION :
		The csplit utility splits file into pieces using
		the patterns args.  If file is a dash (‘-’), csplit
		reads from standard input.

"csreq" : 
	NAME :
		csreq – Expert tool for manipulating Code Signing
		Requirement data
	DESCRIPTION :
		The csreq command manipulates Code Signing Requirement
		data.  It reads one requirement from a file or
		command arguments, converts it into internal form,
		checks it, and then optionally outputs it in a
		different form.

"csrutil" : 
	NAME :
		csrutil – Configure system security policies
	DESCRIPTION :
		csrutil modifies System Integrity Protection settings.
		Some of the commands require the device to be booted
		into the Recovery OS.  Invoke csrutil with no
		arguments to see a full usage statement.

"ctags" : 
	NAME :
		ctags – create a tags file
	DESCRIPTION :
		ctags makes a tags file for ex(1) from the specified
		C, Pascal, Fortran, YACC, lex, and lisp sources.
		A tags file gives the locations of specified objects
		in a group of files.  Each line of the tags file
		contains the object name, the file in which it is
		defined, and a search pattern for the object
		definition, separated by white-space.

"ctf_insert" : 
	NAME :
		ctf_insert - insert Compact C Type Format data into
		a mach_kernel file
	DESCRIPTION :
		ctf_insert inserts CTF (Compact C Type Format) data
		into a mach_kernel binary, storing the data in a
		newly created (__CTF,__ctf) section. This section
		must not be present in the input file.  ctf_insert(1)
		must be passed one -arch argument for each architecture
		in a universal file, or exactly one -arch for a
		thin file.

"cu" : 
	NAME :
		cu - Call up another system
	DESCRIPTION :
		The cu command is used to call up another system
		and act as a dial in terminal.  It can also do
		simple file transfers with no error checking.

"cups-config" : 
	NAME :
		cups-config - get cups api, compiler, directory,
		and link information.
	DESCRIPTION :
		The cups-config command allows application developers
		to determine the necessary command-line options for
		the compiler and linker, as well as the installation
		directories for filters, configuration files, and
		drivers.  All values are reported to the standard
		output.

"cupsaccept" : 
	NAME :
		cupsaccept/cupsreject - accept/reject jobs sent to
		a destination
	DESCRIPTION :
		The cupsaccept command instructs the printing system
		to accept print jobs to the specified destinations.

"cupsctl" : 
	NAME :
		cupsctl - configure cupsd.conf options
	DESCRIPTION :
		cupsctl updates or queries the cupsd.conf file for
		a server. When no changes are requested, the current
		configuration values are written to the standard
		output in the format "name=value", one per line.

"cupsdisable" : 
	NAME :
		cupsdisable, cupsenable - stop/start printers and
		classes
	DESCRIPTION :
		cupsenable starts the named printers or classes
		while cupsdisable stops the named printers or
		classes.

"cupsenable" : 
	NAME :
		cupsdisable, cupsenable - stop/start printers and
		classes
	DESCRIPTION :
		cupsenable starts the named printers or classes
		while cupsdisable stops the named printers or
		classes.

"cupsfilter" : 
	NAME :
		cupsfilter - convert a file to another format using
		cups filters (deprecated)
	DESCRIPTION :
		cupsfilter is a front-end to the CUPS filter subsystem
		which allows you to convert a file to a specific
		format, just as if you had printed the file through
		CUPS. By default, cupsfilter generates a PDF file.
		The converted file is sent to the standard output.

"cupsreject" : 
	NAME :
		cupsaccept/cupsreject - accept/reject jobs sent to
		a destination
	DESCRIPTION :
		The cupsaccept command instructs the printing system
		to accept print jobs to the specified destinations.

"cupstestppd" : 
	NAME :
		cupstestppd - test conformance of ppd files
	DESCRIPTION :
		cupstestppd tests the conformance of PPD files to
		the Adobe PostScript Printer Description file format
		specification version 4.3.  It can also be used to
		list the supported options and available fonts in
		a PPD file.  The results of testing and any other
		output are sent to the standard output.

"curl" : 
	NAME :
		curl - transfer a URL
	DESCRIPTION :
		curl is a tool for transferring data from or to a
		server. It supports these protocols: DICT, FILE,
		FTP, FTPS, GOPHER, GOPHERS, HTTP, HTTPS, IMAP,
		IMAPS, LDAP, LDAPS, MQTT, POP3, POP3S, RTMP, RTMPS,
		RTSP, SCP, SFTP, SMB, SMBS, SMTP, SMTPS, TELNET,
		TFTP, WS and WSS. The command is designed to work
		without user interaction.

"curl-config" : 
	NAME :
		curl-config - Get information about a libcurl
		installation
	DESCRIPTION :
		curl-config displays information about the curl and
		libcurl installation.

"cut" : 
	NAME :
		cut – cut out selected portions of each line of a
		file
	DESCRIPTION :
		The cut utility cuts out selected portions of each
		line (as specified by list) from each file and
		writes them to the standard output.  If no file
		arguments are specified, or a file argument is a
		single dash (‘-’), cut reads from the standard
		input.  The items specified by list can be in terms
		of column position or in terms of fields delimited
		by a special character.  Column and field numbering
		start from 1.

"cvadmin" : 
	NAME :
		cvadmin - Administer an Xsan File System
	DESCRIPTION :
		cvadmin is an interactive command used for general
		purpose administration of an Xsan volume including:

"cvaffinity" : 
	NAME :
		cvaffinity - set, get, or delete the affinity of a
		file or directory
	DESCRIPTION :
		cvaffinity can be used to set an affinity for a
		specific storage pool on a file or directory, or
		list the current affinity.  An affinity is created
		in a storage pool through the volume configuration,
		see snfs_config(5).  It is a name, up to eight (8)
		characters, describing a special media type. Use
		cvadmin(8) to see what affinity sets are assigned
		to the configured storage pools.

"cvcp" : 
	NAME :
		cvcp - Xsan Copy Utility
	DESCRIPTION :
		cvcp provides a high speed, multi-threaded copy
		mechanism for copying directories onto and off of
		an Xsan volume. The utility uses IO strategies and
		multi-threading techniques that exploit the Xsan
		IO model.

"cvdb" : 
	NAME :
		cvdb - Xsan Client File System Debugger
	DESCRIPTION :
		cvdb provides a mechanism for developers and system
		administrators to extract debugging information
		from the Xsan File System client filesystem.  It
		can be used by system administrators to change the
		level of system logging that the client filesystem
		performs.  There is also a switch to retrieve various
		statistics.

"cvdbset" : 
	NAME :
		cvdbset - A program to control cvdb tracing.
	DESCRIPTION :
		cvdbset is a tool for system administrators to
		control cvdb(8) tracing information from the Xsan
		File System client file system.

"cvfsck" : 
	NAME :
		cvfsck - Check and Recover a Xsan Volume
	DESCRIPTION :
		The cvfsck program can check and repair Xsan file
		system metadata corruption due to a system crash,
		bad disk or other catastrophic failure. This program
		also has the ability to list all of the existing
		files and their pertinent statistics, such as inode
		number, size, file type and location in the volume.

"cvfsdb" : 
	NAME :
		cvfsdb - Xsan File System debugging tool
	DESCRIPTION :
		The cvfsdb command is a tool for debugging an Xsan
		volume.

"cvfsid" : 
	NAME :
		cvfsid - Display SNFS System Identifier
	DESCRIPTION :
		cvfsid provides a mechanism for displaying the SNFS
		identifier for the executing system.  For customers
		using client-based licensing, SNFS identifiers are
		used to generate individual client licenses.  This
		identifier string is submitted to Apple Technical
		Support for license authorization keys.  See the
		installation instructions for additional information
		on SNFS licensing.

"cvgather" : 
	NAME :
		cvgather - Compile debugging information for a Xsan
		File System
	DESCRIPTION :
		The cvgather program is used to collect debug
		information from a volume.  This creates a tar file
		of the system's Xsan File System debug logs,
		configuration, version information and disk devices.

"cvlabel" : 
	NAME :
		cvlabel - Label Xsan Disk Devices (LUNs)
	DESCRIPTION :
		cvlabel is used when configuring the Xsan File
		System disks. One host that has visibility to all
		the storage area network disk devices must create
		a list of disk labels, their associated device names
		and optionally the sectors to use. The mount_acfs(8)
		process uses the volume labels to determine which
		disk drive is to be used for Xsan storage pool
		nodes. The label name that is written to a disk
		device must match the [Disk ...] name in the File
		System Manager (FSM) configuration. See snfs_config(5)
		for details of the FSM configuration file.

"cvmkdir" : 
	NAME :
		cvmkdir - Create an Xsan Directory with an Affinity
	DESCRIPTION :
		The cvmkdir command creates a Xsan File System
		directory and attaches an affinity parameter (key)
		to it.  If no option is used and the directory
		exists, the cvmkdir command displays the assigned
		affinity. Once an affinity is assigned to a directory,
		it cannot be altered.  If no key is specified and
		the directory does not exist, the directory will
		not be created.

"cvmkfile" : 
	NAME :
		cvmkfile - Create a pre-allocated file
	DESCRIPTION :
		cvmkfile can be used to pre-allocate a file on the
		Xsan volume.  This is useful and preferable when
		preparing a file for use in a real-time or streaming
		environment as the entire file is represented in
		only one file system extent. Additionally, a file
		can be placed onto a specific storage pool by
		specifying the key value, which is used as the
		affinity locator. See snfs_config(5) for more details
		about affinities.

"cvmkfs" : 
	NAME :
		cvmkfs - Initialize a Xsan Volume
	DESCRIPTION :
		cvmkfs will initialize a Xsan volume optionally
		using volume_name as the name. If no name is supplied,
		a list of volumes configured will be presented.
		Active file systems may not be re-initialized. The
		user will be prompted for a confirmation before
		initializing the volume.

"cvupdatefs" : 
	NAME :
		cvupdatefs - Commit a Xsan Volume configuration
		change
	DESCRIPTION :
		The cvupdatefs program is used to commit a configuration
		change to a Xsan volume. Possible configuration
		changes include storage pool list modification as
		well as volume journal modification.

"cvversions" : 
	NAME :
		cvversions - Display Xsan client/server versions
	DESCRIPTION :
		cvversions will display the revision, build level
		and creation date for the File System Manager (FSM)
		and client subsystems of the Xsan File System.

"c_rehash" : 
	NAME :
		openssl-rehash, c_rehash - Create symbolic links
		to files named by the hash values
	DESCRIPTION :
		This command is generally equivalent to the external
		script c_rehash, except for minor differences noted
		below.

"dappprof" : 
	NAME :
		dappprof - profile user and lib function usage.
		Uses DTrace.
	DESCRIPTION :
		dappprof prints details on user and library call
		times for processes as a summary style aggragation.
		By default the user fuctions are traced, options
		can be used to trace library activity. Output can
		include function counts, elapsed times and on cpu
		times.

"dapptrace" : 
	NAME :
		dapptrace - trace user and library function usage.
		Uses DTrace.
	DESCRIPTION :
		dapptrace prints details on user and library function
		calls. By default it traces user functions only,
		options can be used to trace library activity.

"darwin-debug" : 
	La commande "darwin-debug" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help darwin-debug", "info darwin-debug" ou "darwin-debug" pour essayer d'obtenir davantage d'informations.

"dash" : 
	NAME :
		dash – command interpreter (shell)
	DESCRIPTION :
		dash is the standard command interpreter for the
		system.  The current version of dash is in the
		process of being changed to conform with the POSIX
		1003.2 and 1003.2a specifications for the shell.
		This version has many features which make it appear
		similar in some respects to the Korn shell, but it
		is not a Korn shell clone (see ksh(1)).  Only
		features designated by POSIX, plus a few Berkeley
		extensions, are being incorporated into this shell.
		This man page is not intended to be a tutorial or
		a complete specification of the shell.

"date" : 
	NAME :
		date – display or set date and time
	DESCRIPTION :
		When invoked without arguments, the date utility
		displays the current date and time.  Otherwise,
		depending on the options specified, date will set
		the date and time or print it in a user-defined
		way.

"dbicadmin" : 
	NAME :
		dbicadmin - utility for administrating DBIx::Class
		schemata

"dbicadmin5.30" : 
	NAME :
		dbicadmin - utility for administrating DBIx::Class
		schemata

"dbilogstrip" : 
	NAME :
		dbilogstrip - filter to normalize DBI trace logs
		for diff'ing
	DESCRIPTION :
		Replaces any hex addresses, e.g, 0x128f72ce with
		"0xN".

"dbilogstrip5.30" : 
	NAME :
		dbilogstrip - filter to normalize DBI trace logs
		for diff'ing
	DESCRIPTION :
		Replaces any hex addresses, e.g, 0x128f72ce with
		"0xN".

"dbiprof" : 
	NAME :
		dbiprof - command-line client for DBI::ProfileData
	DESCRIPTION :
		This tool is a command-line client for the
		DBI::ProfileData.  It allows you to analyze the
		profile data file produced by DBI::ProfileDumper
		and produce various useful reports.

"dbiprof5.30" : 
	NAME :
		dbiprof - command-line client for DBI::ProfileData
	DESCRIPTION :
		This tool is a command-line client for the
		DBI::ProfileData.  It allows you to analyze the
		profile data file produced by DBI::ProfileDumper
		and produce various useful reports.

"dbiproxy" : 
	NAME :
		dbiproxy - A proxy server for the DBD::Proxy driver
	DESCRIPTION :
		This tool is just a front end for the DBI::ProxyServer
		package. All it does is picking options from the
		command line and calling DBI::ProxyServer::main().
		See DBI::ProxyServer for details.

"dbiproxy5.30" : 
	NAME :
		dbiproxy - A proxy server for the DBD::Proxy driver
	DESCRIPTION :
		This tool is just a front end for the DBI::ProxyServer
		package. All it does is picking options from the
		command line and calling DBI::ProxyServer::main().
		See DBI::ProxyServer for details.

"dbmmanage" : 
	NAME :
		dbmmanage - Manage user authentication files in DBM
		format

"db_archive" : 
	DESCRIPTION :
		The db_archive utility writes the pathnames of log
		files that are no longer in use (for example, no
		longer involved in active transactions), to the
		standard output, one pathname per line. These log
		files should be written to backup media to provide
		for recovery in the case of catastrophic failure
		(which also requires a snapshot of the database
		files), but they may then be deleted from the system
		to reclaim disk space.

"db_checkpoint" : 
	DESCRIPTION :
		The db_checkpoint utility is a daemon process that
		monitors the database log, and periodically calls
		DB_ENV->txn_checkpoint to checkpoint it.

"db_codegen" : 
	La commande "db_codegen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help db_codegen", "info db_codegen" ou "db_codegen" pour essayer d'obtenir davantage d'informations.

"db_deadlock" : 
	DESCRIPTION :
		The db_deadlock utility traverses the database
		environment lock region, and aborts a lock request
		each time it detects a deadlock or a lock request
		that has timed out. By default, in the case of a
		deadlock, a random lock request is chosen to be
		aborted.

"db_dump" : 
	DESCRIPTION :
		The db_dump utility reads the database file file
		and writes it to the standard output using a portable
		flat-text format understood by the db_load utility.
		The file argument must be a file produced using the
		Berkeley DB library functions.

"db_hotbackup" : 
	La commande "db_hotbackup" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help db_hotbackup", "info db_hotbackup" ou "db_hotbackup" pour essayer d'obtenir davantage d'informations.

"db_load" : 
	DESCRIPTION :
		The db_load utility reads from the standard input
		and loads it into the database file. The database
		file is created if it does not already exist.

"db_printlog" : 
	DESCRIPTION :
		The db_printlog utility is a debugging utility that
		dumps Berkeley DB log files in a human-readable
		format.

"db_recover" : 
	DESCRIPTION :
		The db_recover utility must be run after an unexpected
		application, Berkeley DB, or system failure to
		restore the database to a consistent state. All
		committed transactions are guaranteed to appear
		after db_recover has run, and all uncommitted
		transactions will be completely undone.

"db_stat" : 
	DESCRIPTION :
		The db_stat utility utility displays statistics for
		Berkeley DB environments.

"db_upgrade" : 
	DESCRIPTION :
		The db_upgrade utility upgrades the Berkeley DB
		version of one or more files and the databases they
		contain to the current release version.

"db_verify" : 
	DESCRIPTION :
		The db_verify utility verifies the structure of one
		or more files and the databases they contain.

"dc" : 
	DESCRIPTION :
		dc(1) is an arbitrary-precision calculator.  It
		uses a stack (reverse Polish notation) to store
		numbers and results of computations. Arithmetic
		operations pop arguments off of the stack and push
		the results.

"dd" : 
	NAME :
		dd – convert and copy a file
	DESCRIPTION :
		The dd utility copies the standard input to the
		standard output.  Input data is read and written
		in 512-byte blocks.  If input reads are short, input
		from multiple reads are aggregated to form the
		output block.  When finished, dd displays the number
		of complete and partial input and output blocks and
		truncated input records to the standard error output.

"ddns-confgen" : 
	NAME :
		ddns-confgen - ddns key generation tool
	DESCRIPTION :
		tsig-keygen and ddns-confgen are invocation methods
		for a utility that generates keys for use in TSIG
		signing. The resulting keys can be used, for example,
		to secure dynamic DNS updates to a zone or for the
		rndc command channel.

"debinhex.pl" : 
	NAME :
		debinhex.pl - use Convert::BinHex to decode BinHex
		files
	DESCRIPTION :
		Each file is expected to be a BinHex file.  By
		default, the output file is given the name that the
		BinHex file dictates, regardless of the name of the
		BinHex file.

"debinhex5.30.pl" : 
	La commande "debinhex5.30.pl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help debinhex5.30.pl", "info debinhex5.30.pl" ou "debinhex5.30.pl" pour essayer d'obtenir davantage d'informations.

"declare" : 
	DESCRIPTION :
		Définit les valeurs et les attributs des variables.
		Déclare des variables et leur assigne des attributs.
		Si aucun NOM n'est donné, affiche les attributs et
		les valeurs de toutes les variables.

"defaults" : 
	NAME :
		defaults – access the Mac OS X user defaults system
	DESCRIPTION :
		Defaults allows users to read, write, and delete
		Mac OS X user defaults from a command-line shell.
		Mac OS X applications and other programs use the
		defaults system to record user preferences and other
		information that must be maintained when the
		applications aren't running (such as default font
		for new documents, or the position of an Info panel).
		Much of this information is accessible through an
		application's Preferences panel, but some of it
		isn't, such as the position of the Info panel. You
		can access this information with defaults

"delv" : 
	NAME :
		delv - DNS lookup and validation utility
	DESCRIPTION :
		delv (Domain Entity Lookup & Validation) is a tool
		for sending DNS queries and validating the results,
		using the the same internal resolver and validator
		logic as named.

"demandoc" : 
	NAME :
		demandoc – emit only text of UNIX manuals
	DESCRIPTION :
		The demandoc utility emits only the text portions
		of well-formed mdoc(7) and man(7) UNIX manual files.

"DeRez" : 
	NAME :
		DeRez - decompiles resources (DEPRECATED)
	DESCRIPTION :
		Tools supporting Carbon development, including
		DeRez, were deprecated with Xcode 6.

"derq" : 
	NAME :
		derq – Query and manipulate DER entitlements.
	DESCRIPTION :
		The derq command queries DER encoded entitlements
		using the CoreEntitlements library.

"desdp" : 
	La commande "desdp" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help desdp", "info desdp" ou "desdp" pour essayer d'obtenir davantage d'informations.

"devmodectl" : 
	La commande "devmodectl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help devmodectl", "info devmodectl" ou "devmodectl" pour essayer d'obtenir davantage d'informations.

"DevToolsSecurity" : 
	NAME :
		DevToolsSecurity – Change the security authorization
		policies for developer systems
	DESCRIPTION :
		This tool changes the security authorization policies
		for use of Apple- code-signed debugger and performance
		analysis tools on development systems.

"dev_mkdb" : 
	NAME :
		dev_mkdb – create /dev database
	DESCRIPTION :
		The dev_mkdb command creates a db(3) hash access
		method database in “/var/run/dev.db” which contains
		the names of all of the character and block special
		files in the “/dev” directory, using the file type
		and the st_rdev field as the key.

"df" : 
	NAME :
		df – display free disk space
	DESCRIPTION :
		The df utility displays statistics about the amount
		of free disk space on the specified filesystem or
		on the filesystem of which file is a part. Values
		are displayed in 512-byte per block counts.  If
		neither a file or a filesystem operand is specified,
		statistics for all mounted filesystems are displayed
		(subject to the -t option below).

"diagnose-fu" : 
	La commande "diagnose-fu" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help diagnose-fu", "info diagnose-fu" ou "diagnose-fu" pour essayer d'obtenir davantage d'informations.

"diagtool" : 
	La commande "diagtool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help diagtool", "info diagtool" ou "diagtool" pour essayer d'obtenir davantage d'informations.

"diff" : 
	NAME :
		diff – differential file and directory comparator
	DESCRIPTION :
		The diff utility compares the contents of file1 and
		file2 and writes to the standard output the list
		of changes necessary to convert one file into the
		other.  No output is produced if the files are
		identical.

"diff3" : 
	NAME :
		diff3 - compare three files line by line
	DESCRIPTION :
		Compare three files line by line.

"diffstat" : 
	NAME :
		diffstat - make histogram from diff-output
	DESCRIPTION :
		This program reads the output of diff and displays
		a histogram of the insertions, deletions, and
		modifications per-file.  Diffstat is a program that
		is useful for reviewing large, complex patch files.
		It reads from one or more input files which contain
		output from diff, producing a histogram of the total
		lines changed for each file referenced.

"dig" : 
	NAME :
		dig - DNS lookup utility
	DESCRIPTION :
		dig (domain information groper) is a flexible tool
		for interrogating DNS name servers. It performs DNS
		lookups and displays the answers that are returned
		from the name server(s) that were queried. Most DNS
		administrators use dig to troubleshoot DNS problems
		because of its flexibility, ease of use and clarity
		of output. Other lookup tools tend to have less
		functionality than dig.

"DirectoryService" : 
	NAME :
		dspluginhelperd – support daemon for legacy
		DirectoryService plugins
	DESCRIPTION :
		DirectoryService has been replaced by opendirectoryd(8).
		dspluginhelperd provides support for 3rd party
		DirectoryService plugins.

"dirname" : 
	NAME :
		 basename, dirname – return filename or directory
		 portion of pathname
	DESCRIPTION :
		The basename utility deletes any prefix ending with
		the last slash ‘/’ character present in string
		(after first stripping trailing slashes), and a
		suffix, if given.  The suffix is not stripped if
		it is identical to the remaining characters in
		string.  The resulting filename is written to the
		standard output.  A non-existent suffix is ignored.
		If -a is specified, then every argument is treated
		as a string as if basename were invoked with just
		one argument.  If -s is specified, then the suffix
		is taken as its argument, and all other arguments
		are treated as a string.

"dirs" : 
	DESCRIPTION :
		Affiche la pile de répertoire. Affiche la liste des
		répertoires actuellement mémorisés. Les répertoires
		sont insérés dans la liste avec la commande « pushd
		». Vous pouvez remonter dans la liste en enlevant
		des éléments avec la commande « popd ».

"disklabel" : 
	NAME :
		disklabel – manipulate and query an Apple Label
		disk label
	DESCRIPTION :
		The disklabel utility manipulates ``Apple Label''
		partition metadata. ``Apple Label'' partitions allow
		for a disk device to have a consistent name,
		ownership, and permissions across reboots, even
		though uses a dynamic pseudofilesystem for /dev.

"diskutil" : 
	NAME :
		diskutil – modify, verify and repair local disks
	DESCRIPTION :
		diskutil manipulates the structure of local disks.
		It provides information about, and allows the
		administration of, partitioning schemes, layouts,
		and formats of disks. This includes hard disks,
		solid state disks, optical discs, disk images, APFS
		volumes, CoreStorage volumes, and AppleRAID sets.
		It generally manipulates whole volumes instead of
		individual files and directories.

"disown" : 
	DESCRIPTION :
		Retire des tâches du shell courant. Retire chaque
		argument JOBSPEC de la table des tâches actives.
		Sans JOBSPEC, le shell utilise sa propre notion de
		tâche courante.

"dispqlen.d" : 
	NAME :
		dispqlen.d - dispatcher queue length by CPU. Uses
		DTrace.
	DESCRIPTION :
		The dispatcher queue length is an indication of CPU
		saturation.  It is not an indicatior of utilisation
		- the CPUs may or may not be utilised when the
		dispatcher queue reports a length of zero.

"disque_dur_ejecter" : 
	La commande "disque_dur_ejecter" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help disque_dur_ejecter", "info disque_dur_ejecter" ou "disque_dur_ejecter" pour essayer d'obtenir davantage d'informations.

"distnoted" : 
	NAME :
		distnoted – distributed notification server
	DESCRIPTION :
		distnoted provides distributed notification services.

"ditto" : 
	NAME :
		ditto – copy directory hierarchies, create and
		extract archives
	DESCRIPTION :
		In its first form, ditto copies one or more source
		files or directories to a destination directory.
		If the destination directory does not exist it will
		be created before the first source is copied.  If
		the destination directory already exists then the
		source directories are merged with the previous
		contents of the destination.

"djpeg" : 
	NAME :
		djpeg - decompress a JPEG file to an image file
	DESCRIPTION :
		djpeg decompresses the named JPEG file, or the
		standard input if no file is named, and produces
		an image file on the standard output. PBMPLUS
		(PPM/PGM), BMP, GIF, or Targa output format can be
		selected.

"dmc" : 
	NAME :
		dmc - controls the Disk Mount Conditioner
	DESCRIPTION :
		dmc(1) configures the Disk Mount Conditioner. The
		Disk Mount Conditioner is a kernel provided service
		that can degrade the disk I/O being issued to
		specific mount points, providing the illusion that
		the I/O is executing on a slower device. It can
		also cause the conditioned mount point to advertise
		itself as a different device type, e.g. the disk
		type of an SSD could be set to an HDD. This behavior
		consequently changes various parameters such as
		read-ahead settings, disk I/O throttling, etc.,
		which normally have different behavior depending
		on the underlying device type.

"dmesg" : 
	NAME :
		dmesg – display the system message buffer
	DESCRIPTION :
		Dmesg displays the contents of the system message
		buffer.  This command needs to be run as root.

"dnctl" : 
	NAME :
		dnctl – Traffic shaper control program
	DESCRIPTION :
		The dnctl utility is the user interface for controlling
		the dummynet(4) traffic shaper.

"dns-sd" : 
	NAME :
		dns-sd – Multicast DNS (mDNS) & DNS Service Discovery
		(DNS-SD) Test Tool
	DESCRIPTION :
		The dns-sd command is a network diagnostic tool,
		much like ping(8) or traceroute(8).  However, unlike
		those tools, most of its functionality is not
		implemented in the dns-sd executable itself, but
		in library code that is available to any application.
		The library API that dns-sd uses is documented in
		/usr/include/dns_sd.h.  The dns-sd command replaces
		the older mDNS command.

"do" : 
	La commande "do" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help do", "info do" ou "do" pour essayer d'obtenir davantage d'informations.

"done" : 
	La commande "done" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help done", "info done" ou "done" pour essayer d'obtenir davantage d'informations.

"dot_clean" : 
	NAME :
		dot_clean – Merge ._* files with corresponding
		native files.
	DESCRIPTION :
		For each dir, dot_clean recursively merges all ._*
		files with their corresponding native files according
		to the rules specified with the given arguments.
		By default, if there is an attribute on the native
		file that is also present in the ._ file, the most
		recent attribute will be used.

"drutil" : 
	NAME :
		drutil – interact with CD/DVD burners
	DESCRIPTION :
		drutil uses the DiscRecording framework to interact
		with attached burning devices.  Common verbs include
		burn, erase, eject, help, info, list, status, and
		tray.

"dscacheutil" : 
	NAME :
		dscacheutil – gather information, statistics and
		initiate queries to the Directory Service cache.
	DESCRIPTION :
		dscacheutil does various operations against the
		Directory Service cache including gathering statistics,
		initiating lookups, inspection, cache flush, etc.
		This tool replaces most of the functionality of the
		lookupd tool previously available in the OS.

"dscl" : 
	NAME :
		dscl – Directory Service command line utility
	DESCRIPTION :
		dscl is a general-purpose utility for operating on
		Directory Service directory nodes.  Its commands
		allow one to create, read, and manage Directory
		Service data.  If invoked without any commands,
		dscl runs in an interactive mode, reading commands
		from standard input.  Interactive processing is
		terminated by the quit command.  Leading dashes
		("-") are optional for all commands.

"dsconfigad" : 
	NAME :
		dsconfigad – retrieves/changes configuration for
		Active Directory.
	DESCRIPTION :
		This tool allows command-line configuration of the
		Active Directory. dsconfigad has the same functionality
		for configuring the Active Directory as the Directory
		Utility application.  It requires "admin" privileges
		to the local workstation and to the Directory to
		make changes.

"dsconfigldap" : 
	NAME :
		dsconfigldap – LDAP server configuration/binding
		add/remove tool.
	DESCRIPTION :
		dsconfigldap allows addition or removal of LDAP
		server configurations. Presented below is a discussion
		of possible parameters. Usage has three intents:
		add server config, remove server config, or display
		help.

"dseditgroup" : 
	NAME :
		dseditgroup – group record manipulation tool.
	DESCRIPTION :
		dseditgroup allows manipulation of a single named
		group record on either the default local node or
		the specified DirectoryService node. For the "read"
		operation the authentication search policy (/Search
		node) is consulted. Default behaviour is presented
		below after a discussion of each operation and the
		possible parameters.

"dsenableroot" : 
	NAME :
		dsenableroot – enables or disables the root account.
	DESCRIPTION :
		dsenableroot sets the password for the root account
		if enabling the root user account.  Otherwise, if
		disable [-d] is chosen, the root account passwords
		are removed and the root user is disabled.

"dserr" : 
	NAME :
		dserr – prints a description for an error code.
	DESCRIPTION :
		dserr prints a description for an error code.

"dsexport" : 
	NAME :
		dsexport – export records from OpenDirectory
	DESCRIPTION :
		The dsexport utility exports records from Open
		Directory.

"dsimport" : 
	DESCRIPTION :
		dsimport is a tool for importing records into an
		Open Directory source.

"dsmemberutil" : 
	NAME :
		dsmemberutil – various operations for the membership
		APIs, including state dump, check memberships,
		UUIDs, etc.
	DESCRIPTION :
		dsmemberutil is a program that implements the
		membership API calls in a command line utility.

"dsymutil" : 
	NAME :
		dsymutil - manipulate archived DWARF debug symbol
		files
	DESCRIPTION :
		dsymutil links the DWARF debug information found
		in the object files for an executable executable
		by using debug symbols information contained in its
		symbol table. By default, the linked debug information
		is placed in a .dSYM bundle with the same name as
		the executable.

"dtrace" : 
	NAME :
		dtrace – dynamic tracing compiler and tracing utility
	DESCRIPTION :
		DTrace is a comprehensive dynamic tracing framework
		ported from Solaris. DTrace provides a powerful
		infrastructure that permits administrators, developers,
		and service personnel to concisely answer arbitrary
		questions about the behavior of the operating system
		and user programs.

"dtruss" : 
	NAME :
		dtruss - process syscall details. Uses DTrace.
	DESCRIPTION :
		dtruss prints details on process system calls. It
		is like a DTrace version of truss, and has been
		designed to be less intrusive than truss.

"du" : 
	NAME :
		du – display disk usage statistics
	DESCRIPTION :
		The du utility displays the file system block usage
		for each file argument and for each directory in
		the file hierarchy rooted in each directory argument.
		If no file is specified, the block usage of the
		hierarchy rooted in the current directory is
		displayed.

"dwarfdump" : 
	NAME :
		dwarfdump - dump and verify DWARF debug information
	DESCRIPTION :
		dwarfdump parses DWARF sections in object files,
		archives, and .dSYM bundles and prints their contents
		in human-readable form. Only the .debug_info section
		is printed unless one of the section-specific options
		or --all is specified.

"dyld_info" : 
	NAME :
		dyld_info – Displays information used by dyld in
		programs and dylibs
	DESCRIPTION :
		The dyld_info tool will display information that
		dyld uses from binaries. It supports fixups information
		encoded as opcode based or as chained fixups.

"dyld_usage" : 
	NAME :
		dyld_usage - report dynamic linker activity in
		real-time
	DESCRIPTION :
		The dyld_usage utility presents an ongoing display
		of information pertaining to dynamic linker activity
		within one or more processes. It requires root
		privileges due to the kernel tracing facility it
		uses to operate. By default dyld_usage monitors
		dyld activity in all processes except the running
		dyld_usage process, Terminal, telnetd, sshd, rlogind,
		tcsh, csh, and sh. These defaults can be overridden
		such that output is limited to include or exclude
		a list of processes specified on the command line.
		Processes may be specified either by file name or
		by process id.

"dynamic_pager" : 
	NAME :
		dynamic_pager – swap configuration daemon
	DESCRIPTION :
		The dynamic_pager daemon can be used to specify a
		base name for swapfile names using the "-F" command
		line option or by modifying the configuration plist
		file.

"echo" : 
	DESCRIPTION :
		Écrit les arguments sur la sortie standard. Affiche
		les ARGs, séparés par une espace, sur la sortie
		standard, suivis d'un retour à la ligne.

"ed" : 
	NAME :
		ed, red – text editor
	DESCRIPTION :
		The ed utility is a line-oriented text editor.  It
		is used to create, display, modify and otherwise
		manipulate text files.  When invoked as red, the
		editor runs in "restricted" mode, in which the only
		difference is that the editor restricts the use of
		filenames which start with ‘!’ (interpreted as shell
		commands by ed) or contain a ‘/’.  Note that editing
		outside of the current directory is only prohibited
		if the user does not have write access to the current
		directory.  If a user has write access to the current
		directory, then symbolic links can be created in
		the current directory, in which case red will not
		stop the user from editing the file that the symbolic
		link points to.

"edquota" : 
	NAME :
		edquota – edit user quotas
	DESCRIPTION :
		Edquota is a quota editor.  By default, or if the
		-u flag is specified, one or more users may be
		specified on the command line.  For each user a
		temporary file is created with an ASCII representation
		of the current disk quotas for that user.  The list
		of filesystems with user quotas is determined by
		scanning the mounted filesystems for a .quota.ops.user
		file located at its root.  An editor is invoked on
		the ASCII file.  The editor invoked is vi(1) unless
		the environment variable EDITOR specifies otherwise.

"egrep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"elif" : 
	La commande "elif" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help elif", "info elif" ou "elif" pour essayer d'obtenir davantage d'informations.

"else" : 
	La commande "else" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help else", "info else" ou "else" pour essayer d'obtenir davantage d'informations.

"enable" : 
	DESCRIPTION :
		Active et désactive les commandes intégrées. Active
		et désactive les commandes intégrées du shell.  Les
		désactiver vous permet d'exécuter une commande du
		disque ayant le même nom qu'une commande du shell
		sans utiliser le chemin complet vers le fichier.

"enc2xs" : 
	NAME :
		enc2xs -- Perl Encode Module Generator
	DESCRIPTION :
		enc2xs builds a Perl extension for use by Encode
		from either Unicode Character Mapping files (.ucm)
		or Tcl Encoding Files (.enc).  Besides being used
		internally during the build process of the Encode
		module, you can use enc2xs to add your own encoding
		to perl.  No knowledge of XS is necessary.

"enc2xs5.30" : 
	NAME :
		enc2xs -- Perl Encode Module Generator
	DESCRIPTION :
		enc2xs builds a Perl extension for use by Encode
		from either Unicode Character Mapping files (.ucm)
		or Tcl Encoding Files (.enc).  Besides being used
		internally during the build process of the Encode
		module, you can use enc2xs to add your own encoding
		to perl.  No knowledge of XS is necessary.

"enc2xs5.34" : 
	NAME :
		enc2xs -- Perl Encode Module Generator
	DESCRIPTION :
		enc2xs builds a Perl extension for use by Encode
		from either Unicode Character Mapping files (.ucm)
		or Tcl Encoding Files (.enc).  Besides being used
		internally during the build process of the Encode
		module, you can use enc2xs to add your own encoding
		to perl.  No knowledge of XS is necessary.

"encguess" : 
	NAME :
		encguess - guess character encodings of files
	DESCRIPTION :
		The encoding identification is done by checking one
		encoding type at a time until all but the right
		type are eliminated. The set of encoding types to
		try is defined by the -s parameter and defaults to
		ascii, utf8 and UTF-16/32 with BOM. This can be
		overridden by passing one or more encoding types
		via the -s parameter. If you need to pass in multiple
		suspect encoding types, use a quoted string with
		the a space separating each value.

"encguess5.30" : 
	NAME :
		encguess - guess character encodings of files
	DESCRIPTION :
		The encoding identification is done by checking one
		encoding type at a time until all but the right
		type are eliminated. The set of encoding types to
		try is defined by the -s parameter and defaults to
		ascii, utf8 and UTF-16/32 with BOM. This can be
		overridden by passing one or more encoding types
		via the -s parameter. If you need to pass in multiple
		suspect encoding types, use a quoted string with
		the a space separating each value.

"encguess5.34" : 
	NAME :
		encguess - guess character encodings of files
	DESCRIPTION :
		The encoding identification is done by checking one
		encoding type at a time until all but the right
		type are eliminated. The set of encoding types to
		try is defined by the -s parameter and defaults to
		ascii, utf8 and UTF-16/32 with BOM. This can be
		overridden by passing one or more encoding types
		via the -s parameter. If you need to pass in multiple
		suspect encoding types, use a quoted string with
		the a space separating each value.

"encode_keychange" : 
	NAME :
		encode_keychange - produce the KeyChange string for
		SNMPv3
	DESCRIPTION :
		encode_keychange produces a KeyChange string using
		the old and new passphrases as described in Section
		5 of RFC 2274 "User-based Security Model (USM) for
		version 3 of the Simple Network Management Protocol
		(SNMPv3)". -t option is mandatory and specifies the
		hash transform type to use.

"env" : 
	NAME :
		env – set environment and execute command, or print
		environment
	DESCRIPTION :
		The env utility executes another utility after
		modifying the environment as specified on the command
		line.  Each name=value option specifies the setting
		of an environment variable, name, with a value of
		value.  All such environment variables are set
		before the utility is executed.

"envsubst" : 
	NAME :
		envsubst - substitutes environment variables in
		shell format strings
	DESCRIPTION :
		Substitutes the values of environment variables.

"erb" : 
	NAME :
		erb – Ruby Templating
	DESCRIPTION :
		erb is a command line front-end for ERB library,
		which is an implementation of eRuby.

"errinfo" : 
	NAME :
		errinfo - print errno for syscall fails. Uses DTrace.
	DESCRIPTION :
		errinfo snoops syscall failures and prints the errno
		value and description of the error number.

"esac" : 
	La commande "esac" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help esac", "info esac" ou "esac" pour essayer d'obtenir davantage d'informations.

"eslogger" : 
	NAME :
		eslogger – log Endpoint Security events
	DESCRIPTION :
		eslogger interfaces with Endpoint Security to log
		events to standard output or to the unified logging
		system.

"eval" : 
	DESCRIPTION :
		Exécute des arguments comme s'ils étaient une
		commande du shell. Combine des ARGs en une chaîne
		unique, utilise le résultat comme entrée du shell,
		puis exécute la commande résultante.

"event_rpcgen.py" : 
	La commande "event_rpcgen.py" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help event_rpcgen.py", "info event_rpcgen.py" ou "event_rpcgen.py" pour essayer d'obtenir davantage d'informations.

"ex" : 
	NAME :
		vim - Vi IMproved, a programmer's text editor
	DESCRIPTION :
		Vim is a text editor that is upwards compatible to
		Vi.  It can be used to edit all kinds of plain text.
		It is especially useful for editing programs.

"exec" : 
	DESCRIPTION :
		Remplace le shell par la commande fournie. Exécute
		la COMMANDE, en remplaçant ce shell par le programme
		spécifié. Les ARGUMENTS deviennent ceux de la
		COMMANDE. Si la COMMANDE n'est pas fournie, les
		redirections prennent effet dans le shell courant.

"execsnoop" : 
	NAME :
		execsnoop - snoop new process execution. Uses DTrace.
	DESCRIPTION :
		execsnoop prints details of new processes as they
		are executed. Details such as UID, PID and argument
		listing are printed out.

"exit" : 
	DESCRIPTION :
		Termine le shell. Termine le shell avec le code de
		retour « N ».  Si N est omis, le code de retour est
		celui de la dernière commande exécutée.

"expand" : 
	NAME :
		expand, unexpand – expand tabs to spaces, and vice
		versa
	DESCRIPTION :
		The expand utility processes the named files or the
		standard input writing the standard output with
		tabs changed into blanks.  Backspace characters are
		preserved into the output and decrement the column
		count for tab calculations.  The expand utility is
		useful for pre-processing character files (before
		sorting, looking at specific columns, etc.) that
		contain tabs.

"expect" : 
	NAME :
		expect - programmed dialogue with interactive
		programs, Version 5

"export" : 
	DESCRIPTION :
		Définit l'attribut « export » pour des variables
		du shell. Marque chaque NOM pour exportation
		automatique vers l'environnement des commandes
		exécutées ultérieurement.  Si VALEUR est fournie,
		affecte la VALEUR avant l'exportation.

"expr" : 
	NAME :
		expr – evaluate expression
	DESCRIPTION :
		The expr utility evaluates expression and writes
		the result on standard output.

"extcheck" : 
	La commande "extcheck" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help extcheck", "info extcheck" ou "extcheck" pour essayer d'obtenir davantage d'informations.

"eyapp" : 
	NAME :
		eyapp - A Perl front-end to the Parse::Eyapp module
	DESCRIPTION :
		The eyapp compiler is a front-end to the Parse::Eyapp
		module, which lets you compile Parse::Eyapp grammar
		input files into Perl LALR(1) Object Oriented parser
		modules.

"eyapp5.30" : 
	NAME :
		eyapp - A Perl front-end to the Parse::Eyapp module
	DESCRIPTION :
		The eyapp compiler is a front-end to the Parse::Eyapp
		module, which lets you compile Parse::Eyapp grammar
		input files into Perl LALR(1) Object Oriented parser
		modules.

"false" : 
	DESCRIPTION :
		Renvoie un résultat d'échec. Code de sortie :
		Toujours l'échec.

"fax2ps" : 
	NAME :
		fax2ps - convert a TIFF facsimile to compressed
		PostScript™
	DESCRIPTION :
		fax2ps reads one or more TIFF facsimile image files
		and prints a compressed form of PostScript on the
		standard output that is suitable for printing.

"fax2tiff" : 
	NAME :
		fax2tiff - create a TIFF Class F fax file from raw
		fax data
	DESCRIPTION :
		fax2tiff creates a TIFF file containing  CCITT Group
		3 or Group 4 encoded data from one or more files
		containing "raw" Group 3 or Group 4 encoded data
		(typically obtained directly from a fax modem).  By
		default, each row of data in the resultant TIFF
		file is 1-dimensionally encoded and padded or
		truncated to 1728 pixels, as needed.  The resultant
		image is a set of low resolution (98 lines/inch)
		or medium resolution (196 lines/inch) pages, each
		of which is a single strip of data.  The generated
		file conforms to the TIFF Class F (FAX) specification
		for storing facsimile data.  This means, in particular,
		that each page of the data does not include the
		trailing "return to

"fc" : 
	DESCRIPTION :
		Affiche ou exécute des commandes issues de l'historique.
		« fc » est utilisé pour afficher ou modifier puis
		ré-exécuter les commandes de l'historique des
		commandes. PREMIER et DERNIER peuvent être des
		nombres indiquant la plage ou PREMIER peut être une
		chaîne donnant le début de la commande la plus
		récente recherchée.

"fc-cache" : 
	La commande "fc-cache" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-cache", "info fc-cache" ou "fc-cache" pour essayer d'obtenir davantage d'informations.

"fc-cat" : 
	La commande "fc-cat" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-cat", "info fc-cat" ou "fc-cat" pour essayer d'obtenir davantage d'informations.

"fc-conflist" : 
	La commande "fc-conflist" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-conflist", "info fc-conflist" ou "fc-conflist" pour essayer d'obtenir davantage d'informations.

"fc-list" : 
	La commande "fc-list" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-list", "info fc-list" ou "fc-list" pour essayer d'obtenir davantage d'informations.

"fc-match" : 
	La commande "fc-match" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-match", "info fc-match" ou "fc-match" pour essayer d'obtenir davantage d'informations.

"fc-pattern" : 
	La commande "fc-pattern" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-pattern", "info fc-pattern" ou "fc-pattern" pour essayer d'obtenir davantage d'informations.

"fc-query" : 
	La commande "fc-query" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-query", "info fc-query" ou "fc-query" pour essayer d'obtenir davantage d'informations.

"fc-scan" : 
	La commande "fc-scan" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-scan", "info fc-scan" ou "fc-scan" pour essayer d'obtenir davantage d'informations.

"fc-validate" : 
	La commande "fc-validate" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fc-validate", "info fc-validate" ou "fc-validate" pour essayer d'obtenir davantage d'informations.

"fcgistarter" : 
	NAME :
		fcgistarter - Start a FastCGI program

"fddist" : 
	NAME :
		fddist - file descriptor usage distributions. Uses
		DTrace.
	DESCRIPTION :
		This prints distributions for read and write events
		by file descriptor, by process. This can be used
		to determine which file descriptor a process is
		doing the most I/O with.

"fdesetup" : 
	NAME :
		fdesetup – FileVault configuration tool
	DESCRIPTION :
		fdesetup is used to enable or disable FileVault,
		to list, add, or remove enabled FileVault users,
		and to obtain status about the current state of
		FileVault. Most commands require root access and
		need to be authenticated with either a FileVault
		password, a personal recovery key (if enabled), and
		in some cases the private key from the installed
		institutional recovery key.  Some status related
		commands can be run from a non-root session.

"fdisk" : 
	NAME :
		fdisk – DOS partition maintenance program
	DESCRIPTION :
		In order for the BIOS to boot the kernel, certain
		conventions must be adhered to.  Sector 0 of a
		bootable hard disk must contain boot code, an MBR
		partition table, and a magic number (0xAA55).  These
		MBR partitions (also known as BIOS partitions) can
		be used to break the disk up into several pieces.

"fg" : 
	DESCRIPTION :
		Déplace une tâche au premier plan. Place JOB_SPEC
		au premier plan et en fait la tâche actuelle. Si
		JOB_SPEC n'est pas fourni, le shell utilise sa
		propre notion de tâche actuelle.

"fgrep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"fi" : 
	La commande "fi" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help fi", "info fi" ou "fi" pour essayer d'obtenir davantage d'informations.

"fibreconfig" : 
	NAME :
		fibreconfig – Tool for configuring settings for
		Fibre Channel controllers and targets.

"fido2-assert" : 
	NAME :
		fido2-assert – get/verify a FIDO2 assertion
	DESCRIPTION :
		fido2-assert gets or verifies a FIDO2 assertion.

"fido2-cred" : 
	NAME :
		fido2-cred – make/verify a FIDO2 credential
	DESCRIPTION :
		fido2-cred makes or verifies a FIDO2 credential.

"fido2-token" : 
	NAME :
		fido2-token – find and manage a FIDO2 authenticator
	DESCRIPTION :
		fido2-token manages a FIDO2 authenticator.

"file" : 
	NAME :
		file – determine file type
	DESCRIPTION :
		This manual page documents version 5.04 of the file
		command.

"filebyproc.d" : 
	NAME :
		filebyproc.d - snoop opens by process name. Uses
		DTrace.
	DESCRIPTION :
		filebyproc.d is a DTrace OneLiner to print file
		pathnames as they are opened, including the name
		of the process calling the open.  A line will be
		printed regardless of whether the open is actually
		successful or not.

"FileCheck" : 
	La commande "FileCheck" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help FileCheck", "info FileCheck" ou "FileCheck" pour essayer d'obtenir davantage d'informations.

"filecoordinationd" : 
	NAME :
		filecoordinationd – system-wide file access
		coordination
	DESCRIPTION :
		filecoordinationd is used by the Foundation framework's
		NSFileCoordinator class to coordinate access to
		files by multiple processes, and to message registered
		NSFilePresenters.

"fileproviderctl" : 
	NAME :
		fileproviderctl - introspect file provider extensions
	DESCRIPTION :
		fileproviderctl allows you to control the fileproviderd
		daemon and enumerate and manipulate files.

"filtercalltree" : 
	NAME :
		filtercalltree – Filter or prune a call tree file
		generated by sample or malloc_history
	DESCRIPTION :
		filtercalltree reads a file containing a call tree,
		as generated by the sample(1) or malloc_history(1)
		commands, and filters or prunes it as specified by
		the options.

"find" : 
	NAME :
		find – walk a file hierarchy
	DESCRIPTION :
		The find utility recursively descends the directory
		tree for each path listed, evaluating an expression
		(composed of the “primaries” and “operands” listed
		below) in terms of each file in the tree.

"find-all-symbols" : 
	La commande "find-all-symbols" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help find-all-symbols", "info find-all-symbols" ou "find-all-symbols" pour essayer d'obtenir davantage d'informations.

"findrule" : 
	NAME :
		findrule - command line wrapper to File::Find::Rule
	DESCRIPTION :
		"findrule" mostly borrows the interface from GNU
		find(1) to provide a command-line interface onto
		the File::Find::Rule heirarchy of modules.

"findrule5.30" : 
	NAME :
		findrule - command line wrapper to File::Find::Rule
	DESCRIPTION :
		"findrule" mostly borrows the interface from GNU
		find(1) to provide a command-line interface onto
		the File::Find::Rule heirarchy of modules.

"finger" : 
	NAME :
		finger – user information lookup program
	DESCRIPTION :
		The finger utility displays information about the
		system users.

"firefox" : 
	La commande "firefox" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help firefox", "info firefox" ou "firefox" pour essayer d'obtenir davantage d'informations.

"firmwarepasswd" : 
	NAME :
		firmwarepasswd – tool for setting and removing
		firmware passwords on a system
	DESCRIPTION :
		The firmwarepasswd command is used to add or remove
		firmware passwords from a system as well as check
		status and other options.  The firmwarepasswd command
		requires root privileges to run.

"fixproc" : 
	NAME :
		fixproc - Fixes a process by performing the specified
		action.
	DESCRIPTION :
		Fixes a process named "proc" by performing the
		specified action.  The actions can be check, kill,
		restart, exist, or fix.  The action is specified
		on the command line or is read from a default
		database, which describes the default action to
		take for each process.  The database format and the
		meaning of each action are described below.

"flex" : 
	NAME :
		flex - the fast lexical analyser generator
	DESCRIPTION :
		Generates programs that perform pattern-matching
		on text.

"flex++" : 
	NAME :
		flex - the fast lexical analyser generator
	DESCRIPTION :
		Generates programs that perform pattern-matching
		on text.

"fmt" : 
	NAME :
		fmt – simple text formatter
	DESCRIPTION :
		The fmt utility is a simple text formatter which
		reads the concatenation of input files (or standard
		input if none are given) and produces on standard
		output a version of its input with lines as close
		to the goal length as possible without exceeding
		the maximum.  The goal length defaults to 65 and
		the maximum to 10 more than the goal length.
		Alternatively, a single width parameter can be
		specified either by prepending a hyphen to it or
		by using -w.  For example, “fmt -w 72”, “fmt -72”,
		and “fmt 72 72” all produce identical output.  The
		spacing at the beginning of the input lines is
		preserved in the output, as are blank lines and
		interword spacing.  Lines are joined or split only
		at white

"fold" : 
	NAME :
		fold – fold long lines for finite width output
		device
	DESCRIPTION :
		The fold utility is a filter which folds the contents
		of the specified files, or the standard input if
		no files are specified, breaking the lines to have
		a maximum of 80 columns.

"fonctions_liste" : 
	La commande "fonctions_liste" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fonctions_liste", "info fonctions_liste" ou "fonctions_liste" pour essayer d'obtenir davantage d'informations.

"fonctions_personnelles" : 
	La commande "fonctions_personnelles" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help fonctions_personnelles", "info fonctions_personnelles" ou "fonctions_personnelles" pour essayer d'obtenir davantage d'informations.

"fontrestore" : 
	NAME :
		fontrestore – Restore the available system fonts
		to a pristine state, if possible.
	DESCRIPTION :
		fontrestore detects fonts in /Library/Fonts,
		/System/Library/Fonts, and ~/Library/Fonts which
		are not part of the system install and moves them
		to /Library/Fonts (Removed), /System/Library/Fonts
		(Removed), and ~/Library/Fonts/Fonts (Removed)
		directories. Critical fonts required for operation
		of the user interface are also restored.

"footprint" : 
	NAME :
		footprint – gathers memory information about one
		or more processes
	DESCRIPTION :
		The footprint utility gathers and displays memory
		consumption information for the specified processes
		or memory graph files.

"for" : 
	La commande "for" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help for", "info for" ou "for" pour essayer d'obtenir davantage d'informations.

"freetype-config" : 
	NAME :
		freetype-config - Get information about a libfreetype
		installation
	DESCRIPTION :
		freetype-config returns information needed for
		compiling and linking programs with the FreeType
		library, such as linker flags and compilation
		parameters.  Alternatively, it can be used to query
		information about the FreeType library version
		installed on the system, such as the installation
		(directory path) prefix or the FreeType version
		number.

"fsck" : 
	NAME :
		fsck – filesystem consistency check and interactive
		repair
	DESCRIPTION :
		The first form of fsck preens a standard set of
		filesystems or the specified filesystems.  It is
		normally used in the script /etc/rc during automatic
		reboot.  Here fsck reads the filesystem descriptor
		table (using getfsent(3)) to determine which
		filesystems to check.  Only partitions that have
		``rw,'' ``rq'' or ``ro'' as options, and that have
		non-zero pass number are checked.  Filesystems with
		pass number 1 (normally just the root filesystem)
		are checked one at a time.  When pass 1 completes,
		all remaining filesystems are checked, running one
		process per disk drive.  The disk drive containing
		each filesystem is inferred from the shortest prefix
		of the device name that ends in one or more digits;
		the

"fsck_apfs" : 
	NAME :
		fsck_apfs – APFS consistency check
	DESCRIPTION :
		The fsck_apfs utility verifies and repairs APFS
		containers and volumes.

"fsck_cs" : 
	NAME :
		fsck_cs – verify and repair CoreStorage logical
		volume groups
	DESCRIPTION :
		The fsck_cs utility verifies and repairs CoreStorage
		logical volume group metadata.

"fsck_exfat" : 
	NAME :
		fsck_exfat – Verify and repair ExFAT file systems.
	DESCRIPTION :
		The fsck_exfat utility verifies and repairs ExFAT
		file systems.

"fsck_hfs" : 
	NAME :
		fsck_hfs – HFS file system consistency check
	DESCRIPTION :
		The fsck_hfs utility verifies and repairs HFS+ file
		systems.

"fsck_msdos" : 
	NAME :
		fsck_msdos – DOS/Windows (FAT) file system consistency
		check
	DESCRIPTION :
		The fsck_msdos utility verifies and repairs FAT
		file systems (more commonly known as DOS file
		systems).

"fsck_udf" : 
	NAME :
		fsck_udf – check a UDF filesystem
	DESCRIPTION :
		The fsck_udf command examines a UDF filesystem
		residing on devicePath for correctness.

"fstyp" : 
	NAME :
		fstyp – identify a file system
	DESCRIPTION :
		The fstyp utility attempts to determine what sort
		of volume is present on the given device.  It
		operates by iterating over directories that are
		typically part of the path, searching for files
		matching the pattern fstyp_* and running them.  If
		it finds a match, it will print out the file system
		type name and exit with a return value of 1.  If
		it does not find a match, it will not print out
		anything, and the return value will be 0.

"fstyp_hfs" : 
	NAME :
		fstyp_hfs – check for an HFS volume
	DESCRIPTION :
		The fstyp_hfs utility is not intended to be run
		directly, but rather called by fstyp(8) while it
		is trying to determine which file system type is
		present on the given device.  It returns 1 if it
		thinks the device contains an HFS volume, and 0
		otherwise.

"fstyp_msdos" : 
	NAME :
		fstyp_msdos – check for an MSDOS FAT volume
	DESCRIPTION :
		The fstyp_msdos utility is not intended to be run
		directly, but rather called by fstyp(8) while it
		is trying to determine which file system type is
		present on the given device.  It returns 1 if it
		thinks the device contains an MSDOS FAT volume, and
		0 otherwise.

"fstyp_ntfs" : 
	NAME :
		fstyp_ntfs – check for a Windows NT File System
		(NTFS) volume
	DESCRIPTION :
		The fstyp_ntfs utility is not intended to be run
		directly, but rather called by fstyp(8) while it
		is trying to determine which file system type is
		present on the given device.  It returns 1 if it
		thinks the device contains a Windows NT File System
		(NTFS) volume, and 0 otherwise.

"fstyp_udf" : 
	NAME :
		fstyp_udf – check for a UDF volume
	DESCRIPTION :
		The fstyp_udf utility is not intended to be run
		directly, but rather called by fstyp(8) while it
		is trying to determine which file system type is
		present on the given device.  It returns 1 if it
		thinks the device contains a UDF volume, and 0
		otherwise.

"fs_usage" : 
	NAME :
		fs_usage – report system calls and page faults
		related to filesystem activity in real-time
	DESCRIPTION :
		The fs_usage utility presents an ongoing display
		of system call usage information pertaining to
		filesystem activity.  It requires root privileges
		due to the kernel tracing facility it uses to
		operate.  By default, the activity monitored includes
		all system processes except the running fs_usage
		process, Terminal, telnetd, telnet, sshd, rlogind,
		tcsh, csh, sh, and zsh.  These defaults can be
		overridden such that output is limited to include
		or exclude a list of processes specified by the
		user.

"function" : 
	La commande "function" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help function", "info function" ou "function" pour essayer d'obtenir davantage d'informations.

"funzip" : 
	NAME :
		funzip - filter for extracting from a ZIP archive
		in a pipe
	DESCRIPTION :
		for more details.  details.  funzip without a file
		argument acts as a filter; that is, it assumes that
		a ZIP archive (or a gzip'd(1) file) is being piped
		into standard input, and it extracts the first
		member from the archive to stdout.

"fuser" : 
	NAME :
		fuser - list process IDs of all processes that have
		one or more files open
	DESCRIPTION :
		The fuser utility shall write to standard output
		the process IDs of processes running on the local
		system that have one or more named files open. For
		block special devices, all processes using any file
		on that device are listed.

"fwkdp" : 
	NAME :
		fwkdp – FireWire KDP Tool
	DESCRIPTION :
		Use fwkdp to act as a proxy for the kernel debugging
		KDP protocol over FireWire. It will also accept
		kernel core dump images transmitted over FireWire.
		Additionally, fwkdp can be used to set the boot-args
		necessary for a target machine which is to be
		debugged.

"fwkpfv" : 
	NAME :
		fwkpfv – FireWire kprintf viewer
	DESCRIPTION :
		Use fwkpfv to receive FireWire kprintf logging.
		FireWireKPrintf redirects "kprintf()" logging to
		FireWire. Kernel printfs or "kprintfs" are used by
		many kernel services as a low level logging mechanism.
		They can also be used in third party kernel extensions.
		FireWire kprintfs are available very early in the
		kernel's startup and right until the cpu is powered
		down at sleep, restarted, or shutdown. Similarly,
		they are available almost as soon as the cpu is
		powered when waking. They can be useful for debugging
		kernel code (including KEXTs), particularly sleep/wake
		issues where the display and/or ethernet is
		unavailable.

"g++" : 
	La commande "g++" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help g++", "info g++" ou "g++" pour essayer d'obtenir davantage d'informations.

"gatherheaderdoc" : 
	NAME :
		gatherheaderdoc – header documentation processor
	DESCRIPTION :
		Gatherheaderdoc processes the headerdoc output in
		directory and creates an index page that links to
		each header's documentation.

"gcc" : 
	La commande "gcc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gcc", "info gcc" ou "gcc" pour essayer d'obtenir davantage d'informations.

"gcore" : 
	NAME :
		gcore – get core images of running processes
	DESCRIPTION :
		The gcore program creates a core file image of the
		process specified by pid.  The resulting core file
		can be used with a debugger, e.g.  lldb(1), to
		examine the state of the process.

"gcov" : 
	NAME :
		llvm-cov - emit coverage information
	DESCRIPTION :
		The llvm-cov tool shows code coverage information
		for programs that are instrumented to emit profile
		data. It can be used to work with gcov-style coverage
		or with clang's instrumentation based profiling.

"gdbus" : 
	La commande "gdbus" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gdbus", "info gdbus" ou "gdbus" pour essayer d'obtenir davantage d'informations.

"gdbus-codegen" : 
	La commande "gdbus-codegen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gdbus-codegen", "info gdbus-codegen" ou "gdbus-codegen" pour essayer d'obtenir davantage d'informations.

"gem" : 
	NAME :
		gem – RubyGems program
	DESCRIPTION :
		RubyGems is a sophisticated package manager for the
		Ruby programming language. On OS X the default
		bindir for gems that install binaries is
		/Library/Ruby/bin.

"gencat" : 
	NAME :
		gencat – NLS catalog compiler
	DESCRIPTION :
		The gencat utility merges the text NLS input files
		input-files... into a formatted message catalog
		file output-file.  The file output-file will be
		created if it does not already exist.  If output-file
		does exist, its messages will be included in the
		new output-file.  If set and message numbers collide,
		the new message text defined in input-files... will
		replace the old message text currently contained
		in output-file.

"genstrings" : 
	La commande "genstrings" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help genstrings", "info genstrings" ou "genstrings" pour essayer d'obtenir davantage d'informations.

"gen_bridge_metadata" : 
	NAME :
		gen_bridge_metadata – Objective-C Bridges Metadata
		Generator
	DESCRIPTION :
		gen_bridge_metadata is a tool that generates bridging
		metadata information for a given framework or set
		of headers. The Objective-C bridges supported in
		Mac OS X, such as PyObjC (Python), read this
		information at runtime.

"getconf" : 
	NAME :
		getconf – retrieve standard configuration variables
	DESCRIPTION :
		The getconf utility prints the values of POSIX or
		X/Open path or system configuration variables to
		the standard output.  If a variable is undefined,
		the string “undefined” is output.

"GetFileInfo" : 
	NAME :
		/usr/bin/GetFileInfo – get attributes of files and
		directories (DEPRECATED)
	DESCRIPTION :
		Tools supporting Carbon development, including
		/usr/bin/GetFileInfo, were deprecated with Xcode
		6.

"getopt" : 
	NAME :
		getopt – parse command options
	DESCRIPTION :
		The getopt utility is used to break up options in
		command lines for easy parsing by shell procedures,
		and to check for legal options.  Optstring is a
		string of recognized option letters (see getopt(3));
		if a letter is followed by a colon, the option is
		expected to have an argument which may or may not
		be separated from it by white space.  The special
		option ‘--’ is used to delimit the end of the
		options.  The getopt utility will place ‘--’ in the
		arguments at the end of the options, or recognize
		it if used explicitly.  The shell arguments ($1 $2
		...) are reset so that each option is preceded by
		a ‘-’ and in its own shell argument; each option
		argument is also in its own shell argument.

"getopts" : 
	DESCRIPTION :
		Analyse les options en arguments. « getopts » est
		utilisé par les procédures du shell pour analyser
		les paramètres de position.

"gettext" : 
	NAME :
		gettext - translate message
	DESCRIPTION :
		The gettext program translates a natural language
		message into the user's language, by looking up the
		translation in a message catalog.

"gettext.sh" : 
	La commande "gettext.sh" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gettext.sh", "info gettext.sh" ou "gettext.sh" pour essayer d'obtenir davantage d'informations.

"gettextize" : 
	NAME :
		gettextize - install or upgrade gettext infrastructure
	DESCRIPTION :
		Prepares a source package to use gettext.

"gif2rgb" : 
	La commande "gif2rgb" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gif2rgb", "info gif2rgb" ou "gif2rgb" pour essayer d'obtenir davantage d'informations.

"gifbuild" : 
	La commande "gifbuild" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gifbuild", "info gifbuild" ou "gifbuild" pour essayer d'obtenir davantage d'informations.

"gifclrmp" : 
	La commande "gifclrmp" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gifclrmp", "info gifclrmp" ou "gifclrmp" pour essayer d'obtenir davantage d'informations.

"giffix" : 
	La commande "giffix" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help giffix", "info giffix" ou "giffix" pour essayer d'obtenir davantage d'informations.

"giftext" : 
	La commande "giftext" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help giftext", "info giftext" ou "giftext" pour essayer d'obtenir davantage d'informations.

"giftool" : 
	La commande "giftool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help giftool", "info giftool" ou "giftool" pour essayer d'obtenir davantage d'informations.

"gimp" : 
	La commande "gimp" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gimp", "info gimp" ou "gimp" pour essayer d'obtenir davantage d'informations.

"gio" : 
	La commande "gio" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gio", "info gio" ou "gio" pour essayer d'obtenir davantage d'informations.

"gio-querymodules" : 
	La commande "gio-querymodules" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gio-querymodules", "info gio-querymodules" ou "gio-querymodules" pour essayer d'obtenir davantage d'informations.

"git" : 
	NAME :
		git - the stupid content tracker
	DESCRIPTION :
		Git is a fast, scalable, distributed revision control
		system with an unusually rich command set that
		provides both high-level operations and full access
		to internals.

"git-clang-format" : 
	La commande "git-clang-format" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help git-clang-format", "info git-clang-format" ou "git-clang-format" pour essayer d'obtenir davantage d'informations.

"git-receive-pack" : 
	NAME :
		git-receive-pack - Receive what is pushed into the
		repository
	DESCRIPTION :
		Invoked by git send-pack and updates the repository
		with the information fed from the remote end.

"git-shell" : 
	NAME :
		git-shell - Restricted login shell for Git-only SSH
		access
	DESCRIPTION :
		This is a login shell for SSH accounts to provide
		restricted Git access. It permits execution only
		of server-side Git commands implementing the pull/push
		functionality, plus custom commands present in a
		subdirectory named git-shell-commands in the user’s
		home directory.

"git-upload-archive" : 
	NAME :
		git-upload-archive - Send archive back to git-archive
	DESCRIPTION :
		Invoked by git archive --remote and sends a generated
		archive to the other end over the Git protocol.

"git-upload-pack" : 
	NAME :
		git-upload-pack - Send objects packed back to
		git-fetch-pack
	DESCRIPTION :
		Invoked by git fetch-pack, learns what objects the
		other side is missing, and sends them after packing.

"git_commit_avec_ou_sans_message" : 
	La commande "git_commit_avec_ou_sans_message" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help git_commit_avec_ou_sans_message", "info git_commit_avec_ou_sans_message" ou "git_commit_avec_ou_sans_message" pour essayer d'obtenir davantage d'informations.

"git_push_avant_deteindre_ordinateur" : 
	La commande "git_push_avant_deteindre_ordinateur" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help git_push_avant_deteindre_ordinateur", "info git_push_avant_deteindre_ordinateur" ou "git_push_avant_deteindre_ordinateur" pour essayer d'obtenir davantage d'informations.

"glib-compile-resources" : 
	La commande "glib-compile-resources" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help glib-compile-resources", "info glib-compile-resources" ou "glib-compile-resources" pour essayer d'obtenir davantage d'informations.

"glib-compile-schemas" : 
	La commande "glib-compile-schemas" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help glib-compile-schemas", "info glib-compile-schemas" ou "glib-compile-schemas" pour essayer d'obtenir davantage d'informations.

"glib-genmarshal" : 
	La commande "glib-genmarshal" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help glib-genmarshal", "info glib-genmarshal" ou "glib-genmarshal" pour essayer d'obtenir davantage d'informations.

"glib-gettextize" : 
	La commande "glib-gettextize" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help glib-gettextize", "info glib-gettextize" ou "glib-gettextize" pour essayer d'obtenir davantage d'informations.

"glib-mkenums" : 
	La commande "glib-mkenums" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help glib-mkenums", "info glib-mkenums" ou "glib-mkenums" pour essayer d'obtenir davantage d'informations.

"gm4" : 
	NAME :
		M4 - macro processor
	DESCRIPTION :
		Process macros in FILEs.  If no FILE or if FILE is
		`-', standard input is read.

"gnumake" : 
	NAME :
		make - GNU make utility to maintain groups of
		programs
	DESCRIPTION :
		The purpose of the make utility is to determine
		automatically which pieces of a large program need
		to be recompiled, and issue the commands to recompile
		them.  The manual describes the GNU implementation
		of make, which was written by Richard Stallman and
		Roland McGrath, and is currently maintained by Paul
		Smith.  Our examples show C programs, since they
		are most common, but you can use make with any
		programming language whose compiler can be run with
		a shell command.  In fact, make is not limited to
		programs.  You can use it to describe any task where
		some files must be updated automatically from others
		whenever the others change.

"gobject-query" : 
	La commande "gobject-query" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gobject-query", "info gobject-query" ou "gobject-query" pour essayer d'obtenir davantage d'informations.

"gperf" : 
	NAME :
		gperf - manual page for gperf 3.0.3
	DESCRIPTION :
		GNU 'gperf' generates perfect hash functions.

"gpt" : 
	NAME :
		gpt – GUID partition table maintenance utility
	DESCRIPTION :
		The gpt utility provides the necessary functionality
		to manipulate GUID partition tables (GPTs), but see
		BUGS below for how and where functionality is
		missing.  The basic usage model of the gpt tool
		follows that of the cvs(1) tool.  The general options
		are described in the following paragraph.  The
		remaining paragraphs describe the individual commands
		with their options.  Here we conclude by mentioning
		that a device is either a special file corresponding
		to a disk-like device or a regular file.  The command
		is applied to each device listed on the command
		line.

"gr2fonttest" : 
	La commande "gr2fonttest" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gr2fonttest", "info gr2fonttest" ou "gr2fonttest" pour essayer d'obtenir davantage d'informations.

"graphicssession" : 
	NAME :
		graphicssession – graphics session initialization
		utility
	DESCRIPTION :
		graphicssession is used by the Apple Mac OS X base
		system to initalize graphics contexts. It should
		not be called directly.

"grep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"gresource" : 
	La commande "gresource" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gresource", "info gresource" ou "gresource" pour essayer d'obtenir davantage d'informations.

"groups" : 
	NAME :
		groups – show group memberships
	DESCRIPTION :
		The groups utility has been obsoleted by the id(1)
		utility, and is equivalent to “id -Gn [user]”.  The
		command “id -p” is suggested for normal interactive
		use.

"gsettings" : 
	La commande "gsettings" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gsettings", "info gsettings" ou "gsettings" pour essayer d'obtenir davantage d'informations.

"gssd" : 
	NAME :
		gssd – Generic Security Services Daemon
	DESCRIPTION :
		gssd provides kernel access to the Generic Security
		Services API (GSS- API).

"gtester" : 
	La commande "gtester" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gtester", "info gtester" ou "gtester" pour essayer d'obtenir davantage d'informations.

"gtester-report" : 
	La commande "gtester-report" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help gtester-report", "info gtester-report" ou "gtester-report" pour essayer d'obtenir davantage d'informations.

"gunzip" : 
	NAME :
		gzip, gunzip, zcat – compression/decompression tool
		using Lempel-Ziv coding (LZ77)
	DESCRIPTION :
		The gzip program compresses and decompresses files
		using Lempel-Ziv coding (LZ77).  If no files are
		specified, gzip will compress from standard input,
		or decompress to standard output.  When in compression
		mode, each file will be replaced with another file
		with the suffix, set by the -S suffix option, added,
		if possible.

"gzcat" : 
	NAME :
		gzip, gunzip, zcat – compression/decompression tool
		using Lempel-Ziv coding (LZ77)
	DESCRIPTION :
		The gzip program compresses and decompresses files
		using Lempel-Ziv coding (LZ77).  If no files are
		specified, gzip will compress from standard input,
		or decompress to standard output.  When in compression
		mode, each file will be replaced with another file
		with the suffix, set by the -S suffix option, added,
		if possible.

"gzexe" : 
	NAME :
		gzexe – create auto-decompressing executables
	DESCRIPTION :
		The gzexe utility uses gzip(1) to compress executables,
		producing executables that decompress on-the-fly
		when executed.  This saves disk space, at the cost
		of slower execution times.  The original executables
		are saved by copying each of them to a file with
		the same name with a ‘~’ suffix appended.  After
		verifying that the compressed executables work as
		expected, the backup files can be removed.

"gzip" : 
	NAME :
		gzip, gunzip, zcat – compression/decompression tool
		using Lempel-Ziv coding (LZ77)
	DESCRIPTION :
		The gzip program compresses and decompresses files
		using Lempel-Ziv coding (LZ77).  If no files are
		specified, gzip will compress from standard input,
		or decompress to standard output.  When in compression
		mode, each file will be replaced with another file
		with the suffix, set by the -S suffix option, added,
		if possible.

"h2ph" : 
	NAME :
		h2ph - convert .h C header files to .ph Perl header
		files
	DESCRIPTION :
		h2ph converts any C header files specified to the
		corresponding Perl header file format.  It is most
		easily run while in /usr/include:

"h2ph5.30" : 
	NAME :
		h2ph - convert .h C header files to .ph Perl header
		files
	DESCRIPTION :
		h2ph converts any C header files specified to the
		corresponding Perl header file format.  It is most
		easily run while in /usr/include:

"h2ph5.34" : 
	NAME :
		h2ph - convert .h C header files to .ph Perl header
		files
	DESCRIPTION :
		h2ph converts any C header files specified to the
		corresponding Perl header file format.  It is most
		easily run while in /usr/include:

"h2xs" : 
	NAME :
		h2xs - convert .h C header files to Perl extensions
	DESCRIPTION :
		h2xs builds a Perl extension from C header files.
		The extension will include functions which can be
		used to retrieve the value of any #define statement
		which was in the C header files.

"h2xs5.30" : 
	NAME :
		h2xs - convert .h C header files to Perl extensions
	DESCRIPTION :
		h2xs builds a Perl extension from C header files.
		The extension will include functions which can be
		used to retrieve the value of any #define statement
		which was in the C header files.

"h2xs5.34" : 
	NAME :
		h2xs - convert .h C header files to Perl extensions
	DESCRIPTION :
		h2xs builds a Perl extension from C header files.
		The extension will include functions which can be
		used to retrieve the value of any #define statement
		which was in the C header files.

"halt" : 
	NAME :
		halt, reboot – stopping and restarting the system
	DESCRIPTION :
		The halt and reboot utilities flush the file system
		cache to disk, send all running processes a SIGTERM
		(and subsequently a SIGKILL) and, respectively,
		halt or restart the system.  The action is logged,
		including entering a shutdown record into the user
		accounting database.

"hash" : 
	DESCRIPTION :
		Mémorise ou affiche l'emplacement des programmes.
		Détermine et mémorise le chemin complet de chaque
		commande NOM.  Si aucun argument n'est donné, une
		information sur les commandes mémorisées est affichée.

"hb-info" : 
	La commande "hb-info" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help hb-info", "info hb-info" ou "hb-info" pour essayer d'obtenir davantage d'informations.

"hb-ot-shape-closure" : 
	La commande "hb-ot-shape-closure" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help hb-ot-shape-closure", "info hb-ot-shape-closure" ou "hb-ot-shape-closure" pour essayer d'obtenir davantage d'informations.

"hb-shape" : 
	La commande "hb-shape" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help hb-shape", "info hb-shape" ou "hb-shape" pour essayer d'obtenir davantage d'informations.

"hb-subset" : 
	La commande "hb-subset" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help hb-subset", "info hb-subset" ou "hb-subset" pour essayer d'obtenir davantage d'informations.

"hb-view" : 
	La commande "hb-view" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help hb-view", "info hb-view" ou "hb-view" pour essayer d'obtenir davantage d'informations.

"hdid" : 
	NAME :
		hdid – historical mechanism for attaching disk
		images
	DESCRIPTION :
		Historically, hdid was both the tool used to attach
		a disk image and the user process used to load and
		decompress disk image data for the hard disk image
		(HDI) driver in Mac OS X.  hdid exists only for
		backwards compatibility.

"hdik" : 
	NAME :
		hdik – lightweight tool to attach and mount disk
		images in-kernel
	DESCRIPTION :
		hdik is a simple tool that can be used to attach
		disk images directly to the DiskImages driver.  The
		end result is functionally similar to passing -kernel
		to hdiutil(1)'s attach verb.  hdik does not rely
		upon the presence of DiskImages or other high-level
		frameworks.

"hdiutil" : 
	NAME :
		hdiutil – manipulate disk images (attach, verify,
		create, etc)
	DESCRIPTION :
		hdiutil uses the DiskImages framework to manipulate
		disk images.  Common verbs include attach, detach,
		verify, create, convert, and compact.

"hdxml2manxml" : 
	NAME :
		hdxml2manxml – HeaderDoc XML to MPGL translator

"head" : 
	NAME :
		head – display first lines of a file
	DESCRIPTION :
		This filter displays the first count lines or bytes
		of each of the specified files, or of the standard
		input if no files are specified.  If count is omitted
		it defaults to 10.

"headerdoc2html" : 
	NAME :
		headerdoc2html – header documentation processor
	DESCRIPTION :
		Headerdoc2html processes the header file or files
		and generates HTML documentation based on
		specially-formatted comments.

"heap" : 
	NAME :
		heap – List all the malloc-allocated buffers in the
		process's heap
	DESCRIPTION :
		heap lists the objects currently allocated on the
		heap of the specified process, as well as summary
		data.  Objects are categorized by class name, type
		(Objective-C, C++, or CFType), and binary image.
		C++ objects are identified by the vtable referenced
		from the start of the object, so with multiple
		inheritance this may not give the precise class of
		the object.

"help" : 
	DESCRIPTION :
		Affiche des informations sur les commandes intégrées.
		Affiche de courts résumés des commandes intégrées.
		Si MOTIF est spécifié, une aide détaillée de toutes
		les commandes correspondantes au MOTIF est affichée,
		sinon la liste des sujets d'aide est affichée.

"hexdump" : 
	NAME :
		hexdump – ASCII, decimal, hexadecimal, octal dump
	DESCRIPTION :
		The hexdump utility is a filter which displays the
		specified files, or the standard input, if no files
		are specified, in a user specified format.

"hidutil" : 
	NAME :
		hidutil – HID event system debug utility
	DESCRIPTION :
		See hidutil [help] for more information.

"history" : 
	DESCRIPTION :
		Affiche ou manipule l'historique. Affiche l'historique
		avec les numéros de lignes en préfixant chaque
		élément modifié d'un « * ».  Un argument égal à N
		limite la liste aux N derniers éléments.

"hiutil" : 
	NAME :
		hiutil – utility for creating and examining Help
		Viewer indices
	DESCRIPTION :
		hiutil is used to create .helpindex files in the
		Latent Symantic Mapping (LSM) help format and
		.cshelpindex files in the Spotlight help format.
		It is also capable of examining Spotlight help
		indices using the modes listed below. To examine
		LSM indices, use a version of hiutil earlier than
		2.0.

"hmaptool" : 
	La commande "hmaptool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help hmaptool", "info hmaptool" ou "hmaptool" pour essayer d'obtenir davantage d'informations.

"host" : 
	NAME :
		host - DNS lookup utility
	DESCRIPTION :
		host is a simple utility for performing DNS lookups.
		It is normally used to convert names to IP addresses
		and vice versa. When no arguments or options are
		given, host prints a short summary of its command
		line arguments and options.

"hostinfo" : 
	NAME :
		hostinfo – host information
	DESCRIPTION :
		The hostinfo command displays information about the
		host system on which the command is executing.  The
		output includes a  kernel version description,
		processor configuration data, available physical
		memory, and various scheduling statistics.

"hostname" : 
	NAME :
		 hostname – set or print name of current host system
	DESCRIPTION :
		The hostname utility prints the name of the current
		host.  The super-user can set the hostname by
		supplying an argument.  To keep the hostname between
		reboots, run ‘scutil --set HostName name-of-host’.

"hotspot.d" : 
	NAME :
		hotspot.d - print disk event by location. Uses
		DTrace.
	DESCRIPTION :
		hotspot.d is a simple DTrace script to determine
		if disk activity is occuring in the one place - a
		"hotspot". This helps us understand the system's
		usage of a disk, it does not imply that the existance
		or not of a hotspot is good or bad (often may be
		good, less seeking).

"hpmdiagnose" : 
	NAME :
		hpmdiagnose - collects diagnostic information to
		help troubleshoot USB-C issues.
	DESCRIPTION :
		Use hpmdiagnose to collect diagnostic information
		to help with investigation of USB-C issues.

"htcacheclean" : 
	NAME :
		htcacheclean - Clean up the disk cache

"htdbm" : 
	NAME :
		htdbm - Manipulate DBM password databases

"htdigest" : 
	NAME :
		htdigest - manage user files for digest authentication

"htmltree" : 
	NAME :
		htmltree - Parse the given HTML file(s) and dump
		the parse tree

"htmltree5.30" : 
	NAME :
		htmltree - Parse the given HTML file(s) and dump
		the parse tree

"htpasswd" : 
	NAME :
		htpasswd - Manage user files for basic authentication

"httpd" : 
	NAME :
		httpd - Apache Hypertext Transfer Protocol Server

"httpd-wrapper" : 
	NAME :
		httpd-wrapper – Wrapper script for httpd web server
	DESCRIPTION :
		This tool extracts environment variable definitions
		from a specific plist file and then execs the httpd
		executable, thereby allowing variables to be used
		in Apache's paramaterized config files. It also
		gathers minimal metrics on the usage of Apache.

"httxt2dbm" : 
	NAME :
		httxt2dbm - Generate dbm files for use with RewriteMap

"ibd2sdi" : 
	NAME :
		ibd2sdi - InnoDB utility for extracting serialized
		dictionary information (SDI) from an InnoDB tablespace
	DESCRIPTION :
		ibd2sdi is a utility for extracting serialized
		dictionary information (SDI) from InnoDB tablespace
		files. SDI data is present in all persistent InnoDB
		tablespace files.

"ibtool" : 
	La commande "ibtool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ibtool", "info ibtool" ou "ibtool" pour essayer d'obtenir davantage d'informations.

"iconutil" : 
	NAME :
		iconutil – Utility to convert between '.iconset'
		and '.icns' files.
	DESCRIPTION :
		iconutil converts between '.iconset' and '.icns'
		files, and can extract icons from '.car' asset
		catalog files. The tool takes a single source '.icns'
		file, '.iconset' or a '.car' file and icon name.
		It converts the input to either a '.icns' or
		'.iconset' depending on the value of the -c flag's
		argument.  It is possible to specify the name of
		the output file by passing the file path as the
		argument to the -o flag. If -o is not set iconutil
		will write converted '.icns' file or '.iconset' to
		the same directory as the source file using either
		the name of the icon in the '.car' or the input
		file name with the extension derived from the output
		type.

"iconv" : 
	NAME :
		iconv - character set conversion
	DESCRIPTION :
		The iconv program converts text from one encoding
		to another encoding. More precisely, it converts
		from the encoding given for the -f option to the
		encoding given for the -t option. Either of these
		encodings defaults to the encoding of the current
		locale. All the inputfiles are read and converted
		in turn; if no inputfile is given, the standard
		input is used. The converted text is printed to
		standard output.

"ictool" : 
	La commande "ictool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ictool", "info ictool" ou "ictool" pour essayer d'obtenir davantage d'informations.

"id" : 
	NAME :
		id – return user identity
	DESCRIPTION :
		The id utility displays the user and group names
		and numeric IDs, of the calling process, to the
		standard output.  If the real and effective IDs are
		different, both are displayed, otherwise only the
		real ID is displayed.

"idle3" : 
	La commande "idle3" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help idle3", "info idle3" ou "idle3" pour essayer d'obtenir davantage d'informations.

"idle3.11" : 
	La commande "idle3.11" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help idle3.11", "info idle3.11" ou "idle3.11" pour essayer d'obtenir davantage d'informations.

"idlj" : 
	La commande "idlj" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help idlj", "info idlj" ou "idlj" pour essayer d'obtenir davantage d'informations.

"idn" : 
	NAME :
		idn - Internationalized Domain Names command line
		tool
	DESCRIPTION :
		Internationalized Domain Name (IDN) convert STRINGS,
		or standard input.

"if" : 
	La commande "if" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help if", "info if" ou "if" pour essayer d'obtenir davantage d'informations.

"ifconfig" : 
	NAME :
		ifconfig – configure network interface parameters
	DESCRIPTION :
		The ifconfig utility is used to assign an address
		to a network interface and/or configure network
		interface parameters.

"ifnames" : 
	NAME :
		 ifnames - Extract CPP conditionals from a set of
		 files
	DESCRIPTION :
		Scan all of the C source FILES (or the standard
		input, if none are given) and write to the standard
		output a sorted list of all the identifiers that
		appear in those files in '#if', '#elif', '#ifdef',
		or '#ifndef' directives.  Print each identifier on
		a line, followed by a space-separated list of the
		files in which that identifier occurs.

"imptrace" : 
	NAME :
		imptrace – report importance donation events in
		real time
	DESCRIPTION :
		The imptrace utility displays a trace of importance
		donation events. Importance donation is used by
		adaptive jobs on the system to manage their priority
		on the system. See xpc_transaction_begin(3) and
		launchd.plist(5) for more information about the
		mechanism and its use.

"in" : 
	La commande "in" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help in", "info in" ou "in" pour essayer d'obtenir davantage d'informations.

"indent" : 
	NAME :
		indent – indent and format C program source
	DESCRIPTION :
		indent is a C program formatter.  It reformats the
		C program in the input_file according to the switches.
		The switches which can be specified are described
		below.  They may appear before or after the file
		names.

"infocmp" : 
	NAME :
		infocmp - compare or print out terminfo descriptions
	DESCRIPTION :
		infocmp can be used to compare a binary terminfo
		entry with other terminfo entries, rewrite a terminfo
		description to take advantage of the use= terminfo
		field, or print out a terminfo description from the
		binary file (term) in a variety of formats.  In all
		cases, the boolean fields will be printed first,
		followed by the numeric fields, followed by the
		string fields.

"infotocap" : 
	NAME :
		infotocap - convert a terminfo description into a
		termcap description
	DESCRIPTION :
		infotocap looks in file for terminfo descriptions.
		For each one found, an equivalent termcap description
		is written to standard output. Terminfo use
		capabilities are translated directly to termcap tc
		capabilities.

"innochecksum" : 
	NAME :
		innochecksum - offline InnoDB file checksum utility
	DESCRIPTION :
		innochecksum prints checksums for InnoDB files.
		This tool reads an InnoDB tablespace file, calculates
		the checksum for each page, compares the calculated
		checksum to the stored checksum, and reports
		mismatches, which indicate damaged pages. It was
		originally developed to speed up verifying the
		integrity of tablespace files after power outages
		but can also be used after file copies. Because
		checksum mismatches cause InnoDB to deliberately
		shut down a running server, it may be preferable
		to use this tool rather than waiting for an
		in-production server to encounter the damaged pages.

"install" : 
	NAME :
		install – install binaries
	DESCRIPTION :
		The file(s) are copied to the target file or
		directory.  If the destination is a directory, then
		the file is copied into directory with its original
		filename.  If the target file already exists, it
		is either renamed to file.old if the -b option is
		given or overwritten if permissions allow.  An
		alternate backup suffix may be specified via the
		-B option's argument.

"installer" : 
	NAME :
		installer – system software and package installer
		tool.
	DESCRIPTION :
		The installer command is used to install macOS
		installer packages to a specified domain or volume.
		The installer command installs a single package per
		invocation, which is specified with the -package
		parameter ( -pkg is accepted as a synonym).  It may
		be either a single package or a metapackage.  In
		the case of the metapackage, the packages which are
		part of the default install will be installed unless
		disqualified by a package's check tool(s).

"install_name_tool" : 
	NAME :
		 install_name_tool - change dynamic shared library
		 install names
	DESCRIPTION :
		Install_name_tool changes the dynamic shared library
		install names and or adds, changes or deletes the
		rpaths recorded in a Mach-O binary. For this tool
		to work when the install names or rpaths are larger
		the binary should be built with the ld(1)
		-headerpad_max_install_names option.

"instmodsh" : 
	NAME :
		instmodsh - A shell to examine installed modules
	DESCRIPTION :
		A little interface to ExtUtils::Installed to examine
		installed modules, validate your packlists and even
		create a tarball from an installed module.

"instmodsh5.30" : 
	NAME :
		instmodsh - A shell to examine installed modules
	DESCRIPTION :
		A little interface to ExtUtils::Installed to examine
		installed modules, validate your packlists and even
		create a tarball from an installed module.

"instmodsh5.34" : 
	NAME :
		instmodsh - A shell to examine installed modules
	DESCRIPTION :
		A little interface to ExtUtils::Installed to examine
		installed modules, validate your packlists and even
		create a tarball from an installed module.

"intercept-build" : 
	La commande "intercept-build" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help intercept-build", "info intercept-build" ou "intercept-build" pour essayer d'obtenir davantage d'informations.

"IOAccelMemory" : 
	La commande "IOAccelMemory" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help IOAccelMemory", "info IOAccelMemory" ou "IOAccelMemory" pour essayer d'obtenir davantage d'informations.

"ioalloccount" : 
	NAME :
		ioalloccount – Summarize IOKit memory usage.
	DESCRIPTION :
		ioalloccount displays some accounting of memory
		allocated by IOKit allocators, including object
		instances, in the kernel.

"ioclasscount" : 
	DESCRIPTION :
		ioclasscount displays the instance counts of
		OSObject-based C++ classes in the kernel, incremented
		by the number of direct subclasses that have an
		instance count of at least 1.  For example, if
		IONetworkController is not directly instantiated,
		but its direct subclass IOEthernetController has
		any instances, then IONetworkController's instance
		count will be at least 1; if another direct subclass
		such as IOFWController has any instances, then
		IONetworkController's instance count will be at
		least 2; and so on.  (This modification of instance
		counts prevents unloading of kexts defining
		superclasses that have no instances, but whose
		subclasses in other kexts have instances.)

"iofile.d" : 
	NAME :
		iofile.d - I/O wait time by file and process. Uses
		DTrace.
	DESCRIPTION :
		This prints the total I/O wait times for each
		filename by process. This can help determine why
		an application is performing poorly by identifying
		which file they are waiting on, and the total times.
		Both disk and NFS I/O are measured.

"iofileb.d" : 
	NAME :
		iofileb.d - I/O bytes by file and process. Uses
		DTrace.
	DESCRIPTION :
		This prints a summary of requested disk activity
		by pathname, providing totals of the I/O events in
		bytes. It is a companion to the iofile.d script -
		which prints in terms of I/O wait time, not bytes.
		I/O wait time is a better metric for understanding
		performance issues.  Both disk and NFS I/O are
		measured.

"IOMFB_FDR_Loader" : 
	La commande "IOMFB_FDR_Loader" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help IOMFB_FDR_Loader", "info IOMFB_FDR_Loader" ou "IOMFB_FDR_Loader" pour essayer d'obtenir davantage d'informations.

"iopattern" : 
	NAME :
		iopattern - print disk I/O pattern. Uses DTrace.
	DESCRIPTION :
		This prints details on the I/O access pattern for
		the disks, such as percentage of events that were
		of a random or sequential nature.  By default totals
		for all disks are printed.

"iopending" : 
	NAME :
		iopending - plot number of pending disk events.
		Uses DTrace.
	DESCRIPTION :
		This samples the number of disk events that are
		pending and plots a distribution graph. By doing
		this the "serialness" or "parallelness" of disk
		behaviour can be distinguished.  A high occurance
		of a pending value of more than 1 is an indication
		of saturation.

"ioreg" : 
	NAME :
		ioreg – show I/O Kit registry
	DESCRIPTION :
		ioreg displays the I/O Kit registry.  It shows the
		hierarchical registry structure as an inverted tree.
		The provider-client relationships among those objects
		is shown as follows:

"IOSDebug" : 
	La commande "IOSDebug" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help IOSDebug", "info IOSDebug" ou "IOSDebug" pour essayer d'obtenir davantage d'informations.

"iosnoop" : 
	NAME :
		iosnoop - snoop I/O events as they occur. Uses
		DTrace.
	DESCRIPTION :
		iosnoop prints I/O events as they happen, with
		useful details such as UID, PID, block number, size,
		filename, etc.

"iostat" : 
	NAME :
		iostat – report I/O statistics
	DESCRIPTION :
		The iostat utility displays kernel I/O statistics
		on terminal, device and cpu operations.  The first
		statistics that are printed are averaged over the
		system uptime.  To get information about the current
		activity, a suitable wait time should be specified,
		so that the subsequent sets of printed statistics
		will be averaged over that time.

"iotop" : 
	NAME :
		iotop - display top disk I/O events by process.
		Uses DTrace.
	DESCRIPTION :
		iotop tracks disk I/O by process, and prints a
		summary report that is refreshed every interval.

"ip2cc" : 
	NAME :
		ip2cc - lookup country from IP address or hostname
	DESCRIPTION :
		Ip2cc is a program to lookup countries of IP
		addresses. Ip2cc has two modes: interactive and
		non-interactive.  Interactive mode allows the user
		to query more than one hostname.  Non-interactive
		mode is used to print just the country for a single
		host.

"ip2cc5.30" : 
	NAME :
		ip2cc - lookup country from IP address or hostname
	DESCRIPTION :
		Ip2cc is a program to lookup countries of IP
		addresses. Ip2cc has two modes: interactive and
		non-interactive.  Interactive mode allows the user
		to query more than one hostname.  Non-interactive
		mode is used to print just the country for a single
		host.

"ipconfig" : 
	NAME :
		ipconfig – view and control IP configuration state
	DESCRIPTION :
		ipconfig is a utility that communicates with the
		IPConfiguration agent to retrieve and set IP
		configuration parameters.  It should only be used
		in a test and debug context.  Using it for any other
		purpose is strongly discouraged. Public API's in
		the SystemConfiguration framework are currently the
		only supported way to access and control the state
		of IPConfiguration.

"ipcount" : 
	La commande "ipcount" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ipcount", "info ipcount" ou "ipcount" pour essayer d'obtenir davantage d'informations.

"ipcount5.30" : 
	La commande "ipcount5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ipcount5.30", "info ipcount5.30" ou "ipcount5.30" pour essayer d'obtenir davantage d'informations.

"ipcrm" : 
	NAME :
		ipcrm – remove the specified message queues, semaphore
		sets, and shared segments
	DESCRIPTION :
		The ipcrm utility removes the specified message
		queues, semaphores and shared memory segments.
		These System V IPC objects can be specified by their
		creation ID or any associated key.

"ipcs" : 
	NAME :
		ipcs – report System V interprocess communication
		facilities status
	DESCRIPTION :
		The ipcs utility provides information on System V
		interprocess communication (IPC) facilities on the
		system.

"iperf3-darwin" : 
	NAME :
		iperf3-darwin – perform network throughput tests
	DESCRIPTION :
		The iperf3-darwin is a tool for performing network
		throughput measurements based on the ESnet iperf3
		project that has been modified to use functionalities
		specific to Darwin.

"ippeveprinter" : 
	NAME :
		ippeveprinter - an ipp everywhere printer application
		for cups
	DESCRIPTION :
		ippeveprinter is a simple Internet Printing Protocol
		(IPP) server conforming to the IPP Everywhere (PWG
		5100.14) specification. It can be used to test
		client software or act as a very basic print server
		that runs a command for every job that is printed.

"ippfind" : 
	NAME :
		ippfind - find internet printing protocol printers
	DESCRIPTION :
		ippfind finds services registered with a DNS server
		or available through local devices.  Its primary
		purpose is to find IPP printers and show their URIs,
		show their current status, or run commands.

"ipptool" : 
	NAME :
		ipptool - perform internet printing protocol requests
	DESCRIPTION :
		ipptool sends IPP requests to the specified printer-uri
		and tests and/or displays the results.  Each named
		testfile defines one or more requests, including
		the expected response status, attributes, and values.
		Output is either a plain text, formatted text, CSV,
		or XML report on the standard output, with a non-zero
		exit status indicating that one or more tests have
		failed.  The testfile format is described in
		ipptoolfile(5).

"iptab" : 
	La commande "iptab" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help iptab", "info iptab" ou "iptab" pour essayer d'obtenir davantage d'informations.

"iptab5.30" : 
	La commande "iptab5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help iptab5.30", "info iptab5.30" ou "iptab5.30" pour essayer d'obtenir davantage d'informations.

"irb" : 
	NAME :
		irb – Interactive Ruby Shell
	DESCRIPTION :
		irb is the REPL(read-eval-print loop) environment
		for Ruby programs.

"jar" : 
	NAME :
		jar - create an archive for classes and resources,
		and manipulate or restore individual classes or
		resources from an archive
	DESCRIPTION :
		The jar command is a general-purpose archiving and
		compression tool, based on the ZIP and ZLIB compression
		formats.  Initially, the jar command was designed
		to package Java applets (not supported since JDK
		11) or applications; however, beginning with JDK
		9, users can use the jar command to create modular
		JARs.  For transportation and deployment, it's
		usually more convenient to package modules as modular
		JARs.

"jarsigner" : 
	NAME :
		jarsigner - sign and verify Java Archive (JAR) files
	DESCRIPTION :
		The jarsigner tool has two purposes:

"java" : 
	NAME :
		java - launch a Java application
	DESCRIPTION :
		The java command starts a Java application.  It
		does this by starting the Java Virtual Machine
		(JVM), loading the specified class, and calling
		that class's main() method.  The method must be
		declared public and static, it must not return any
		value, and it must accept a String array as a
		parameter.  The method declaration has the following
		form:

"javac" : 
	NAME :
		javac - read Java declarations and compile them
		into class files
	DESCRIPTION :
		The javac command reads source files that contain
		module, package and type declarations written in
		the Java programming language, and compiles them
		into class files that run on the Java Virtual
		Machine.

"javadoc" : 
	NAME :
		javadoc - generate HTML pages of API documentation
		from Java source files
	DESCRIPTION :
		The javadoc tool parses the declarations and
		documentation comments in a set of Java source files
		and produces corresponding HTML pages that describe
		(by default) the public and protected classes,
		nested classes (but not anonymous inner classes),
		interfaces, constructors, methods, and fields.  You
		can use the javadoc tool to generate the API
		documentation or the implementation documentation
		for a set of source files.

"javah" : 
	La commande "javah" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help javah", "info javah" ou "javah" pour essayer d'obtenir davantage d'informations.

"javap" : 
	NAME :
		javap - disassemble one or more class files
	DESCRIPTION :
		The javap command disassembles one or more class
		files.  The output depends on the options used.
		When no options are used, the javap command prints
		the protected and public fields, and methods of the
		classes passed to it.

"javapackager" : 
	La commande "javapackager" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help javapackager", "info javapackager" ou "javapackager" pour essayer d'obtenir davantage d'informations.

"javaws" : 
	La commande "javaws" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help javaws", "info javaws" ou "javaws" pour essayer d'obtenir davantage d'informations.

"jbig2dec" : 
	NAME :
		jbig2dec - File format converter specialized in
		JBIG2 decoding
	DESCRIPTION :
		The jbig2dec command converts JBIG2 files to png
		or pbm files.

"jcmd" : 
	NAME :
		jcmd - send diagnostic command requests to a running
		Java Virtual Machine (JVM)
	DESCRIPTION :
		The jcmd utility is used to send diagnostic command
		requests to the JVM.  It must be used on the same
		machine on which the JVM is running, and have the
		same effective user and group identifiers that were
		used to launch the JVM.  Each diagnostic command
		has its own set of arguments.  To display the
		description, syntax, and a list of available arguments
		for a diagnostic command, use the name of the command
		as the argument.  For example:

"jconsole" : 
	NAME :
		jconsole - start a graphical console to monitor and
		manage Java applications
	DESCRIPTION :
		The jconsole command starts a graphical console
		tool that lets you monitor and manage Java applications
		and virtual machines on a local or remote machine.

"jcontrol" : 
	La commande "jcontrol" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help jcontrol", "info jcontrol" ou "jcontrol" pour essayer d'obtenir davantage d'informations.

"jdb" : 
	NAME :
		jdb - find and fix bugs in Java platform programs
	DESCRIPTION :
		The Java Debugger (JDB) is a simple command-line
		debugger for Java classes.  The jdb command and its
		options call the JDB.  The jdb command demonstrates
		the Java Platform Debugger Architecture and provides
		inspection and debugging of a local or remote JVM.

"jdeprscan" : 
	NAME :
		jdeprscan - static analysis tool that scans a jar
		file (or some other aggregation of class files) for
		uses of deprecated API elements
	DESCRIPTION :
		The jdeprscan tool is a static analysis tool provided
		by the JDK that scans a JAR file or some other
		aggregation of class files for uses of deprecated
		API elements.  The deprecated APIs identified by
		the jdeprscan tool are only those that are defined
		by Java SE.  Deprecated APIs defined by third-party
		libraries aren't reported.

"jdeps" : 
	NAME :
		jdeps - launch the Java class dependency analyzer
	DESCRIPTION :
		The jdeps command shows the package-level or
		class-level dependencies of Java class files.  The
		input class can be a path name to a .class file, a
		directory, a JAR file, or it can be a fully qualified
		class name to analyze all class files.  The options
		determine the output.  By default, the jdeps command
		writes the dependencies to the system output.  The
		command can generate the dependencies in DOT language
		(see the -dotoutput option).

"jfr" : 
	NAME :
		jfr - parse and print Flight Recorder files
	DESCRIPTION :
		The jfr command provides a tool for interacting
		with flight recorder files (.jfr).  The main function
		is to filter, summarize and output flight recording
		files into human readable format.  There is also
		support for merging and splitting recording files.

"jhat" : 
	La commande "jhat" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help jhat", "info jhat" ou "jhat" pour essayer d'obtenir davantage d'informations.

"jhsdb" : 
	NAME :
		jhsdb - attach to a Java process or launch a
		postmortem debugger to analyze the content of a
		core dump from a crashed Java Virtual Machine (JVM)
	DESCRIPTION :
		You can use the jhsdb tool to attach to a Java
		process or to launch a postmortem debugger to analyze
		the content of a core-dump from a crashed Java
		Virtual Machine (JVM).  This command is experimental
		and unsupported.

"jimage" : 
	La commande "jimage" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help jimage", "info jimage" ou "jimage" pour essayer d'obtenir davantage d'informations.

"jinfo" : 
	NAME :
		jinfo - generate Java configuration information for
		a specified Java process
	DESCRIPTION :
		The jinfo command prints Java configuration information
		for a specified Java process.  The configuration
		information includes Java system properties and JVM
		command-line flags.  If the specified process is
		running on a 64-bit JVM, then you might need to
		specify the -J-d64 option, for example:

"jjs" : 
	La commande "jjs" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help jjs", "info jjs" ou "jjs" pour essayer d'obtenir davantage d'informations.

"jlink" : 
	NAME :
		jlink - assemble and optimize a set of modules and
		their dependencies into a custom runtime image
	DESCRIPTION :
		The jlink tool links a set of modules, along with
		their transitive dependences, to create a custom
		runtime image.

"jmap" : 
	NAME :
		jmap - print details of a specified process
	DESCRIPTION :
		The jmap command prints details of a specified
		running process.

"jmc" : 
	La commande "jmc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help jmc", "info jmc" ou "jmc" pour essayer d'obtenir davantage d'informations.

"jmod" : 
	NAME :
		jmod - create JMOD files and list the content of
		existing JMOD files
	DESCRIPTION :
		Note: For most development tasks, including deploying
		modules on the module path or publishing them to a
		Maven repository, continue to package modules in
		modular JAR files.  The jmod tool is intended for
		modules that have native libraries or other
		configuration files or for modules that you intend
		to link, with the jlink tool, to a runtime image.

"jobs" : 
	DESCRIPTION :
		Affiche l'état des tâches. Affiche la liste des
		tâches actives.  JOBSPEC restreint l'affichage à
		cette tâche. S'il n'y a pas d'option, l'état de
		toutes les tâches actives est affiché.

"join" : 
	NAME :
		join – relational database operator
	DESCRIPTION :
		The join utility performs an “equality join” on the
		specified files and writes the result to the standard
		output.  The “join field” is the field in each file
		by which the files are compared.  The first field
		in each line is used by default.  There is one line
		in the output for each pair of lines in file1 and
		file2 which have identical join fields.  Each output
		line consists of the join field, the remaining
		fields from file1 and then the remaining fields
		from file2.

"jot" : 
	NAME :
		jot – print sequential or random data
	DESCRIPTION :
		The jot utility is used to print out increasing,
		decreasing, random, or redundant data, usually
		numbers, one per line.

"jpackage" : 
	NAME :
		jpackage - tool for packaging self-contained Java
		applications.
	DESCRIPTION :
		The jpackage tool will take as input a Java application
		and a Java run- time image, and produce a Java
		application image that includes all the necessary
		dependencies.  It will be able to produce a native
		package in a platform-specific format, such as an
		exe on Windows or a dmg on macOS.  Each format must
		be built on the platform it runs on, there is no
		cross-platform support.  The tool will have options
		that allow packaged applications to be customized
		in various ways.

"jpegtran" : 
	NAME :
		jpegtran - lossless transformation of JPEG files
	DESCRIPTION :
		jpegtran performs various useful transformations
		of JPEG files.  It can translate the coded
		representation from one variant of JPEG to another,
		for example from baseline JPEG to progressive JPEG
		or vice versa.  It can also perform some rearrangements
		of the image data, for example turning an image
		from landscape to portrait format by rotation.

"jpgicc" : 
	NAME :
		jpgicc - little cms ICC profile applier for JPEG.
	DESCRIPTION :
		lcms is a standalone CMM engine, which deals with
		the color management. It implements a fast
		transformation between ICC profiles.  jpgicc is a
		little cms ICC profile applier for JPEG.

"jps" : 
	NAME :
		jps - list the instrumented JVMs on the target
		system
	DESCRIPTION :
		The jps command lists the instrumented Java HotSpot
		VMs on the target system.  The command is limited
		to reporting information on JVMs for which it has
		the access permissions.

"jrunscript" : 
	NAME :
		jrunscript - run a command-line script shell that
		supports interactive and batch modes
	DESCRIPTION :
		The jrunscript command is a language-independent
		command-line script shell.  The jrunscript command
		supports both an interactive (read-eval- print)
		mode and a batch (-f option) mode of script execution.
		By default, JavaScript is the language used, but
		the -l option can be used to specify a different
		language.  By using Java to scripting language
		communication, the jrunscript command supports an
		exploratory programming style.

"jsadebugd" : 
	La commande "jsadebugd" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help jsadebugd", "info jsadebugd" ou "jsadebugd" pour essayer d'obtenir davantage d'informations.

"jshell" : 
	NAME :
		jshell - interactively evaluate declarations,
		statements, and expressions of the Java programming
		language in a read-eval-print loop (REPL)
	DESCRIPTION :
		JShell provides a way to interactively evaluate
		declarations, statements, and expressions of the
		Java programming language, making it easier to learn
		the language, explore unfamiliar code and APIs, and
		prototype complex code.  Java statements, variable
		definitions, method definitions, class definitions,
		import statements, and expressions are accepted.
		The bits of code entered are called snippets.

"json_pp" : 
	NAME :
		json_pp - JSON::PP command utility
	DESCRIPTION :
		json_pp converts between some input and output
		formats (one of them is JSON).  This program was
		copied from json_xs and modified.

"json_pp5.30" : 
	NAME :
		json_pp - JSON::PP command utility
	DESCRIPTION :
		json_pp converts between some input and output
		formats (one of them is JSON).  This program was
		copied from json_xs and modified.

"json_pp5.34" : 
	NAME :
		json_pp - JSON::PP command utility
	DESCRIPTION :
		json_pp converts between some input and output
		formats (one of them is JSON).  This program was
		copied from json_xs and modified.

"json_xs" : 
	NAME :
		json_xs - JSON::XS commandline utility
	DESCRIPTION :
		json_xs converts between some input and output
		formats (one of them is JSON).

"json_xs5.30" : 
	NAME :
		json_xs - JSON::XS commandline utility
	DESCRIPTION :
		json_xs converts between some input and output
		formats (one of them is JSON).

"jstack" : 
	NAME :
		jstack - print Java stack traces of Java threads
		for a specified Java process
	DESCRIPTION :
		The jstack command prints Java stack traces of Java
		threads for a specified Java process.  For each
		Java frame, the full class name, method name, byte
		code index (BCI), and line number, when available,
		are printed.  C++ mangled names aren't demangled.
		To demangle C++ names, the output of this command
		can be piped to c++filt.  When the specified process
		is running on a 64-bit JVM, you might need to specify
		the -J-d64 option, for example: jstack -J-d64 pid.

"jstat" : 
	NAME :
		jstat - monitor JVM statistics
	DESCRIPTION :
		The jstat command displays performance statistics
		for an instrumented Java HotSpot VM.  The target
		JVM is identified by its virtual machine identifier,
		or vmid option.

"jstatd" : 
	NAME :
		jstatd - monitor the creation and termination of
		instrumented Java HotSpot VMs
	DESCRIPTION :
		The jstatd command is an RMI server application
		that monitors for the creation and termination of
		instrumented Java HotSpot VMs and provides an
		interface to enable remote monitoring tools, jstat
		and jps, to attach to JVMs that are running on the
		local host and collect information about the JVM
		process.

"jvisualvm" : 
	La commande "jvisualvm" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help jvisualvm", "info jvisualvm" ou "jvisualvm" pour essayer d'obtenir davantage d'informations.

"jwebserver" : 
	NAME :
		jwebserver - launch the Java Simple Web Server
	DESCRIPTION :
		The jwebserver tool provides a minimal HTTP server,
		designed to be used for prototyping, testing, and
		debugging.  It serves a single directory hierarchy,
		and only serves static files.  Only HTTP/1.1 is
		supported; HTTP/2 and HTTPS are not supported.

"kadmin" : 
	NAME :
		kadmin – Kerberos administration utility
	DESCRIPTION :
		The kadmin program is used to make modifications
		to the Kerberos database, either remotely via the
		kadmind(8) daemon, or locally (with the -l option).

"kadmin.local" : 
	NAME :
		kadmin.local – compatiblity shim for MIT Kerberos
		kadmin.local
	DESCRIPTION :
		kadmin.local kadmin.local is deprecated, use kadmin
		-l instead.

"kcc" : 
	NAME :
		kcc – Kerberos credential cache tools

"kcditto" : 
	NAME :
		kcditto – macOS kernel management helper tool
	DESCRIPTION :
		kcditto is a helper tool for the staging of kext
		collection files.  It looks for kext collection
		files on volume where the kcditto binary exists,
		and copies all the boot kext collections into the
		preboot volume where efiboot can find them.

"kdcsetup" : 
	NAME :
		kdcsetup – Kerberos – Open Directory Single Sign
		On
	DESCRIPTION :
		kdcsetup is a tool for configuring an Apple Open
		Directory KDC. It creates the needed Kerberos files
		and data for an Open Director master.

"kdestroy" : 
	NAME :
		kdestroy – remove one credential or destroy the
		current ticket file
	DESCRIPTION :
		kdestroy removes one credential or the current set
		of tickets.

"KernelEventAgent" : 
	NAME :
		KernelEventAgent – utility to provide user interface
		on behalf of the kernel
	DESCRIPTION :
		The KernelEventAgent utility is responsible for
		displaying disk full and unresponsive file server
		messages.  It is not intended to be run directly,
		but rather started by the system.  The few options
		it has are intended as debugging aids.  Typically,
		allowing the system to start KernelEventAgent by
		itself, without any additional options, is the ideal
		configuration.

"kextcache" : 
	NAME :
		kextcache – create kext cache files
	DESCRIPTION :
		The kextcache program creates kext caches, which
		speed up kext loading operations.  It is invoked
		automatically as needed to rebuild system caches.

"kextfind" : 
	NAME :
		kextfind – find kernel extensions (kexts) based on
		a variety of criteria and print information
	DESCRIPTION :
		The kextfind utility locates and prints information,
		or generates reports, about kernel extensions (kexts)
		matching the search criteria in query from among
		those in the named directory and extension arguments.
		If no directories or extensions are specified,
		kextfind searches /System/Library/Extensions and
		/Library/Extensions.  Searches are performed via
		kext management logic as used by kextload(8) and
		kextd(8), by which only kexts directly in the
		repository directory or kexts explicitly named (and
		their immediate plugins) are eligible; this is
		specifically not an exhaustive, recursive filesystem
		search.

"kextlibs" : 
	NAME :
		kextlibs – find OSBundleLibraries needed by a kext
	DESCRIPTION :
		The kextlibs utility searches for library kexts
		that define symbols needed for linking by kext,
		printing their bundle identifiers and versions to
		stdout.  If the kext has a multiple-architecture
		executable, libraries are resolved for each
		architecture.  If any symbols are not found, or are
		found in multiple libraries, the numbers of such
		symbols are printed to standard error after the
		library kext information for each architecture.

"kextload" : 
	NAME :
		kextload – load kernel extensions (kexts) into the
		kernel
	DESCRIPTION :
		The kextload program is used to explicitly load
		kernel extensions (kexts).  For most kexts, kextload
		must run as the superuser (root). Kexts installed
		under /System/ with an OSBundleAllowUserLoad property
		set to true may be loaded via kextload by non-root
		users.

"kextstat" : 
	NAME :
		kextstat – display status of loaded kernel extensions
		(kexts)
	DESCRIPTION :
		The kextstat utility displays the status of any
		kexts currently loaded in the kernel.  The following
		information is shown for each loaded kext:

"kextunload" : 
	NAME :
		kextunload – terminate driver I/O Kit driver instances
		and unload kernel extensions (kexts)
	DESCRIPTION :
		The kextunload program is used to terminate and
		unregister I/O Kit objects associated with a kernel
		extension (kext) and to unload the code and
		personalities for that kext.  kextunload must run
		with superuser privileges.

"kextutil" : 
	NAME :
		kextutil – load, diagnose problems with, and generate
		symbols for kernel extensions (kexts)
	DESCRIPTION :
		The kextutil program is used to explicitly load
		kernel extensions (kexts), diagnose problems with
		kexts, and to generate symbol files for debugging
		kexts.  In order to load a kext into the kernel
		kextutil must run as the superuser; for all other
		uses it can run as any user.

"keytool" : 
	NAME :
		keytool - a key and certificate management utility
	DESCRIPTION :
		The keytool command is a key and certificate
		management utility.  It enables users to administer
		their own public/private key pairs and associated
		certificates for use in self-authentication (where
		a user authenticates themselves to other users and
		services) or data integrity and authentication
		services, by using digital signatures.  The keytool
		command also enables users to cache the public keys
		(in the form of certificates) of their communicating
		peers.

"kgetcred" : 
	NAME :
		kgetcred – get a ticket for a particular service
	DESCRIPTION :
		kgetcred obtains a ticket for a service.  Usually
		tickets for services are obtained automatically
		when needed but sometimes for some odd reason you
		want to obtain a particular ticket or of a special
		type.

"kill" : 
	DESCRIPTION :
		Envoie un signal à une tâche. Envoie le signal nommé
		par SIGSPEC ou SIGNUM au processus identifié par
		PID ou JOBSPEC. Si SIGSPEC et SIGNUM ne sont pas
		donnés, alors SIGTERM est envoyé.

"kill.d" : 
	NAME :
		kill.d - snoop process signals as they occur. Uses
		DTrace.
	DESCRIPTION :
		kill.d is a simple DTrace program to print details
		of process signals as they are sent, such as the
		PID source and destination, signal number and result.

"killall" : 
	NAME :
		killall – kill processes by name
	DESCRIPTION :
		The killall utility kills processes selected by
		name, as opposed to the selection by PID as done
		by kill(1).  By default, it will send a TERM signal
		to all processes with a real UID identical to the
		caller of killall that match the name procname.
		The super-user is allowed to kill any process.

"kinit" : 
	NAME :
		kinit – acquire initial tickets
	DESCRIPTION :
		kinit is used to authenticate to the Kerberos server
		as principal, or if none is given, a system generated
		default (typically your login name at the default
		realm), and acquire a ticket granting ticket that
		can later be used to obtain tickets for other
		services.

"klist" : 
	NAME :
		klist – list Kerberos credentials
	DESCRIPTION :
		klist reads and displays the current tickets in the
		credential cache (also known as the ticket file).

"klist_cdhashes" : 
	NAME :
		klist_cdhashes – reports information about kexts
		that have been approved via SKEL
	DESCRIPTION :
		The klist_cdhashes command should not be run directly.

"kmutil" : 
	DESCRIPTION :
		kmutil is a multipurpose tool for managing kernel
		extensions (kexts) and kext collections on disk.
		It takes a subcommand and a number of options, some
		of which are common to multiple commands.

"kpasswd" : 
	NAME :
		kpasswd – Kerberos 5 password changing program
	DESCRIPTION :
		kpasswd is the client for changing passwords.

"krb5-config" : 
	NAME :
		krb5-config – give information on how to link code
		against Heimdal libraries
	DESCRIPTION :
		krb5-config tells the application programmer what
		special flags to use to compile and link programs
		against the libraries installed by Heimdal.

"krbservicesetup" : 
	NAME :
		krbservicesetup – Kerberos – Open Directory Single
		Sign On
	DESCRIPTION :
		krbservicesetup is used by sso_util to configure
		Kerberized services on the current host. It uses
		kadmin to add service principals to the KDC database
		and create the krb5.keytab file. And then edits/creates
		the config files of the given service to use the
		proper service principal. krbservicesetup knows how
		to configure the FTP, AFP, POP, IMAP, SMTP and SSH
		services shipped by Apple in Mac OS X 10.3
		krbservicesetup takes either a service_type,
		service_principal pair or a plist file with a list
		of services to configure. The plist file also allows
		more control over the options used when creating
		the principals.

"ksh" : 
	NAME :
		ksh, rksh, pfksh - KornShell, a standard/restricted
		command and programming language
	DESCRIPTION :
		Ksh is a command and programming language that
		executes commands read from a terminal or a file.
		Rksh is a restricted version of the command interpreter
		ksh; it is used to set up login names and execution
		environments whose capabilities are more controlled
		than those of the standard shell.  Rpfksh is a
		profile shell version of the command interpreter
		ksh; it is used to to execute commands with the
		attributes specified by the user's profiles (see
		pfexec(1)).  See Invocation below for the meaning
		of arguments to the shell.

"kswitch" : 
	NAME :
		kswitch – switch between default credential caches
	DESCRIPTION :
		Supported options:

"ktrace" : 
	NAME :
		ktrace – record kernel trace files
	DESCRIPTION :
		ktrace can configure the system to trace events,
		or record them to a file, and print a human-readable
		representation of the events.

"ktutil" : 
	NAME :
		ktutil – manage Kerberos keytabs
	DESCRIPTION :
		ktutil is a program for managing keytabs.  Supported
		options:

"lam" : 
	NAME :
		lam – laminate files
	DESCRIPTION :
		The lam utility copies the named files side by side
		onto the standard output.  The n-th input lines
		from the input files are considered fragments of
		the single long n-th output line into which they
		are assembled.  The name `-' means the standard
		input, and may be repeated.

"languagesetup" : 
	NAME :
		languagesetup – Set the primary language for the
		system.
	DESCRIPTION :
		languagesetup The languagesetup command changes the
		default system language that is chosen in the Setup
		Assistant application. This affects the language
		shown at login window.

"last" : 
	NAME :
		last – indicate last logins of users and ttys
	DESCRIPTION :
		Last will list the sessions of specified users,
		ttys, and hosts, in reverse time order.  Each line
		of output contains the user name, the tty from which
		the session was conducted, any hostname, the start
		and stop times for the session, and the duration
		of the session.  If the session is still continuing
		or was cut short by a crash or shutdown, last will
		so indicate.

"lastcomm" : 
	NAME :
		lastcomm – show last commands executed in reverse
		order
	DESCRIPTION :
		lastcomm gives information on previously executed
		commands.  With no arguments, lastcomm prints
		information about all the commands recorded during
		the current accounting file's lifetime.

"lastwords" : 
	NAME :
		lastwords - print syscalls before exit. Uses DTrace.
	DESCRIPTION :
		This prints the last few system calls for processes
		matching the given name, when they exit. This makes
		use of a ring buffer so that the impact on the
		system is minimised.

"latency" : 
	NAME :
		latency – monitors scheduling and interrupt latency
	DESCRIPTION :
		The latency utility provides scheduling and
		interrupt-latency statistics. Due to the kernel
		tracing facility it uses to operate, the command
		requires root privileges.

"launchctl" : 
	NAME :
		launchctl – Interfaces with launchd
	DESCRIPTION :
		launchctl interfaces with launchd to manage and
		inspect daemons, agents and XPC services.

"launchd" : 
	NAME :
		launchd – System wide and per-user daemon/agent
		manager
	DESCRIPTION :
		launchd manages processes, both for the system as
		a whole and for individual users.

"layerutil" : 
	NAME :
		layerutil create compiled layered image stack
	DESCRIPTION :
		Creates a compiled layered image stack (lcr) file
		from a layered input file source, such as an lsr
		or suitably structured Photoshop (psd) file. If the
		psd file's basename ends with @Yx Y will be treated
		as the scale factor of the psd file. If gpu compression
		is not specified then lossy compression is used.

"ld" : 
	NAME :
		ld – linker
	DESCRIPTION :
		The ld command combines several object files and
		libraries, resolves references, and produces an
		output file.  ld can produce a final linked image
		(executable, dylib, or bundle), or with the -r
		option, produce another object file.  If the -o
		option is not used, the output file produced is
		named "a.out".

"ld.lld" : 
	La commande "ld.lld" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ld.lld", "info ld.lld" ou "ld.lld" pour essayer d'obtenir davantage d'informations.

"ld64.lld" : 
	La commande "ld64.lld" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ld64.lld", "info ld64.lld" ou "ld64.lld" pour essayer d'obtenir davantage d'informations.

"ldapadd" : 
	NAME :
		ldapmodify, ldapadd - LDAP modify entry and LDAP
		add entry tools
	DESCRIPTION :
		ldapmodify is a shell-accessible interface to the
		ldap_add_ext(3), ldap_modify_ext(3), ldap_delete_ext(3)
		and ldap_rename(3).  library calls.  ldapadd is
		implemented as a hard link to the ldapmodify tool.
		When invoked as ldapadd the -a (add new entry) flag
		is turned on automatically.

"ldapcompare" : 
	NAME :
		ldapcompare - LDAP compare tool
	DESCRIPTION :
		ldapcompare is a shell-accessible interface to the
		ldap_compare_ext(3) library call.

"ldapdelete" : 
	NAME :
		ldapdelete - LDAP delete entry tool
	DESCRIPTION :
		ldapdelete is a shell-accessible interface to the
		ldap_delete_ext(3) library call.

"ldapexop" : 
	NAME :
		ldapexop - issue LDAP extended operations
	DESCRIPTION :
		ldapexop issues the LDAP extended operation specified
		by oid or one of the special keywords whoami, cancel,
		or refresh.

"ldapmodify" : 
	NAME :
		ldapmodify, ldapadd - LDAP modify entry and LDAP
		add entry tools
	DESCRIPTION :
		ldapmodify is a shell-accessible interface to the
		ldap_add_ext(3), ldap_modify_ext(3), ldap_delete_ext(3)
		and ldap_rename(3).  library calls.  ldapadd is
		implemented as a hard link to the ldapmodify tool.
		When invoked as ldapadd the -a (add new entry) flag
		is turned on automatically.

"ldapmodrdn" : 
	NAME :
		ldapmodrdn - LDAP rename entry tool
	DESCRIPTION :
		ldapmodrdn is a shell-accessible interface to the
		ldap_rename(3) library call.

"ldappasswd" : 
	NAME :
		ldappasswd - change the password of an LDAP entry
	DESCRIPTION :
		ldappasswd is a tool to set the password of an LDAP
		user.  ldappasswd uses the LDAPv3 Password Modify
		(RFC 3062) extended operation.

"ldapsearch" : 
	NAME :
		ldapsearch - LDAP search tool
	DESCRIPTION :
		ldapsearch is a shell-accessible interface to the
		ldap_search_ext(3) library call.

"ldapurl" : 
	NAME :
		ldapurl - LDAP URL formatting tool
	DESCRIPTION :
		ldapurl is a command that allows to either compose
		or decompose LDAP URIs.

"ldapwhoami" : 
	NAME :
		ldapwhoami - LDAP who am i? tool
	DESCRIPTION :
		ldapwhoami implements the LDAP "Who Am I?" extended
		operation.

"leaks" : 
	NAME :
		leaks – Search a process's memory for unreferenced
		malloc buffers
	DESCRIPTION :
		leaks identifies leaked memory -- memory that the
		application has allocated, but has been lost and
		cannot be freed.  Specifically, leaks examines a
		specified process's memory for values that may be
		pointers to malloc-allocated buffers.  Any buffer
		reachable from a pointer in writable global memory
		(e.g., __DATA segments), a register, or on the stack
		is assumed to be memory in use.  Any buffer reachable
		from a pointer in a reachable malloc-allocated
		buffer is also assumed to be in use.  The buffers
		which are not reachable are leaks; the buffers could
		never be freed because no pointer exists in memory
		to the buffer, and thus free() could never be called
		for these buffers.  Such buffers waste

"leave" : 
	NAME :
		leave – remind you when you have to leave
	DESCRIPTION :
		The leave utility waits until the specified time,
		then reminds you that you have to leave.  You are
		reminded 5 minutes and 1 minute before the actual
		time, at the time, and every minute thereafter.
		When you log off, leave exits just before it would
		have printed the next message.

"less" : 
	NAME :
		less - opposite of more
	DESCRIPTION :
		Less is a program similar to more(1), but which
		allows backward movement in the file as well as
		forward movement.  Also, less does not have to read
		the entire input file before starting, so with large
		input files it starts up faster than text editors
		like vi(1).  Less uses termcap (or terminfo on some
		systems), so it can run on a variety of terminals.
		There is even limited support for hardcopy terminals.
		(On a hardcopy terminal, lines which should be
		printed at the top of the screen are prefixed with
		a caret.)

"lessecho" : 
	NAME :
		lessecho - expand metacharacters
	DESCRIPTION :
		lessecho is a program that simply echos its arguments
		on standard output.  But any metacharacter in the
		output is preceded by an "escape" character, which
		by default is a backslash.

"let" : 
	DESCRIPTION :
		Évalue des expressions arithmétiques. Chaque ARG
		est une expression arithmétique à évaluer. L'évaluation
		est faite avec des entiers de largeur fixe sans
		vérification de dépassement, mais la division par
		zéro est interceptée et signalée comme une erreur.
		La liste suivante d'opérateurs est groupée par
		niveau d'égale priorité. Les niveaux sont listés
		par priorité décroissante.

"lex" : 
	NAME :
		flex - the fast lexical analyser generator
	DESCRIPTION :
		Generates programs that perform pattern-matching
		on text.

"libnetcfg" : 
	NAME :
		libnetcfg - configure libnet
	DESCRIPTION :
		The libnetcfg utility can be used to configure the
		libnet.  Starting from perl 5.8 libnet is part of
		the standard Perl distribution, but the libnetcfg
		can be used for any libnet installation.

"libnetcfg5.30" : 
	NAME :
		libnetcfg - configure libnet
	DESCRIPTION :
		The libnetcfg utility can be used to configure the
		libnet.  Starting from perl 5.8 libnet is part of
		the standard Perl distribution, but the libnetcfg
		can be used for any libnet installation.

"libnetcfg5.34" : 
	NAME :
		libnetcfg - configure libnet
	DESCRIPTION :
		The libnetcfg utility can be used to configure the
		libnet.  Starting from perl 5.8 libnet is part of
		the standard Perl distribution, but the libnetcfg
		can be used for any libnet installation.

"libpng-config" : 
	La commande "libpng-config" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help libpng-config", "info libpng-config" ou "libpng-config" pour essayer d'obtenir davantage d'informations.

"libpng16-config" : 
	La commande "libpng16-config" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help libpng16-config", "info libpng16-config" ou "libpng16-config" pour essayer d'obtenir davantage d'informations.

"libtool" : 
	NAME :
		libtool - create libraries ranlib - add or update
		the table of contents of archive libraries
	DESCRIPTION :
		The libtool command takes the specified input object
		files and creates a library for use with the link
		editor, ld(1).  The library's name is specified by
		output (the argument to the -o flag).  The input
		object files may be in any correct format that
		contains object files (``universal'' files, archives,
		object files).  Libtool will not put any non-object
		input file into the output library (unlike ranlib,
		which allows this in the archives it operates on).

"link" : 
	NAME :
		ln, link – link files
	DESCRIPTION :
		The ln utility creates a new directory entry (linked
		file) for the file name specified by target_file.
		The target_file will be created with the same file
		modes as the source_file.  It is useful for maintaining
		multiple copies of a file in many places at once
		without using up storage for the “copies”; instead,
		a link “points” to the original copy.  There are
		two types of links; hard links and symbolic links.
		How a link “points” to a file is one of the differences
		between a hard and symbolic link.

"linkicc" : 
	NAME :
		linkicc - little cms device link generator.
	DESCRIPTION :
		lcms is a standalone CMM engine, which deals with
		the color management. It implements a fast
		transformation between ICC profiles.  linkicc is a
		little cms device link generator.

"lipo" : 
	NAME :
		lipo - create or operate on universal files
	DESCRIPTION :
		The lipo tool creates or operates on ``universal''
		(multi-architecture) files. Generally, lipo reads
		a single input file and writes to a single output
		file, although some commands and options accept
		multiple input files.  lipo will only ever write
		to a single output file, and input files are never
		modified in place.

"llc" : 
	La commande "llc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llc", "info llc" ou "llc" pour essayer d'obtenir davantage d'informations.

"lld" : 
	La commande "lld" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help lld", "info lld" ou "lld" pour essayer d'obtenir davantage d'informations.

"lld-link" : 
	La commande "lld-link" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help lld-link", "info lld-link" ou "lld-link" pour essayer d'obtenir davantage d'informations.

"lldb" : 
	NAME :
		lldb - LLDB Documentation
	DESCRIPTION :
		lldb is a fully featured debugger. It is a command
		line interface to the LLDB debugger library.

"lldb-argdumper" : 
	La commande "lldb-argdumper" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help lldb-argdumper", "info lldb-argdumper" ou "lldb-argdumper" pour essayer d'obtenir davantage d'informations.

"lldb-instr" : 
	La commande "lldb-instr" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help lldb-instr", "info lldb-instr" ou "lldb-instr" pour essayer d'obtenir davantage d'informations.

"lldb-server" : 
	La commande "lldb-server" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help lldb-server", "info lldb-server" ou "lldb-server" pour essayer d'obtenir davantage d'informations.

"lldb-vscode" : 
	La commande "lldb-vscode" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help lldb-vscode", "info lldb-vscode" ou "lldb-vscode" pour essayer d'obtenir davantage d'informations.

"lli" : 
	La commande "lli" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help lli", "info lli" ou "lli" pour essayer d'obtenir davantage d'informations.

"lli-child-target" : 
	La commande "lli-child-target" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help lli-child-target", "info lli-child-target" ou "lli-child-target" pour essayer d'obtenir davantage d'informations.

"llvm-addr2line" : 
	La commande "llvm-addr2line" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-addr2line", "info llvm-addr2line" ou "llvm-addr2line" pour essayer d'obtenir davantage d'informations.

"llvm-ar" : 
	La commande "llvm-ar" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-ar", "info llvm-ar" ou "llvm-ar" pour essayer d'obtenir davantage d'informations.

"llvm-as" : 
	La commande "llvm-as" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-as", "info llvm-as" ou "llvm-as" pour essayer d'obtenir davantage d'informations.

"llvm-bcanalyzer" : 
	La commande "llvm-bcanalyzer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-bcanalyzer", "info llvm-bcanalyzer" ou "llvm-bcanalyzer" pour essayer d'obtenir davantage d'informations.

"llvm-bitcode-strip" : 
	La commande "llvm-bitcode-strip" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-bitcode-strip", "info llvm-bitcode-strip" ou "llvm-bitcode-strip" pour essayer d'obtenir davantage d'informations.

"llvm-c-test" : 
	La commande "llvm-c-test" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-c-test", "info llvm-c-test" ou "llvm-c-test" pour essayer d'obtenir davantage d'informations.

"llvm-cat" : 
	La commande "llvm-cat" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-cat", "info llvm-cat" ou "llvm-cat" pour essayer d'obtenir davantage d'informations.

"llvm-cfi-verify" : 
	La commande "llvm-cfi-verify" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-cfi-verify", "info llvm-cfi-verify" ou "llvm-cfi-verify" pour essayer d'obtenir davantage d'informations.

"llvm-config" : 
	La commande "llvm-config" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-config", "info llvm-config" ou "llvm-config" pour essayer d'obtenir davantage d'informations.

"llvm-cov" : 
	NAME :
		llvm-cov - emit coverage information
	DESCRIPTION :
		The llvm-cov tool shows code coverage information
		for programs that are instrumented to emit profile
		data. It can be used to work with gcov-style coverage
		or with clang's instrumentation based profiling.

"llvm-cvtres" : 
	La commande "llvm-cvtres" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-cvtres", "info llvm-cvtres" ou "llvm-cvtres" pour essayer d'obtenir davantage d'informations.

"llvm-cxxdump" : 
	La commande "llvm-cxxdump" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-cxxdump", "info llvm-cxxdump" ou "llvm-cxxdump" pour essayer d'obtenir davantage d'informations.

"llvm-cxxfilt" : 
	NAME :
		llvm-cxxfilt - LLVM symbol name demangler
	DESCRIPTION :
		llvm-cxxfilt is a symbol demangler that can be used
		as a replacement for the GNU c++filt tool. It takes
		a series of symbol names and prints their demangled
		form on the standard output stream. If a name cannot
		be demangled, it is simply printed as is.

"llvm-cxxmap" : 
	La commande "llvm-cxxmap" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-cxxmap", "info llvm-cxxmap" ou "llvm-cxxmap" pour essayer d'obtenir davantage d'informations.

"llvm-debuginfo-analyzer" : 
	La commande "llvm-debuginfo-analyzer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-debuginfo-analyzer", "info llvm-debuginfo-analyzer" ou "llvm-debuginfo-analyzer" pour essayer d'obtenir davantage d'informations.

"llvm-debuginfod" : 
	La commande "llvm-debuginfod" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-debuginfod", "info llvm-debuginfod" ou "llvm-debuginfod" pour essayer d'obtenir davantage d'informations.

"llvm-debuginfod-find" : 
	La commande "llvm-debuginfod-find" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-debuginfod-find", "info llvm-debuginfod-find" ou "llvm-debuginfod-find" pour essayer d'obtenir davantage d'informations.

"llvm-diff" : 
	La commande "llvm-diff" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-diff", "info llvm-diff" ou "llvm-diff" pour essayer d'obtenir davantage d'informations.

"llvm-dis" : 
	La commande "llvm-dis" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-dis", "info llvm-dis" ou "llvm-dis" pour essayer d'obtenir davantage d'informations.

"llvm-dlltool" : 
	La commande "llvm-dlltool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-dlltool", "info llvm-dlltool" ou "llvm-dlltool" pour essayer d'obtenir davantage d'informations.

"llvm-dwarfdump" : 
	La commande "llvm-dwarfdump" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-dwarfdump", "info llvm-dwarfdump" ou "llvm-dwarfdump" pour essayer d'obtenir davantage d'informations.

"llvm-dwarfutil" : 
	La commande "llvm-dwarfutil" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-dwarfutil", "info llvm-dwarfutil" ou "llvm-dwarfutil" pour essayer d'obtenir davantage d'informations.

"llvm-dwp" : 
	La commande "llvm-dwp" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-dwp", "info llvm-dwp" ou "llvm-dwp" pour essayer d'obtenir davantage d'informations.

"llvm-exegesis" : 
	La commande "llvm-exegesis" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-exegesis", "info llvm-exegesis" ou "llvm-exegesis" pour essayer d'obtenir davantage d'informations.

"llvm-extract" : 
	La commande "llvm-extract" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-extract", "info llvm-extract" ou "llvm-extract" pour essayer d'obtenir davantage d'informations.

"llvm-g++" : 
	La commande "llvm-g++" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-g++", "info llvm-g++" ou "llvm-g++" pour essayer d'obtenir davantage d'informations.

"llvm-gcc" : 
	La commande "llvm-gcc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-gcc", "info llvm-gcc" ou "llvm-gcc" pour essayer d'obtenir davantage d'informations.

"llvm-gsymutil" : 
	La commande "llvm-gsymutil" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-gsymutil", "info llvm-gsymutil" ou "llvm-gsymutil" pour essayer d'obtenir davantage d'informations.

"llvm-ifs" : 
	La commande "llvm-ifs" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-ifs", "info llvm-ifs" ou "llvm-ifs" pour essayer d'obtenir davantage d'informations.

"llvm-install-name-tool" : 
	La commande "llvm-install-name-tool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-install-name-tool", "info llvm-install-name-tool" ou "llvm-install-name-tool" pour essayer d'obtenir davantage d'informations.

"llvm-jitlink" : 
	La commande "llvm-jitlink" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-jitlink", "info llvm-jitlink" ou "llvm-jitlink" pour essayer d'obtenir davantage d'informations.

"llvm-jitlink-executor" : 
	La commande "llvm-jitlink-executor" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-jitlink-executor", "info llvm-jitlink-executor" ou "llvm-jitlink-executor" pour essayer d'obtenir davantage d'informations.

"llvm-lib" : 
	La commande "llvm-lib" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-lib", "info llvm-lib" ou "llvm-lib" pour essayer d'obtenir davantage d'informations.

"llvm-libtool-darwin" : 
	La commande "llvm-libtool-darwin" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-libtool-darwin", "info llvm-libtool-darwin" ou "llvm-libtool-darwin" pour essayer d'obtenir davantage d'informations.

"llvm-link" : 
	La commande "llvm-link" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-link", "info llvm-link" ou "llvm-link" pour essayer d'obtenir davantage d'informations.

"llvm-lipo" : 
	La commande "llvm-lipo" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-lipo", "info llvm-lipo" ou "llvm-lipo" pour essayer d'obtenir davantage d'informations.

"llvm-lto" : 
	La commande "llvm-lto" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-lto", "info llvm-lto" ou "llvm-lto" pour essayer d'obtenir davantage d'informations.

"llvm-lto2" : 
	La commande "llvm-lto2" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-lto2", "info llvm-lto2" ou "llvm-lto2" pour essayer d'obtenir davantage d'informations.

"llvm-mc" : 
	La commande "llvm-mc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-mc", "info llvm-mc" ou "llvm-mc" pour essayer d'obtenir davantage d'informations.

"llvm-mca" : 
	La commande "llvm-mca" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-mca", "info llvm-mca" ou "llvm-mca" pour essayer d'obtenir davantage d'informations.

"llvm-ml" : 
	La commande "llvm-ml" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-ml", "info llvm-ml" ou "llvm-ml" pour essayer d'obtenir davantage d'informations.

"llvm-modextract" : 
	La commande "llvm-modextract" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-modextract", "info llvm-modextract" ou "llvm-modextract" pour essayer d'obtenir davantage d'informations.

"llvm-mt" : 
	La commande "llvm-mt" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-mt", "info llvm-mt" ou "llvm-mt" pour essayer d'obtenir davantage d'informations.

"llvm-nm" : 
	NAME :
		llvm-nm - list LLVM bitcode and object file's symbol
		table
	DESCRIPTION :
		The llvm-nm utility lists the names of symbols from
		LLVM bitcode files, object files, and archives.
		Each symbol is listed along with some simple
		information about its provenance. If no filename
		is specified, a.out is used as the input. If - is
		used as a filename, llvm-nm will read a file from
		its standard input stream.

"llvm-objcopy" : 
	La commande "llvm-objcopy" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-objcopy", "info llvm-objcopy" ou "llvm-objcopy" pour essayer d'obtenir davantage d'informations.

"llvm-objdump" : 
	NAME :
		llvm-objdump - LLVM's object file dumper
	DESCRIPTION :
		The llvm-objdump utility prints the contents of
		object files and final linked images named on the
		command line. If no file name is specified,
		llvm-objdump will attempt to read from a.out. If -
		is used as a file name, llvm-objdump will process
		a file on its standard input stream.

"llvm-opt-report" : 
	La commande "llvm-opt-report" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-opt-report", "info llvm-opt-report" ou "llvm-opt-report" pour essayer d'obtenir davantage d'informations.

"llvm-otool" : 
	NAME :
		llvm-otool - the otool-compatible command line
		parser for llvm-objdump
	DESCRIPTION :
		The command line shim llvm-otool takes all the same
		options as the original otool(1) command and executes
		an equivalent objdump(1) command.  Its original
		intent was to ease changing usage over from otool(1)
		to the LLVM-based open source objdump(1) command
		with similar functionality. However, objdump(1)
		prefers different usage, with different behavior
		semantics and different output requirements.
		objdump(1) is no longer the preferred tool for
		inspecting modern Mach-O binaries. The llvm-otool
		command-line shim remains behind so that people who
		prefer to use the open source objdump(1) tool with
		otool- style semantics can continue to do so, mostly
		to support scripts or

"llvm-pdbutil" : 
	La commande "llvm-pdbutil" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-pdbutil", "info llvm-pdbutil" ou "llvm-pdbutil" pour essayer d'obtenir davantage d'informations.

"llvm-PerfectShuffle" : 
	La commande "llvm-PerfectShuffle" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-PerfectShuffle", "info llvm-PerfectShuffle" ou "llvm-PerfectShuffle" pour essayer d'obtenir davantage d'informations.

"llvm-profdata" : 
	NAME :
		llvm-profdata - Profile data tool
	DESCRIPTION :
		The llvm-profdata tool is a small utility for working
		with profile data files.

"llvm-profgen" : 
	La commande "llvm-profgen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-profgen", "info llvm-profgen" ou "llvm-profgen" pour essayer d'obtenir davantage d'informations.

"llvm-ranlib" : 
	La commande "llvm-ranlib" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-ranlib", "info llvm-ranlib" ou "llvm-ranlib" pour essayer d'obtenir davantage d'informations.

"llvm-rc" : 
	La commande "llvm-rc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-rc", "info llvm-rc" ou "llvm-rc" pour essayer d'obtenir davantage d'informations.

"llvm-readelf" : 
	La commande "llvm-readelf" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-readelf", "info llvm-readelf" ou "llvm-readelf" pour essayer d'obtenir davantage d'informations.

"llvm-readobj" : 
	La commande "llvm-readobj" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-readobj", "info llvm-readobj" ou "llvm-readobj" pour essayer d'obtenir davantage d'informations.

"llvm-reduce" : 
	La commande "llvm-reduce" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-reduce", "info llvm-reduce" ou "llvm-reduce" pour essayer d'obtenir davantage d'informations.

"llvm-remark-size-diff" : 
	La commande "llvm-remark-size-diff" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-remark-size-diff", "info llvm-remark-size-diff" ou "llvm-remark-size-diff" pour essayer d'obtenir davantage d'informations.

"llvm-remarkutil" : 
	La commande "llvm-remarkutil" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-remarkutil", "info llvm-remarkutil" ou "llvm-remarkutil" pour essayer d'obtenir davantage d'informations.

"llvm-rtdyld" : 
	La commande "llvm-rtdyld" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-rtdyld", "info llvm-rtdyld" ou "llvm-rtdyld" pour essayer d'obtenir davantage d'informations.

"llvm-sim" : 
	La commande "llvm-sim" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-sim", "info llvm-sim" ou "llvm-sim" pour essayer d'obtenir davantage d'informations.

"llvm-size" : 
	La commande "llvm-size" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-size", "info llvm-size" ou "llvm-size" pour essayer d'obtenir davantage d'informations.

"llvm-split" : 
	La commande "llvm-split" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-split", "info llvm-split" ou "llvm-split" pour essayer d'obtenir davantage d'informations.

"llvm-stress" : 
	La commande "llvm-stress" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-stress", "info llvm-stress" ou "llvm-stress" pour essayer d'obtenir davantage d'informations.

"llvm-strings" : 
	La commande "llvm-strings" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-strings", "info llvm-strings" ou "llvm-strings" pour essayer d'obtenir davantage d'informations.

"llvm-strip" : 
	La commande "llvm-strip" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-strip", "info llvm-strip" ou "llvm-strip" pour essayer d'obtenir davantage d'informations.

"llvm-symbolizer" : 
	La commande "llvm-symbolizer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-symbolizer", "info llvm-symbolizer" ou "llvm-symbolizer" pour essayer d'obtenir davantage d'informations.

"llvm-tapi-diff" : 
	La commande "llvm-tapi-diff" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-tapi-diff", "info llvm-tapi-diff" ou "llvm-tapi-diff" pour essayer d'obtenir davantage d'informations.

"llvm-tblgen" : 
	La commande "llvm-tblgen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-tblgen", "info llvm-tblgen" ou "llvm-tblgen" pour essayer d'obtenir davantage d'informations.

"llvm-tli-checker" : 
	La commande "llvm-tli-checker" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-tli-checker", "info llvm-tli-checker" ou "llvm-tli-checker" pour essayer d'obtenir davantage d'informations.

"llvm-undname" : 
	La commande "llvm-undname" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-undname", "info llvm-undname" ou "llvm-undname" pour essayer d'obtenir davantage d'informations.

"llvm-windres" : 
	La commande "llvm-windres" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-windres", "info llvm-windres" ou "llvm-windres" pour essayer d'obtenir davantage d'informations.

"llvm-xray" : 
	La commande "llvm-xray" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help llvm-xray", "info llvm-xray" ou "llvm-xray" pour essayer d'obtenir davantage d'informations.

"ln" : 
	NAME :
		ln, link – link files
	DESCRIPTION :
		The ln utility creates a new directory entry (linked
		file) for the file name specified by target_file.
		The target_file will be created with the same file
		modes as the source_file.  It is useful for maintaining
		multiple copies of a file in many places at once
		without using up storage for the “copies”; instead,
		a link “points” to the original copy.  There are
		two types of links; hard links and symbolic links.
		How a link “points” to a file is one of the differences
		between a hard and symbolic link.

"loads.d" : 
	NAME :
		loads.d - print load averages. Uses DTrace.
	DESCRIPTION :
		These are the same load averages that the "uptime"
		command prints.  The purpose of this script is to
		demonstrate fetching these values from the DTrace
		language.

"local" : 
	DESCRIPTION :
		Définit des variables locales. Crée une variable
		locale nommée NOM, avec une valeur VALEUR.  OPTION
		peut être n'importe quelle option acceptée par «
		declare ».

"locale" : 
	NAME :
		locale – display locale settings
	DESCRIPTION :
		locale displays information about the current locale,
		or a list of all available locales.

"localedef" : 
	NAME :
		localedef – define locale environment
	DESCRIPTION :
		The localedef utility reads source definitions for
		one or more locale categories belonging to the same
		locale from the file named in the -i option (if
		specified) or from standard input.

"localemanager" : 
	NAME :
		localemanager – Configure OpenDirectory Server
		Locales
	DESCRIPTION :
		localemanager creates, destroys, and edits OpenDirectory
		Server Locale information.  Locales are collections
		of OpenDirectory servers to assist clients in
		locating the nearest OpenDirectory Server.

"locate" : 
	NAME :
		locate – find filenames quickly
	DESCRIPTION :
		The locate program searches a database for all
		pathnames which match the specified pattern.  The
		database is recomputed periodically (usually weekly
		or daily), and contains the pathnames of all files
		which are publicly accessible.

"lockstat" : 
	NAME :
		lockstat - report kernel lock and profiling statistics
	DESCRIPTION :
		The lockstat utility gathers and displays kernel
		locking and profiling statistics. lockstat allows
		you to specify which events to watch (for example,
		spin on adaptive mutex, block on read access to
		rwlock due to waiting writers, and so forth) how
		much data to gather for each event, and how to
		display the data. By default, lockstat monitors all
		lock contention events, gathers frequency and timing
		data about those events, and displays the data in
		decreasing frequency order, so that the most common
		events appear first.

"log" : 
	NAME :
		log – Access system wide log messages created by
		os_log, os_trace and other logging systems.
	DESCRIPTION :
		log is used to access system wide log messages
		created by os_log, os_trace and other logging
		systems.  Some commands require root privileges.

"logger" : 
	NAME :
		logger – make entries in the system log
	DESCRIPTION :
		Logger provides a shell command interface to the
		syslog(3) system log module.

"login" : 
	NAME :
		login – log into the computer
	DESCRIPTION :
		The login utility logs users (and pseudo-users)
		into the computer system.

"logname" : 
	NAME :
		 logname – display user's login name
	DESCRIPTION :
		The logname utility writes the user's login name
		to standard output followed by a newline.

"logout" : 
	DESCRIPTION :
		Termine un shell de connexion. Termine un shell de
		connexion avec le code de sortie N.  Renvoie une
		erreur s'il n'est pas exécuté dans un shell de
		connexion.

"logresolve" : 
	NAME :
		logresolve - Resolve IP-addresses to hostnames in
		Apache log files

"logs_liste_et_afficher_contenu_fichier" : 
	La commande "logs_liste_et_afficher_contenu_fichier" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help logs_liste_et_afficher_contenu_fichier", "info logs_liste_et_afficher_contenu_fichier" ou "logs_liste_et_afficher_contenu_fichier" pour essayer d'obtenir davantage d'informations.

"look" : 
	NAME :
		look – display lines beginning with a given string
	DESCRIPTION :
		The look utility displays any lines in file which
		contain string as a prefix.  As look performs a
		binary search, the lines in file must be sorted.

"lorder" : 
	NAME :
		lorder – list dependencies for object files
	DESCRIPTION :
		The lorder utility uses nm(1) to determine
		interdependencies in the list of object files
		specified on the command line.  lorder outputs a
		list of file names where the first file contains a
		symbol which is defined by the second file.

"lp" : 
	NAME :
		lp - print files
	DESCRIPTION :
		lp submits files for printing or alters a pending
		job.  Use a filename of "-" to force printing from
		the standard input.

"lpadmin" : 
	NAME :
		lpadmin - configure cups printers and classes
	DESCRIPTION :
		lpadmin configures printer and class queues provided
		by CUPS.  It can also be used to set the server
		default printer or class.

"lpc" : 
	NAME :
		lpc - line printer control program (deprecated)
	DESCRIPTION :
		lpc provides limited control over printer and class
		queues provided by CUPS. It can also be used to
		query the state of queues.

"lpinfo" : 
	NAME :
		lpinfo - show available devices or drivers (deprecated)
	DESCRIPTION :
		lpinfo lists the available devices or drivers known
		to the CUPS server. The first form (-m) lists the
		available drivers, while the second form (-v) lists
		the available devices.

"lpmove" : 
	NAME :
		lpmove - move a job or all jobs to a new destination
	DESCRIPTION :
		lpmove moves the specified job or all jobs from
		source to destination. job can be the job ID number
		or the old destination and job ID.

"lpoptions" : 
	NAME :
		lpoptions - display or set printer options and
		defaults
	DESCRIPTION :
		lpoptions displays or sets printer options and
		defaults.  If no printer is specified using the -p
		option, the default printer is used as described
		in lp(1).

"lpq" : 
	NAME :
		lpq - show printer queue status
	DESCRIPTION :
		lpq shows the current print queue status on the
		named printer.  Jobs queued on the default destination
		will be shown if no printer or class is specified
		on the command-line.

"lpr" : 
	NAME :
		lpr - print files
	DESCRIPTION :
		lpr submits files for printing.  Files named on the
		command line are sent to the named printer or the
		default destination if no destination is specified.
		If no files are listed on the command-line, lpr
		reads the print file from the standard input.

"lprm" : 
	NAME :
		lprm - cancel print jobs
	DESCRIPTION :
		lprm cancels print jobs that have been queued for
		printing.  If no arguments are supplied, the current
		job on the default destination is canceled.  You
		can specify one or more job ID numbers to cancel
		those jobs or use the - option to cancel all jobs.

"lpstat" : 
	NAME :
		lpstat - print cups status information
	DESCRIPTION :
		lpstat displays status information about the current
		classes, jobs, and printers.  When run with no
		arguments, lpstat will list active jobs queued by
		the current user.

"ls" : 
	NAME :
		ls – list directory contents
	DESCRIPTION :
		For each operand that names a file of a type other
		than directory, ls displays its name as well as any
		requested, associated information.  For each operand
		that names a file of type directory, ls displays
		the names of files contained within that directory,
		as well as any requested, associated information.

"lsappinfo" : 
	NAME :
		lsappinfo - Control and query CoreApplicationServices
		about the app state on the system

"lsbom" : 
	NAME :
		lsbom – list contents of a bom file
	DESCRIPTION :
		The lsbom command interprets the contents of binary
		bom (bom(5)) files. For each file in a bom, lsbom
		prints the file path and/or requested information.

"lskq" : 
	NAME :
		lskq – display process kqueue state
	DESCRIPTION :
		The lskq command enumerates kqueues and registered
		kevents of running processes.

"lsm" : 
	NAME :
		lsm - Latent Semantic Mapping tool
	DESCRIPTION :
		The Latent Semantic Mapping framework is a language
		independent, Unicode based technology that builds
		maps and uses them to classify texts into one of a
		number of categories.

"lsmp" : 
	NAME :
		lsmp – Display mach port information for processes
		on the system
	DESCRIPTION :
		The lsmp command prints information about every
		active right in a task's port space, giving a view
		into the inter-process communication behavior of
		that task.

"lsof" : 
	NAME :
		lsof - list open files
	DESCRIPTION :
		Lsof revision 4.91 lists on its standard output
		file information about files opened by processes
		for the following UNIX dialects:

"lsvfs" : 
	NAME :
		lsvfs – list installed virtual file systems
	DESCRIPTION :
		The lsvfs command lists information about the
		currently loaded virtual file system modules.  When
		vfsname arguments are given, lsvfs lists information
		about the specified VFS modules.  Otherwise, lsvfs
		lists all currently loaded modules.  The information
		is as follows:

"lwp-download" : 
	NAME :
		lwp-download - Fetch large files from the web
	DESCRIPTION :
		The lwp-download program will save the file at url
		to a local file.

"lwp-download5.30" : 
	NAME :
		lwp-download - Fetch large files from the web
	DESCRIPTION :
		The lwp-download program will save the file at url
		to a local file.

"lwp-dump" : 
	NAME :
		lwp-dump - See what headers and content is returned
		for a URL
	DESCRIPTION :
		The lwp-dump program will get the resource identified
		by the URL and then dump the response object to
		STDOUT.  This will display the headers returned and
		the initial part of the content, escaped so that
		it's safe to display even binary content.  The
		escapes syntax used is the same as for Perl's double
		quoted strings.  If there is no content the string
		"(no content)" is shown in its place.

"lwp-dump5.30" : 
	NAME :
		lwp-dump - See what headers and content is returned
		for a URL
	DESCRIPTION :
		The lwp-dump program will get the resource identified
		by the URL and then dump the response object to
		STDOUT.  This will display the headers returned and
		the initial part of the content, escaped so that
		it's safe to display even binary content.  The
		escapes syntax used is the same as for Perl's double
		quoted strings.  If there is no content the string
		"(no content)" is shown in its place.

"lwp-mirror" : 
	NAME :
		lwp-mirror - Simple mirror utility
	DESCRIPTION :
		This program can be used to mirror a document from
		a WWW server.  The document is only transferred if
		the remote copy is newer than the local copy.  If
		the local copy is newer nothing happens.

"lwp-mirror5.30" : 
	NAME :
		lwp-mirror - Simple mirror utility
	DESCRIPTION :
		This program can be used to mirror a document from
		a WWW server.  The document is only transferred if
		the remote copy is newer than the local copy.  If
		the local copy is newer nothing happens.

"lwp-request" : 
	NAME :
		lwp-request - Simple command line user agent
	DESCRIPTION :
		This program can be used to send requests to WWW
		servers and your local file system. The request
		content for POST and PUT methods is read from stdin.
		The content of the response is printed on stdout.
		Error messages are printed on stderr.  The program
		returns a status value indicating the number of
		URLs that failed.

"lwp-request5.30" : 
	NAME :
		lwp-request - Simple command line user agent
	DESCRIPTION :
		This program can be used to send requests to WWW
		servers and your local file system. The request
		content for POST and PUT methods is read from stdin.
		The content of the response is printed on stdout.
		Error messages are printed on stderr.  The program
		returns a status value indicating the number of
		URLs that failed.

"lz4" : 
	NAME :
		lz4 - lz4, unlz4, lz4cat - Compress or decompress
		.lz4 files
	DESCRIPTION :
		lz4 is an extremely fast lossless compression
		algorithm, based on byte-aligned LZ77 family of
		compression scheme. lz4 offers compression speeds
		> 500 MB/s per core, linearly scalable with multi-core
		CPUs. It features an extremely fast decoder, offering
		speed in multiple GB/s per core, typically reaching
		RAM speed limit on multi-core systems. The native
		file format is the .lz4 format.

"lz4c" : 
	NAME :
		lz4 - lz4, unlz4, lz4cat - Compress or decompress
		.lz4 files
	DESCRIPTION :
		lz4 is an extremely fast lossless compression
		algorithm, based on byte-aligned LZ77 family of
		compression scheme. lz4 offers compression speeds
		> 500 MB/s per core, linearly scalable with multi-core
		CPUs. It features an extremely fast decoder, offering
		speed in multiple GB/s per core, typically reaching
		RAM speed limit on multi-core systems. The native
		file format is the .lz4 format.

"lz4cat" : 
	NAME :
		lz4 - lz4, unlz4, lz4cat - Compress or decompress
		.lz4 files
	DESCRIPTION :
		lz4 is an extremely fast lossless compression
		algorithm, based on byte-aligned LZ77 family of
		compression scheme. lz4 offers compression speeds
		> 500 MB/s per core, linearly scalable with multi-core
		CPUs. It features an extremely fast decoder, offering
		speed in multiple GB/s per core, typically reaching
		RAM speed limit on multi-core systems. The native
		file format is the .lz4 format.

"lzcat" : 
	NAME :
		xz, unxz, xzcat, lzma, unlzma, lzcat - Compress or
		decompress .xz and .lzma files
	DESCRIPTION :
		xz is a general-purpose data compression tool with
		command line syntax similar to gzip(1) and bzip2(1).
		The native file format is the .xz format, but the
		legacy .lzma format used by LZMA Utils and raw
		compressed streams with no container format headers
		are also supported. In addition, decompression of
		the .lz format used by lzip is supported.

"lzcmp" : 
	NAME :
		xzcmp, xzdiff, lzcmp, lzdiff - compare compressed
		files
	DESCRIPTION :
		xzcmp and xzdiff invoke cmp(1) or diff(1) on files
		compressed with xz(1), lzma(1), gzip(1), bzip2(1),
		lzop(1), or zstd(1).  All options specified are
		passed directly to cmp(1) or diff(1).  If only one
		file is specified, then the files compared are file1
		(which must have a suffix of a supported compression
		format) and file1 from which the compression format
		suffix has been stripped.  If two files are specified,
		then they are uncompressed if necessary and fed to
		cmp(1) or diff(1).  The exit status from cmp(1) or
		diff(1) is preserved unless a decompression error
		occurs; then exit status is 2.

"lzdiff" : 
	NAME :
		xzcmp, xzdiff, lzcmp, lzdiff - compare compressed
		files
	DESCRIPTION :
		xzcmp and xzdiff invoke cmp(1) or diff(1) on files
		compressed with xz(1), lzma(1), gzip(1), bzip2(1),
		lzop(1), or zstd(1).  All options specified are
		passed directly to cmp(1) or diff(1).  If only one
		file is specified, then the files compared are file1
		(which must have a suffix of a supported compression
		format) and file1 from which the compression format
		suffix has been stripped.  If two files are specified,
		then they are uncompressed if necessary and fed to
		cmp(1) or diff(1).  The exit status from cmp(1) or
		diff(1) is preserved unless a decompression error
		occurs; then exit status is 2.

"lzegrep" : 
	NAME :
		xzgrep - search compressed files for a regular
		expression
	DESCRIPTION :
		xzgrep invokes grep(1) on files which may be either
		uncompressed or compressed with xz(1), lzma(1),
		gzip(1), bzip2(1), lzop(1), or zstd(1). All options
		specified are passed directly to grep(1).

"lzfgrep" : 
	NAME :
		xzgrep - search compressed files for a regular
		expression
	DESCRIPTION :
		xzgrep invokes grep(1) on files which may be either
		uncompressed or compressed with xz(1), lzma(1),
		gzip(1), bzip2(1), lzop(1), or zstd(1). All options
		specified are passed directly to grep(1).

"lzgrep" : 
	NAME :
		xzgrep - search compressed files for a regular
		expression
	DESCRIPTION :
		xzgrep invokes grep(1) on files which may be either
		uncompressed or compressed with xz(1), lzma(1),
		gzip(1), bzip2(1), lzop(1), or zstd(1). All options
		specified are passed directly to grep(1).

"lzless" : 
	NAME :
		xzless, lzless - view xz or lzma compressed (text)
		files
	DESCRIPTION :
		xzless is a filter that displays text from compressed
		files to a terminal.  It works on files compressed
		with xz(1) or lzma(1).  If no files are given,
		xzless reads from standard input.

"lzma" : 
	NAME :
		xz, unxz, xzcat, lzma, unlzma, lzcat - Compress or
		decompress .xz and .lzma files
	DESCRIPTION :
		xz is a general-purpose data compression tool with
		command line syntax similar to gzip(1) and bzip2(1).
		The native file format is the .xz format, but the
		legacy .lzma format used by LZMA Utils and raw
		compressed streams with no container format headers
		are also supported. In addition, decompression of
		the .lz format used by lzip is supported.

"lzmadec" : 
	NAME :
		xzdec, lzmadec - Small .xz and .lzma decompressors
	DESCRIPTION :
		xzdec is a liblzma-based decompression-only tool
		for .xz (and only .xz) files.  xzdec is intended
		to work as a drop-in replacement for xz(1) in the
		most common situations where a script has been
		written to use xz --decompress --stdout (and possibly
		a few other commonly used options) to decompress
		.xz files.  lzmadec is identical to xzdec except
		that lzmadec supports .lzma files instead of .xz
		files.

"lzmainfo" : 
	NAME :
		lzmainfo - show information stored in the .lzma
		file header
	DESCRIPTION :
		lzmainfo shows information stored in the .lzma file
		header.  It reads the first 13 bytes from the
		specified file, decodes the header, and prints it
		to standard output in human readable format.  If
		no files are given or file is -, standard input is
		read.

"lzmore" : 
	NAME :
		xzmore, lzmore - view xz or lzma compressed (text)
		files
	DESCRIPTION :
		xzmore is a filter which allows examination of xz(1)
		or lzma(1) compressed text files one screenful at
		a time on a soft-copy terminal.

"m4" : 
	NAME :
		M4 - macro processor
	DESCRIPTION :
		Process macros in FILEs.  If no FILE or if FILE is
		`-', standard input is read.

"macbinary" : 
	NAME :
		applesingle, binhex, macbinary – encode and decode
		files
	DESCRIPTION :
		applesingle, binhex, macbinary are implemented as
		a single tool with multiple names.  All invocations
		support the three verbs encode, decode, and probe.

"macerror" : 
	DESCRIPTION :
		The macerror script translates Mac error numbers
		into their symbolic name and description.

"macerror5.30" : 
	DESCRIPTION :
		The macerror script translates Mac error numbers
		into their symbolic name and description.

"machine" : 
	NAME :
		machine – print machine type
	DESCRIPTION :
		The machine command displays the machine type.

"mail" : 
	NAME :
		mail, mailx – send and receive mail

"mailq" : 
	NAME :
		sendmail - Postfix to Sendmail compatibility interface
	DESCRIPTION :
		The Postfix sendmail(1) command implements the
		Postfix to Sendmail compatibility interface.  For
		the sake of compatibility with existing applications,
		some Sendmail command-line options are recognized
		but silently ignored.

"mailx" : 
	NAME :
		mail, mailx – send and receive mail

"make" : 
	NAME :
		make - GNU make utility to maintain groups of
		programs
	DESCRIPTION :
		The purpose of the make utility is to determine
		automatically which pieces of a large program need
		to be recompiled, and issue the commands to recompile
		them.  The manual describes the GNU implementation
		of make, which was written by Richard Stallman and
		Roland McGrath, and is currently maintained by Paul
		Smith.  Our examples show C programs, since they
		are most common, but you can use make with any
		programming language whose compiler can be run with
		a shell command.  In fact, make is not limited to
		programs.  You can use it to describe any task where
		some files must be updated automatically from others
		whenever the others change.

"malloc_history" : 
	NAME :
		malloc_history – Show the malloc and anonymous VM
		allocations that the process has performed
	DESCRIPTION :
		malloc_history inspects a given process and lists
		the malloc and anonymous VM allocations performed
		by it.  Anonymous VM allocations are from calls
		such as mach_vm_allocate that allocate raw Virtual
		Memory that is not backed by a file.  Allocations
		of the VM regions underlying the malloc heaps are
		ignored.  malloc_history relies on information
		provided by the standard malloc library when malloc
		stack logging has been enabled for the target
		process.  See below for further information.

"man" : 
	NAME :
		man, apropos, whatis – display online manual
		documentation pages
	DESCRIPTION :
		The man utility finds and displays online manual
		documentation pages.  If mansect is provided, man
		restricts the search to the specific section of the
		manual.

"mandoc" : 
	NAME :
		mandoc – format manual pages
	DESCRIPTION :
		The mandoc utility formats manual pages for display.

"mandoc_soelim" : 
	NAME :
		soelim – interpret .so requests in manpages
	DESCRIPTION :
		soelim reads files lines by lines.

"manpath" : 
	NAME :
		manpath – display search path for manual pages
	DESCRIPTION :
		The manpath utility determines the user's manual
		search path from the user's PATH, and local
		configuration files.  This result is echoed to the
		standard output.

"mapfile" : 
	DESCRIPTION :
		Lit des lignes depuis l'entrée standard vers une
		variable tableau indexé. Lit des lignes depuis
		l'entrée standard vers la variable tableau indexé
		TABLEAU ou depuis le descripteur de fichier FD si
		l'option « -u » est utilisée. La variable MAPFILE
		est le TABLEAU par défaut.

"mcxquery" : 
	NAME :
		mcxquery – Managed Client (MCX) compositor query
		tool
	DESCRIPTION :
		mcxquery is a utility to determine the effective
		managed preferences for a user logging in to a
		workgroup from a specific computer.

"mcxrefresh" : 
	NAME :
		mcxrefresh – Managed Client (MCX) preference refresh
		tool
	DESCRIPTION :
		mcxrefresh is a utility to force the client to
		re-read the managed preferences on the server for
		a user.   This tool must be run with elevated
		privileges.  Note that this may return a zero status
		if the user could not be found but there were
		computer settings available.  You must use the -a
		parameter to enter a password if you are requesting
		a refresh of an Active Directory server.

"md5" : 
	NAME :
		md5 – calculate a message-digest fingerprint
		(checksum) for a file
	DESCRIPTION :
		The md5 utility takes as input a message of arbitrary
		length and produces as output a “fingerprint” or
		“message digest” of the input.  It is conjectured
		that it is computationally infeasible to produce
		two messages having the same message digest, or to
		produce any message having a given prespecified
		target message digest.  The MD5 algorithm is intended
		for digital signature applications, where a large
		file must be “compressed” in a secure manner before
		being encrypted with a private (secret) key under
		a public-key cryptosystem such as RSA.

"mddiagnose" : 
	NAME :
		mddiagnose – gather information to aid in diagnosing
		Spotlight issues
	DESCRIPTION :
		The mddiagnose tool gathers system and Spotlight
		information in order to assist Apple when investigating
		issues related to Spotlight. A great deal of
		information is harvested, spanning system state,
		system and Spotlight details.

"mdfind" : 
	NAME :
		mdfind – finds files matching a given query
	DESCRIPTION :
		The mdfind command consults the central metadata
		store and returns a list of files that match the
		given metadata query. The query can be a string or
		a query expression.

"mdimport" : 
	NAME :
		mdimport – import file hierarchies into the metadata
		datastore.
	DESCRIPTION :
		mdimport is used to test Spotlight plug-ins, list
		the installed plug-ins and schema, and re-index
		files handled by a plug-in when a new plug-in is
		installed.

"mdls" : 
	NAME :
		mdls – lists the metadata attributes for the specified
		file
	DESCRIPTION :
		The mdls command prints the values of all the
		metadata attributes associated with the files
		provided as an argument.

"mDNSResponder" : 
	NAME :
		mDNSResponder – Multicast and Unicast DNS daemon
	DESCRIPTION :
		mDNSResponder (also known as mdnsd on some systems)
		is a daemon invoked at boot time to implement
		Multicast DNS and DNS Service Discovery. On Mac OS
		X 10.6 (Snow Leopard), mDNSResponder is also the
		system-wide Unicast DNS Resolver.

"mDNSResponderHelper" : 
	NAME :
		mDNSResponderHelper – mDNS privilege separation
		helper
	DESCRIPTION :
		mDNSResponderHelper is an executable invoked by
		launchd to provide privilege separation to the
		mDNSResponder daemon.

"mdutil" : 
	NAME :
		mdutil – manage the metadata stores used by Spotlight
	DESCRIPTION :
		The mdutil command is useful for managing the
		metadata stores for mounted volumes.

"memory_pressure" : 
	NAME :
		memory_pressure – Tool to apply real or simulate
		memory pressure on the system
	DESCRIPTION :
		A tool to apply real or simulate memory pressure
		on the system

"mesg" : 
	NAME :
		mesg – display (do not display) messages from other
		users
	DESCRIPTION :
		The mesg utility is invoked by a user to control
		write access others have to a terminal device.
		Write access is allowed by default, and programs
		such as talk(1) and write(1) may display messages
		on the terminal.

"mg" : 
	NAME :
		mg – emacs-like text editor
	DESCRIPTION :
		mg is intended to be a small, fast, and portable
		editor for people who can't (or don't want to) run
		emacs for one reason or another, or are not familiar
		with the vi(1) editor.  It is compatible with emacs
		because there shouldn't be any reason to learn more
		editor types than emacs or vi(1).

"mib2c" : 
	NAME :
		mib2c -- generate template code for extending the
		agent  VAR=VAL] MIBNODE
	DESCRIPTION :
		The mib2c tool is designed to take a portion of the
		MIB tree (as defined by a MIB file) and generate
		the template C code necessary to implement the
		relevant management objects within it.

"mib2c-update" : 
	NAME :
		mib2c-update - script to merge custom code into
		updated mib2c code
	DESCRIPTION :
		Use mib2c-update to generate your mib2c code
		templates, and it will track the original code and
		the changes you make to the code. If the mib2c
		template changes (bug fixes, enhances in later
		releases), re- running mib2c will update the template
		and then attempt to re-apply your changes.

"mig" : 
	NAME :
		mig - Mach Interface Generator
	DESCRIPTION :
		The mig command invokes the Mach Interface Generator
		to generate Remote Procedure Call (RPC) code for
		client-server style Mach IPC from specification
		files.

"mkbom" : 
	NAME :
		mkbom – create a bill-of-materials file
	DESCRIPTION :
		The mkbom command creates a bill-of-materials
		(bom(5)).  You must specify either a source as a
		directory, or a text file containing a file listing
		as outputted by lsbom(8).  mkbom will generate
		bill-of-materials information based on that
		information.  Any existing bomfile will be overwritten
		by the new bomfile.

"mkdir" : 
	NAME :
		mkdir – make directories
	DESCRIPTION :
		The mkdir utility creates the directories named as
		operands, in the order specified, using mode
		“rwxrwxrwx” (0777) as modified by the current
		umask(2).

"mkextunpack" : 
	NAME :
		mkextunpack – extract or list the contents of a
		multikext (mkext) archive
	DESCRIPTION :
		The mkextunpack program lists the contents of a
		multikext file, mkext_file, or unarchives the
		contents into output_directory (which must exist).
		The -v option causes mkextunpack to print the name
		if each kext as it finds them.

"mkfifo" : 
	NAME :
		mkfifo – make fifos
	DESCRIPTION :
		The mkfifo utility creates the fifos requested, in
		the order specified.

"mkfile" : 
	NAME :
		mkfile – create a file
	DESCRIPTION :
		mkfile creates one or more files that are suitable
		for use as NFS-mounted swap areas.  The sticky bit
		is set, and the file is padded with zeroes by
		default.  Non-root users must set the sticky bit
		using chmod(1).  The default size unit is bytes,
		but the following suffixes may be used to multiply
		by the given factor: b (512), k (1024), m (1048576),
		and g (1073741824).

"mklocale" : 
	NAME :
		mklocale – make LC_CTYPE locale files
	DESCRIPTION :
		The mklocale utility reads a LC_CTYPE source file
		from standard input and produces a LC_CTYPE binary
		file on standard output suitable for placement in
		/usr/share/locale/language/LC_CTYPE.

"mknod" : 
	NAME :
		mknod – make device special file
	DESCRIPTION :
		The mknod command creates device special files, or
		fifos.

"mkpassdb" : 
	NAME :
		mkpassdb – Mac OS X Server Password Server database
		creation tool
	DESCRIPTION :
		mkpassdb creates or modifies the password server
		database directly.

"mktemp" : 
	NAME :
		mktemp – make temporary file name (unique)
	DESCRIPTION :
		The mktemp utility takes each of the given file
		name templates and overwrites a portion of it to
		create a file name.  This file name is unique and
		suitable for use by the application.  The template
		may be any file name with some number of ‘Xs’
		appended to it, for example /tmp/temp.XXXX.  The
		trailing ‘Xs’ are replaced with the current process
		number and/or a unique letter combination.  The
		number of unique file names mktemp can return depends
		on the number of ‘Xs’ provided; six ‘Xs’ will result
		in mktemp selecting 1 of 56800235584 (62 ** 6)
		possible file names.  On case-insensitive file
		systems, the effective number of unique names is
		significantly less; given six ‘Xs’, mktemp will
		instead select 1

"mlir-cpu-runner" : 
	La commande "mlir-cpu-runner" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-cpu-runner", "info mlir-cpu-runner" ou "mlir-cpu-runner" pour essayer d'obtenir davantage d'informations.

"mlir-linalg-ods-yaml-gen" : 
	La commande "mlir-linalg-ods-yaml-gen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-linalg-ods-yaml-gen", "info mlir-linalg-ods-yaml-gen" ou "mlir-linalg-ods-yaml-gen" pour essayer d'obtenir davantage d'informations.

"mlir-lsp-server" : 
	La commande "mlir-lsp-server" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-lsp-server", "info mlir-lsp-server" ou "mlir-lsp-server" pour essayer d'obtenir davantage d'informations.

"mlir-opt" : 
	La commande "mlir-opt" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-opt", "info mlir-opt" ou "mlir-opt" pour essayer d'obtenir davantage d'informations.

"mlir-pdll" : 
	La commande "mlir-pdll" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-pdll", "info mlir-pdll" ou "mlir-pdll" pour essayer d'obtenir davantage d'informations.

"mlir-pdll-lsp-server" : 
	La commande "mlir-pdll-lsp-server" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-pdll-lsp-server", "info mlir-pdll-lsp-server" ou "mlir-pdll-lsp-server" pour essayer d'obtenir davantage d'informations.

"mlir-reduce" : 
	La commande "mlir-reduce" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-reduce", "info mlir-reduce" ou "mlir-reduce" pour essayer d'obtenir davantage d'informations.

"mlir-tblgen" : 
	La commande "mlir-tblgen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-tblgen", "info mlir-tblgen" ou "mlir-tblgen" pour essayer d'obtenir davantage d'informations.

"mlir-translate" : 
	La commande "mlir-translate" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mlir-translate", "info mlir-translate" ou "mlir-translate" pour essayer d'obtenir davantage d'informations.

"mnthome" : 
	NAME :
		mnthome – mount an AFP (AppleShare) home directory
		with the correct privileges
	DESCRIPTION :
		The mnthome command unmounts the AFP (AppleShare)
		home directory that was automounted as guest, and
		remounts it with the correct privileges by logging
		into the AFP server using the current username and
		password. This command also allows you to have guest
		access turned off on your AFP server too and still
		have AFP home directories work with "su".

"modularize" : 
	La commande "modularize" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help modularize", "info modularize" ou "modularize" pour essayer d'obtenir davantage d'informations.

"moose-outdated" : 
	La commande "moose-outdated" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help moose-outdated", "info moose-outdated" ou "moose-outdated" pour essayer d'obtenir davantage d'informations.

"moose-outdated5.30" : 
	La commande "moose-outdated5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help moose-outdated5.30", "info moose-outdated5.30" ou "moose-outdated5.30" pour essayer d'obtenir davantage d'informations.

"more" : 
	NAME :
		less - opposite of more
	DESCRIPTION :
		Less is a program similar to more(1), but which
		allows backward movement in the file as well as
		forward movement.  Also, less does not have to read
		the entire input file before starting, so with large
		input files it starts up faster than text editors
		like vi(1).  Less uses termcap (or terminfo on some
		systems), so it can run on a variety of terminals.
		There is even limited support for hardcopy terminals.
		(On a hardcopy terminal, lines which should be
		printed at the top of the screen are prefixed with
		a caret.)

"mount" : 
	NAME :
		mount – mount file systems
	DESCRIPTION :
		The mount command calls the mount(2) system call
		to prepare and graft a special device or the remote
		node (rhost:path) on to the file system tree at the
		point mount_point, which must be a directory.  If
		either special or mount_point are not provided, the
		appropriate information is obtained via the getfsent(3)
		library routines.

"mount_9p" : 
	NAME :
		mount_9p – mount a 9P volume
	DESCRIPTION :
		The mount_9p command mounts the 9P filesystem
		associated with fs_tag.

"mount_acfs" : 
	NAME :
		mount_acfs - mount an Xsan volume
	DESCRIPTION :
		mount_acfs is a mount helper utility that mounts
		an Xsan volume on client machines.  This command
		is normally executed by mount(8) when the -t acfs
		option is used.

"mount_afp" : 
	NAME :
		mount_afp – mount an afp (AppleShare) filesystem
	DESCRIPTION :
		The mount_afp command mounts the AFP volume denoted
		by the afp_url
		afp://[user[;AUTH=uamname][:password]@]host[:port]/volumename
		at the mount point indicated by node.

"mount_apfs" : 
	NAME :
		mount_apfs – mount an APFS volume
	DESCRIPTION :
		The mount_apfs command attaches the APFS volume
		indicated by the device special to the global file
		system namespace at the location indicated by
		directory.  This command is normally executed by
		diskarbitrationd(8) or mount(8) at boot time.

"mount_cd9660" : 
	NAME :
		mount_cd9660 – mount an ISO-9660 filesystem
	DESCRIPTION :
		The mount_cd9660 command attaches the ISO-9660
		filesystem residing on the device special to the
		global filesystem namespace at the location indicated
		by node.  This command is normally executed by
		mount(8) at boot time.

"mount_cddafs" : 
	NAME :
		mount_cddafs – mount an Audio CD
	DESCRIPTION :
		The mount_cddafs command attaches the CDDA filesystem
		residing on the device special to the global
		filesystem namespace at the location indicated by
		node.  This command is normally executed by mount(8)
		at boot time.

"mount_devfs" : 
	La commande "mount_devfs" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mount_devfs", "info mount_devfs" ou "mount_devfs" pour essayer d'obtenir davantage d'informations.

"mount_exfat" : 
	NAME :
		mount_exfat – mount an ExFAT file system
	DESCRIPTION :
		The mount_exfat command mounts the ExFAT filesystem
		from the device device onto the directory given by
		directory.  This command is normally invoked by
		diskarbitrationd(8) as the result of detecting a
		device with a file system recognized as ExFAT.  It
		can also be invoked indirectly by executing the
		mount(8) command with the -t exfat option.

"mount_fdesc" : 
	NAME :
		mount_fdesc – mount the file-descriptor file system
	DESCRIPTION :
		The mount_fdesc command attaches an instance of the
		per-process file descriptor namespace to the global
		filesystem namespace.  The conventional mount point
		is /dev and the filesystem should be union mounted
		in order to augment, rather than replace, the
		existing entries in /dev.  This command is normally
		executed by mount(8) at boot time.

"mount_ftp" : 
	NAME :
		mount_ftp – mount a FTP filesystem
	DESCRIPTION :
		The mount_ftp command mounts a FTP-enabled server
		directory at ftp://host[:port][/path] at the mount
		point indicated by node.

"mount_hfs" : 
	NAME :
		mount_hfs – mount an HFS+ file system
	DESCRIPTION :
		The mount_hfs command attaches the HFS file system
		residing on the device special to the global file
		system namespace at the location indicated by
		directory.  This command is normally executed by
		mount(8) at boot time.

"mount_lifs" : 
	La commande "mount_lifs" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mount_lifs", "info mount_lifs" ou "mount_lifs" pour essayer d'obtenir davantage d'informations.

"mount_msdos" : 
	NAME :
		mount_msdos – mount an MS-DOS file system
	DESCRIPTION :
		The mount_msdos command attaches the MS-DOS filesystem
		residing on the device special to the global
		filesystem namespace at the location indicated by
		node.  This command is normally executed by mount(8)
		at boot time, but can be used by any user to mount
		an MS-DOS file system on any directory that they
		own (provided, of course, that they have appropriate
		access to the device that contains the file system).

"mount_nfs" : 
	NAME :
		mount_nfs – mount NFS file systems
	DESCRIPTION :
		The mount_nfs command calls the mount(2) system
		call to prepare and graft a remote NFS file system
		( server:/path ) on to the file system tree at the
		point directory.

"mount_smbfs" : 
	NAME :
		mount_smbfs – mount a shared resource from an SMB
		file server
	DESCRIPTION :
		The mount_smbfs command mounts a share from a remote
		server using SMB/CIFS protocol.

"mount_tmpfs" : 
	NAME :
		mount_tmpfs – mount a tmpfs volume
	DESCRIPTION :
		The mount_tmpfs command creates a new tmpfs (in-memory
		file system) volume and attaches it to the global
		file system namespace at the location indicated by
		directory.

"mount_udf" : 
	NAME :
		mount_udf – mount a UDF filesystem
	DESCRIPTION :
		The mount_udf command attaches the UDF filesystem
		residing on the device devicePath to the global
		filesystem namespace at the location indicated by
		mountPath.  This command is normally executed by
		mount(8) at boot time.

"mount_virtiofs" : 
	NAME :
		mount_virtiofs – mount a virtio-fs volume
	DESCRIPTION :
		The mount_virtiofs command attaches the virtio-fs
		file system associated with fs_tag to the global
		file system namespace at the location indicated by
		directory.

"mount_webdav" : 
	NAME :
		mount_webdav – mount a WebDAV filesystem
	DESCRIPTION :
		The mount_webdav command mounts the WebDAV-enabled
		server directory at [scheme://]host[:port][/path]
		at the mount point indicated by node.

"mp2bug" : 
	NAME :
		mp2bug – mod_perl bug report generator
	DESCRIPTION :
		The mp2bug utilty is used to generate a mod_perl
		bug report template which can be filled out and
		submitted to the mod_perl project.

"mpioutil" : 
	NAME :
		mpioutil – Tool for configuring paths to multipathed
		logical units.
	DESCRIPTION :
		mpioutil is a utility to manage multiple paths to
		a SCSI Logical Unit.

"msgattrib" : 
	NAME :
		msgattrib - attribute matching and manipulation on
		message catalog
	DESCRIPTION :
		Filters the messages of a translation catalog
		according to their attributes, and manipulates the
		attributes.

"msgcat" : 
	NAME :
		msgcat - combines several message catalogs
	DESCRIPTION :
		Concatenates and merges the specified PO files.
		Find messages which are common to two or more of
		the specified PO files.  By using the --more-than
		option, greater commonality may be requested before
		messages are printed.  Conversely, the --less-than
		option may be used to specify less commonality
		before messages are printed (i.e. --less-than=2
		will only print the unique messages).  Translations,
		comments, extracted comments, and file positions
		will be cumulated, except that if --use-first is
		specified, they will be taken from the first PO
		file to define them.

"msgcmp" : 
	NAME :
		msgcmp - compare message catalog and template
	DESCRIPTION :
		Compare two Uniforum style .po files to check that
		both contain the same set of msgid strings.  The
		def.po file is an existing PO file with the
		translations.  The ref.pot file is the last created
		PO file, or a PO Template file (generally created
		by xgettext).  This is useful for checking that you
		have translated each and every message in your
		program.  Where an exact match cannot be found,
		fuzzy matching is used to produce better diagnostics.

"msgcomm" : 
	NAME :
		msgcomm - match two message catalogs
	DESCRIPTION :
		Find messages which are common to two or more of
		the specified PO files.  By using the --more-than
		option, greater commonality may be requested before
		messages are printed.  Conversely, the --less-than
		option may be used to specify less commonality
		before messages are printed (i.e.  --less-than=2
		will only print the unique messages). Translations,
		comments and extracted comments will be preserved,
		but only from the first PO file to define them.
		File positions from all PO files will be cumulated.

"msgconv" : 
	NAME :
		msgconv - character set conversion for message
		catalog
	DESCRIPTION :
		Converts a translation catalog to a different
		character encoding.

"msgen" : 
	NAME :
		msgen - create English message catalog
	DESCRIPTION :
		Creates an English translation catalog.  The input
		file is the last created English PO file, or a PO
		Template file (generally created by xgettext).
		Untranslated entries are assigned a translation
		that is identical to the msgid.

"msgexec" : 
	NAME :
		msgexec - process translations of message catalog
	DESCRIPTION :
		Applies a command to all translations of a translation
		catalog.  The COMMAND can be any program that reads
		a translation from standard input.  It is invoked
		once for each translation.  Its output becomes
		msgexec's output.  msgexec's return code is the
		maximum return code across all invocations.

"msgfilter" : 
	NAME :
		msgfilter - edit translations of message catalog
	DESCRIPTION :
		Applies a filter to all translations of a translation
		catalog.

"msgfmt" : 
	NAME :
		msgfmt - compile message catalog to binary format
	DESCRIPTION :
		Generate binary message catalog from textual
		translation description.

"msggrep" : 
	NAME :
		msggrep - pattern matching on message catalog
	DESCRIPTION :
		Extracts all messages of a translation catalog that
		match a given pattern or belong to some given source
		files.

"msginit" : 
	NAME :
		msginit - initialize a message catalog
	DESCRIPTION :
		Creates a new PO file, initializing the meta
		information with values from the user's environment.

"msgmerge" : 
	NAME :
		msgmerge - merge message catalog and template
	DESCRIPTION :
		Merges two Uniforum style .po files together.  The
		def.po file is an existing PO file with translations
		which will be taken over to the newly created file
		as long as they still match; comments will be
		preserved, but extracted comments and file positions
		will be discarded. The ref.pot file is the last
		created PO file with up-to-date source references
		but old translations, or a PO Template file (generally
		created by xgettext); any translations or comments
		in the file will be discarded, however dot comments
		and file positions will be preserved. Where an exact
		match cannot be found, fuzzy matching is used to
		produce better results.

"msgunfmt" : 
	NAME :
		msgunfmt - uncompile message catalog from binary
		format
	DESCRIPTION :
		Convert binary message catalog to Uniforum style
		.po file.

"msguniq" : 
	NAME :
		msguniq - unify duplicate translations in message
		catalog
	DESCRIPTION :
		Unifies duplicate translations in a translation
		catalog.  Finds duplicate translations of the same
		message ID.  Such duplicates are invalid input for
		other programs like msgfmt, msgmerge or msgcat.  By
		default, duplicates are merged together.  When using
		the --repeated option, only duplicates are output,
		and all other messages are discarded.  Comments and
		extracted comments will be cumulated, except that
		if --use-first is specified, they will be taken
		from the first translation.  File positions will
		be cumulated.  When using the --unique option,
		duplicates are discarded.

"mtree" : 
	NAME :
		mtree – map a directory hierarchy
	DESCRIPTION :
		The mtree utility compares the file hierarchy rooted
		in the current directory against a specification
		read from the standard input.  Messages are written
		to the standard output for any files whose
		characteristics do not match the specifications,
		or which are missing from either the file hierarchy
		or the specification.

"mv" : 
	NAME :
		mv – move files
	DESCRIPTION :
		In its first form, the mv utility renames the file
		named by the source operand to the destination path
		named by the target operand.  This form is assumed
		when the last operand does not name an already
		existing directory.

"myisamchk" : 
	NAME :
		myisamchk - MyISAM table-maintenance utility
	DESCRIPTION :
		The myisamchk utility gets information about your
		database tables or checks, repairs, or optimizes
		them.  myisamchk works with MyISAM tables (tables
		that have .MYD and .MYI files for storing data and
		indexes).

"myisamlog" : 
	NAME :
		myisamlog - display MyISAM log file contents
	DESCRIPTION :
		myisamlog processes the contents of a MyISAM log
		file. To create such a file, start the server with
		a --log-isam=log_file option.

"myisampack" : 
	NAME :
		myisampack - generate compressed, read-only MyISAM
		tables
	DESCRIPTION :
		The myisampack utility compresses MyISAM tables.
		myisampack works by compressing each column in the
		table separately. Usually, myisampack packs the
		data file 40% to 70%.

"myisam_ftdump" : 
	NAME :
		myisam_ftdump - display full-text index information
	DESCRIPTION :
		myisam_ftdump displays information about FULLTEXT
		indexes in MyISAM tables. It reads the MyISAM index
		file directly, so it must be run on the server host
		where the table is located. Before using myisam_ftdump,
		be sure to issue a FLUSH TABLES statement first if
		the server is running.

"mysql" : 
	NAME :
		mysql - the MySQL command-line client
	DESCRIPTION :
		mysql is a simple SQL shell with input line editing
		capabilities. It supports interactive and noninteractive
		use. When used interactively, query results are
		presented in an ASCII-table format. When used
		noninteractively (for example, as a filter), the
		result is presented in tab-separated format. The
		output format can be changed using command options.

"mysql.server" : 
	NAME :
		mysql.server - MySQL server startup script
	DESCRIPTION :
		MySQL distributions on Unix and Unix-like system
		include a script named mysql.server, which starts
		the MySQL server using mysqld_safe. It can be used
		on systems such as Linux and Solaris that use System
		V-style run directories to start and stop system
		services. It is also used by the macOS Startup Item
		for MySQL.

"mysqladmin" : 
	NAME :
		mysqladmin - a MySQL server administration program
	DESCRIPTION :
		mysqladmin is a client for performing administrative
		operations. You can use it to check the server's
		configuration and current status, to create and
		drop databases, and more.

"mysqlbinlog" : 
	NAME :
		mysqlbinlog - utility for processing binary log
		files
	DESCRIPTION :
		The server's binary log consists of files containing
		“events” that describe modifications to database
		contents. The server writes these files in binary
		format. To display their contents in text format,
		use the mysqlbinlog utility. You can also use
		mysqlbinlog to display the contents of relay log
		files written by a replica server in a replication
		setup because relay logs have the same format as
		binary logs. The binary log and relay log are
		discussed further in Section 5.4.4, “The Binary
		Log”, and Section 17.2.4, “Relay Log and Replication
		Metadata Repositories”.

"mysqlcheck" : 
	NAME :
		mysqlcheck - a table maintenance program
	DESCRIPTION :
		The mysqlcheck client performs table maintenance:
		It checks, repairs, optimizes, or analyzes tables.

"mysqld" : 
	NAME :
		mysqld - the MySQL server
	DESCRIPTION :
		mysqld, also known as MySQL Server, is a single
		multithreaded program that does most of the work
		in a MySQL installation. It does not spawn additional
		processes. MySQL Server manages access to the MySQL
		data directory that contains databases and tables.
		The data directory is also the default location for
		other information such as log files and status
		files.

"mysqldump" : 
	NAME :
		mysqldump - a database backup program
	DESCRIPTION :
		The mysqldump client utility performs logical
		backups, producing a set of SQL statements that can
		be executed to reproduce the original database
		object definitions and table data. It dumps one or
		more MySQL databases for backup or transfer to
		another SQL server. The mysqldump command can also
		generate output in CSV, other delimited text, or
		XML format.

"mysqldumpslow" : 
	NAME :
		mysqldumpslow - Summarize slow query log files
	DESCRIPTION :
		The MySQL slow query log contains information about
		queries that take a long time to execute (see Section
		5.4.5, “The Slow Query Log”). mysqldumpslow parses
		MySQL slow query log files and summarizes their
		contents.

"mysqld_multi" : 
	NAME :
		mysqld_multi - manage multiple MySQL servers
	DESCRIPTION :
		mysqld_multi is designed to manage several mysqld
		processes that listen for connections on different
		Unix socket files and TCP/IP ports. It can start
		or stop servers, or report their current status.

"mysqld_safe" : 
	NAME :
		mysqld_safe - MySQL server startup script
	DESCRIPTION :
		mysqld_safe is the recommended way to start a mysqld
		server on Unix. mysqld_safe adds some safety features
		such as restarting the server when an error occurs
		and logging runtime information to an error log. A
		description of error logging is given later in this
		section.

"mysqlimport" : 
	NAME :
		mysqlimport - a data import program
	DESCRIPTION :
		The mysqlimport client provides a command-line
		interface to the LOAD DATA SQL statement. Most
		options to mysqlimport correspond directly to clauses
		of LOAD DATA syntax. See Section 13.2.9, “LOAD DATA
		Statement”.

"mysqlpump" : 
	NAME :
		mysqlpump - a database backup program
	DESCRIPTION :
		•   mysqlpump Invocation Syntax

"mysqlrouter" : 
	NAME :
		mysqlrouter - MySQL Router
	DESCRIPTION :
		•   mysqlrouter Option Summaries

"mysqlrouter_keyring" : 
	La commande "mysqlrouter_keyring" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mysqlrouter_keyring", "info mysqlrouter_keyring" ou "mysqlrouter_keyring" pour essayer d'obtenir davantage d'informations.

"mysqlrouter_passwd" : 
	NAME :
		mysqlrouter_passwd - MySQL Router Password Tool
	DESCRIPTION :
		The mysqlrouter_passwd utility is a command line
		application to manage the accounts in the passwd
		file.

"mysqlrouter_plugin_info" : 
	NAME :
		mysqlrouter_plugin_info - MySQL Router Plugin
		Information
	DESCRIPTION :
		The mysqlrouter_plugin_info utility is a debugging
		tool that inspects a MySQL Router plugin for potential
		conflicts and general problems.

"mysqlshow" : 
	NAME :
		mysqlshow - display database, table, and column
		information
	DESCRIPTION :
		The mysqlshow client can be used to quickly see
		which databases exist, their tables, or a table's
		columns or indexes.

"mysqlslap" : 
	NAME :
		mysqlslap - a load emulation client
	DESCRIPTION :
		mysqlslap is a diagnostic program designed to emulate
		client load for a MySQL server and to report the
		timing of each stage. It works as if multiple clients
		are accessing the server.

"mysqltest" : 
	La commande "mysqltest" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mysqltest", "info mysqltest" ou "mysqltest" pour essayer d'obtenir davantage d'informations.

"mysqltest_safe_process" : 
	La commande "mysqltest_safe_process" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mysqltest_safe_process", "info mysqltest_safe_process" ou "mysqltest_safe_process" pour essayer d'obtenir davantage d'informations.

"mysqlxtest" : 
	La commande "mysqlxtest" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mysqlxtest", "info mysqlxtest" ou "mysqlxtest" pour essayer d'obtenir davantage d'informations.

"mysql_client_test" : 
	La commande "mysql_client_test" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mysql_client_test", "info mysql_client_test" ou "mysql_client_test" pour essayer d'obtenir davantage d'informations.

"mysql_config" : 
	NAME :
		mysql_config - display options for compiling clients
	DESCRIPTION :
		mysql_config provides you with useful information
		for compiling your MySQL client and connecting it
		to MySQL. It is a shell script, so it is available
		only on Unix and Unix-like systems.

"mysql_config_editor" : 
	NAME :
		mysql_config_editor - configure authentication
		information for connecting to MySQL server
	DESCRIPTION :
		The mysql_config_editor utility enables you to store
		authentication credentials in an obfuscated login
		path file named .mylogin.cnf. The file location is
		the %APPDATA%\MySQL directory on Windows and the
		current user's home directory on non-Windows systems.
		The file can be read later by MySQL client programs
		to obtain authentication credentials for connecting
		to MySQL Server.

"mysql_keyring_encryption_test" : 
	La commande "mysql_keyring_encryption_test" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mysql_keyring_encryption_test", "info mysql_keyring_encryption_test" ou "mysql_keyring_encryption_test" pour essayer d'obtenir davantage d'informations.

"mysql_migrate_keyring" : 
	La commande "mysql_migrate_keyring" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help mysql_migrate_keyring", "info mysql_migrate_keyring" ou "mysql_migrate_keyring" pour essayer d'obtenir davantage d'informations.

"mysql_secure_installation" : 
	NAME :
		mysql_secure_installation - improve MySQL installation
		security
	DESCRIPTION :
		This program enables you to improve the security
		of your MySQL installation in the following ways:

"mysql_ssl_rsa_setup" : 
	NAME :
		mysql_ssl_rsa_setup - create SSL/RSA files
	DESCRIPTION :
		This program creates the SSL certificate and key
		files and RSA key-pair files required to support
		secure connections using SSL and secure password
		exchange using RSA over unencrypted connections,
		if those files are missing.  mysql_ssl_rsa_setup
		can also be used to create new SSL files if the
		existing ones have expired.

"mysql_tzinfo_to_sql" : 
	NAME :
		mysql_tzinfo_to_sql - load the time zone tables
	DESCRIPTION :
		The mysql_tzinfo_to_sql program loads the time zone
		tables in the mysql database. It is used on systems
		that have a zoneinfo database (the set of files
		describing time zones). Examples of such systems
		are Linux, FreeBSD, Solaris, and macOS. One likely
		location for these files is the /usr/share/zoneinfo
		directory (/usr/share/lib/zoneinfo on Solaris). If
		your system does not have a zoneinfo database, you
		can use the downloadable package described in Section
		5.1.15, “MySQL Server Time Zone Support”.

"mysql_upgrade" : 
	NAME :
		mysql_upgrade - check and upgrade MySQL tables

"my_print_defaults" : 
	NAME :
		my_print_defaults - display options from option
		files
	DESCRIPTION :
		my_print_defaults displays the options that are
		present in option groups of option files. The output
		indicates what options are used by programs that
		read the specified option groups. For example, the
		mysqlcheck program reads the [mysqlcheck] and
		[client] option groups. To see what options are
		present in those groups in the standard option
		files, invoke my_print_defaults like this:

"nano" : 

"native2ascii" : 
	La commande "native2ascii" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help native2ascii", "info native2ascii" ou "native2ascii" pour essayer d'obtenir davantage d'informations.

"nbdst" : 
	NAME :
		nbdst – NetBoot deferred shadow tool
	DESCRIPTION :
		nbdst is used during NetBoot to associate a shadow
		file with the disk image being used as the root
		device. After the shadow file is attached, subsequent
		writes to the root device will be redirected to the
		shadow file, which normally resides on local storage.
		nbdst is invoked by /etc/rc.netboot

"nc" : 
	NAME :
		nc – arbitrary TCP and UDP connections and listens
	DESCRIPTION :
		The nc (or netcat) utility is used for just about
		anything under the sun involving TCP or UDP.  It
		can open TCP connections, send UDP packets, listen
		on arbitrary TCP and UDP ports, do port scanning,
		and deal with both IPv4 and IPv6.  Unlike telnet(1),
		nc scripts nicely, and separates error messages
		onto standard error instead of sending them to
		standard output, as telnet(1) does with some.

"ncal" : 
	NAME :
		cal, ncal – displays a calendar and the date of
		Easter
	DESCRIPTION :
		The cal utility displays a simple calendar in
		traditional format and ncal offers an alternative
		layout, more options and the date of Easter.  The
		new format is a little cramped but it makes a year
		fit on a 25x80 terminal.  If arguments are not
		specified, the current month is displayed.

"ncctl" : 
	NAME :
		ncctl – Control NFS kernel credentials
	DESCRIPTION :
		ncctl controls the caller's kernel Kerberos credentials
		for any of the specified path's associated NFS
		mounts.  If no paths are specified then all the
		caller's associated credentials for all NFS file
		systems are acted upon by the command given.

"ncdestroy" : 
	NAME :
		ncctl – Control NFS kernel credentials
	DESCRIPTION :
		ncctl controls the caller's kernel Kerberos credentials
		for any of the specified path's associated NFS
		mounts.  If no paths are specified then all the
		caller's associated credentials for all NFS file
		systems are acted upon by the command given.

"ncinit" : 
	NAME :
		ncctl – Control NFS kernel credentials
	DESCRIPTION :
		ncctl controls the caller's kernel Kerberos credentials
		for any of the specified path's associated NFS
		mounts.  If no paths are specified then all the
		caller's associated credentials for all NFS file
		systems are acted upon by the command given.

"nclist" : 
	NAME :
		ncctl – Control NFS kernel credentials
	DESCRIPTION :
		ncctl controls the caller's kernel Kerberos credentials
		for any of the specified path's associated NFS
		mounts.  If no paths are specified then all the
		caller's associated credentials for all NFS file
		systems are acted upon by the command given.

"ncurses5.4-config" : 
	NAME :
		ncurses5.4-config – report configuration and linking
		information for ncurses
	DESCRIPTION :
		The ncurses5.4-config utility reports ncurses
		configuration and linking information.

"ndp" : 
	NAME :
		ndp – control/diagnose IPv6 neighbor discovery
		protocol
	DESCRIPTION :
		The ndp command manipulates the address mapping
		table used by Neighbor Discovery Protocol (NDP).

"net-server" : 
	NAME :
		net-server - Base Net::Server starting module
	DESCRIPTION :
		The net-server program gives a simple way to test
		out code and try port connection parameters.  Though
		the running server can be robust enough for full
		tim use, it is anticipated that this binary will
		just be used for basic testing of net-server ports,
		acting as a simple echo server, or for running
		development scripts as CGI.

"net-server5.30" : 
	NAME :
		net-server - Base Net::Server starting module
	DESCRIPTION :
		The net-server program gives a simple way to test
		out code and try port connection parameters.  Though
		the running server can be robust enough for full
		tim use, it is anticipated that this binary will
		just be used for basic testing of net-server ports,
		acting as a simple echo server, or for running
		development scripts as CGI.

"net-snmp-cert" : 
	La commande "net-snmp-cert" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help net-snmp-cert", "info net-snmp-cert" ou "net-snmp-cert" pour essayer d'obtenir davantage d'informations.

"net-snmp-config" : 
	NAME :
		net-snmp-config - returns information about installed
		net-snmp libraries and binaries
	DESCRIPTION :
		The net-snmp-config shell script is designed to
		retrieve the configuration information about the
		libraries and binaries dealing with the Simple
		Network Management Protocol (SNMP), built from the
		net-snmp source package. The information is
		particularily useful for applications that need to
		link against the SNMP libraries and hence must know
		about any other libraries that must be linked in
		as well.

"net-snmp-create-v3-user" : 
	NAME :
		net-snmp-create-v3-user - create a SNMPv3 user in
		net-snmp configuration file
	DESCRIPTION :
		The net-snmp-create-v3-user shell script is designed
		to create a new user in net-snmp configuration file
		(/var/net-snmp/snmpd.conf by default).

"netbiosd" : 
	NAME :
		netbiosd – NetBIOS protocol daemon
	DESCRIPTION :
		netbiosd is responsible for interacting with NetBIOS
		networks.  netbiosd registers and defends one or
		more NetBIOS name, depending on the set of configured
		services. It also browses and scavenges names from
		the NetBIOS network, making them available to the
		system through mDNSResponder.

"netstat" : 
	NAME :
		netstat – show network status
	DESCRIPTION :
		The netstat command symbolically displays the
		contents of various network-related data structures.
		There are a number of output formats, depending on
		the options for the information presented.  The
		first form of the command displays a list of active
		sockets for each protocol.  The second form presents
		the contents of one of the other network data
		structures according to the option selected. Using
		the third form, with a wait interval specified,
		netstat will continuously display the information
		regarding packet traffic on the configured network
		interfaces.  The fourth form displays statistics
		for the specified protocol or address family. If a
		wait interval is specified, the protocol

"nettop" : 
	NAME :
		nettop – Display updated information about the
		network
	DESCRIPTION :
		The nettop program displays a list of sockets or
		routes. The counts for network structures are updated
		periodically. While the program is running the
		following keys may be used:

"networkQuality" : 
	NAME :
		networkQuality – Network quality testing tool
	DESCRIPTION :
		networkQuality allows for measuring the different
		aspects of Network Quality, including:

"networksetup" : 
	NAME :
		networksetup – configuration tool for network
		settings in System Preferences.
	DESCRIPTION :
		The networksetup command is used to configure network
		settings typically configured in the System Preferences
		application.  The networksetup command requires at
		least admin privileges to change network settings.
		If the "Require an administrator password to access
		system-wide preferences" option is selected in
		System Preferences > Security & Privacy, then root
		privileges are required to change network settings.

"newaliases" : 
	NAME :
		sendmail - Postfix to Sendmail compatibility interface
	DESCRIPTION :
		The Postfix sendmail(1) command implements the
		Postfix to Sendmail compatibility interface.  For
		the sake of compatibility with existing applications,
		some Sendmail command-line options are recognized
		but silently ignored.

"newfs_apfs" : 
	NAME :
		newfs_apfs – construct a new APFS volume
	DESCRIPTION :
		The newfs_apfs command creates a new APFS container
		on the device and/or adds a new APFS volume to a
		container.

"newfs_exfat" : 
	NAME :
		newfs_exfat – construct a new ExFAT file system
	DESCRIPTION :
		The newfs_exfat utility creates an ExFAT file system
		on device special. If the -R option is not given,
		and the device is already formatted as ExFAT, it
		will preserve the partition offset, bytes per
		cluster, FAT offset and size, number of FATs, offset
		to start of clusters, number of clusters, volume
		serial number, and volume name (label).  If a volume
		name was specified via the -v option, that name is
		used instead of the volume's previous name.

"newfs_hfs" : 
	NAME :
		newfs_hfs – construct a new HFS Plus file system
	DESCRIPTION :
		Newfs_hfs builds an HFS Plus file system on the
		specified special device. Before running newfs_hfs
		the disk should be partitioned using the Disk Utility
		application or pdisk(8).

"newfs_msdos" : 
	NAME :
		newfs_msdos – construct a new MS-DOS (FAT) file
		system
	DESCRIPTION :
		The newfs_msdos utility creates a FAT12, FAT16, or
		FAT32 file system on device special, using disktab(5)
		entry disktype to determine geometry, if required.

"newfs_udf" : 
	NAME :
		newfs_udf – construct a new UDF file system.
	DESCRIPTION :
		The newfs_udf command builds the UDF filesystem on
		the specified special device.

"newgrp" : 
	NAME :
		newgrp – change to a new group
	DESCRIPTION :
		The newgrp utility creates a new shell execution
		environment with modified real and effective group
		IDs.

"newproc.d" : 
	NAME :
		newproc.d - snoop new processes. Uses DTrace.
	DESCRIPTION :
		newproc.d is a DTrace OneLiner to snoop new processes
		as they are run. The argument listing is printed.

"newsyslog" : 
	NAME :
		newsyslog – maintain system log files to manageable
		sizes
	DESCRIPTION :
		The newsyslog utility should be scheduled to run
		periodically by cron(8). When it is executed it
		archives log files if necessary.  If a log file is
		determined to require archiving, newsyslog rearranges
		the files so that “logfile” is empty, “logfile.0”
		has the last period's logs in it, “logfile.1” has
		the next to last period's logs in it, and so on,
		up to a user-specified number of archived logs.
		Optionally the archived logs can be compressed to
		save space.

"ne_pas_run_fichier_de_sauvegarde_du_contenu_bash_profil" : 
	La commande "ne_pas_run_fichier_de_sauvegarde_du_contenu_bash_profil" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ne_pas_run_fichier_de_sauvegarde_du_contenu_bash_profil", "info ne_pas_run_fichier_de_sauvegarde_du_contenu_bash_profil" ou "ne_pas_run_fichier_de_sauvegarde_du_contenu_bash_profil" pour essayer d'obtenir davantage d'informations.

"nfs4mapid" : 
	NAME :
		nfs4mapid – shows NFSv4 mappings from uids or gids
		to over the wire string names and string names to
		uids or gids.
	DESCRIPTION :
		In the first form, nfs4mapid shows translations
		from NFSv4 string representations of users, and
		with the -G option, groups, to the corresponding
		local uids and gids.  In the second form shows the
		translations from opendirectoy GUIDS to NFSv4
		strings. The well known strings names (which are
		distinguished by a trailing ‘@’ ), such as "OWNER@"
		and "GROUP@" are represented locally by GUIDs and
		may not map to uids or gids. To map those GUIDS to
		NFSv4 strings use this form. The first form can be
		used to map the well known ids to GUIDs.  nfs4mapid
		does this by looking at the trailing ‘@’ sign. Note
		that NFSv4 well known names are always groups and
		are used in ACEs.  In the third form, it

"nfsd" : 
	NAME :
		nfsd – NFS server daemon
	DESCRIPTION :
		nfsd runs on an NFS server machine to service NFS
		and MOUNT protocol requests from NFS client machines.
		In order for a machine to act as an NFS server an
		NFS exports file, /etc/exports, must exist and the
		nfsd service must be enabled.

"nfsiod" : 
	NAME :
		nfsiod – local NFS asynchronous I/O
	DESCRIPTION :
		nfsiod is deprecated.  See nfs.conf(5) for NFS
		configuration info.

"nfsstat" : 
	NAME :
		nfsstat – display NFS statistics
	DESCRIPTION :
		nfsstat displays statistics kept about NFS client
		and server activity, active user activity, exported
		directories, and mount information.  In the absence
		of any options, nfsstat displays NFS client and
		server statistics.

"ngettext" : 
	NAME :
		ngettext - translate message and choose plural form
	DESCRIPTION :
		The ngettext program translates a natural language
		message into the user's language, by looking up the
		translation in a message catalog, and chooses the
		appropriate plural form, which depends on the number
		COUNT and the language of the message catalog where
		the translation was found.

"nice" : 
	NAME :
		nice – execute a utility at an altered scheduling
		priority
	DESCRIPTION :
		The nice utility runs utility at an altered scheduling
		priority, by incrementing its “nice” value by the
		specified increment, or a default value of 10.  The
		lower the nice value of a process, the higher its
		scheduling priority.

"nl" : 
	NAME :
		nl – line numbering filter
	DESCRIPTION :
		The nl utility reads lines from the named file,
		applies a configurable line numbering filter
		operation, and writes the result to the standard
		output.  If file is a single dash (‘-’) or absent,
		nl reads from the standard input.

"nlcontrol" : 
	NAME :
		nlcontrol – NETLOGON secure channel utility
	DESCRIPTION :
		The NETLOGON channel is a secure connection to a
		Windows Domain Controller that is used for non-Kerberos
		user authentication.  nlcontrol can be used to
		manipulate and test the status of the NETLOGON
		channel.

"nm" : 
	NAME :
		nm - display name list (symbol table)
	DESCRIPTION :
		As of Xcode 8.0 the default nm(1) tool is llvm-nm(1).
		For the most part nm(1) and llvm-nm(1) have the
		same options; notable exceptions include -f, -s,
		and -L as described below. This document explains
		options common between the two commands as well as
		some historically relevant options supported by
		nm-classic(1).  More help on options for llvm-nm(1)
		is provided when running it with the --help option.

"nmedit" : 
	NAME :
		nmedit - change global symbols to local symbols
	DESCRIPTION :
		Nmedit changes the global symbols not listed in the
		list_file file of the -s list_file option to static
		symbols.  Undefined symbols and common symbols are
		not affected and shouldn't be listed in list_file.
		For dynamic libraries symbols are turned into private
		extern symbols that are no longer external (rather
		than static symbols).  This is done so that the
		references between modules of a dynamic library are
		resolved to the symbols in the dynamic library.
		Nmedit differs from strip(1) in that it also changes
		the symbolic debugging information (produce by the
		-g option to cc(1)) for the global symbols it changes
		to static symbols so that the resulting object can
		still be used with

"nohup" : 
	NAME :
		nohup – invoke a utility immune to hangups
	DESCRIPTION :
		The nohup utility invokes utility with its arguments
		and at this time sets the signal SIGHUP to be
		ignored.  If the standard output is a terminal, the
		standard output is appended to the file nohup.out
		in the current directory.  If standard error is a
		terminal, it is directed to the same place as the
		standard output.

"nologin" : 
	NAME :
		nologin – politely refuse a login
	DESCRIPTION :
		The nologin is intended as a replacement shell field
		for accounts that have been disabled.

"not" : 
	La commande "not" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help not", "info not" ou "not" pour essayer d'obtenir davantage d'informations.

"notifyd" : 
	NAME :
		notifyd – notification server
	DESCRIPTION :
		notifyd is the server for the Mac OS X notification
		system described in notify(3).  The server is started
		automatically by launchd during system startup.

"notifyutil" : 
	NAME :
		notifyutil – notification command line utility
	DESCRIPTION :
		notifyutil is a command-line utility for interacting
		with the notify(3) notification system and the
		notifyd(8) server.  It may be used to post
		notifications, detect and report notifications, and
		to examine and set the state values associated with
		notification keys.

"nscurl" : 
	La commande "nscurl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help nscurl", "info nscurl" ou "nscurl" pour essayer d'obtenir davantage d'informations.

"nslookup" : 
	NAME :
		nslookup - query Internet name servers interactively
	DESCRIPTION :
		Nslookup is a program to query Internet domain name
		servers.  Nslookup has two modes: interactive and
		non-interactive. Interactive mode allows the user
		to query name servers for information about various
		hosts and domains or to print a list of hosts in a
		domain. Non-interactive mode is used to print just
		the name and requested information for a host or
		domain.

"nsupdate" : 
	NAME :
		nsupdate - Dynamic DNS update utility
	DESCRIPTION :
		nsupdate is used to submit Dynamic DNS Update
		requests as defined in RFC 2136 to a name server.
		This allows resource records to be added or removed
		from a zone without manually editing the zone file.
		A single update request can contain requests to add
		or remove more than one resource record.

"nvptx-arch" : 
	La commande "nvptx-arch" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help nvptx-arch", "info nvptx-arch" ou "nvptx-arch" pour essayer d'obtenir davantage d'informations.

"nvram" : 
	NAME :
		nvram – manipulate firmware NVRAM variables
	DESCRIPTION :
		The nvram command allows manipulation of firmware
		NVRAM variables.  It can be used to get or set a
		variable.  It can also be used to print all of the
		variables or set a list of variables from a file.
		Changes to NVRAM variables are only saved by clean
		restart or shutdown.

"obj2yaml" : 
	La commande "obj2yaml" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help obj2yaml", "info obj2yaml" ou "obj2yaml" pour essayer d'obtenir davantage d'informations.

"objdump" : 
	NAME :
		llvm-objdump - LLVM's object file dumper
	DESCRIPTION :
		The llvm-objdump utility prints the contents of
		object files and final linked images named on the
		command line. If no file name is specified,
		llvm-objdump will attempt to read from a.out. If -
		is used as a file name, llvm-objdump will process
		a file on its standard input stream.

"ocspcheck" : 
	La commande "ocspcheck" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ocspcheck", "info ocspcheck" ou "ocspcheck" pour essayer d'obtenir davantage d'informations.

"ocspd" : 
	NAME :
		ocspd – OCSP and CRL Daemon
	DESCRIPTION :
		ocspd performs caching and network fetching of
		Certificate Revocation Lists (CRLs) and Online
		Certificate Status Protocol (OCSP) responses. It
		is used by Security.framework during certificate
		verification. Security.framework communicates with
		ocspd via a private RPC interface. When Security.framework
		determines that a CRL is needed, or that it needs
		to perform an OCSP transaction, it performs an RPC
		to ocspd which then examines its cache to see if
		the appropriate CRL or OCSP response exists and is
		still valid. If so, that entity is returned to
		Security.framework. If no entry is found in cache,
		ocspd obtains it from the network, saving the result
		in cache before returning it to Security.framework.

"od" : 
	NAME :
		od – octal, decimal, hex, ASCII dump
	DESCRIPTION :
		The od utility is a filter which displays the
		specified files, or standard input if no files are
		specified, in a user specified format.

"odutil" : 
	NAME :
		odutil – allows caller to examine or change state
		of opendirectoryd(8)
	DESCRIPTION :
		Use odutil to look at internal state information
		for opendirectoryd, enable or disable logging, or
		change statistics settings.

"open" : 
	NAME :
		open – open files and directories
	DESCRIPTION :
		The open command opens a file (or a directory or
		URL), just as if you had double-clicked the file's
		icon. If no application name is specified, the
		default application as determined via LaunchServices
		is used to open the specified files.

"opendiff" : 
	La commande "opendiff" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help opendiff", "info opendiff" ou "opendiff" pour essayer d'obtenir davantage d'informations.

"opensnoop" : 
	NAME :
		opensnoop - snoop file opens as they occur. Uses
		DTrace.
	DESCRIPTION :
		opensnoop tracks file opens. As a process issues a
		file open, details such as UID, PID and pathname
		are printed out.

"openssl" : 
	NAME :
		openssl - OpenSSL command line program
	DESCRIPTION :
		OpenSSL is a cryptography toolkit implementing the
		Secure Sockets Layer (SSL v2/v3) and Transport Layer
		Security (TLS v1) network protocols and related
		cryptography standards required by them.

"opj_compress" : 
	NAME :
		opj_compress - This program reads in an image of a
		certain type and converts it to a jpeg2000 file.
		It is part of the OpenJPEG library.

"opj_decompress" : 
	NAME :
		opj_decompress - This program reads in a jpeg2000
		image and converts it to another image type. It is
		part of the OpenJPEG library.

"opj_dump" : 
	NAME :
		opj_dump - This program reads in a jpeg2000 image
		and dumps the contents to stdout. It is part of the
		OpenJPEG library.

"opt" : 
	La commande "opt" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help opt", "info opt" ou "opt" pour essayer d'obtenir davantage d'informations.

"orbd" : 
	La commande "orbd" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help orbd", "info orbd" ou "orbd" pour essayer d'obtenir davantage d'informations.

"ordi_eteindre" : 
	La commande "ordi_eteindre" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ordi_eteindre", "info ordi_eteindre" ou "ordi_eteindre" pour essayer d'obtenir davantage d'informations.

"ordi_redemarrer" : 
	La commande "ordi_redemarrer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help ordi_redemarrer", "info ordi_redemarrer" ou "ordi_redemarrer" pour essayer d'obtenir davantage d'informations.

"osacompile" : 
	NAME :
		osacompile – compile AppleScripts and other OSA
		language scripts
	DESCRIPTION :
		osacompile compiles the given files, or standard
		input if none are listed, into a single output
		script.  Files may be plain text or other compiled
		scripts.  The options are as follows:

"osadecompile" : 
	NAME :
		osadecompile – display compiled AppleScripts or
		other OSA language scripts
	DESCRIPTION :
		osadecompile writes the source text of the specified
		compiled script file to standard output.  There are
		no options; the correct language component is
		determined from the contents of the script file
		itself.

"osalang" : 
	NAME :
		osalang – information about installed OSA languages
	DESCRIPTION :
		osalang prints information about installed OSA
		languages.  With no options, it prints an unadorned
		list of language names to standard output.  These
		names can be passed to the -l options of osacompile(1)
		and osascript(1).  The options are as follows:

"osascript" : 
	NAME :
		osascript – execute OSA scripts (AppleScript,
		JavaScript, etc.)
	DESCRIPTION :
		osascript executes the given OSA script, which may
		be plain text or a compiled script (.scpt) created
		by Script Editor or osacompile(1).  By default,
		osascript treats plain text as AppleScript, but you
		can change this using the -l option.  To get a list
		of the OSA languages installed on your system, use
		osalang(1).

"otctl" : 
	NAME :
		otctl – Command line interface do provide diagnostic
		information for iCloud Keychain syncing
	DESCRIPTION :
		Command line interface do provide trust and sycning
		related information in sysdiagnose.

"otool" : 
	NAME :
		otool-classic - object file displaying tool
	DESCRIPTION :
		The otool-classic command displays specified parts
		of object files or libraries. It is the preferred
		tool for inspecting Mach-O binaries, especially for
		binaries that are bad, corrupted, or fuzzed. It is
		also useful in situations when inspecting files
		with new or "bleeding-edge" Mach-O file format
		changes.

"pack200" : 
	La commande "pack200" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pack200", "info pack200" ou "pack200" pour essayer d'obtenir davantage d'informations.

"package-stash-conflicts" : 

"package-stash-conflicts5.30" : 

"pagesize" : 
	NAME :
		pagesize – print system page size
	DESCRIPTION :
		The pagesize utility prints the size of a page of
		memory in bytes, as returned by getpagesize(3).
		This program is useful in constructing portable
		shell scripts.

"pagestuff" : 
	NAME :
		pagestuff - Mach-O file page analysis tool
	DESCRIPTION :
		pagestuff shows how a structure of a Mach-O or
		universal file corresponds to logical pages on the
		current system. Structural information includes the
		location and extent of file headers, sections and
		segments, symbol tables, code signatures, etc. When
		displaying a universal file, all architectures will
		be shown unless otherwise specified by the -arch
		flag.

"pal2rgb" : 
	NAME :
		pal2rgb - convert a palette color TIFF image to a
		full color image
	DESCRIPTION :
		pal2rgb converts a palette color image to a full
		color image by applying the colormap of the palette
		image to each sample to generate a full color RGB
		image.

"par.pl" : 
	NAME :
		par.pl - Make and run Perl Archives
	DESCRIPTION :
		This stand-alone command offers roughly the same
		feature as "perl -MPAR", except that it takes the
		pre-loaded .par files via "-Afoo.par" instead of
		"-MPAR=foo.par".

"par5.30.pl" : 
	La commande "par5.30.pl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help par5.30.pl", "info par5.30.pl" ou "par5.30.pl" pour essayer d'obtenir davantage d'informations.

"parl" : 
	NAME :
		parl - Binary PAR Loader
	DESCRIPTION :
		This stand-alone command offers roughly the same
		feature as "perl -MPAR", except that it takes the
		pre-loaded .par files via "-Afoo.par" instead of
		"-MPAR=foo.par".

"parl5.30" : 
	NAME :
		parl - Binary PAR Loader
	DESCRIPTION :
		This stand-alone command offers roughly the same
		feature as "perl -MPAR", except that it takes the
		pre-loaded .par files via "-Afoo.par" instead of
		"-MPAR=foo.par".

"parldyn" : 
	La commande "parldyn" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help parldyn", "info parldyn" ou "parldyn" pour essayer d'obtenir davantage d'informations.

"parldyn5.30" : 
	La commande "parldyn5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help parldyn5.30", "info parldyn5.30" ou "parldyn5.30" pour essayer d'obtenir davantage d'informations.

"passwd" : 
	NAME :
		asn1parse, ca, ciphers, cmp, cms, crl, crl2pkcs7,
		dgst, dhparam, dsa, dsaparam, ec, ecparam, enc,
		engine, errstr, gendsa, genpkey, genrsa, info, kdf,
		mac, nseq, ocsp, passwd, pkcs12, pkcs7, pkcs8, pkey,
		pkeyparam, pkeyutl, prime, rand, rehash, req, rsa,
		rsautl, s_client, s_server, s_time, sess_id, smime,
		speed, spkac, srp, storeutl, ts, verify, version,
		x509 - OpenSSL application commands
	DESCRIPTION :
		Every cmd listed above is a (sub-)command of the
		openssl(1) application.  It has its own detailed
		manual page at openssl-cmd(1). For example, to view
		the manual page for the openssl dgst command, type
		"man openssl-dgst".

"PasswordService" : 
	NAME :
		PasswordService – Mac OS X Server Password Server
		daemon
	DESCRIPTION :
		In the first synopsis form, PasswordService prints
		a usage summary or version information and quits.
		In the second form, PasswordService acts as a
		password server.

"paste" : 
	NAME :
		paste – merge corresponding or subsequent lines of
		files
	DESCRIPTION :
		The paste utility concatenates the corresponding
		lines of the given input files, replacing all but
		the last file's newline characters with a single
		tab character, and writes the resulting lines to
		standard output.  If end-of-file is reached on an
		input file while other input files still contain
		data, the file is treated as if it were an endless
		source of empty lines.

"patch" : 
	NAME :
		patch – apply a diff file to an original
	DESCRIPTION :
		patch will take a patch file containing any of the
		four forms of difference listing produced by the
		diff(1) program and apply those differences to an
		original file, producing a patched version.  If
		patchfile is omitted, or is a hyphen, the patch
		will be read from the standard input.

"pathchk" : 
	NAME :
		pathchk – check pathnames
	DESCRIPTION :
		The pathchk utility checks whether each of the
		specified pathname arguments is valid or portable.

"pathopens.d" : 
	NAME :
		pathopens.d - full pathnames opened ok count. Uses
		DTrace.
	DESCRIPTION :
		This program prints a count of the number of times
		files have been successfully opened. This is somewhat
		special in that the full pathname is calculated,
		even if the file open referred to a relative pathname.

"pax" : 
	NAME :
		pax – read and write file archives and copy directory
		hierarchies
	DESCRIPTION :
		pax will read, write, and list the members of an
		archive file and will copy directory hierarchies.
		pax operation is independent of the specific archive
		format and supports a wide variety of different
		archive formats. A list of supported archive formats
		can be found under the description of the -x option.

"pbcopy" : 
	NAME :
		pbcopy, pbpaste - provide copying and pasting to
		the pasteboard (the Clipboard) from command line
	DESCRIPTION :
		pbcopy takes the standard input and places it in
		the specified pasteboard. If no pasteboard is
		specified, the general pasteboard will be used by
		default.  The input is placed in the pasteboard as
		plain text data unless it begins with the Encapsulated
		PostScript (EPS) file header or the Rich Text Format
		(RTF) file header, in which case it is placed in
		the pasteboard as one of those data types.

"pbpaste" : 
	NAME :
		pbcopy, pbpaste - provide copying and pasting to
		the pasteboard (the Clipboard) from command line
	DESCRIPTION :
		pbcopy takes the standard input and places it in
		the specified pasteboard. If no pasteboard is
		specified, the general pasteboard will be used by
		default.  The input is placed in the pasteboard as
		plain text data unless it begins with the Encapsulated
		PostScript (EPS) file header or the Rich Text Format
		(RTF) file header, in which case it is placed in
		the pasteboard as one of those data types.

"pcap-config" : 
	NAME :
		pcap-config - write libpcap compiler and linker
		flags to standard output
	DESCRIPTION :
		When run with the --cflags option, pcap-config
		writes to the standard output the -I compiler flags
		required to include libpcap's header files.  When
		run with the --libs option, pcap-config writes to
		the standard output the -L and -l linker flags
		required to link with libpcap, including -l flags
		for libraries required by libpcap.  When run with
		the --additional-libs option, pcap-config writes
		to the standard output the -L and -l flags for
		libraries required by libpcap, but not the -lpcap
		flag to link with libpcap itself.

"pcre2-config" : 
	NAME :
		pcre2-config - program to return PCRE2 configuration
	DESCRIPTION :
		pcre2-config returns the configuration of the
		installed PCRE2 libraries and the options required
		to compile a program to use them. Some of the options
		apply only to the 8-bit, or 16-bit, or 32-bit
		libraries, respectively, and are not available for
		libraries that have not been built. If an unavailable
		option is encountered, the "usage" information is
		output.

"pcre2grep" : 
	NAME :
		pcre2grep - a grep with Perl-compatible regular
		expressions.
	DESCRIPTION :
		pcre2grep searches files for character patterns,
		in the same way as other grep commands do, but it
		uses the PCRE2 regular expression library to support
		patterns that are compatible with the regular
		expressions of Perl 5. See pcre2syntax(3) for a
		quick-reference summary of pattern syntax, or
		pcre2pattern(3) for a full description of the syntax
		and semantics of the regular expressions that PCRE2
		supports.

"pcre2test" : 
	NAME :
		pcre2test - a program for testing Perl-compatible
		regular expressions.
	DESCRIPTION :
		If pcre2test is given two filename arguments, it
		reads from the first and writes to the second. If
		the first name is "-", input is taken from the
		standard input. If pcre2test is given only one
		argument, it reads from that file and writes to
		stdout. Otherwise, it reads from stdin and writes
		to stdout.

"pcsctest" : 
	La commande "pcsctest" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pcsctest", "info pcsctest" ou "pcsctest" pour essayer d'obtenir davantage d'informations.

"pdisk" : 
	NAME :
		pdisk – Apple partition table editor
	DESCRIPTION :
		pdisk is a menu driven program which partitions
		disks using the standard Apple disk partitioning
		scheme described in "Inside Macintosh: Devices".
		It does not support the Intel/DOS partitioning
		scheme supported by fdisk(8).

"periodic" : 
	NAME :
		periodic – run periodic system functions
	DESCRIPTION :
		The periodic utility is intended to be called by
		launchd(8) to execute shell scripts located in the
		specified directory.

"perl" : 
	NAME :
		perl - The Perl 5 language interpreter
	DESCRIPTION :
		Perl officially stands for Practical Extraction and
		Report Language, except when it doesn't.

"perl5.30" : 
	NAME :
		perl - The Perl 5 language interpreter
	DESCRIPTION :
		Perl officially stands for Practical Extraction and
		Report Language, except when it doesn't.

"perl5.34" : 
	NAME :
		perl - The Perl 5 language interpreter
	DESCRIPTION :
		Perl officially stands for Practical Extraction and
		Report Language, except when it doesn't.

"perlbug" : 
	NAME :
		perlbug - how to submit bug reports on Perl
	DESCRIPTION :
		This program is designed to help you generate bug
		reports (and thank- you notes) about perl5 and the
		modules which ship with it.

"perlbug5.30" : 
	NAME :
		perlbug - how to submit bug reports on Perl
	DESCRIPTION :
		This program is designed to help you generate bug
		reports (and thank- you notes) about perl5 and the
		modules which ship with it.

"perlbug5.34" : 
	NAME :
		perlbug - how to submit bug reports on Perl
	DESCRIPTION :
		This program is designed to help you generate bug
		reports (and thank- you notes) about perl5 and the
		modules which ship with it.

"perldoc" : 
	NAME :
		perldoc - Look up Perl documentation in Pod format.
	DESCRIPTION :
		perldoc looks up documentation in .pod format that
		is embedded in the perl installation tree or in a
		perl script, and displays it using a variety of
		formatters.  This is primarily used for the
		documentation for the perl library modules.

"perldoc5.30" : 
	NAME :
		perldoc - Look up Perl documentation in Pod format.
	DESCRIPTION :
		perldoc looks up documentation in .pod format that
		is embedded in the perl installation tree or in a
		perl script, and displays it using a variety of
		formatters.  This is primarily used for the
		documentation for the perl library modules.

"perldoc5.34" : 
	NAME :
		perldoc - Look up Perl documentation in Pod format.
	DESCRIPTION :
		perldoc looks up documentation in .pod format that
		is embedded in the perl installation tree or in a
		perl script, and displays it using a variety of
		formatters.  This is primarily used for the
		documentation for the perl library modules.

"perlivp" : 
	NAME :
		perlivp - Perl Installation Verification Procedure
	DESCRIPTION :
		The perlivp program is set up at Perl source code
		build time to test the Perl version it was built
		under.  It can be used after running:

"perlivp5.30" : 
	NAME :
		perlivp - Perl Installation Verification Procedure
	DESCRIPTION :
		The perlivp program is set up at Perl source code
		build time to test the Perl version it was built
		under.  It can be used after running:

"perlivp5.34" : 
	NAME :
		perlivp - Perl Installation Verification Procedure
	DESCRIPTION :
		The perlivp program is set up at Perl source code
		build time to test the Perl version it was built
		under.  It can be used after running:

"perlthanks" : 
	NAME :
		perlbug - how to submit bug reports on Perl
	DESCRIPTION :
		This program is designed to help you generate bug
		reports (and thank- you notes) about perl5 and the
		modules which ship with it.

"perlthanks5.30" : 
	NAME :
		perlbug - how to submit bug reports on Perl
	DESCRIPTION :
		This program is designed to help you generate bug
		reports (and thank- you notes) about perl5 and the
		modules which ship with it.

"perlthanks5.34" : 
	NAME :
		perlbug - how to submit bug reports on Perl
	DESCRIPTION :
		This program is designed to help you generate bug
		reports (and thank- you notes) about perl5 and the
		modules which ship with it.

"perror" : 
	NAME :
		perror - display MySQL error message information
	DESCRIPTION :
		perror displays the error message for MySQL or
		operating system error codes. Invoke perror like
		this:

"pfctl" : 
	NAME :
		pfctl – control the packet filter (PF) and network
		address translation (NAT) device
	DESCRIPTION :
		The pfctl utility communicates with the packet
		filter device.  It allows ruleset and parameter
		configuration and retrieval of status information
		from the packet filter.

"pgrep" : 
	NAME :
		pgrep, pkill – find or signal processes by name
	DESCRIPTION :
		The pgrep command searches the process table on the
		running system and prints the process IDs of all
		processes that match the criteria given on the
		command line.

"pico" : 

"piconv" : 
	NAME :
		piconv -- iconv(1), reinvented in perl
	DESCRIPTION :
		piconv is perl version of iconv, a character encoding
		converter widely available for various Unixen today.
		This script was primarily a technology demonstrator
		for Perl 5.8.0, but you can use piconv in the place
		of iconv for virtually any case.

"piconv5.30" : 
	NAME :
		piconv -- iconv(1), reinvented in perl
	DESCRIPTION :
		piconv is perl version of iconv, a character encoding
		converter widely available for various Unixen today.
		This script was primarily a technology demonstrator
		for Perl 5.8.0, but you can use piconv in the place
		of iconv for virtually any case.

"piconv5.34" : 
	NAME :
		piconv -- iconv(1), reinvented in perl
	DESCRIPTION :
		piconv is perl version of iconv, a character encoding
		converter widely available for various Unixen today.
		This script was primarily a technology demonstrator
		for Perl 5.8.0, but you can use piconv in the place
		of iconv for virtually any case.

"pidpersec.d" : 
	NAME :
		pidpersec.d - print new PIDs per sec. Uses DTrace.
	DESCRIPTION :
		This script prints the number of new processes
		created per second.

"ping" : 
	NAME :
		ping – send ICMP ECHO_REQUEST packets to network
		hosts
	DESCRIPTION :
		The ping utility uses the ICMP protocol's mandatory
		ECHO_REQUEST datagram to elicit an ICMP ECHO_RESPONSE
		from a host or gateway.  ECHO_REQUEST datagrams
		(“pings”) have an IP and ICMP header, followed by
		a “struct timeval” and then an arbitrary number of
		“pad” bytes used to fill out the packet.  The options
		are as follows:

"ping6" : 
	NAME :
		ping6 – send ICMPv6 ECHO_REQUEST packets to network
		hosts
	DESCRIPTION :
		The ping6 utility uses the ICMPv6 protocol's mandatory
		ICMP6_ECHO_REQUEST datagram to elicit an ICMP6_ECHO_REPLY
		from a host or gateway. ICMP6_ECHO_REQUEST datagrams
		(``pings'') have an IPv6 header, and ICMPv6 header
		formatted as documented in RFC2463.  The options
		are as follows:

"pip3" : 
	La commande "pip3" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pip3", "info pip3" ou "pip3" pour essayer d'obtenir davantage d'informations.

"pip3.11" : 
	La commande "pip3.11" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pip3.11", "info pip3.11" ou "pip3.11" pour essayer d'obtenir davantage d'informations.

"pkg-config" : 
	NAME :
		pkg-config - Return metainformation about installed
		libraries  variable=VARIABLENAME=VARIABLEVALUE]
		[--print-variables] [--uninstalled] [--exists]
		[--atleast-version=VERSION] [--exact-
	DESCRIPTION :
		The pkg-config program is used to retrieve information
		about installed libraries in the system.  It is
		typically used to compile and link against one or
		more libraries.  Here is a typical usage scenario
		in a Makefile:

"pkgbuild" : 
	NAME :
		pkgbuild – Build a macOS Installer component package
		from on-disk files
	DESCRIPTION :
		A “component package” contains payload to be installed
		by the OS X Installer. Although a component package
		can be installed on its own, it is typically
		incorporated into a product archive, along with a
		“distribution” and localized resources, using
		productbuild(1).

"pkgutil" : 
	NAME :
		pkgutil – Query and manipulate Mac OS X Installer
		packages and receipts.
	DESCRIPTION :
		pkgutil reads and manipulates Mac OS X Installer
		flat packages, and provides access to the “receipt”
		database used by the Installer. Options are processed
		first, and affect the operation of all commands.
		Multiple commands are performed sequentially in the
		given order.

"pkill" : 
	NAME :
		pgrep, pkill – find or signal processes by name
	DESCRIPTION :
		The pgrep command searches the process table on the
		running system and prints the process IDs of all
		processes that match the criteria given on the
		command line.

"pl" : 
	NAME :
		pl – ASCII property list utility
	DESCRIPTION :
		pl can be used to check the syntax of old-style
		ASCII property list files.

"pl2pm" : 
	NAME :
		pl2pm - Rough tool to translate Perl4 .pl files to
		Perl5 .pm modules.
	DESCRIPTION :
		pl2pm is a tool to aid in the conversion of Perl4-style
		.pl library files to Perl5-style library modules.
		Usually, your old .pl file will still work fine and
		you should only use this tool if you plan to update
		your library to use some of the newer Perl 5 features,
		such as AutoLoading.

"pl2pm5.30" : 
	NAME :
		pl2pm - Rough tool to translate Perl4 .pl files to
		Perl5 .pm modules.
	DESCRIPTION :
		pl2pm is a tool to aid in the conversion of Perl4-style
		.pl library files to Perl5-style library modules.
		Usually, your old .pl file will still work fine and
		you should only use this tool if you plan to update
		your library to use some of the newer Perl 5 features,
		such as AutoLoading.

"pl2pm5.34" : 
	NAME :
		pl2pm - Rough tool to translate Perl4 .pl files to
		Perl5 .pm modules.
	DESCRIPTION :
		pl2pm is a tool to aid in the conversion of Perl4-style
		.pl library files to Perl5-style library modules.
		Usually, your old .pl file will still work fine and
		you should only use this tool if you plan to update
		your library to use some of the newer Perl 5 features,
		such as AutoLoading.

"plockstat" : 
	NAME :
		plockstat - front-end to DTrace to print statistics
		about POSIX mutexes and read/write locks

"pluginkit" : 
	NAME :
		pluginkit – plugin plug-in extension pluginkit
	DESCRIPTION :
		pluginkit manages the PlugInKit subsystem for the
		current user. It can query the plug-in database and
		make limited interventions for debugging and
		development.

"plutil" : 
	NAME :
		plutil – property list utility
	DESCRIPTION :
		plutil can be used to check the syntax of property
		list files, or convert a plist file from one format
		to another.  Specifying - as an input file reads
		from stdin.

"pmset" : 
	NAME :
		pmset – manipulate power management settings
	DESCRIPTION :
		pmset manages power management settings such as
		idle sleep timing, wake on administrative access,
		automatic restart on power loss, etc.

"png-fix-itxt" : 
	La commande "png-fix-itxt" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help png-fix-itxt", "info png-fix-itxt" ou "png-fix-itxt" pour essayer d'obtenir davantage d'informations.

"pngfix" : 
	La commande "pngfix" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pngfix", "info pngfix" ou "pngfix" pour essayer d'obtenir davantage d'informations.

"pod2html" : 
	NAME :
		pod2html - convert .pod files to .html files
	DESCRIPTION :
		Converts files from pod format (see perlpod) to
		HTML format.

"pod2html5.30" : 
	NAME :
		pod2html - convert .pod files to .html files
	DESCRIPTION :
		Converts files from pod format (see perlpod) to
		HTML format.

"pod2html5.34" : 
	NAME :
		pod2html - convert .pod files to .html files
	DESCRIPTION :
		Converts files from pod format (see perlpod) to
		HTML format.

"pod2man" : 
	NAME :
		pod2man - Convert POD data to formatted *roff input
	DESCRIPTION :
		pod2man is a front-end for Pod::Man, using it to
		generate *roff input from POD source.  The resulting
		*roff code is suitable for display on a terminal
		using nroff(1), normally via man(1), or printing
		using troff(1).

"pod2man5.30" : 
	NAME :
		pod2man - Convert POD data to formatted *roff input
	DESCRIPTION :
		pod2man is a front-end for Pod::Man, using it to
		generate *roff input from POD source.  The resulting
		*roff code is suitable for display on a terminal
		using nroff(1), normally via man(1), or printing
		using troff(1).

"pod2man5.34" : 
	NAME :
		pod2man - Convert POD data to formatted *roff input
	DESCRIPTION :
		pod2man is a front-end for Pod::Man, using it to
		generate *roff input from POD source.  The resulting
		*roff code is suitable for display on a terminal
		using nroff(1), normally via man(1), or printing
		using troff(1).

"pod2readme" : 
	NAME :
		pod2readme - Intelligently generate a README file
		from POD
	DESCRIPTION :
		This utility will use Pod::Readme to extract a
		README file from a POD document.

"pod2readme5.30" : 
	NAME :
		pod2readme - Intelligently generate a README file
		from POD
	DESCRIPTION :
		This utility will use Pod::Readme to extract a
		README file from a POD document.

"pod2text" : 
	NAME :
		pod2text - Convert POD data to formatted ASCII text
	DESCRIPTION :
		pod2text is a front-end for Pod::Text and its
		subclasses.  It uses them to generate formatted
		ASCII text from POD source.  It can optionally use
		either termcap sequences or ANSI color escape
		sequences to format the text.

"pod2text5.30" : 
	NAME :
		pod2text - Convert POD data to formatted ASCII text
	DESCRIPTION :
		pod2text is a front-end for Pod::Text and its
		subclasses.  It uses them to generate formatted
		ASCII text from POD source.  It can optionally use
		either termcap sequences or ANSI color escape
		sequences to format the text.

"pod2text5.34" : 
	NAME :
		pod2text - Convert POD data to formatted ASCII text
	DESCRIPTION :
		pod2text is a front-end for Pod::Text and its
		subclasses.  It uses them to generate formatted
		ASCII text from POD source.  It can optionally use
		either termcap sequences or ANSI color escape
		sequences to format the text.

"pod2usage" : 
	NAME :
		pod2usage - print usage messages from embedded pod
		docs in files
	DESCRIPTION :
		pod2usage will read the given input file looking
		for pod documentation and will print the corresponding
		usage message.  If no input file is specified then
		standard input is read.

"pod2usage5.30" : 
	NAME :
		pod2usage - print usage messages from embedded pod
		docs in files
	DESCRIPTION :
		pod2usage will read the given input file looking
		for pod documentation and will print the corresponding
		usage message.  If no input file is specified then
		standard input is read.

"pod2usage5.34" : 
	NAME :
		pod2usage - print usage messages from embedded pod
		docs in files
	DESCRIPTION :
		pod2usage will read the given input file looking
		for pod documentation and will print the corresponding
		usage message.  If no input file is specified then
		standard input is read.

"podchecker" : 
	NAME :
		podchecker - check the syntax of POD format
		documentation files
	DESCRIPTION :
		podchecker will read the given input files looking
		for POD syntax errors in the POD documentation and
		will print any errors it find to STDERR. At the
		end, it will print a status message indicating the
		number of errors found.

"podchecker5.30" : 
	NAME :
		podchecker - check the syntax of POD format
		documentation files
	DESCRIPTION :
		podchecker will read the given input files looking
		for POD syntax errors in the POD documentation and
		will print any errors it find to STDERR. At the
		end, it will print a status message indicating the
		number of errors found.

"podchecker5.34" : 
	NAME :
		podchecker - check the syntax of POD format
		documentation files
	DESCRIPTION :
		podchecker will read the given input files looking
		for POD syntax errors in the POD documentation and
		will print any errors it find to STDERR. At the
		end, it will print a status message indicating the
		number of errors found.

"podselect" : 
	NAME :
		podselect - print selected sections of pod documentation
		on standard output
	DESCRIPTION :
		podselect will read the given input files looking
		for pod documentation and will print out (in raw
		pod format) all sections that match one ore more
		of the given section specifications. If no section
		specifications are given than all pod sections
		encountered are output.

"policytool" : 
	La commande "policytool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help policytool", "info policytool" ou "policytool" pour essayer d'obtenir davantage d'informations.

"popd" : 
	DESCRIPTION :
		Enlève des répertoires de la pile. Enlève des
		éléments de la pile des répertoires. S'il n'y a pas
		d'argument, le répertoire en haut de la pile est
		enlevé et le nouveau sommet de la pile devient le
		répertoire de travail.

"postalias" : 
	NAME :
		postalias - Postfix alias database maintenance
	DESCRIPTION :
		The postalias(1) command creates or queries one or
		more Postfix alias databases, or updates an existing
		one. The input and output file formats are expected
		to be compatible with Sendmail version 8, and are
		expected to be suitable for the use as NIS alias
		maps.

"postcat" : 
	NAME :
		postcat - show Postfix queue file contents
	DESCRIPTION :
		The postcat(1) command prints the contents of the
		named files in human-readable form. The files are
		expected to be in Postfix queue file format. If no
		files are specified on the command line, the program
		reads from standard input.

"postconf" : 
	NAME :
		postconf - Postfix configuration utility
	DESCRIPTION :
		By default, the postconf(1) command displays the
		values of main.cf configuration parameters, and
		warns about possible mis-typed parameter names
		(Postfix 2.9 and later).  The command can also
		change main.cf configuration parameter values, or
		display other configuration information about the
		Postfix mail system.

"postdrop" : 
	NAME :
		postdrop - Postfix mail posting utility
	DESCRIPTION :
		The postdrop(1) command creates a file in the
		maildrop directory and copies its standard input
		to the file.

"postfix" : 
	NAME :
		postfix - Postfix control program
	DESCRIPTION :
		This command is reserved for the superuser. To
		submit mail, use the Postfix sendmail(1) command.

"postkick" : 
	NAME :
		postkick - kick a Postfix service
	DESCRIPTION :
		The postkick(1) command sends request to the specified
		service over a local transport channel.  This command
		makes Postfix private IPC accessible for use in,
		for example, shell scripts.

"postlock" : 
	NAME :
		postlock - lock mail folder and execute command
	DESCRIPTION :
		The postlock(1) command locks file for exclusive
		access, and executes command. The locking method
		is compatible with the Postfix UNIX-style local
		delivery agent.

"postlog" : 
	NAME :
		postlog - Postfix-compatible logging utility
	DESCRIPTION :
		The postlog(1) command implements a Postfix-compatible
		logging interface for use in, for example, shell
		scripts.

"postmap" : 
	NAME :
		postmap - Postfix lookup table management
	DESCRIPTION :
		The postmap(1) command creates or queries one or
		more Postfix lookup tables, or updates an existing
		one. The input and output file formats are expected
		to be compatible with:

"postmulti" : 
	NAME :
		postmulti - Postfix multi-instance manager
	DESCRIPTION :
		The postmulti(1) command allows a Postfix administrator
		to manage multiple Postfix instances on a single
		host.

"postqueue" : 
	NAME :
		postqueue - Postfix queue control
	DESCRIPTION :
		The postqueue(1) command implements the Postfix
		user interface for queue management. It implements
		operations that are traditionally available via the
		sendmail(1) command.  See the postsuper(1) command
		for queue operations that require super-user
		privileges such as deleting a message from the queue
		or changing the status of a message.

"postsuper" : 
	NAME :
		postsuper - Postfix superintendent
	DESCRIPTION :
		The postsuper(1) command does maintenance jobs on
		the Postfix queue. Use of the command is restricted
		to the superuser.  See the postqueue(1) command for
		unprivileged queue operations such as listing or
		flushing the mail queue.

"powermetrics" : 
	DESCRIPTION :
		powermetrics gathers and display CPU usage statistics
		(divided into time spent in user mode and supervisor
		mode), timer and interrupt wakeup frequency (total
		and, for near-idle workloads, those that resulted
		in package idle exits), and on supported platforms,
		interrupt frequencies (categorized by CPU number),
		package C-state statistics (an indication of the
		time the core complex + integrated graphics, if
		any, were in low- power idle states), CPU frequency
		distribution during the sample. The tool may also
		display estimated power consumed by various SoC
		subsystems, such as CPU, GPU, ANE (Apple Neural
		Engine).  Note: Average power values reported by
		powermetrics are estimated and may be inaccurate -
		hence they

"power_report.sh" : 
	La commande "power_report.sh" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help power_report.sh", "info power_report.sh" ou "power_report.sh" pour essayer d'obtenir davantage d'informations.

"pp" : 
	NAME :
		pp - PAR Packager
	DESCRIPTION :
		pp creates standalone executables from Perl programs,
		using the compressed packager provided by PAR, and
		dependency detection heuristics offered by
		Module::ScanDeps.  Source files are compressed
		verbatim without compilation.

"pp-trace" : 
	La commande "pp-trace" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pp-trace", "info pp-trace" ou "pp-trace" pour essayer d'obtenir davantage d'informations.

"pp5.30" : 
	NAME :
		pp - PAR Packager
	DESCRIPTION :
		pp creates standalone executables from Perl programs,
		using the compressed packager provided by PAR, and
		dependency detection heuristics offered by
		Module::ScanDeps.  Source files are compressed
		verbatim without compilation.

"ppdc" : 
	NAME :
		ppdc - cups ppd compiler (deprecated)
	DESCRIPTION :
		ppdc compiles PPDC source files into one or more
		PPD files.  This program is deprecated and will be
		removed in a future release of CUPS.

"ppdhtml" : 
	NAME :
		ppdhtml - cups html summary generator (deprecated)
	DESCRIPTION :
		ppdhtml reads a driver information file and produces
		a HTML summary page that lists all of the drivers
		in a file and the supported options. This program
		is deprecated and will be removed in a future release
		of CUPS.

"ppdi" : 
	NAME :
		ppdi - import ppd files (deprecated)
	DESCRIPTION :
		ppdi imports one or more PPD files into a PPD
		compiler source file. Multiple languages of the
		same PPD file are merged into a single printer
		definition to facilitate accurate changes for all
		localizations.  This program is deprecated and will
		be removed in a future release of CUPS.

"ppdmerge" : 
	NAME :
		ppdmerge - merge ppd files (deprecated)
	DESCRIPTION :
		ppdmerge merges two or more PPD files into a single,
		multi-language PPD file.  This program is deprecated
		and will be removed in a future release of CUPS.

"ppdpo" : 
	NAME :
		ppdpo - ppd message catalog generator (deprecated)
	DESCRIPTION :
		ppdpo extracts UI strings from PPDC source files
		and updates either a GNU gettext or macOS strings
		format message catalog source file for translation.
		This program is deprecated and will be removed in
		a future release of CUPS.

"ppm2tiff" : 
	NAME :
		ppm2tiff - create a TIFF file from PPM, PGM and PBM
		image files
	DESCRIPTION :
		ppm2tiff converts a file in the PPM, PGM and PBM
		image formats to TIFF. By default, the TIFF image
		is created with data samples packed
		(PlanarConfiguration=1), compressed with the Packbits
		algorithm (Compression=32773), and with each strip
		no more than 8 kilobytes. These characteristics can
		be overridden, or explicitly specified with the
		options described below.

"pppd" : 
	NAME :
		pppd - Point-to-Point Protocol Daemon
	DESCRIPTION :
		PPP is the protocol used for establishing internet
		links over dial-up modems, DSL connections, and
		many other types of point-to-point links. The pppd
		daemon works together with the kernel PPP driver
		to establish and maintain a PPP link with another
		system (called the peer) and to negotiate Internet
		Protocol (IP) addresses for each end of the link.
		Pppd can also authenticate the peer and/or supply
		authentication information to the peer.  PPP can
		be used with other network protocols besides IP,
		but such use is becoming increasingly rare.

"pr" : 
	NAME :
		pr – print files
	DESCRIPTION :
		The pr utility is a printing and pagination filter
		for text files.  When multiple input files are
		specified, each is read, formatted, and written to
		standard output.  By default, the input is separated
		into 66-line pages, each with

"praudit" : 
	NAME :
		praudit – print the contents of audit trail files
	DESCRIPTION :
		The praudit utility prints the contents of the audit
		trail files to the standard output in human-readable
		form.  If no file argument is specified, the standard
		input is used by default.

"priclass.d" : 
	NAME :
		priclass.d - priority distribution by scheduling
		class. Uses DTrace.
	DESCRIPTION :
		This is a simple DTrace script that samples at 1000
		Hz the current thread's scheduling class and priority.
		A distribution plot is printed.

"pridist.d" : 
	NAME :
		pridist.d - process priority distribution. Uses
		DTrace.
	DESCRIPTION :
		This is a simple DTrace script that samples at 1000
		Hz which process is on the CPUs, and what the
		priority is. A distribution plot is printed.

"printenv" : 
	NAME :
		printenv – print out the environment
	DESCRIPTION :
		The printenv utility prints out the names and values
		of the variables in the environment, with one
		name/value pair per line.  If name is specified,
		only its value is printed.

"printf" : 
	DESCRIPTION :
		Formate et affiche des ARGUMENTS en contrôlant le
		FORMAT. Options : -v var    affecte la sortie à la
		variable VAR du shell plutôt que de l'afficher sur
		la sortie standard

"procsystime" : 
	NAME :
		procsystime - analyse system call times. Uses DTrace.
	DESCRIPTION :
		procsystime prints details on system call times for
		processes, both the elapsed times and on-cpu times
		can be printed.

"productbuild" : 
	NAME :
		productbuild – Build a product archive for the macOS
		Installer or the Mac App Store.
	DESCRIPTION :
		A product archive is a flat file with a .pkg
		extension.  productbuild creates a deployable product
		archive, which can be used with the OS X Installer,
		or submitted to the Mac App Store. It has 5 different
		modes, as shown in the SYNOPSIS above:

"productsign" : 
	NAME :
		productsign – Sign a macOS Installer product archive
	DESCRIPTION :
		productsign adds a digital signature to a product
		archive previously created with productbuild(1).
		Although you can add a digital signature at the
		time you run productbuild(1), you may wish to add
		a signature later, once the product archive has
		been tested and is ready to deploy. If you run
		productsign on a product archive that was previously
		signed, the existing signature will be replaced.

"profiles" : 
	NAME :
		profiles – Profiles Tool for macOS.
	DESCRIPTION :
		profiles is used to handle various profile types
		on macOS.   Starting with macOS 11.0 (profiles tool
		8.0 or later), this tool cannot be used to install
		configuration profiles.  You should add your profiles
		using the System Settings Profiles preference pane.
		Additionally, startup profiles are no longer
		supported.

"prove" : 
	NAME :
		prove - Run tests through a TAP harness.

"prove5.30" : 
	NAME :
		prove - Run tests through a TAP harness.

"prove5.34" : 
	NAME :
		prove - Run tests through a TAP harness.

"ps" : 
	NAME :
		ps – process status
	DESCRIPTION :
		The ps utility displays a header line, followed by
		lines containing information about all of your
		processes that have controlling terminals.

"psicc" : 
	NAME :
		psicc - little cms PostScript converter.
	DESCRIPTION :
		lcms is a standalone CMM engine, which deals with
		the color management. It implements a fast
		transformation between ICC profiles.  psicc is a
		little cms PostScript converter.

"psm" : 
	NAME :
		psm – Command line interface to Apple Silicon
		password slot manager
	DESCRIPTION :
		A simple command line interface which lets you check
		the status of slots, and remove any orphaned slots.
		Slots are automatically allocated on a first come
		first serve bases, and this tool allows you to see
		which users are currently being protected by a
		specific slot.  To free up a slot you may need to
		delete an existing user from the system that is
		currently enrolled; thus allowing the next password
		change to enroll into Apple Silicon password slot
		manager.

"pstopdf" : 
	NAME :
		pstopdf – convert PostScript input into a PDF
		document.
	DESCRIPTION :
		pstopdf is a tool to convert PostScript input data
		into a PDF document. The input data may come from
		a file or may be read from stdin. The PDF document
		is always written to a file. The name of the output
		PDF file is derived from the name of the input file
		or may be explicitly named using the -o option.

"ptar" : 
	NAME :
		ptar - a tar-like program written in perl
	DESCRIPTION :
		ptar is a small, tar look-alike program that uses
		the perl module Archive::Tar to extract, create and
		list tar archives.

"ptar5.30" : 
	NAME :
		ptar - a tar-like program written in perl
	DESCRIPTION :
		ptar is a small, tar look-alike program that uses
		the perl module Archive::Tar to extract, create and
		list tar archives.

"ptar5.34" : 
	NAME :
		ptar - a tar-like program written in perl
	DESCRIPTION :
		ptar is a small, tar look-alike program that uses
		the perl module Archive::Tar to extract, create and
		list tar archives.

"ptardiff" : 
	NAME :
		ptardiff - program that diffs an extracted archive
		against an unextracted one
	DESCRIPTION :
		ptardiff is a small program that diffs an extracted
		archive against an unextracted one, using the perl
		module Archive::Tar.

"ptardiff5.30" : 
	NAME :
		ptardiff - program that diffs an extracted archive
		against an unextracted one
	DESCRIPTION :
		ptardiff is a small program that diffs an extracted
		archive against an unextracted one, using the perl
		module Archive::Tar.

"ptardiff5.34" : 
	NAME :
		ptardiff - program that diffs an extracted archive
		against an unextracted one
	DESCRIPTION :
		ptardiff is a small program that diffs an extracted
		archive against an unextracted one, using the perl
		module Archive::Tar.

"ptargrep" : 
	NAME :
		ptargrep - Apply pattern matching to the contents
		of files in a tar archive
	DESCRIPTION :
		This utility allows you to apply pattern matching
		to the contents of files contained in a tar archive.
		You might use this to identify all files in an
		archive which contain lines matching the specified
		pattern and either print out the pathnames or extract
		the files.

"ptargrep5.30" : 
	NAME :
		ptargrep - Apply pattern matching to the contents
		of files in a tar archive
	DESCRIPTION :
		This utility allows you to apply pattern matching
		to the contents of files contained in a tar archive.
		You might use this to identify all files in an
		archive which contain lines matching the specified
		pattern and either print out the pathnames or extract
		the files.

"ptargrep5.34" : 
	NAME :
		ptargrep - Apply pattern matching to the contents
		of files in a tar archive
	DESCRIPTION :
		This utility allows you to apply pattern matching
		to the contents of files contained in a tar archive.
		You might use this to identify all files in an
		archive which contain lines matching the specified
		pattern and either print out the pathnames or extract
		the files.

"purge" : 
	NAME :
		purge – force disk cache to be purged (flushed and
		emptied)
	DESCRIPTION :
		Purge can be used to approximate initial boot
		conditions with a cold disk buffer cache for
		performance analysis.  It does not affect anonymous
		memory that has been allocated through malloc,
		vm_allocate, etc.

"pushd" : 
	DESCRIPTION :
		Ajoute un répertoire à la pile. Ajoute un répertoire
		en haut de la pile des répertoires, ou permute la
		pile, de façon que le répertoire en haut de la pile
		devienne le nouveau répertoire de travail. S'il n'y
		a pas d'argument, les deux répertoires en haut de
		la pile sont échangés.

"pwd" : 
	DESCRIPTION :
		Affiche le nom du répertoire de travail courant.
		Options : -L        affiche la valeur de $PWD s'il
		nomme le répertoire de travail courant -P        affiche
		le répertoire physique, sans aucun lien symbolique

"pwd_mkdb" : 
	NAME :
		pwd_mkdb – generate the password databases
	DESCRIPTION :
		The pwd_mkdb utility creates db(3) style secure and
		insecure databases for the specified file.  These
		databases are then installed into /etc/spwd.db and
		/etc/pwd.db respectively.  The file is installed
		into /etc/master.passwd.  The file must be in the
		correct format (see passwd(5)).  It is important
		to note that the format used in this system is
		different from the historic Version 7 style format.

"pwpolicy" : 
	NAME :
		pwpolicy – gets and sets password policies
	DESCRIPTION :
		pwpolicy manipulates password policies.

"pydoc3" : 
	La commande "pydoc3" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pydoc3", "info pydoc3" ou "pydoc3" pour essayer d'obtenir davantage d'informations.

"pydoc3.11" : 
	La commande "pydoc3.11" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pydoc3.11", "info pydoc3.11" ou "pydoc3.11" pour essayer d'obtenir davantage d'informations.

"pyenv" : 
	NAME :
		pyenv - Simple Python version management
	DESCRIPTION :
		pyenv lets you easily switch between multiple
		versions of Python. It's simple, unobtrusive, and
		follows the UNIX tradition of single-purpose tools
		that do one thing well.

"pyenv-install" : 
	La commande "pyenv-install" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pyenv-install", "info pyenv-install" ou "pyenv-install" pour essayer d'obtenir davantage d'informations.

"pyenv-uninstall" : 
	La commande "pyenv-uninstall" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pyenv-uninstall", "info pyenv-uninstall" ou "pyenv-uninstall" pour essayer d'obtenir davantage d'informations.

"python-build" : 
	La commande "python-build" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help python-build", "info python-build" ou "python-build" pour essayer d'obtenir davantage d'informations.

"python3" : 
	NAME :
		python - an interpreted, interactive, object-oriented
		programming language
	DESCRIPTION :
		Python is an interpreted, interactive, object-oriented
		programming language that combines remarkable power
		with very clear syntax.  For an introduction to
		programming in Python, see the Python Tutorial.
		The Python Library Reference documents built-in and
		standard types, constants, functions and modules.
		Finally, the Python Reference Manual describes the
		syntax and semantics of the core language in (perhaps
		too) much detail.  (These documents may be located
		via the INTERNET RESOURCES below; they may be
		installed on your system as well.)

"python3-config" : 
	La commande "python3-config" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help python3-config", "info python3-config" ou "python3-config" pour essayer d'obtenir davantage d'informations.

"python3.11" : 
	NAME :
		python - an interpreted, interactive, object-oriented
		programming language
	DESCRIPTION :
		Python is an interpreted, interactive, object-oriented
		programming language that combines remarkable power
		with very clear syntax.  For an introduction to
		programming in Python, see the Python Tutorial.
		The Python Library Reference documents built-in and
		standard types, constants, functions and modules.
		Finally, the Python Reference Manual describes the
		syntax and semantics of the core language in (perhaps
		too) much detail.  (These documents may be located
		via the INTERNET RESOURCES below; they may be
		installed on your system as well.)

"python3.11-config" : 
	La commande "python3.11-config" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help python3.11-config", "info python3.11-config" ou "python3.11-config" pour essayer d'obtenir davantage d'informations.

"pzstd" : 
	La commande "pzstd" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help pzstd", "info pzstd" ou "pzstd" pour essayer d'obtenir davantage d'informations.

"qlmanage" : 
	NAME :
		qlmanage – Quick Look Server debug and management
		tool
	DESCRIPTION :
		qlmanage allows you to test your Quick Look generators
		and manage Quick Look Server.

"qprofdiff" : 
	La commande "qprofdiff" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help qprofdiff", "info qprofdiff" ou "qprofdiff" pour essayer d'obtenir davantage d'informations.

"question_oui_non" : 
	La commande "question_oui_non" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help question_oui_non", "info question_oui_non" ou "question_oui_non" pour essayer d'obtenir davantage d'informations.

"quota" : 
	NAME :
		quota – display disk usage and limits
	DESCRIPTION :
		Quota displays users' disk usage and limits.  By
		default only the user quotas are printed.

"quotacheck" : 
	NAME :
		quotacheck – filesystem quota consistency checker
	DESCRIPTION :
		Quotacheck examines each filesystem, builds a table
		of current disk usage, and compares this table
		against that recorded in the disk quota file for
		the filesystem.  If any inconsistencies are detected,
		both the quota file and the current system copy of
		the incorrect quotas are updated (the latter only
		occurs if an active filesystem is checked).  By
		default both user and group quotas are checked.

"quotaoff" : 
	NAME :
		quotaon, quotaoff – turn filesystem quotas on and
		off
	DESCRIPTION :
		Quotaon announces to the system that disk quotas
		should be enabled on one or more filesystems.
		Quotaoff announces to the system that the specified
		filesystems should have disk quotas turned off.
		The filesystem must be mounted and it must have the
		appropriate mount option file located at its root,
		the .quota.ops.user file for user quota configuration,
		and the .quota.ops.group file for group quota
		configuration.  Quotaon also expects each filesystem
		to have the appropriate quota data files located
		at its root, the .quota.user file for user data,
		and the .quota.group file for group data.  These
		filenames and their root location cannot be overridden.
		By default, quotaon will attempt to enable both
		user and

"quotaon" : 
	NAME :
		quotaon, quotaoff – turn filesystem quotas on and
		off
	DESCRIPTION :
		Quotaon announces to the system that disk quotas
		should be enabled on one or more filesystems.
		Quotaoff announces to the system that the specified
		filesystems should have disk quotas turned off.
		The filesystem must be mounted and it must have the
		appropriate mount option file located at its root,
		the .quota.ops.user file for user quota configuration,
		and the .quota.ops.group file for group quota
		configuration.  Quotaon also expects each filesystem
		to have the appropriate quota data files located
		at its root, the .quota.user file for user data,
		and the .quota.group file for group data.  These
		filenames and their root location cannot be overridden.
		By default, quotaon will attempt to enable both
		user and

"racoon" : 
	NAME :
		racoon – IKE (ISAKMP/Oakley) key management daemon
	DESCRIPTION :
		racoon is used to setup and maintain an IPSec tunnel
		or transport channel, between two devices, over
		which network traffic is conveyed securely.  This
		security is made possible by cryptographic keys and
		operations on both devices.  racoon relies on a
		standardized network protocol (IKE) to automatically
		negotiate and manage the cryptographic keys (e.g.
		security associations) that are necessary for the
		IPSec tunnel or transport channel to function.
		racoon speaks the IKE (ISAKMP/Oakley) key management
		protocol, to establish security associations with
		other hosts.  The SPD (Security Policy Database)
		in the kernel usually triggers racoon.  racoon
		usually sends all informational messages, warnings
		and

"rails" : 
	La commande "rails" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help rails", "info rails" ou "rails" pour essayer d'obtenir davantage d'informations.

"rake" : 
	La commande "rake" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help rake", "info rake" ou "rake" pour essayer d'obtenir davantage d'informations.

"ranlib" : 
	NAME :
		libtool - create libraries ranlib - add or update
		the table of contents of archive libraries
	DESCRIPTION :
		The libtool command takes the specified input object
		files and creates a library for use with the link
		editor, ld(1).  The library's name is specified by
		output (the argument to the -o flag).  The input
		object files may be in any correct format that
		contains object files (``universal'' files, archives,
		object files).  Libtool will not put any non-object
		input file into the output library (unlike ranlib,
		which allows this in the archives it operates on).

"rarpd" : 
	NAME :
		rarpd – Reverse ARP Daemon
	DESCRIPTION :
		Rarpd services Reverse ARP requests on the Ethernet
		connected to interface. Upon receiving a request,
		rarpd maps the target hardware address to an IP
		address via its name, which must be present in both
		the ethers(5) and hosts(5) databases.  If a host
		does not exist in both databases, the translation
		cannot proceed and a reply will not be sent.

"raw2tiff" : 
	NAME :
		raw2tiff - create a TIFF file from a raw data file
	DESCRIPTION :
		raw2tiff converts a raw byte sequence into TIFF.
		By default, the TIFF image is created with data
		samples packed (PlanarConfiguration=1), compressed
		with the PackBits algorithm (Compression=32773),
		and with each strip no more than 8 kilobytes.  These
		characteristics can overridden, or explicitly
		specified with the options described below.

"rdjpgcom" : 
	NAME :
		rdjpgcom - display text comments from a JPEG file
	DESCRIPTION :
		rdjpgcom reads the named JPEG/JFIF file, or the
		standard input if no file is named, and prints any
		text comments found in the file on the standard
		output.

"rdoc" : 
	La commande "rdoc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help rdoc", "info rdoc" ou "rdoc" pour essayer d'obtenir davantage d'informations.

"read" : 
	DESCRIPTION :
		Lit une ligne depuis l'entrée standard et la découper
		en morceaux. Lit une simple ligne depuis l'entrée
		standard ou depuis le descripteur de fichier FD si
		l'option « -u » est fournie.  La ligne est découpée
		en morceaux comme des mots, et le premier mot est
		assigné au premier NOM, le deuxième mot au deuxième
		NOM, et ainsi de suite, le dernier NOM récupérant
		la liste des mots restants. Seuls les caractères
		trouvés dans $IFS sont reconnus comme délimiteurs
		de mots. Par défaut, la barre oblique inverse échappe
		les caractères délimiteurs et les sauts de ligne.

"readarray" : 
	DESCRIPTION :
		Lit des lignes depuis un fichier vers une variable
		tableau. Synonyme de « mapfile ».

"readlink" : 
	NAME :
		stat, readlink – display file status
	DESCRIPTION :
		The stat utility displays information about the
		file pointed to by file. Read, write, or execute
		permissions of the named file are not required, but
		all directories listed in the pathname leading to
		the file must be searchable.  If no argument is
		given, stat displays information about the file
		descriptor for standard input.

"README" : 
	La commande "README" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help README", "info README" ou "README" pour essayer d'obtenir davantage d'informations.

"readonly" : 
	DESCRIPTION :
		Marque des variables du shell comme non modifiables.
		Marque chaque NOM comme étant en lecture seule ;
		les valeurs de ces NOMs ne peuvent plus être modifiées
		par des affectations ultérieures.  Si VALEUR est
		fournie, lui affecter la VALEUR avant le passage
		en lecture seule.

"realpath" : 
	NAME :
		realpath – return resolved physical path
	DESCRIPTION :
		The realpath utility uses the realpath(3) function
		to resolve all symbolic links, extra ‘/’ characters
		and references to /./ and /../ in path.  If path
		is absent, the current working directory (‘.’) is
		assumed.

"reboot" : 
	NAME :
		halt, reboot – stopping and restarting the system
	DESCRIPTION :
		The halt and reboot utilities flush the file system
		cache to disk, send all running processes a SIGTERM
		(and subsequently a SIGKILL) and, respectively,
		halt or restart the system.  The action is logged,
		including entering a shutdown record into the user
		accounting database.

"recode-sr-latin" : 
	NAME :
		recode-sr-latin - convert Serbian text from Cyrillic
		to Latin script
	DESCRIPTION :
		Recode Serbian text from Cyrillic to Latin script.
		The input text is read from standard input.  The
		converted text is output to standard output.

"redaction_message_erreur_dans_fichier" : 
	La commande "redaction_message_erreur_dans_fichier" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help redaction_message_erreur_dans_fichier", "info redaction_message_erreur_dans_fichier" ou "redaction_message_erreur_dans_fichier" pour essayer d'obtenir davantage d'informations.

"remplissage_tableau_app_ouverte" : 
	La commande "remplissage_tableau_app_ouverte" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help remplissage_tableau_app_ouverte", "info remplissage_tableau_app_ouverte" ou "remplissage_tableau_app_ouverte" pour essayer d'obtenir davantage d'informations.

"rendre_executable_tous" : 
	La commande "rendre_executable_tous" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help rendre_executable_tous", "info rendre_executable_tous" ou "rendre_executable_tous" pour essayer d'obtenir davantage d'informations.

"renice" : 
	NAME :
		renice – alter priority of running processes
	DESCRIPTION :
		The renice utility alters the scheduling priority
		of one or more running processes.  The following
		who parameters are interpreted as process ID's,
		process group ID's, user ID's or user names.  The
		renice'ing of a process group causes all processes
		in the process group to have their scheduling
		priority altered.  The renice'ing of a user causes
		all processes owned by the user to have their
		scheduling priority altered.  By default, the
		processes to be affected are specified by their
		process ID's.

"repairHomePermissions" : 
	La commande "repairHomePermissions" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help repairHomePermissions", "info repairHomePermissions" ou "repairHomePermissions" pour essayer d'obtenir davantage d'informations.

"repquota" : 
	NAME :
		repquota – summarize quotas for a file system
	DESCRIPTION :
		Repquota prints a summary of the disk usage and
		quotas for the specified file systems.

"reset" : 
	NAME :
		tput, reset - initialize a terminal or query terminfo
		database
	DESCRIPTION :
		The tput utility uses the terminfo database to make
		the values of terminal-dependent capabilities and
		information available to the shell (see sh(1)), to
		initialize or reset the terminal, or return the
		long name of the requested terminal type.  The
		result depends upon the capability's type:

"ResMerger" : 
	NAME :
		/usr/bin/ResMerger – Merges resource forks or files
		into one resource file (DEPRECATED)
	DESCRIPTION :
		Tools supporting Carbon development, including
		/usr/bin/ResMerger, were deprecated with Xcode 6.

"resolveLinks" : 
	NAME :
		resolveLinks – Resolves link requests in collections
		of HTML files

"retourne_choix_si_nb_correct_selon_select" : 
	La commande "retourne_choix_si_nb_correct_selon_select" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help retourne_choix_si_nb_correct_selon_select", "info retourne_choix_si_nb_correct_selon_select" ou "retourne_choix_si_nb_correct_selon_select" pour essayer d'obtenir davantage d'informations.

"return" : 
	DESCRIPTION :
		Termine depuis une fonction du shell. Entraîne
		l'arrêt d'une fonction ou d'un script sourcé, avec
		le code de retour spécifié par N. Si N est omis,
		le code de retour est celui de la dernière commande
		exécutée à l'intérieur de la fonction ou du script

"rev" : 
	NAME :
		rev – reverse lines of a file
	DESCRIPTION :
		The rev utility copies the specified files to the
		standard output, reversing the order of characters
		in every line.  If no files are specified, the
		standard input is read.

"Rez" : 
	NAME :
		Rez - compiles resources (DEPRECATED)
	DESCRIPTION :
		Tools supporting Carbon development, including Rez,
		were deprecated with Xcode 6.

"ri" : 
	NAME :
		ri – Ruby API reference front end  [--server[=PORT]]
		[--[no-]list-doc-dirs] [--no-standard-docs]
		[--[no-]{system|site|gems|home}] [--[no-]profile]
		[--dump=CACHE] [name ...]
	DESCRIPTION :
		ri is a command-line front end for the Ruby API
		reference.  You can search and read the API reference
		for classes and methods with ri.

"rm" : 
	NAME :
		rm, unlink – remove directory entries
	DESCRIPTION :
		The rm utility attempts to remove the non-directory
		type files specified on the command line.  If the
		permissions of the file do not permit writing, and
		the standard input device is a terminal, the user
		is prompted (on the standard error output) for
		confirmation.

"rmdir" : 
	NAME :
		rmdir – remove directories
	DESCRIPTION :
		The rmdir utility removes the directory entry
		specified by each directory argument, provided it
		is empty.

"rmic" : 
	La commande "rmic" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help rmic", "info rmic" ou "rmic" pour essayer d'obtenir davantage d'informations.

"rmid" : 
	La commande "rmid" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help rmid", "info rmid" ou "rmid" pour essayer d'obtenir davantage d'informations.

"rmiregistry" : 
	NAME :
		rmiregistry - create and start a remote object
		registry on the specified port on the current host
	DESCRIPTION :
		The rmiregistry command creates and starts a remote
		object registry on the specified port on the current
		host.  If the port is omitted, then the registry
		is started on port 1099.  The rmiregistry command
		produces no output and is typically run in the
		background, for example:

"rotatelogs" : 
	NAME :
		rotatelogs - Piped logging program to rotate Apache
		logs

"route" : 
	NAME :
		route – manually manipulate the routing tables
	DESCRIPTION :
		Route is a utility used to manually manipulate the
		network routing tables.  It normally is not needed,
		as a system routing table management daemon such
		as routed(8), should tend to this task.

"rpc.lockd" : 
	NAME :
		rpc.lockd – NFS file locking daemon
	DESCRIPTION :
		The rpc.lockd utility provides monitored and
		unmonitored file and record locking services in an
		NFS environment.  To monitor the status of hosts
		requesting locks, the locking daemon typically
		operates in conjunction with rpc.statd(8).

"rpc.statd" : 
	NAME :
		rpc.statd – host status monitoring daemon
	DESCRIPTION :
		The rpc.statd utility is a daemon which cooperates
		with rpc.statd daemons on other hosts to provide a
		status monitoring service.  The daemon accepts
		requests from programs running on the local host
		(typically, rpc.lockd(8), the NFS file locking
		daemon) to monitor the status of specified hosts.
		If a monitored host crashes and restarts, the remote
		daemon will notify the local daemon, which in turn
		will notify the local program(s) which requested
		the monitoring service.  Conversely, if this host
		crashes and restarts, the statd.notify service will
		be started to notify all of the hosts which were
		being monitored at the time of the crash.

"rpcbind" : 
	NAME :
		rpcbind – portmap
	DESCRIPTION :
		Rpcbind is a server that converts RPC program numbers
		into DARPA protocol port numbers.  It is a replacement
		for the older portmap program.

"rpcgen" : 
	NAME :
		rpcgen – Remote Procedure Call (RPC) protocol
		compiler
	DESCRIPTION :
		rpcgen is a tool that generates C code to implement
		an RPC protocol.  The input to rpcgen is a language
		similar to C known as RPC Language (Remote Procedure
		Call Language).  rpcgen is normally used as in the
		first synopsis where it takes an input file and
		generates up to four output files.  If the infile
		is named proto.x, then rpcgen will generate a header
		file in proto.h, XDR routines in proto_xdr.c,
		server-side stubs in proto_svc.c, and client-side
		stubs in proto_clnt.c.  With the -T option, it will
		also generate the RPC dispatch table in proto_tbl.i.
		With the -Sc option, it will also generate sample
		code which would illustrate how to use the remote
		procedures on the client side. This code would be

"rpcinfo" : 
	NAME :
		rpcinfo – report RPC information
	DESCRIPTION :
		rpcinfo makes an RPC call to an RPC server and
		reports what it finds. Unless otherwise specified
		or noted below the default rpcbind protocol version
		is 3.  With no or only generic options, call the
		rpcbind dump procedure on the specified host or
		“localhost” if host is not specified and display
		the results.  For versions 3 and 4 display the
		program number, the version, the “netid”, the
		universal address that the services is listening
		on, symbolic name of the program if known, and then
		the owner that registered the ⟨program, version,
		netid, address⟩ tuple.  For version 2 of the protocol
		list the program, version, protocol, port, and
		symbolic program name.  See the option below for a
		typically more useful

"rs" : 
	NAME :
		rs – reshape a data array
	DESCRIPTION :
		The rs utility reads the standard input, interpreting
		each line as a row of blank-separated entries in
		an array, transforms the array according to the
		options, and writes it on the standard output.  With
		no arguments it transforms stream input into a
		columnar format convenient for terminal viewing.

"rsync" : 
	NAME :
		rsync - faster, flexible replacement for rcp
	DESCRIPTION :
		rsync is a program that behaves in much the same
		way that rcp does, but has many more options and
		uses the rsync remote-update protocol to greatly
		speed up file transfers when the destination file
		is being updated.

"rtadvd" : 
	NAME :
		rtadvd – router advertisement daemon
	DESCRIPTION :
		rtadvd sends router advertisement packets to the
		specified interfaces.

"ruby" : 
	NAME :
		ruby – Interpreted object-oriented scripting language
	DESCRIPTION :
		Ruby is an interpreted scripting language for quick
		and easy object- oriented programming.  It has many
		features to process text files and to do system
		management tasks (like in Perl).  It is simple,
		straight- forward, and extensible.

"run-clang-tidy" : 
	La commande "run-clang-tidy" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help run-clang-tidy", "info run-clang-tidy" ou "run-clang-tidy" pour essayer d'obtenir davantage d'informations.

"rview" : 
	NAME :
		vim - Vi IMproved, a programmer's text editor
	DESCRIPTION :
		Vim is a text editor that is upwards compatible to
		Vi.  It can be used to edit all kinds of plain text.
		It is especially useful for editing programs.

"rvim" : 
	NAME :
		vim - Vi IMproved, a programmer's text editor
	DESCRIPTION :
		Vim is a text editor that is upwards compatible to
		Vi.  It can be used to edit all kinds of plain text.
		It is especially useful for editing programs.

"rwbypid.d" : 
	NAME :
		rwbypid.d - read/write calls by PID. Uses DTrace.
	DESCRIPTION :
		This script tracks the number of reads and writes
		at the syscall level by processes, printing the
		totals in a report. This matches reads and writes
		whether they succeed or not.

"rwbytype.d" : 
	NAME :
		rwbytype.d - read/write bytes by vnode type. Uses
		DTrace.
	DESCRIPTION :
		This program identifies the vnode type of read/write
		activity - whether that is for regular files,
		sockets, character special devices, etc. This is
		measuring at the application level, so file activity
		may well be cached by the system.

"rwsnoop" : 
	NAME :
		rwsnoop - snoop read/write events. Uses DTrace.
	DESCRIPTION :
		This is measuring reads and writes at the application
		level. This matches the syscalls read, write, pread
		and pwrite.

"sa" : 
	NAME :
		sa – print system accounting statistics
	DESCRIPTION :
		The sa utility reports on, cleans up, and generally
		maintains system accounting files.

"safaridriver" : 
	La commande "safaridriver" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help safaridriver", "info safaridriver" ou "safaridriver" pour essayer d'obtenir davantage d'informations.

"SafeEjectGPU" : 
	NAME :
		SafeEjectGPU – Facilitate safe eject/disconnect of
		eGPU(s) from system
	DESCRIPTION :
		The SafeEjectGPU command is used to prepare for
		safe eject/disconnect of eGPUs from the system.
		This involves interacting with apps to migrate off
		of ejecting eGPU(s), and triggering the eject itself.
		This tool can also be used to view what GPUs are
		attached to the system, their eject status, and
		what apps hold references to each.

"sample" : 
	NAME :
		sample – Profile a process during a time interval
	DESCRIPTION :
		sample is a command-line tool for gathering data
		about the running behavior of a process.  It suspends
		the process at specified intervals (by default,
		every 1 millisecond), records the call stacks of
		all threads in the process at that time, then resumes
		the process.  The analysis done by sample is called
		``sampling'' because it only checks the state of
		the program at the sampling points.  The analysis
		may miss execution of some functions that are not
		executing during one of the samples, but sample
		still provides useful data about commonly executing
		functions.

"sampleproc" : 
	NAME :
		sampleproc - sample processes on the CPUs. Uses
		DTrace.
	DESCRIPTION :
		This program samples which process is on each CPU,
		at a particular configurable rate. This can be used
		as an estimate for which process is consuming the
		most CPU time.

"sancov" : 
	La commande "sancov" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help sancov", "info sancov" ou "sancov" pour essayer d'obtenir davantage d'informations.

"sandbox-exec" : 
	NAME :
		sandbox-exec – execute within a sandbox (DEPRECATED)
	DESCRIPTION :
		The sandbox-exec command is DEPRECATED.  Developers
		who wish to sandbox an app should instead adopt the
		App Sandbox feature described in the App Sandbox
		Design Guide.  The sandbox-exec command enters a
		sandbox using a profile specified by the -f, -n,
		or -p option and executes command with arguments.

"sanstats" : 
	La commande "sanstats" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help sanstats", "info sanstats" ou "sanstats" pour essayer d'obtenir davantage d'informations.

"say" : 
	NAME :
		say - Convert text to audible speech
	DESCRIPTION :
		This tool uses the Speech Synthesis manager to
		convert input text to audible speech and either
		play it through the sound output device chosen in
		System Preferences or save it to an AIFF file.

"scala" : 
	La commande "scala" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help scala", "info scala" ou "scala" pour essayer d'obtenir davantage d'informations.

"scalac" : 
	La commande "scalac" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help scalac", "info scalac" ou "scalac" pour essayer d'obtenir davantage d'informations.

"scaladoc" : 
	La commande "scaladoc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help scaladoc", "info scaladoc" ou "scaladoc" pour essayer d'obtenir davantage d'informations.

"scan-build" : 
	NAME :
		scan-build – Clang static analyzer
	DESCRIPTION :
		scan-build is a Perl script that invokes the Clang
		static analyzer. Options used by scan-build or by
		the analyzer appear first, followed by the build_command
		and any build_options normally used to build the
		target system.

"scan-build-py" : 
	La commande "scan-build-py" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help scan-build-py", "info scan-build-py" ou "scan-build-py" pour essayer d'obtenir davantage d'informations.

"scan-view" : 
	La commande "scan-view" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help scan-view", "info scan-view" ou "scan-view" pour essayer d'obtenir davantage d'informations.

"scandeps.pl" : 
	NAME :
		scandeps.pl - Scan file prerequisites
	DESCRIPTION :
		scandeps.pl is a simple-minded utility that prints
		out the "PREREQ_PM" section needed by modules.

"scandeps5.30.pl" : 
	La commande "scandeps5.30.pl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help scandeps5.30.pl", "info scandeps5.30.pl" ou "scandeps5.30.pl" pour essayer d'obtenir davantage d'informations.

"schemagen" : 
	La commande "schemagen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help schemagen", "info schemagen" ou "schemagen" pour essayer d'obtenir davantage d'informations.

"scp" : 
	NAME :
		scp – OpenSSH secure file copy
	DESCRIPTION :
		scp copies files between hosts on a network.

"screen" : 
	NAME :
		screen - screen manager with VT100/ANSI terminal
		emulation
	DESCRIPTION :
		Screen is a full-screen window manager that multiplexes
		a physical terminal between several processes
		(typically interactive shells). Each virtual terminal
		provides the functions of a DEC VT100 terminal and,
		in addition, several control functions from the ISO
		6429 (ECMA 48, ANSI X3.64) and ISO 2022 standards
		(e.g. insert/delete line and support for multiple
		character sets).  There is a scrollback history
		buffer for each virtual terminal and a copy-and-paste
		mechanism that allows moving text regions between
		windows.

"screencapture" : 
	NAME :
		screencapture – capture images from the screen and
		save them to a file or the clipboard
	DESCRIPTION :
		The screencapture utility is not very well documented
		to date.  A list of options follows.

"script" : 
	NAME :
		script – make typescript of terminal session
	DESCRIPTION :
		The script utility makes a typescript of everything
		printed on your terminal.  It is useful for students
		who need a hardcopy record of an interactive session
		as proof of an assignment, as the typescript file
		can be printed out later with lpr(1).

"scselect" : 
	NAME :
		scselect – Select system configuration "location"
	DESCRIPTION :
		scselect provides access to the system configuration
		sets, commonly referred to as "locations".  When
		invoked with no arguments, scselect displays the
		names and associated identifiers for each defined
		"location" and indicates which is currently active.
		scselect also allows the user to select or change
		the active "location" by specifying its name or
		identifier.  Changing the "location" causes an
		immediate system re- configuration, unless the -n
		option is supplied.

"scutil" : 
	NAME :
		scutil – Manage system configuration parameters
	DESCRIPTION :
		Invoked with no options, scutil provides a command
		line interface to the "dynamic store" data maintained
		by configd(8).  Interaction with this data (using
		the SystemConfiguration.framework SCDynamicStore
		APIs) is handled with a set of commands read from
		standard input.  A list of the available commands
		is available by entering the help directive.

"sc_auth" : 
	NAME :
		sc_auth – SmartCard authorization setup script
	DESCRIPTION :
		sc_auth configures a local user account to permit
		authentication using a supported SmartCard.
		Authentication is via asymmetric key (also known
		as public-key) encryption.  sc_auth works with
		signing keys, but not encryption keys.

"sc_usage" : 
	NAME :
		sc_usage – show system call usage statistics
	DESCRIPTION :
		sc_usage displays an ongoing sample of system call
		and page fault usage statistics for a given process
		in a “top-like” fashion.  It requires root privileges
		due to the kernel tracing facility it uses to
		operate.

"sdef" : 
	La commande "sdef" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help sdef", "info sdef" ou "sdef" pour essayer d'obtenir davantage d'informations.

"sdiff" : 
	NAME :
		sdiff – side-by-side diff
	DESCRIPTION :
		sdiff displays two files side by side, with any
		differences between the two highlighted as follows:
		new lines are marked with ‘>’; deleted lines are
		marked with ‘<’; and changed lines are marked with
		‘|’.

"sdp" : 
	La commande "sdp" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help sdp", "info sdp" ou "sdp" pour essayer d'obtenir davantage d'informations.

"sdx" : 
	NAME :
		sdx - Starkit Developer eXtension
	DESCRIPTION :
		sdx combines a number of functions into a single
		command-line developer utility. Its most common use
		is to create, browse, and unravel Starkits:

"security" : 
	NAME :
		security – Command line interface to keychains and
		Security framework
	DESCRIPTION :
		A simple command line interface which lets you
		administer keychains, manipulate keys and certificates,
		and do just about anything the Security framework
		is capable of from the command line.

"securityd" : 
	NAME :
		securityd – Security context daemon for keychain
		and cryptographic operations
	DESCRIPTION :
		securityd maintains security contexts and arbitrates
		cryptographic operations.  Access to keychain items
		is routed through securityd to enforce access
		controls and to keep private keys out of user process
		address space.  All user interaction with securityd
		is mediated through the Security Agent.

"sed" : 
	NAME :
		sed – stream editor
	DESCRIPTION :
		The sed utility reads the specified files, or the
		standard input if no files are specified, modifying
		the input as specified by a list of commands.  The
		input is then written to the standard output.

"seeksize.d" : 
	NAME :
		seeksize.d - print disk event seek report. Uses
		DTrace.
	DESCRIPTION :
		seeksize.d is a simple DTrace program to print a
		report of disk event seeks by process.

"segedit" : 
	NAME :
		segedit - extract and replace sections from object
		files
	DESCRIPTION :
		segedit extracts or replaces named sections from
		the input_file. When extracting sections, segedit
		will write the contents of each requested section
		into data_file. When replacing sections, segedit
		will write a new output_file formed from the
		input_file and the requested replacement section
		content from data_file. The segment and section
		names are the same as those given to ld(1) with the
		-sectcreate option. The segment and section names
		of an object file can be examined with the -l option
		to otool(1).  Only sections in segments that have
		no relocation to or from them (i.e., segments marked
		with the SG_NORELOC flag) can be replaced but all
		sections can be extracted.

"select" : 
	La commande "select" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help select", "info select" ou "select" pour essayer d'obtenir davantage d'informations.

"select_parmi_liste" : 
	La commande "select_parmi_liste" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help select_parmi_liste", "info select_parmi_liste" ou "select_parmi_liste" pour essayer d'obtenir davantage d'informations.

"sendmail" : 
	NAME :
		sendmail - Postfix to Sendmail compatibility interface
	DESCRIPTION :
		The Postfix sendmail(1) command implements the
		Postfix to Sendmail compatibility interface.  For
		the sake of compatibility with existing applications,
		some Sendmail command-line options are recognized
		but silently ignored.

"seq" : 
	NAME :
		seq – print sequences of numbers
	DESCRIPTION :
		The seq utility prints a sequence of numbers, one
		per line (default), from first (default 1), to near
		last as possible, in increments of incr (default
		1).  When first is larger than last, the default
		incr is -1.

"serialver" : 
	NAME :
		serialver - return the serialVersionUID for one or
		more classes in a form suitable for copying into
		an evolving class
	DESCRIPTION :
		The serialver command returns the serialVersionUID
		for one or more classes in a form suitable for
		copying into an evolving class.  When called with
		no arguments, the serialver command prints a usage
		line.

"serverinfo" : 
	La commande "serverinfo" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help serverinfo", "info serverinfo" ou "serverinfo" pour essayer d'obtenir davantage d'informations.

"servertool" : 
	La commande "servertool" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help servertool", "info servertool" ou "servertool" pour essayer d'obtenir davantage d'informations.

"set" : 
	DESCRIPTION :
		Définit ou invalide des valeurs d'options et des
		paramètres de position du shell. Change la valeur
		des attributs du shell et des paramètres de position,
		ou affiche les noms et valeurs des variables du
		shell.

"set-xcode-analyzer" : 
	La commande "set-xcode-analyzer" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help set-xcode-analyzer", "info set-xcode-analyzer" ou "set-xcode-analyzer" pour essayer d'obtenir davantage d'informations.

"SetFile" : 
	NAME :
		/usr/bin/SetFile – set attributes of files and
		directories (DEPRECATED)
	DESCRIPTION :
		Tools supporting Carbon development, including
		/usr/bin/SetFile, were deprecated with Xcode 6.

"setkey" : 
	NAME :
		setkey – manually manipulate the IPsec SA/SP database
	DESCRIPTION :
		setkey adds, updates, dumps, or flushes Security
		Association Database (SAD) entries as well as
		Security Policy Database (SPD) entries in the kernel.

"setquota" : 
	NAME :
		setquota – Sets home directory quotas on the local
		computer.
	DESCRIPTION :
		setquota provides several options for creating and
		updating home directory quotas.  Quotas are set as
		specified in the individual user record.

"setregion" : 
	DESCRIPTION :
		setregion is used by the DVD Player to set the disc
		region code for a DVD drive.

"setuids.d" : 
	NAME :
		setuids.d - snoop setuid calls as they occur. Uses
		DTrace.
	DESCRIPTION :
		setuids.d is a simple DTrace program to print details
		of setuid calls, where a process assumes a different
		UID. These are usually related to login events.

"sfltool" : 
	NAME :
		sfltool – tool for testing and debugging SharedFileList.
	DESCRIPTION :
		sfltool a tool for testing and debugging SharedFileList.
		sfltool archive [-z] creates a directory containing
		a snapshot of the content of the SharedFileList
		persistant store locations. Adding -z will archive
		the a directory.

"sftp" : 
	NAME :
		sftp – OpenSSH secure file transfer
	DESCRIPTION :
		sftp is a file transfer program, similar to ftp(1),
		which performs all operations over an encrypted
		ssh(1) transport.  It may also use many features
		of ssh, such as public key authentication and
		compression.

"sh" : 
	NAME :
		sh – POSIX-compliant command interpreter
	DESCRIPTION :
		sh is a POSIX-compliant command interpreter (shell).
		It is implemented by re-execing as either bash(1),
		dash(1), or zsh(1) as determined by the symbolic
		link located at /private/var/select/sh.  If
		/private/var/select/sh does not exist or does not
		point to a valid shell, sh will use one of the
		supported shells.

"shar" : 
	NAME :
		shar – create a shell archive of files
	DESCRIPTION :
		The shar command writes a sh(1) shell script to the
		standard output which will recreate the file hierarchy
		specified by the command line operands. Directories
		will be recreated and must be specified before the
		files they contain (the find(1) utility does this
		correctly).

"sharing" : 
	NAME :
		sharing – create share points for smb services.
	DESCRIPTION :
		A list of flags and their descriptions:

"shasum" : 
	NAME :
		shasum - Print or Check SHA Checksums
	DESCRIPTION :
		Running shasum is often the quickest way to compute
		SHA message digests.  The user simply feeds data
		to the script through files or standard input, and
		then collects the results from standard output.

"shasum5.30" : 
	NAME :
		shasum - Print or Check SHA Checksums
	DESCRIPTION :
		Running shasum is often the quickest way to compute
		SHA message digests.  The user simply feeds data
		to the script through files or standard input, and
		then collects the results from standard output.

"shasum5.34" : 
	NAME :
		shasum - Print or Check SHA Checksums
	DESCRIPTION :
		Running shasum is often the quickest way to compute
		SHA message digests.  The user simply feeds data
		to the script through files or standard input, and
		then collects the results from standard output.

"shazam" : 
	NAME :
		shazam – A utility for ShazamKit
	DESCRIPTION :
		The shazam utility offers commands for creating
		signatures as well as processing custom catalogs
		to build better audio experiences with ShazamKit.
		Current operations supported for a custom catalog
		include:

"shift" : 
	DESCRIPTION :
		Décale des paramètres de position. Renomme les
		paramètres de position $N+1,$N+2 ... à $1,$2 ...
		Si N n'est pas donné, il est supposé égal à 1.

"shlock" : 
	NAME :
		shlock – create or verify a lock file for shell
		scripts
	DESCRIPTION :
		NOTE: The shlock command is deprecated, and lockf(1)
		should be used instead.

"shopt" : 
	DESCRIPTION :
		Active ou désactive des options du shell. Change
		la valeur de chaque option du shell NOMOPT.  S'il
		n'y a pas d'argument à l'option, liste chaque NOMOPT
		fourni ou toutes les options du shell si aucun
		NOMOPT est donné, avec une indication montrant si
		chacun est actif ou non.

"shortcuts" : 
	NAME :
		shortcuts – Command-line utility for running
		shortcuts.
	DESCRIPTION :
		The shortcuts command is used to run, list, view
		or sign shortcuts. To create or edit a shortcut,
		use the Shortcuts application.

"showmount" : 
	NAME :
		showmount – show remote NFS mounts on host
	DESCRIPTION :
		showmount shows status information about the NFS
		server on host.  By default it prints the names of
		all hosts that have NFS file systems mounted on the
		host. See NFS: Network File System Protocol
		Specification, RFC 1094, Appendix A, and NFS: Network
		File System Version 3 Protocol Specification,
		Appendix I, for a detailed description of the
		protocol.

"shutdown" : 
	NAME :
		shutdown – close down the system at a given time
	DESCRIPTION :
		The shutdown utility provides an automated shutdown
		procedure for super- users to nicely notify users
		when the system is shutting down, saving them from
		system administrators, hackers, and gurus, who would
		otherwise not bother with such niceties.

"sigdist.d" : 
	NAME :
		sigdist.d - signal distribution by process. Uses
		DTrace.
	DESCRIPTION :
		This is a simple DTrace script that prints the
		number of signals recieved by process and signal
		number. This script is also available as
		/usr/demo/dtrace/sig.d, where it originates.

"signaux_et_combinaison_de_touche" : 
	La commande "signaux_et_combinaison_de_touche" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help signaux_et_combinaison_de_touche", "info signaux_et_combinaison_de_touche" ou "signaux_et_combinaison_de_touche" pour essayer d'obtenir davantage d'informations.

"sips" : 
	NAME :
		sips – scriptable image processing system.
	DESCRIPTION :
		This tool is used to query or modify raster image
		files and ColorSync ICC profiles.  Its functionality
		can also be used through the "Image Events" AppleScript
		suite.  It also supports executing JavaScript to
		either modify or generate images.

"size" : 
	NAME :
		size - print the size of the sections in an object
		file
	DESCRIPTION :
		Size (without the -m option) prints the (decimal)
		number of bytes required by the __TEXT, __DATA and
		__OBJC segments.  All other segments are totaled
		and that size is listed in the `others' column.
		The final two columns is the sum in decimal and
		hexadecimal.  If no file is specified, a.out is
		used.

"skywalkctl" : 
	NAME :
		skywalkctl - Interact with Skywalk subsystem
	DESCRIPTION :
		skywalkctl is a utility used to interact with the
		Skywalk subsystem, which provides the plumbing
		between various networking-related pieces of software
		and hardware.  It should only be used in a test and
		debug context. Using it for any other purpose is
		strongly discouraged.  It is composed of multiple
		commands list below. Use skywalkctl COMMAND help
		to see usage of each command.

"slapacl" : 
	NAME :
		slapacl - Check access to a list of attributes.
	DESCRIPTION :
		slapacl is used to check the behavior of slapd(8)
		by verifying access to directory data according to
		the access control list directives defined in its
		configuration.  It opens the slapd.conf(5) configuration
		file or the slapd-config(5) backend, reads in the
		access/olcAccess directives, and then parses the
		attr list given on the command-line; if none is
		given, access to the entry pseudo-attribute is
		tested.

"slapadd" : 
	NAME :
		slapadd - Add entries to a SLAPD database
	DESCRIPTION :
		Slapadd is used to add entries specified in LDAP
		Directory Interchange Format (LDIF) to a slapd(8)
		database.  It opens the given database determined
		by the database number or suffix and adds entries
		corresponding to the provided LDIF to the database.
		Databases configured as subordinate of this one are
		also updated, unless -g is specified.  The LDIF
		input is read from standard input or the specified
		file.

"slapauth" : 
	NAME :
		slapauth - Check a list of string-represented IDs
		for LDAP authc/authz
	DESCRIPTION :
		Slapauth is used to check the behavior of the slapd
		in mapping identities for authentication and
		authorization purposes, as specified in slapd.conf(5).
		It opens the slapd.conf(5) configuration file or
		the slapd-config(5) backend, reads in the
		authz-policy/olcAuthzPolicy and authz-regexp/olcAuthzRegexp
		directives, and then parses the ID list given on
		the command-line.

"slapcat" : 
	NAME :
		slapcat - SLAPD database to LDIF utility
	DESCRIPTION :
		Slapcat is used to generate an LDAP Directory
		Interchange Format (LDIF) output based upon the
		contents of a slapd(8) database.  It opens the given
		database determined by the database number or suffix
		and writes the corresponding LDIF to standard output
		or the specified file. Databases configured as
		subordinate of this one are also output, unless -g
		is specified.

"slapconfig" : 
	NAME :
		slapconfig -- tool to configure slapd and related
		daemons
	DESCRIPTION :
		slapconfig is a utility for configuring slapd. It
		must be run by root.

"slapdn" : 
	NAME :
		slapdn - Check a list of string-represented LDAP
		DNs based on schema syntax
	DESCRIPTION :
		Slapdn is used to check the conformance of a DN
		based on the schema defined in slapd(8) and that
		loaded via slapd.conf(5).  It opens the slapd.conf(5)
		configuration file or the slapd-config (5) backend,
		reads in the schema definitions, and then parses
		the DN list given on the command-line.

"slapindex" : 
	NAME :
		slapindex - Reindex entries in a SLAPD database
	DESCRIPTION :
		Slapindex is used to regenerate slapd(8) indices
		based upon the current contents of a database.  It
		opens the given database determined by the database
		number or suffix and updates the indices for all
		values of all attributes of all entries. If a list
		of specific attributes is provided on the command
		line, only the indices for those attributes will
		be regenerated.  Databases configured as subordinate
		of this one are also re-indexed, unless -g is
		specified.

"slappasswd" : 
	NAME :
		slappasswd - OpenLDAP password utility
	DESCRIPTION :
		Slappasswd is used to generate an userPassword value
		suitable for use with ldapmodify(1), slapd.conf(5)
		rootpw configuration directive or the slapd-config(5)
		olcRootPW configuration directive.

"slapschema" : 
	NAME :
		slapschema - SLAPD in-database schema checking
		utility
	DESCRIPTION :
		Slapschema is used to check schema compliance of
		the contents of a slapd(8) database.  It opens the
		given database determined by the database number
		or suffix and checks the compliance of its contents
		with the corresponding schema. Errors are written
		to standard output or the specified file.  Databases
		configured as subordinate of this one are also
		output, unless -g is specified.

"slaptest" : 
	NAME :
		slaptest - Check the suitability of the OpenLDAP
		slapd configuration
	DESCRIPTION :
		Slaptest is used to check the conformance of the
		slapd(8) configuration.  It opens the slapd.conf(5)
		configuration file or the slapd-config(5) backend,
		and parses it according to the general and the
		backend-specific rules, checking its sanity.

"sleep" : 
	NAME :
		sleep – suspend execution for an interval of time
	DESCRIPTION :
		The sleep command suspends execution for a minimum
		of seconds.

"slogin" : 
	NAME :
		ssh – OpenSSH remote login client
	DESCRIPTION :
		ssh (SSH client) is a program for logging into a
		remote machine and for executing commands on a
		remote machine.  It is intended to provide secure
		encrypted communications between two untrusted hosts
		over an insecure network.  X11 connections, arbitrary
		TCP ports and UNIX-domain sockets can also be
		forwarded over the secure channel.

"smbd" : 
	NAME :
		smbd – SMB server daemon
	DESCRIPTION :
		smbd runs on a server machine to service SMB protocol
		requests from SMB client machines.

"smbdiagnose" : 
	NAME :
		smbdiagnose – gather information to aid in diagnosing
		SMB file sharing issues
	DESCRIPTION :
		The smbdiagnose tool gathers system information and
		network traffic in order to assist Apple when
		investigating issues related to SMB file sharing.
		A great deal of information is harvested, spanning
		system state, system configuration, and network
		traffic related to SMB file sharing.

"smbutil" : 
	NAME :
		smbutil – interface to the SMB requester
	DESCRIPTION :
		The smbutil command is used to control SMB requester
		and issue various commands.

"sndiskmove" : 
	NAME :
		sndiskmove - Xsan File System Disk Mover Utility
	DESCRIPTION :
		sndiskmove is a utility for migrating the contents
		of a disk to a new disk.  This is accomplished by
		copying the data from the disk and relabeling the
		destination disk with the name of the source disk.
		The source disk is also relabeled with its original
		name with a suffix added (this is ".old" by default
		but this can be changed with the -s option.

"snfsdefrag" : 
	NAME :
		snfsdefrag - Xsan File System File Defrag Utility
	DESCRIPTION :
		snfsdefrag is a utility for defragmenting files on
		a Xsan volume by relocating the data in a file to
		a smaller set of extents.  Reducing the number of
		extents in a file improves performance by minimizing
		disk head movement when performing I/O.  In addition,
		with fewer extents, Xsan File System Manager (FSM)
		overhead is reduced.

"snmp-bridge-mib" : 
	NAME :
		snmp-bridge-mib - provide Linux bridge information
		via SNMP
	DESCRIPTION :
		The snmp-bridge-mib is an extension to net-snmp.
		It collects information about a bridge in a Linux
		system and exports them for query from other (remote)
		systems for management purposes.

"snmpbulkget" : 
	NAME :
		snmpbulkget - communicates with a network entity
		using SNMP GETBULK requests.
	DESCRIPTION :
		snmpbulkget is an SNMP application that uses the
		SNMP GETBULK request to query a network entity
		efficiently for information.  One or more object
		identifiers (OIDs) may be given as arguments on the
		command line.  Each variable name is given in the
		format specified in variables(5).

"snmpbulkwalk" : 
	NAME :
		snmpbulkwalk - retrieve a subtree of management
		values using SNMP GETBULK requests
	DESCRIPTION :
		snmpbulkwalk is an SNMP application that uses SNMP
		GETBULK requests to query a network entity efficiently
		for a tree of information.

"snmpconf" : 
	NAME :
		snmpconf - creates and modifies SNMP configuration
		files
	DESCRIPTION :
		snmpconf is a simple Perl script that walks you
		through setting up a configuration file step by
		step.  It should be fairly straight forward to use.
		Merely run it and answer its questions.

"snmpd" : 
	NAME :
		snmpd - daemon to respond to SNMP request packets.
	DESCRIPTION :
		snmpd is an SNMP agent which binds to a port and
		awaits requests from SNMP management software.  Upon
		receiving a request, it processes the request(s),
		collects the requested information and/or performs
		the requested operation(s) and returns the information
		to the sender.

"snmpdelta" : 
	NAME :
		snmpdelta - Monitor delta differences in SNMP Counter
		values
	DESCRIPTION :
		snmpdelta will monitor the specified integer valued
		OIDs, and report changes over time.

"snmpdf" : 
	NAME :
		snmpdf - display disk space usage on a network
		entity via SNMP
	DESCRIPTION :
		snmpdf is simply a networked verison of the typical
		df command.  It checks the disk space on the remote
		machine by examining the HOST- RESOURCES-MIB's
		hrStorageTable or the UCD-SNMP-MIB's dskTable.  By
		default, the hrStorageTable is preferred as it
		typically contains more information.  However, the
		-Cu argument can be passed to snmpdf to force the
		usage of the dskTable.

"snmpget" : 
	NAME :
		snmpget - communicates with a network entity using
		SNMP GET requests
	DESCRIPTION :
		snmpget is an SNMP application that uses the SNMP
		GET request to query for information on a network
		entity.  One or more object identifiers (OIDs) may
		be given as arguments on the command line.  Each
		variable name is given in the format specified in
		variables(5).

"snmpgetnext" : 
	NAME :
		snmpgetnext - communicates with a network entity
		using SNMP GETNEXT requests
	DESCRIPTION :
		snmpget is an SNMP application that uses the SNMP
		GETNEXT request to query for information on a network
		entity.  One or more object identifiers (OIDs) may
		be given as arguments on the command line.  Each
		variable name is given in the format specified in
		variables(5).  For each one, the variable that is
		lexicographically "next" in the remote entity's MIB
		will be returned.

"snmpinform" : 
	NAME :
		snmptrap, snmpinform - sends an SNMP notification
		to a manager
	DESCRIPTION :
		snmptrap is an SNMP application that uses the SNMP
		TRAP operation to send information to a network
		manager.  One or more object identifiers (OIDs) can
		be given as arguments on the command line.  A type
		and a value must accompany each object identifier.
		Each variable name is given in the format specified
		in variables(5).

"snmpnetstat" : 
	NAME :
		snmpnetstat - display networking status and
		configuration information from a network entity via
		SNMP
	DESCRIPTION :
		The snmpnetstat command symbolically displays the
		values of various network-related information
		retrieved from a remote system using the SNMP
		protocol.  There are a number of output formats,
		depending on the options for the information
		presented.  The first form of the command displays
		a list of active sockets.  The second form presents
		the values of other network-related information
		according to the option selected. Using the third
		form, with an interval specified, snmpnetstat will
		continuously display the information regarding
		packet traffic on the configured network interfaces.
		The fourth form displays statistics about the named
		protocol.

"snmpset" : 
	NAME :
		snmpset - communicates with a network entity using
		SNMP SET requests
	DESCRIPTION :
		snmpset is an SNMP application that uses the SNMP
		SET request to set information on a network entity.
		One or more object identifiers (OIDs) must be given
		as arguments on the command line.  A type and a
		value to be set must accompany each object identifier.
		Each variable name is given in the format specified
		in variables(5).

"snmpstatus" : 
	NAME :
		snmpstatus - retrieves a fixed set of management
		information from a network entity
	DESCRIPTION :
		snmpstatus is an SNMP application that retrieves
		several important statistics from a network entity.

"snmptable" : 
	NAME :
		snmptable - retrieve an SNMP table and display it
		in tabular form
	DESCRIPTION :
		snmptable is an SNMP application that repeatedly
		uses the SNMP GETNEXT or GETBULK requests to query
		for information on a network entity.  The parameter
		TABLE-OID must specify an SNMP table.

"snmptest" : 
	NAME :
		snmptest - communicates with a network entity using
		SNMP requests
	DESCRIPTION :
		snmptest is a flexible SNMP application that can
		monitor and manage information on a network entity.

"snmptranslate" : 
	NAME :
		snmptranslate - translate MIB OID names between
		numeric and textual forms
	DESCRIPTION :
		snmptranslate is an application that translates one
		or more SNMP object identifier values from their
		symbolic (textual) forms into their numerical forms
		(or vice versa).

"snmptrap" : 
	NAME :
		snmptrap, snmpinform - sends an SNMP notification
		to a manager
	DESCRIPTION :
		snmptrap is an SNMP application that uses the SNMP
		TRAP operation to send information to a network
		manager.  One or more object identifiers (OIDs) can
		be given as arguments on the command line.  A type
		and a value must accompany each object identifier.
		Each variable name is given in the format specified
		in variables(5).

"snmptrapd" : 
	NAME :
		snmptrapd - Receive and log SNMP trap messages.
	DESCRIPTION :
		snmptrapd is an SNMP application that receives and
		logs SNMP TRAP and INFORM messages.

"snmpusm" : 
	NAME :
		snmpusm - creates and maintains SNMPv3 users on a
		network entity
	DESCRIPTION :
		snmpusm is an SNMP application that can be used to
		do simple maintenance on the users known to an SNMP
		agent, by manipulating the agent's User-based
		Security Module (USM) table.  The user needs write
		access to the usmUserTable MIB table.  This tool
		can be used to create, delete, clone, and change
		the passphrase of users configured on a running
		SNMP agent.

"snmpvacm" : 
	NAME :
		snmpvacm - creates and maintains SNMPv3 View-based
		Access Control entries on a network entity  GROUPNAME
		snmpvacm [COMMON OPTIONS] AGENT createView [-Ce]
		NAME SUBTREE MASK snmpvacm [COMMON OPTIONS]  AGENT
		createAccess GROUPNAME [CONTEXTPREFIX]
	DESCRIPTION :
		snmpvacm is an SNMP application that can be used
		to do simple maintenance on the View-based Control
		Module (VACM) tables of an SNMP agent.  The SNMPv3
		VACM specifications (see RFC2575) define assorted
		tables to specify groups of users, MIB views, and
		authorised access settings.  These snmpvacm commands
		effectively create or delete rows in the appropriate
		one of these tables, and match the equivalent
		configure directives which are documented in the
		snmpd.conf(5) man page.

"snmpwalk" : 
	NAME :
		snmpwalk - retrieve a subtree of management values
		using SNMP GETNEXT requests
	DESCRIPTION :
		snmpwalk is an SNMP application that uses SNMP
		GETNEXT requests to query a network entity for a
		tree of information.

"snquota" : 
	NAME :
		snquota - Xsan Quota Configuration Utility
	DESCRIPTION :
		The snquota command manipulates the quota system
		in the Xsan volume.

"sntp" : 
	NAME :
		sntp – A very Simple Network Time Protocol client
		program
	DESCRIPTION :
		-d       Enable debug logging.

"sntpd" : 
	NAME :
		sntpd – very Simple Network Time Protocol server
		daemon
	DESCRIPTION :
		sntpd is an SNTP server daemon, which can provide
		time synchronization services to clients such as
		timed(8).

"softwareupdate" : 
	NAME :
		softwareupdate – system software update tool
	DESCRIPTION :
		Software Update checks for new and updated versions
		of your software based on information about your
		computer and current software.

"sort" : 
	NAME :
		sort – sort or merge records (lines) of text and
		binary files
	DESCRIPTION :
		The sort utility sorts text and binary files by
		lines.  A line is a record separated from the
		subsequent record by a newline (default) or NUL ´´
		character (-z option).  A record can contain any
		printable or unprintable characters.  Comparisons
		are based on one or more sort keys extracted from
		each line of input, and are performed lexicographically,
		according to the current locale's collating rules
		and the specified command-line options that can
		tune the actual sorting behavior.  By default, if
		keys are not given, sort uses entire lines for
		comparison.

"source" : 
	DESCRIPTION :
		Exécute des commandes depuis un fichier dans le
		shell actuel. Lit et exécute des commandes depuis
		NOMFICHIER dans le shell actuel.  Les éléments dans
		$PATH sont utilisés pour trouver le répertoire
		contenant NOMFICHIER. Si des ARGUMENTS sont fournis,
		ils deviennent les paramètres de position lorsque
		NOMFICHIER est exécuté.

"sourcekit-lsp" : 
	La commande "sourcekit-lsp" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help sourcekit-lsp", "info sourcekit-lsp" ou "sourcekit-lsp" pour essayer d'obtenir davantage d'informations.

"spctl" : 
	NAME :
		spctl – SecAssessment system policy security
	DESCRIPTION :
		spctl manages the security assessment policy
		subsystem.

"spfd" : 
	NAME :
		spfd - simple forking daemon to provide SPF query
		services
	DESCRIPTION :
		spfd is a simple forking Sender Policy Framework
		(SPF) query proxy server.  spfd receives and answers
		SPF query requests on a TCP/IP or UNIX domain socket.

"spfd5.30" : 
	NAME :
		spfd - simple forking daemon to provide SPF query
		services
	DESCRIPTION :
		spfd is a simple forking Sender Policy Framework
		(SPF) query proxy server.  spfd receives and answers
		SPF query requests on a TCP/IP or UNIX domain socket.

"spfquery" : 
	NAME :
		spfquery - (Mail::SPF) - Checks if a given set of
		e-mail parameters matches a domain's SPF policy
	DESCRIPTION :
		spfquery checks if a given set of e-mail parameters
		(e.g., the SMTP sender's IP address) matches the
		responsible domain's Sender Policy Framework (SPF)
		policy.  For more information on SPF see
		<http://www.openspf.org>.

"spfquery5.30" : 
	NAME :
		spfquery - (Mail::SPF) - Checks if a given set of
		e-mail parameters matches a domain's SPF policy
	DESCRIPTION :
		spfquery checks if a given set of e-mail parameters
		(e.g., the SMTP sender's IP address) matches the
		responsible domain's Sender Policy Framework (SPF)
		policy.  For more information on SPF see
		<http://www.openspf.org>.

"spindump" : 
	NAME :
		spindump – Profile entire system during a time
		interval
	DESCRIPTION :
		spindump is used by various system components to
		create reports when an unresponsive application is
		force quit. Reports are stored at:

"splain" : 
	NAME :
		diagnostics, splain - produce verbose warning
		diagnostics
	DESCRIPTION :
		The "diagnostics" Pragma This module extends the
		terse diagnostics normally emitted by both the perl
		compiler and the perl interpreter (from running
		perl with a -w switch or "use warnings"), augmenting
		them with the more explicative and endearing
		descriptions found in perldiag.  Like the other
		pragmata, it affects the compilation phase of your
		program rather than merely the execution phase.

"splain5.30" : 
	NAME :
		diagnostics, splain - produce verbose warning
		diagnostics
	DESCRIPTION :
		The "diagnostics" Pragma This module extends the
		terse diagnostics normally emitted by both the perl
		compiler and the perl interpreter (from running
		perl with a -w switch or "use warnings"), augmenting
		them with the more explicative and endearing
		descriptions found in perldiag.  Like the other
		pragmata, it affects the compilation phase of your
		program rather than merely the execution phase.

"splain5.34" : 
	NAME :
		diagnostics, splain - produce verbose warning
		diagnostics
	DESCRIPTION :
		The "diagnostics" Pragma This module extends the
		terse diagnostics normally emitted by both the perl
		compiler and the perl interpreter (from running
		perl with a -w switch or "use warnings"), augmenting
		them with the more explicative and endearing
		descriptions found in perldiag.  Like the other
		pragmata, it affects the compilation phase of your
		program rather than merely the execution phase.

"split" : 
	NAME :
		split – split a file into pieces
	DESCRIPTION :
		The split utility reads the given file and breaks
		it up into files of 1000 lines each (if no options
		are specified), leaving the file unchanged.  If
		file is a single dash (‘-’) or absent, split reads
		from the standard input.

"split-file" : 
	La commande "split-file" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help split-file", "info split-file" ou "split-file" pour essayer d'obtenir davantage d'informations.

"SplitForks" : 
	NAME :
		/usr/bin/SplitForks – Divide a two-fork HFS file
		into AppleDouble format resource and data files.
		(DEPRECATED)
	DESCRIPTION :
		Tools supporting Carbon development, including
		/usr/bin/SplitForks, were deprecated with Xcode 6.

"spray" : 
	NAME :
		spray – send many packets to host
	DESCRIPTION :
		Spray sends multiple RPC packets to host and records
		how many of them were correctly received and how
		long it took.

"sqlite3" : 
	NAME :
		sqlite3 - A command line interface for SQLite version
		3
	DESCRIPTION :
		To start a sqlite3 interactive session, invoke the
		sqlite3 command and optionally provide the name of
		a database file.  If the database file does not
		exist, it will be created.  If the database file
		does exist, it will be opened.

"ssh" : 
	NAME :
		ssh – OpenSSH remote login client
	DESCRIPTION :
		ssh (SSH client) is a program for logging into a
		remote machine and for executing commands on a
		remote machine.  It is intended to provide secure
		encrypted communications between two untrusted hosts
		over an insecure network.  X11 connections, arbitrary
		TCP ports and UNIX-domain sockets can also be
		forwarded over the secure channel.

"ssh-add" : 
	NAME :
		ssh-add – adds private key identities to the OpenSSH
		authentication agent
	DESCRIPTION :
		ssh-add adds private key identities to the
		authentication agent, ssh-agent(1).  When run without
		arguments, it adds the files ~/.ssh/id_rsa,
		~/.ssh/id_ecdsa, ~/.ssh/id_ecdsa_sk, ~/.ssh/id_ed25519,
		~/.ssh/id_ed25519_sk, and ~/.ssh/id_dsa.  After
		loading a private key, ssh-add will try to load
		corresponding certificate information from the
		filename obtained by appending -cert.pub to the
		name of the private key file.  Alternative file
		names can be given on the command line.

"ssh-agent" : 
	NAME :
		ssh-agent – OpenSSH authentication agent
	DESCRIPTION :
		ssh-agent is a program to hold private keys used
		for public key authentication.  Through use of
		environment variables the agent can be located and
		automatically used for authentication when logging
		in to other machines using ssh(1).

"ssh-copy-id" : 
	NAME :
		ssh-copy-id – use locally available keys to authorise
		logins on a remote machine
	DESCRIPTION :
		ssh-copy-id is a script that uses ssh(1) to log
		into a remote machine (presumably using a login
		password, so password authentication should be
		enabled, unless you've done some clever use of
		multiple identities).  It assembles a list of one
		or more fingerprints (as described below) and tries
		to log in with each key, to see if any of them are
		already installed (of course, if you are not using
		ssh-agent(1) this may result in you being repeatedly
		prompted for pass-phrases).  It then assembles a
		list of those that failed to log in, and using ssh,
		enables logins with those keys on the remote server.
		By default it adds the keys by appending them to
		the remote user's ~/.ssh/authorized_keys (creating
		the

"ssh-keygen" : 
	NAME :
		ssh-keygen – OpenSSH authentication key utility
	DESCRIPTION :
		ssh-keygen generates, manages and converts
		authentication keys for ssh(1).  ssh-keygen can
		create keys for use by SSH protocol version 2.

"ssh-keyscan" : 
	NAME :
		ssh-keyscan – gather SSH public keys from servers
	DESCRIPTION :
		ssh-keyscan is a utility for gathering the public
		SSH host keys of a number of hosts.  It was designed
		to aid in building and verifying ssh_known_hosts
		files, the format of which is documented in sshd(8).
		ssh-keyscan provides a minimal interface suitable
		for use by shell and perl scripts.

"sshd" : 
	NAME :
		sshd – OpenSSH daemon
	DESCRIPTION :
		sshd (OpenSSH Daemon) is the daemon program for
		ssh(1).  It provides secure encrypted communications
		between two untrusted hosts over an insecure network.

"sso_util" : 
	NAME :
		sso_util – Kerberos – Open Directory Single Sign
		On
	DESCRIPTION :
		sso_util is a tool for setting up, interrogating
		and removing Kerberos configurations within the
		Apple Single Sign On environment. This tool can
		configure services, create and consume encrypted
		config records and tear down Kerberos installations

"stapler" : 
	NAME :
		stapler – Attach and validate tickets for notarized
		executables
	DESCRIPTION :
		The stapler utility attaches tickets for notarized
		executables to app bundles, disk images, and packages.

"stat" : 
	NAME :
		stat, readlink – display file status
	DESCRIPTION :
		The stat utility displays information about the
		file pointed to by file. Read, write, or execute
		permissions of the named file are not required, but
		all directories listed in the pathname leading to
		the file must be searchable.  If no argument is
		given, stat displays information about the file
		descriptor for standard input.

"streamzip" : 
	NAME :
		streamzip - create a zip file from stdin
	DESCRIPTION :
		This program will read data from "stdin", compress
		it into a zip container and, by default, write a
		streamed zip file to "stdout". No temporary files
		are created.

"stringdups" : 
	NAME :
		stringdups – Identify duplicate strings or other
		objects in malloc blocks of a target process
	DESCRIPTION :
		stringdups examines the content of malloc blocks
		in the specified target process.  For all blocks
		which have the same content, it shows a line with
		the number of such blocks, their total allocated
		size (the total size in the malloc heap, not just
		the specific size of their content), and the average
		allocated size.

"strings" : 
	NAME :
		strings - find the printable strings in a object,
		or other binary, file
	DESCRIPTION :
		Strings looks for ASCII strings in a binary file
		or standard input. Strings is useful for identifying
		random object files and many other things.  A string
		is any sequence of 4 (the default) or more printing
		characters [ending at, but not including, any other
		character or EOF]. Unless the - flag is given,
		strings looks in all sections of the object files
		except the (__TEXT,__text) section.  If no files
		are specified standard input is read.

"strip" : 
	NAME :
		strip - remove symbols
	DESCRIPTION :
		strip removes or modifies the symbol table attached
		to the output of the assembler and link editor.
		This is useful to save space after a program has
		been debugged and to limit dynamically bound symbols.

"stty" : 
	NAME :
		stty – set the options for a terminal device interface
	DESCRIPTION :
		The stty utility sets or reports on terminal
		characteristics for the device that is its standard
		input.  If no options or arguments are specified,
		it reports the settings of a subset of characteristics
		as well as additional ones if they differ from their
		default values.  Otherwise it modifies the terminal
		state according to the specified arguments. Some
		combinations of arguments are mutually exclusive
		on some terminal types.

"su" : 
	NAME :
		su – substitute user identity
	DESCRIPTION :
		The su utility requests appropriate user credentials
		via PAM and switches to that user ID (the default
		user is the superuser).  A shell is then executed.

"sudo" : 
	NAME :
		sudo, sudoedit - execute a command as another user
	DESCRIPTION :
		sudo allows a permitted user to execute a command
		as the superuser or another user, as specified by
		the security policy.  The invoking user's real (not
		effective) user-ID is used to determine the user
		name with which to query the security policy.

"sum" : 
	NAME :
		cksum, sum – display file checksums and block counts
	DESCRIPTION :
		The cksum utility writes to the standard output
		three whitespace separated fields for each input
		file.  These fields are a checksum CRC, the total
		number of octets in the file and the file name.  If
		no file name is specified, the standard input is
		used and no file name is written.

"suspend" : 
	DESCRIPTION :
		Suspend shell execution. Suspend the execution of
		this shell until it receives a SIGCONT signal.
		Unless forced, login shells and shells without job
		control cannot be suspended.

"swcutil" : 
	La commande "swcutil" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help swcutil", "info swcutil" ou "swcutil" pour essayer d'obtenir davantage d'informations.

"swift" : 
	NAME :
		swift -- Safe, fast, and expressive general-purpose
		programming language
	DESCRIPTION :
		Swift is a general-purpose programming language
		built using a modern approach to safety, performance,
		and software design patterns.

"swift-inspect" : 
	La commande "swift-inspect" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help swift-inspect", "info swift-inspect" ou "swift-inspect" pour essayer d'obtenir davantage d'informations.

"swiftc" : 
	La commande "swiftc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help swiftc", "info swiftc" ou "swiftc" pour essayer d'obtenir davantage d'informations.

"sw_vers" : 
	NAME :
		sw_vers – print macOS system version information
	DESCRIPTION :
		sw_vers prints macOS version information for the
		currently running operating system on the local
		machine.

"symbols" : 
	NAME :
		symbols – display symbol information about a file
		or process
	DESCRIPTION :
		The symbols command may be used to examine library,
		symbol, and source line information in files and
		running processes.

"symbolscache" : 
	NAME :
		symbolscache – display and modify symbolcache
		information
	DESCRIPTION :
		The symbolscache command may be used to list, add,
		and delete entries from the global symbolscache.

"sync" : 
	NAME :
		sync – force completion of pending disk writes
		(flush cache)
	DESCRIPTION :
		The sync utility can be called to ensure that all
		disk writes have been completed before the processor
		is halted in a way not suitably done by reboot(8)
		or halt(8).  Generally, it is preferable to use
		reboot(8) or halt(8) to shut down the system, as
		they may perform additional actions such as
		resynchronizing the hardware clock and flushing
		internal caches before performing a final sync.

"sysadminctl" : 
	La commande "sysadminctl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help sysadminctl", "info sysadminctl" ou "sysadminctl" pour essayer d'obtenir davantage d'informations.

"syscallbypid.d" : 
	NAME :
		syscallbypid.d - syscalls by process ID. Uses DTrace.
	DESCRIPTION :
		This reports the number of each type of system call
		made by PID.  This is useful to identify which
		process is causing the most system calls.

"syscallbyproc.d" : 
	NAME :
		syscallbyproc.d - syscalls by process name. Uses
		DTrace.
	DESCRIPTION :
		syscallbyproc.d is a DTrace OneLiner to a report
		of the number of system calls made by process name.

"syscallbysysc.d" : 
	NAME :
		syscallbysysc.d - syscalls by syscall. Uses DTrace.
	DESCRIPTION :
		syscallbysysc.d is a DTrace OneLiner to a report
		of the number of each type of system call made.

"syscapturediags" : 
	La commande "syscapturediags" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help syscapturediags", "info syscapturediags" ou "syscapturediags" pour essayer d'obtenir davantage d'informations.

"sysctl" : 
	NAME :
		sysctl – get or set kernel state
	DESCRIPTION :
		The sysctl utility retrieves kernel state and allows
		processes with appropriate privilege to set kernel
		state.  The state to be retrieved or set is described
		using a “Management Information Base” (“MIB”) style
		name, described as a dotted set of components.

"sysdiagnose" : 
	NAME :
		sysdiagnose – gathers system-wide diagnostic
		information helpful in investigating system performance
		issues
	DESCRIPTION :
		The sysdiagnose tool gathers system diagnostic
		information helpful in investigating system performance
		issues.  A great deal of information is harvested,
		spanning system state and configuration. The data
		is stored /var/tmp directory.  sysdiagnose needs
		to be run as root. To cancel an in-flight sysdiagnose
		triggered via command line interface, press Ctrl-\.
		sysdiagnose is automatically triggered when the
		following key chord is pressed:
		Control-Option-Command-Shift-Period.

"syslog" : 
	NAME :
		syslog – Apple System Log utility
	DESCRIPTION :
		syslog is a command-line utility for a variety of
		tasks relating to the Apple System Log (ASL) facility.
		It provides mechanisms for sending and viewing log
		messages, copying log messages to ASL format data
		store files, and for controlling the flow of log
		messages from client processes.

"syslogd" : 
	NAME :
		syslogd – Apple System Log server
	DESCRIPTION :
		The syslogd server receives and processes log
		messages.  Several modules receive input messages
		through various channels, including UNIX domain
		sockets associated with the syslog(3), asl(3), and
		kernel printf APIs, and optionally on a UDP socket
		from network clients.

"systemextensionsctl" : 
	NAME :
		systemextensionsctl – System Extensions control
	DESCRIPTION :
		The systemextensionsctl program can be used to list
		and control System Extensions installed on this
		machine.

"systemkeychain" : 
	NAME :
		systemkeychain – creates system keychains and allows
		keychains to unlock keychains
	DESCRIPTION :
		The systemkeychain can be used to create a system
		keychain, make it possible for a keychain to unlock
		another keychain, or test unlocking a keychain.

"systemsetup" : 
	NAME :
		systemsetup – configuration tool for certain machine
		settings in System Preferences.
	DESCRIPTION :
		The systemsetup command is used to configure certain
		per-machine settings typically configured in the
		System Preferences application.  The systemsetup
		command requires at least "admin" privileges to
		run.

"systemsoundserverd" : 
	DESCRIPTION :
		systemsoundserverd is a daemon used for Core Audio
		related purposes. systemsoundserverd was introduced
		with OSX version 10.11.

"systemstats" : 
	NAME :
		systemstats – summarize system stats
	DESCRIPTION :
		The systemstats command shows a selection of
		statistics about system power usage.

"system_profiler" : 
	NAME :
		system_profiler – reports system hardware and
		software configuration.
	DESCRIPTION :
		system_profiler reports on the hardware and software
		configuration of the system.  It can generate plain
		text reports or XML reports which can be opened
		with System Information.app, or JSON reports

"tab2space" : 
	NAME :
		tab2space - Utility to expand tabs and ensure
		consistent line endings
	DESCRIPTION :
		tab2space expands tab characters into a specific
		number of spaces.  It also normalizes line endings
		into a single format.

"tableau_affichage_contenu_avec_message" : 
	La commande "tableau_affichage_contenu_avec_message" de type "function" ne possède pas de page manuel dédiée.
	Tapez "help tableau_affichage_contenu_avec_message", "info tableau_affichage_contenu_avec_message" ou "tableau_affichage_contenu_avec_message" pour essayer d'obtenir davantage d'informations.

"tabs" : 
	NAME :
		tabs – set terminal tabs
	DESCRIPTION :
		The tabs utility displays a series of characters
		that clear the hardware terminal tab settings then
		initialises tab stops at specified positions, and
		optionally adjusts the margin.

"tail" : 
	NAME :
		tail – display the last part of a file
	DESCRIPTION :
		The tail utility displays the contents of file or,
		by default, its standard input, to the standard
		output.

"tailspin" : 
	NAME :
		tailspin – configure, save and print tailspin output
	DESCRIPTION :
		tailspin configures the system to continuously
		sample callstacks of processes and select kdebug
		events in the kernel trace buffer. When tailspin
		data is recorded to a file, the tailspin file will
		contain information about the system state from
		about 20s prior to the save. The tailspind daemon
		is a helper daemon for the tailspin feature and
		should not be run manually.

"talk" : 
	NAME :
		talk – talk to another user
	DESCRIPTION :
		The talk utility is a visual communication program
		which copies lines from your terminal to that of
		another user.

"tar" : 
	NAME :
		tar – manipulate tape archives
	DESCRIPTION :
		tar creates and manipulates streaming archive files.
		This implementation can extract from tar, pax, cpio,
		zip, jar, ar, xar, rpm, 7-zip, and ISO 9660 cdrom
		images and can create tar, pax, cpio, ar, zip,
		7-zip, and shar archives.

"taskinfo" : 
	NAME :
		taskinfo – display policy information from kernel
	DESCRIPTION :
		The taskinfo utility gathers and displays policy
		information from the kernel.  It is not indended
		to be run directly by users.

"taskpolicy" : 
	NAME :
		taskpolicy – execute a program with an altered I/O
		or scheduling policy or change settings of already
		running process
	DESCRIPTION :
		The taskpolicy program uses the setiopolicy_np(3)
		and setpriority(2) APIs to execute a program with
		altered I/O or scheduling policies.  All children
		of the specified program also inherit these policies.

"tblgen-lsp-server" : 
	La commande "tblgen-lsp-server" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help tblgen-lsp-server", "info tblgen-lsp-server" ou "tblgen-lsp-server" pour essayer d'obtenir davantage d'informations.

"tbtdiagnose" : 
	NAME :
		tbtdiagnose - collects diagnostic information to
		help troubleshoot Thunderbolt issues.
	DESCRIPTION :
		Use tbtdiagnose to collect diagnostic information
		to help with investigation of Thunderbolt issues.

"tccutil" : 
	NAME :
		tccutil – manage the privacy database
	DESCRIPTION :
		The tccutil command manages the privacy database,
		which stores decisions the user has made about
		whether apps may access personal data.

"tclsh" : 
	NAME :
		tclsh - Simple shell containing Tcl interpreter
	DESCRIPTION :
		Tclsh is a shell-like application that reads Tcl
		commands from its standard input or from a file and
		evaluates them.  If invoked with no arguments then
		it runs interactively, reading Tcl commands from
		standard input and printing command results and
		error messages to standard output.  It runs until
		the exit command is invoked or until it reaches
		end-of-file on its standard input.  If there exists
		a file .tclshrc (or tclshrc.tcl on the Windows
		platforms) in the home directory of the user,
		interactive tclsh evaluates the file as a Tcl script
		just before reading the first command from standard
		input.

"tclsh8.5" : 
	NAME :
		tclsh - Simple shell containing Tcl interpreter
	DESCRIPTION :
		Tclsh is a shell-like application that reads Tcl
		commands from its standard input or from a file and
		evaluates them.  If invoked with no arguments then
		it runs interactively, reading Tcl commands from
		standard input and printing command results and
		error messages to standard output.  It runs until
		the exit command is invoked or until it reaches
		end-of-file on its standard input.  If there exists
		a file .tclshrc (or tclshrc.tcl on the Windows
		platforms) in the home directory of the user,
		interactive tclsh evaluates the file as a Tcl script
		just before reading the first command from standard
		input.

"tcpdump" : 
	NAME :
		tcpdump - dump traffic on a network
	DESCRIPTION :
		Tcpdump prints out a description of the contents
		of packets on a network interface that match the
		Boolean expression; the description is preceded by
		a time stamp, printed, by default, as hours, minutes,
		seconds, and fractions of a second since midnight.
		It can also be run with the -w flag, which causes
		it to save the packet data to a file for later
		analysis, and/or with the -r flag, which causes it
		to read from a saved packet file rather than to
		read packets from a network interface. It can also
		be run with the -V flag, which causes it to read a
		list of saved packet files. In all cases, only
		packets that match expression will be processed by
		tcpdump.

"tcsh" : 
	NAME :
		tcsh - C shell with file name completion and command
		line editing
	DESCRIPTION :
		tcsh is an enhanced but completely compatible version
		of the Berkeley UNIX C shell, csh(1).  It is a
		command language interpreter usable both as an
		interactive login shell and a shell script command
		processor.  It includes a command-line editor (see
		The command-line editor), programmable word completion
		(see Completion and listing), spelling correction
		(see Spelling correction), a history mechanism (see
		History substitution), job control (see Jobs) and
		a C-like syntax.  The NEW FEATURES section describes
		major enhancements of tcsh over csh(1). Throughout
		this manual, features of tcsh not found in most
		csh(1) implementations (specifically, the 4.4BSD
		csh) are labeled with `(+)',

"tee" : 
	NAME :
		tee – duplicate standard input
	DESCRIPTION :
		The tee utility copies standard input to standard
		output, making a copy in zero or more files.  The
		output is unbuffered.

"terminal_relancer_avec_parametres_d_origine" : 
	La commande "terminal_relancer_avec_parametres_d_origine" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help terminal_relancer_avec_parametres_d_origine", "info terminal_relancer_avec_parametres_d_origine" ou "terminal_relancer_avec_parametres_d_origine" pour essayer d'obtenir davantage d'informations.

"test" : 
	DESCRIPTION :
		Évalue une expression conditionnelle. Se termine
		avec le code de retour 0 (vrai) ou 1 (faux) selon
		le résultat de l'évaluation de EXPR. Les expressions
		peuvent être unaires ou binaires. Les expressions
		unaires sont souvent utilisées pour examiner l'état
		d'un fichier. Il existe aussi des opérateurs de
		chaîne, ainsi que des opérateurs de comparaison
		numériques.

"test-yaml" : 
	La commande "test-yaml" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help test-yaml", "info test-yaml" ou "test-yaml" pour essayer d'obtenir davantage d'informations.

"test-yaml5.30" : 
	La commande "test-yaml5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help test-yaml5.30", "info test-yaml5.30" ou "test-yaml5.30" pour essayer d'obtenir davantage d'informations.

"textutil" : 
	NAME :
		textutil – text utility
	DESCRIPTION :
		textutil can be used to manipulate text files of
		various formats, using the mechanisms provided by
		the Cocoa text system.

"tftp" : 
	NAME :
		tftp – trivial file transfer program
	DESCRIPTION :
		The tftp utility is the user interface to the
		Internet TFTP (Trivial File Transfer Protocol),
		which allows users to transfer files to and from a
		remote machine.  The remote host (and optional port)
		may be specified on the command line, in which case
		tftp uses host (and port) as the default for future
		transfers (see the connect command below).

"then" : 
	La commande "then" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help then", "info then" ou "then" pour essayer d'obtenir davantage d'informations.

"thermal" : 
	La commande "thermal" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help thermal", "info thermal" ou "thermal" pour essayer d'obtenir davantage d'informations.

"tic" : 
	NAME :
		tic - the terminfo entry-description compiler
	DESCRIPTION :
		The command tic translates a terminfo file from
		source format into compiled format.  The compiled
		format is necessary for use with the library routines
		in ncurses(3X).

"tidy" : 
	NAME :
		tidy - validate, correct, and pretty-print HTML
		files
	DESCRIPTION :
		Tidy reads HTML, XHTML and XML files and writes
		cleaned up markup.  For HTML varians, it detects
		and corrects many common coding errors and strives
		to produce visually equivalent markup that is both
		W3C complaint and works on most browsers.  A common
		use of Tidy is to convert plain HTML to XHTML.  For
		generic XML files, Tidy is limited to correcting
		basic well-formedness errors and pretty printing.

"tidy_changelog" : 
	NAME :
		tidy_changelog - command-line tool for CPAN::Changes
	DESCRIPTION :
		Takes a changelog file, parse it using CPAN::Changes
		and prints out the resulting output.  If a file is
		not given, the program will see if there is one
		file in the current directory beginning by 'change'
		(case- insensitive) and, if so, assume it to be the
		changelog.

"tidy_changelog5.30" : 
	NAME :
		tidy_changelog - command-line tool for CPAN::Changes
	DESCRIPTION :
		Takes a changelog file, parse it using CPAN::Changes
		and prints out the resulting output.  If a file is
		not given, the program will see if there is one
		file in the current directory beginning by 'change'
		(case- insensitive) and, if so, assume it to be the
		changelog.

"tiff2bw" : 
	NAME :
		tiff2bw - convert a color TIFF image to greyscale
	DESCRIPTION :
		tiff2bw converts an RGB or Palette color TIFF image
		to a greyscale image by combining percentages of
		the red, green, and blue channels. By default,
		output samples are created by taking 28% of the red
		channel, 59% of the green channel, and 11% of the
		blue channel.  To alter these percentages, the -R,
		-G, and -B options may be used.

"tiff2icns" : 
	NAME :
		tiff2icns – converts TIFF to icns format
	DESCRIPTION :
		tiff2icns can be used to convert TIFF images to
		'icns' files used by Icon Services.

"tiff2pdf" : 
	NAME :
		tiff2pdf - convert a TIFF image to a PDF document
	DESCRIPTION :
		tiff2pdf opens a TIFF image and writes a PDF document
		to standard output.

"tiff2ps" : 
	NAME :
		tiff2ps - convert TIFF image to PostScript™
	DESCRIPTION :
		tiff2ps reads TIFF images and writes PostScript™
		or Encapsulated PostScript™ (EPS) on the standard
		output. By default tiff2ps writes Encapsulated
		PostScript™ for the first image in the specified
		TIFF image file.

"tiff2rgba" : 
	NAME :
		tiff2rgba - convert a TIFF image to RGBA color space
	DESCRIPTION :
		tiff2rgba converts a wide variety of TIFF images
		into an RGBA TIFF image.  This includes the ability
		to translate different color spaces and photometric
		interpretation into RGBA, support for alpha blending,
		and translation of many different bit depths into
		a 32bit RGBA image.

"tiffcmp" : 
	NAME :
		tiffcmp - compare two TIFF files
	DESCRIPTION :
		tiffcmp compares the tags and data in two files
		created according to the Tagged Image File Format,
		Revision 6.0.  The schemes used for compressing
		data in each file are immaterial when data are
		compared-data are compared on a scanline-by-scanline
		basis after decompression.  Most directory tags are
		checked; notable exceptions are: GrayResponseCurve,
		ColorResponseCurve, and ColorMap tags.  Data will
		not be compared if any of the BitsPerSample,
		SamplesPerPixel, or ImageWidth values are not equal.
		By default, tiffcmp will terminate if it encounters
		any difference.

"tiffcp" : 
	NAME :
		tiffcp - copy (and possibly convert) a TIFF file
	DESCRIPTION :
		tiffcp combines one or more files created according
		to the Tag Image File Format, Revision 6.0 into a
		single TIFF file.  Because the output file may be
		compressed using a different algorithm than the
		input files, tiffcp is most often used to convert
		between different compression schemes.

"tiffcrop" : 
	NAME :
		tiffcrop - select, copy, crop, convert, extract,
		and/or process one or more TIFF files
	DESCRIPTION :
		tiffcrop processes one or more files created according
		to the Tag Image File Format, Revision 6.0,
		specification into one or more TIFF file(s). tiffcrop
		is most often used to extract portions of an image
		for processing with bar code recognizer or OCR
		software when that software cannot restrict the
		region of interest to a specific portion of the
		image or to improve efficiency when the regions of
		interest must be rotated.  It can also be used to
		subdivide all or part of a processed image into
		smaller sections and export individual images or
		sections of images as separate files or separate
		images within one or more files derived from the
		original input image or images.

"tiffdither" : 
	NAME :
		tiffdither - convert a greyscale TIFF image to
		bilevel using dithering
	DESCRIPTION :
		tiffdither converts a single channel 8-bit greyscale
		image to a bilevel image using Floyd-Steinberg error
		propagation with thresholding.

"tiffdump" : 
	NAME :
		tiffdump - print verbatim information about TIFF
		files
	DESCRIPTION :
		tiffdump displays directory information from files
		created according to the Tag Image File Format,
		Revision 6.0.  The header of each TIFF file (magic
		number, version, and first directory offset) is
		displayed, followed by the tag contents of each
		directory in the file.  For each tag, the name,
		data type, count, and value(s) is displayed.  When
		the symbolic name for a tag or data type is known,
		the symbolic name is displayed followed by it's
		numeric (decimal) value.  Tag values are displayed
		enclosed in <> characters immediately preceded by
		the value of the count field.  For example, an
		ImageWidth tag might be displayed as ImageWidth
		(256) SHORT (3) 1<800>.

"tiffinfo" : 
	NAME :
		tiffinfo - print information about TIFF files
	DESCRIPTION :
		tiffinfo displays information about files created
		according to the Tag Image File Format, Revision
		6.0. By default, the contents of each TIFF directory
		in each file are displayed, with the value of each
		tag shown symbolically (where sensible).

"tiffmedian" : 
	NAME :
		tiffmedian - apply the median cut algorithm to data
		in a TIFF file
	DESCRIPTION :
		tiffmedian applies the median cut algorithm to an
		RGB image in input.tif to generate a palette image
		that is written to output.tif. The generated colormap
		has, by default, 256 entries.  The image data is
		quantized by mapping each pixel to the closest color
		values in the colormap.

"tiffset" : 
	NAME :
		tiffset - set or unset a field in a TIFF header
	DESCRIPTION :
		tiffset sets the value of a TIFF header to a specified
		value or removes an existing setting.

"tiffsplit" : 
	NAME :
		tiffsplit - split a multi-image TIFF into single-image
		TIFF files
	DESCRIPTION :
		tiffsplit takes a multi-directory (page) TIFF file
		and creates one or more single-directory (page)
		TIFF files from it.  The output files are given
		names created by concatenating a prefix, a lexically
		ordered suffix in the range [aaa--zzz], the suffix
		.tif (e.g. xaaa.tif, xaab.tif, …, xzzz.tif).  If a
		prefix is not specified on the command line, the
		default prefix of x is used.

"tiffutil" : 
	NAME :
		tiffutil - manipulates tiff files
	DESCRIPTION :
		tiffutil lets you manipulate TIFF files. The list
		of options (also available by running the program
		without any options) follows:

"tificc" : 
	NAME :
		tificc - little cms ICC profile applier for TIFF.
	DESCRIPTION :
		lcms is a standalone CMM engine, which deals with
		the color management. It implements a fast
		transformation between ICC profiles.  tificc is a
		little cms ICC profile applier for TIFF.

"time" : 
	La commande "time" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help time", "info time" ou "time" pour essayer d'obtenir davantage d'informations.

"timerfires" : 
	NAME :
		timerfires – analyze timers as they fire
	DESCRIPTION :
		The timerfires utility lists timers as they fire.

"timer_analyser.d" : 
	La commande "timer_analyser.d" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help timer_analyser.d", "info timer_analyser.d" ou "timer_analyser.d" pour essayer d'obtenir davantage d'informations.

"times" : 
	DESCRIPTION :
		Affiche les temps des processus. Affiche le cumul
		des temps utilisateur et système pour le shell et
		tous ses processus fils.

"timesyncanalyse" : 
	NAME :
		timesyncanalyse – time synchronization analysis
		tool.
	DESCRIPTION :
		The timesyncanalyse executable is used for analysis
		of test data for time synchronization testing.

"tjbench" : 
	La commande "tjbench" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help tjbench", "info tjbench" ou "tjbench" pour essayer d'obtenir davantage d'informations.

"tkcon" : 
	NAME :
		tkcon - Tk console replacement
	DESCRIPTION :
		TkCon is a replacement for the standard console
		that comes with Tk (on Windows/Mac, but also works
		on Unix).  The console itself provides many more
		features than the standard console.  TkCon works
		on all platforms where Tcl/Tk is available.  It is
		meant primarily to aid one when working with the
		little details inside Tcl and Tk, giving Unix users
		the GUI console provided by default in the Mac and
		Windows Tk.

"tkmib" : 
	NAME :
		tkmib - an interactive graphical MIB browser for
		SNMP
	DESCRIPTION :
		Simple Network Management Protocol (SNMP) provides
		a framework for exchange of the management information
		between the agents (servers) and clients. The
		Management Information Bases (MIBs) contain a formal
		description of a set of network objects that can
		be managed using the SNMP for a particular agent.
		tkmib is a graphical user interface for browsing
		the MIBs. It is also capable of sending or retrieving
		the SNMP management information to/from the remote
		agents interactively.

"tkpp" : 
	NAME :
		tkpp - frontend to pp written in Perl/Tk.
	DESCRIPTION :
		Tkpp is a GUI frontend to pp, which can turn perl
		scripts into stand- alone PAR files, perl scripts
		or executables.

"tkpp5.30" : 
	NAME :
		tkpp - frontend to pp written in Perl/Tk.
	DESCRIPTION :
		Tkpp is a GUI frontend to pp, which can turn perl
		scripts into stand- alone PAR files, perl scripts
		or executables.

"tmdiagnose" : 
	NAME :
		tmdiagnose – gather information to aid in diagnosing
		Time Machine issues
	DESCRIPTION :
		The tmdiagnose tool gathers system and backup
		information in order to assist Apple when investigating
		issues related to Time Machine. A great deal of
		information is harvested, spanning system state,
		system and backup configuration, and snapshot
		details.

"tmutil" : 
	NAME :
		tmutil – Time Machine utility
	DESCRIPTION :
		tmutil provides methods of controlling and interacting
		with Time Machine, as well as examining and
		manipulating Time Machine backups. Common abilities
		include restoring data from backups, editing
		exclusions, and comparing backups.

"tnameserv" : 
	La commande "tnameserv" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help tnameserv", "info tnameserv" ou "tnameserv" pour essayer d'obtenir davantage d'informations.

"TODO" : 
	La commande "TODO" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help TODO", "info TODO" ou "TODO" pour essayer d'obtenir davantage d'informations.

"toe" : 
	NAME :
		toe - table of (terminfo) entries
	DESCRIPTION :
		With no options, toe lists all available terminal
		types by primary name with descriptions.  File
		arguments specify the directories to be scanned;
		if no such arguments are given, your default terminfo
		directory is scanned.  If you also specify the -h
		option, a directory header will be issued as each
		directory is entered.

"top" : 
	NAME :
		top – display sorted information about processes
	DESCRIPTION :
		The top program periodically displays a sorted list
		of system processes. The default sorting key is
		pid, but other keys can be used instead. Various
		output options are available.

"tops" : 
	NAME :
		tops - perform in-place substitutions on code.
	DESCRIPTION :
		tops is a tool that performs in-place substitutions
		on source files according to a set of rules. Each
		tops rule describes a particular translation. For
		example, one tops rule might specify that occurrences
		of the token 'Application' should be converted to
		'NSApplication'. In tops syntax, this rule will
		appear as:

"topsyscall" : 
	NAME :
		topsyscall - top syscalls by syscall name. Uses
		DTrace.
	DESCRIPTION :
		This program continually prints a report of the top
		system calls, and refreshes the display every 1
		second or as specified at the command line.

"topsysproc" : 
	NAME :
		topsysproc - top syscalls by process name. Uses
		DTrace.
	DESCRIPTION :
		This program continually prints a report of the
		number of system calls by process name, and refreshes
		the display every 1 second or as specified at the
		command line. Similar data can be fetched with
		"prstat -m".

"touch" : 
	NAME :
		touch – change file access and modification times
	DESCRIPTION :
		The touch utility sets the modification and access
		times of files.  If any file does not exist, it is
		created with default permissions.

"tput" : 
	NAME :
		tput, reset - initialize a terminal or query terminfo
		database
	DESCRIPTION :
		The tput utility uses the terminfo database to make
		the values of terminal-dependent capabilities and
		information available to the shell (see sh(1)), to
		initialize or reset the terminal, or return the
		long name of the requested terminal type.  The
		result depends upon the capability's type:

"tr" : 
	NAME :
		tr – translate characters
	DESCRIPTION :
		The tr utility copies the standard input to the
		standard output with substitution or deletion of
		selected characters.

"trace" : 
	NAME :
		bounce - Postfix delivery status reports
	DESCRIPTION :
		The bounce(8) daemon maintains per-message log files
		with delivery status information. Each log file is
		named after the queue file that it corresponds to,
		and is kept in a queue subdirectory named after the
		service name in the master.cf file (either bounce,
		defer or trace). This program expects to be run
		from the master(8) process manager.

"traceroute" : 
	NAME :
		traceroute – print the route packets take to network
		host
	DESCRIPTION :
		The Internet is a large and complex aggregation of
		network hardware, connected together by gateways.
		Tracking the route one's packets follow (or finding
		the miscreant gateway that's discarding your packets)
		can be difficult.  traceroute utilizes the IP
		protocol `time to live' field and attempts to elicit
		an ICMP TIME_EXCEEDED response from each gateway
		along the path to some host.

"traceroute6" : 
	NAME :
		traceroute6 – print the route IPv6 packets will
		take to a network node
	DESCRIPTION :
		The traceroute6 utility uses the IPv6 protocol hop
		limit field to elicit an ICMPv6 TIME_EXCEEDED
		response from each gateway along the path to some
		host.

"transicc" : 
	NAME :
		transicc - little cms ColorSpace conversion calculator.
	DESCRIPTION :
		lcms is a standalone CMM engine, which deals with
		the color management. It implements a fast
		transformation between ICC profiles.  transicc is
		a lcms ColorSpace conversion calculator.

"trap" : 
	DESCRIPTION :
		Intercepter des signaux et d'autres événements.
		Définit et active des gestionnaires à lancer lorsque
		le shell reçoit des signaux ou sous d'autres
		conditions.

"traptoemail" : 
	NAME :
		traptoemail - snmptrapd handler script to convert
		snmp traps into emails
	DESCRIPTION :
		converts snmp traps into email messages.

"treereg" : 
	NAME :
		treereg - Compiler for Tree Regular Expressions
	DESCRIPTION :
		"Treereg" translates a tree grammar specification
		file (default extension ".trg" describing a set of
		tree patterns and the actions to modify them using
		tree-terms like:

"treereg5.30" : 
	NAME :
		treereg - Compiler for Tree Regular Expressions
	DESCRIPTION :
		"Treereg" translates a tree grammar specification
		file (default extension ".trg" describing a set of
		tree patterns and the actions to modify them using
		tree-terms like:

"trimforce" : 
	NAME :
		trimforce – enable TRIM commands on third-party
		drives
	DESCRIPTION :
		trimforce enables sending TRIM commands to third-party
		drives attached to an AHCI controller.  By default,
		TRIM commands are not sent to third- party drives.
		Use extreme caution when enabling TRIM, as some
		drives may not correctly handle the commands.
		trimforce must be run by the system administrator.

"true" : 
	DESCRIPTION :
		Renvoie un résultat de succès. Code de retour :
		Succès.

"truncate" : 
	NAME :
		truncate – truncate, extend the length of files,
		or perform space management in files
	DESCRIPTION :
		The truncate utility adjusts the length of each
		regular file given on the command-line, or performs
		space management with the given offset and the
		length over a regular file given on the command-line.

"trustcachectl" : 
	La commande "trustcachectl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help trustcachectl", "info trustcachectl" ou "trustcachectl" pour essayer d'obtenir davantage d'informations.

"tset" : 
	NAME :
		tset, reset - terminal initialization
	DESCRIPTION :
		Tset initializes terminals.  Tset first determines
		the type of terminal that you are using.  This
		determination is done as follows, using the first
		terminal type found.

"tsig-keygen" : 
	NAME :
		ddns-confgen - ddns key generation tool
	DESCRIPTION :
		tsig-keygen and ddns-confgen are invocation methods
		for a utility that generates keys for use in TSIG
		signing. The resulting keys can be used, for example,
		to secure dynamic DNS updates to a zone or for the
		rndc command channel.

"tsort" : 
	NAME :
		tsort – topological sort of a directed graph
	DESCRIPTION :
		tsort takes a list of pairs of node names representing
		directed arcs in a graph and prints the nodes in
		topological order on standard output. Input is taken
		from the named file, or from standard input if no
		file is given.

"tty" : 
	NAME :
		tty – return user's terminal name
	DESCRIPTION :
		The tty utility writes the name of the terminal
		attached to standard input to standard output.  The
		name that is written is the string returned by
		ttyname(3).  If the standard input is not a terminal,
		the message “not a tty” is written.  The options
		are as follows:

"type" : 
	DESCRIPTION :
		Affiche des informations sur le type de commande.
		Pour chaque NOM, indique comment il serait interprété
		s'il était utilisé comme un nom de commande.

"typeset" : 
	DESCRIPTION :
		Définit des valeurs et des attributs de variables.
		Un synonyme de « déclare ».  Consultez « help declare
		».

"uasysdiagnose" : 
	NAME :
		uasysdiagone – Produce system diagnostics for
		UserActivity.framework
	DESCRIPTION :
		uasysdiagone is used to dump a diagnostic state
		from useractivityd.

"ul" : 
	NAME :
		ul – do underlining
	DESCRIPTION :
		The ul utility reads the named files (or standard
		input if none are given) and translates occurrences
		of underscores to the sequence which indicates
		underlining for the terminal in use, as specified
		by the environment variable TERM.  The file
		/etc/termcap is read to determine the appropriate
		sequences for underlining.  If the terminal is
		incapable of underlining, but is capable of a
		standout mode then that is used instead.  If the
		terminal can overstrike, or handles underlining
		automatically, ul degenerates to cat(1).  If the
		terminal cannot underline, underlining is ignored.

"ulimit" : 
	DESCRIPTION :
		Modifie les limites de ressources du shell. Fournit
		un contrôle sur les ressources disponibles au shell
		et aux processus qu'il crée, sur les systèmes qui
		permettent un tel contrôle.

"umask" : 
	DESCRIPTION :
		Affiche ou définit le masque de mode de fichier.
		Définit le masque de création de fichier comme étant
		MODE.  Si MODE est omis, affiche la valeur courante
		du MASQUE.

"umount" : 
	NAME :
		umount – unmount filesystems
	DESCRIPTION :
		The umount command unmounts a mounted filesystem
		(volume), removing it from the filesystem namespace.
		It calls the unmount(2) system call to remove a
		special device or the remote node (rhost:path) from
		the filesystem tree at the point node.  If either
		special or node are not provided, the appropriate
		information is taken from the list of filesystems
		provided by getfsent(3).

"umtool" : 
	NAME :
		umtool – Diagnostic tool for UserManagement
	DESCRIPTION :
		Use umtool to output diagnostic data for troubleshooting
		UserManagement.

"unalias" : 
	DESCRIPTION :
		Enlève chaque NOM de la liste des alias actuels.
		Options : -a        retire toutes les définitions
		d'alias

"uname" : 
	NAME :
		 uname – display information about the system
	DESCRIPTION :
		The uname command writes the name of the operating
		system implementation to standard output.  When
		options are specified, strings representing one or
		more system characteristics are written to standard
		output.

"uncompress" : 
	NAME :
		compress, uncompress – compress and expand data
	DESCRIPTION :
		The compress utility reduces the size of files using
		adaptive Lempel-Ziv coding.  Each file is renamed
		to the same name plus the extension .Z.  A file
		argument with a .Z extension will be ignored except
		it will cause an error exit after other arguments
		are processed.  If compression would not reduce the
		size of a file, the file is ignored.

"unexpand" : 
	NAME :
		expand, unexpand – expand tabs to spaces, and vice
		versa
	DESCRIPTION :
		The expand utility processes the named files or the
		standard input writing the standard output with
		tabs changed into blanks.  Backspace characters are
		preserved into the output and decrement the column
		count for tab calculations.  The expand utility is
		useful for pre-processing character files (before
		sorting, looking at specific columns, etc.) that
		contain tabs.

"UnicodeNameMappingGenerator" : 
	La commande "UnicodeNameMappingGenerator" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help UnicodeNameMappingGenerator", "info UnicodeNameMappingGenerator" ou "UnicodeNameMappingGenerator" pour essayer d'obtenir davantage d'informations.

"unifdef" : 
	NAME :
		unifdef, unifdefall – remove preprocessor conditionals
		from code
	DESCRIPTION :
		The unifdef utility selectively processes conditional
		cpp(1) directives. It removes from a file both the
		directives and any additional text that they specify
		should be removed, while otherwise leaving the file
		alone.

"unifdefall" : 
	NAME :
		unifdef, unifdefall – remove preprocessor conditionals
		from code
	DESCRIPTION :
		The unifdef utility selectively processes conditional
		cpp(1) directives. It removes from a file both the
		directives and any additional text that they specify
		should be removed, while otherwise leaving the file
		alone.

"uniq" : 
	NAME :
		uniq – report or filter out repeated lines in a
		file
	DESCRIPTION :
		The uniq utility reads the specified input_file
		comparing adjacent lines, and writes a copy of each
		unique input line to the output_file.  If input_file
		is a single dash (‘-’) or absent, the standard input
		is read. If output_file is absent, standard output
		is used for output.  The second and succeeding
		copies of identical adjacent input lines are not
		written. Repeated lines in the input will not be
		detected if they are not adjacent, so it may be
		necessary to sort the files first.

"units" : 
	NAME :
		units – conversion program
	DESCRIPTION :
		The units program converts quantities expressed in
		various scales to their equivalents in other scales.
		The units program can only handle multiplicative
		scale changes.  It cannot convert Celsius to
		Fahrenheit, for example.  It works interactively
		by prompting the user for input:

"universalaccessd" : 
	NAME :
		universalaccessd – universal access server
	DESCRIPTION :
		universalaccessd provides universal access services.

"unlink" : 
	NAME :
		rm, unlink – remove directory entries
	DESCRIPTION :
		The rm utility attempts to remove the non-directory
		type files specified on the command line.  If the
		permissions of the file do not permit writing, and
		the standard input device is a terminal, the user
		is prompted (on the standard error output) for
		confirmation.

"unlz4" : 
	NAME :
		lz4 - lz4, unlz4, lz4cat - Compress or decompress
		.lz4 files
	DESCRIPTION :
		lz4 is an extremely fast lossless compression
		algorithm, based on byte-aligned LZ77 family of
		compression scheme. lz4 offers compression speeds
		> 500 MB/s per core, linearly scalable with multi-core
		CPUs. It features an extremely fast decoder, offering
		speed in multiple GB/s per core, typically reaching
		RAM speed limit on multi-core systems. The native
		file format is the .lz4 format.

"unlzma" : 
	NAME :
		xz, unxz, xzcat, lzma, unlzma, lzcat - Compress or
		decompress .xz and .lzma files
	DESCRIPTION :
		xz is a general-purpose data compression tool with
		command line syntax similar to gzip(1) and bzip2(1).
		The native file format is the .xz format, but the
		legacy .lzma format used by LZMA Utils and raw
		compressed streams with no container format headers
		are also supported. In addition, decompression of
		the .lz format used by lzip is supported.

"unpack200" : 
	La commande "unpack200" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help unpack200", "info unpack200" ou "unpack200" pour essayer d'obtenir davantage d'informations.

"unset" : 
	DESCRIPTION :
		Annule des valeurs ou des attributs de variables
		et de fonctions du shell. Pour chaque NOM, annule
		la variable ou fonction correspondante.

"unsetpassword" : 
	La commande "unsetpassword" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help unsetpassword", "info unsetpassword" ou "unsetpassword" pour essayer d'obtenir davantage d'informations.

"until" : 
	La commande "until" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help until", "info until" ou "until" pour essayer d'obtenir davantage d'informations.

"unvis" : 
	NAME :
		unvis – revert a visual representation of data back
		to original form
	DESCRIPTION :
		unvis is the inverse function of vis(1).  It reverts
		a visual representation of data back to its original
		form on standard output.

"unxz" : 
	NAME :
		xz, unxz, xzcat, lzma, unlzma, lzcat - Compress or
		decompress .xz and .lzma files
	DESCRIPTION :
		xz is a general-purpose data compression tool with
		command line syntax similar to gzip(1) and bzip2(1).
		The native file format is the .xz format, but the
		legacy .lzma format used by LZMA Utils and raw
		compressed streams with no container format headers
		are also supported. In addition, decompression of
		the .lz format used by lzip is supported.

"unzip" : 
	NAME :
		unzip - list, test and extract compressed files in
		a ZIP archive
	DESCRIPTION :
		unzip will list, test, or extract files from a ZIP
		archive, commonly found on MS-DOS systems.  The
		default behavior (with no options) is to extract
		into the current directory (and subdirectories below
		it) all files from the specified ZIP archive.  A
		companion program, zip(1L), creates ZIP archives;
		both programs are compatible with archives created
		by PKWARE's PKZIP and PKUNZIP for MS-DOS, but in
		many cases the program options or default behaviors
		differ.

"unzipsfx" : 
	NAME :
		unzipsfx - self-extracting stub for prepending to
		ZIP archives
	DESCRIPTION :
		unzipsfx is a modified version of unzip(1L) designed
		to be prepended to existing ZIP archives in order
		to form self-extracting archives. Instead of taking
		its first non-flag argument to be the zipfile(s)
		to be extracted, unzipsfx seeks itself under the
		name by which it was invoked and tests or extracts
		the contents of the appended archive. Because the
		executable stub adds bulk to the archive (the whole
		purpose of which is to be as small as possible), a
		number of the less-vital capabilities in regular
		unzip have been removed.  Among these are the usage
		(or help) screen, the listing and diagnostic functions
		(-l and -v), the ability to decompress older
		compression formats (the

"unzstd" : 
	NAME :
		zstd - zstd, zstdmt, unzstd, zstdcat - Compress or
		decompress .zst files
	DESCRIPTION :
		zstd is a fast lossless compression algorithm and
		data compression tool, with command line syntax
		similar to gzip(1) and xz(1). It is based on the
		LZ77 family, with further FSE & huff0 entropy stages.
		zstd offers highly configurable compression speed,
		from fast modes at > 200 MB/s per core, to strong
		modes with excellent compression ratios. It also
		features a very fast decoder, with speeds > 500
		MB/s per core.

"update_dyld_shared_cache" : 
	La commande "update_dyld_shared_cache" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help update_dyld_shared_cache", "info update_dyld_shared_cache" ou "update_dyld_shared_cache" pour essayer d'obtenir davantage d'informations.

"update_mcdp29xx" : 
	La commande "update_mcdp29xx" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help update_mcdp29xx", "info update_mcdp29xx" ou "update_mcdp29xx" pour essayer d'obtenir davantage d'informations.

"uptime" : 
	NAME :
		uptime – show how long system has been running
	DESCRIPTION :
		The uptime utility displays the current time, the
		length of time the system has been up, the number
		of users, and the load average of the system over
		the last 1, 5, and 15 minutes.

"usbcfwflasher" : 
	La commande "usbcfwflasher" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help usbcfwflasher", "info usbcfwflasher" ou "usbcfwflasher" pour essayer d'obtenir davantage d'informations.

"usernoted" : 
	NAME :
		usernoted – Notification Center daemon
	DESCRIPTION :
		usernoted is a daemon that provides notification
		services on the system

"users" : 
	NAME :
		users – list current users
	DESCRIPTION :
		The users utility lists the login names of the users
		currently on the system, in sorted order, space
		separated, on a single line.

"uttype" : 
	NAME :
		uttype - Information about Uniform Type Identifiers
	DESCRIPTION :
		This command can be used to query the system
		programmatically about Uniform Type Identifiers
		(UTTypes for short) known to it.

"uuchk" : 
	NAME :
		uuchk - display information read from the UUCP
		configuration files.
	DESCRIPTION :
		The uuchk program displays information read from
		the UUCP configuration files.  It should be used
		to ensure that UUCP has been configured correctly.

"uucico" : 
	NAME :
		uucico - UUCP file transfer daemon
	DESCRIPTION :
		The uucico daemon processes file transfer requests
		queued by uucp (1) and uux (1).  It is started when
		uucp or uux is run (unless they are given the -r
		option).  It is also typically started periodically
		using entries in the crontab table(s).

"uuconv" : 
	NAME :
		uuconv - convert UUCP configuration files
	DESCRIPTION :
		The uuconv program can be used to convert UUCP
		configuration files from one format to another.
		This can be useful for administrators converting
		from an older UUCP package.  Taylor UUCP is able
		to read and use old configuration file formats, but
		some new features can not be selected using the old
		formats.

"uucp" : 
	NAME :
		uucp - Unix to Unix copy
	DESCRIPTION :
		The uucp command copies files between systems.  Each
		file argument is either a pathname on the local
		machine or is of the form

"uudecode" : 
	NAME :
		uuencode, uudecode – encode/decode a binary file
	DESCRIPTION :
		The uuencode and uudecode utilities are used to
		transmit binary files over transmission mediums
		that do not support other than simple ASCII data.

"uuencode" : 
	NAME :
		uuencode, uudecode – encode/decode a binary file
	DESCRIPTION :
		The uuencode and uudecode utilities are used to
		transmit binary files over transmission mediums
		that do not support other than simple ASCII data.

"uuidgen" : 
	NAME :
		uuidgen – generates new UUID strings
	DESCRIPTION :
		The uuidgen command generates a Universally Unique
		IDentifier (UUID), a 128-bit value guaranteed to
		be unique over both space and time.

"uulog" : 
	NAME :
		uulog - display entries in the UUCP log file.
	DESCRIPTION :
		The uulog program can be used to display entries
		in the UUCP log file. It can select the entries for
		a particular system or a particular user. You can
		use it to see what has happened to your queued jobs
		in the past.  Different options may be used to
		select which parts of the file to display.

"uuname" : 
	NAME :
		uuname - list remote UUCP sites
	DESCRIPTION :
		By default, the uuname program simply lists the
		names of all the remote systems mentioned in the
		UUCP configuration files.

"uupick" : 
	NAME :
		uupick - retrieve files transferred by uuto
	DESCRIPTION :
		The uupick program is used to conveniently retrieve
		files transferred by the uuto program.

"uusched" : 
	NAME :
		uusched - schedule the uucp file transport program
	DESCRIPTION :
		The uusched program is actually just a shell script
		which invokes the uucico daemon.  It is provided
		for backward compatibility.  It causes uucico to
		call all systems for which there is work.  Any
		option which may be given to uucico may also be
		given to uusched .  For more details, see uucico(8)
		.

"uustat" : 
	NAME :
		uustat - UUCP status inquiry and control
	DESCRIPTION :
		The uustat command can display various types of
		status information about the UUCP system.  It can
		also be used to cancel or rejuvenate requests made
		by uucp (1) or uux (1).

"uuto" : 
	NAME :
		uuto - send files to a particular user on a remote
		system
	DESCRIPTION :
		The uuto program may be used to conveniently send
		files to a particular user on a remote system.  It
		will arrange for mail to be sent to the remote user
		when the files arrive on the remote system, and he
		or she may easily retrieve the files using the
		uupick program.  Note that uuto does not provide
		any security--any user on the remote system can
		examine the files.

"uux" : 
	NAME :
		uux - Remote command execution over UUCP
	DESCRIPTION :
		The uux command is used to execute a command on a
		remote system, or to execute a command on the local
		system using files from remote systems. The command
		is not executed immediately; the request is queued
		until the uucico (8) daemon calls the system and
		executes it.  The daemon is started automatically
		unless one of the -r or --nouucico options is given.

"uuxqt" : 
	NAME :
		uuxqt - UUCP execution daemon
	DESCRIPTION :
		The uuxqt daemon executes commands requested by uux
		(1) from either the local system or from remote
		systems.  It is started automatically by the uucico
		(8) daemon (unless uucico (8) is given the -q or
		--nouuxqt option).

"variables_perso_liste" : 
	La commande "variables_perso_liste" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help variables_perso_liste", "info variables_perso_liste" ou "variables_perso_liste" pour essayer d'obtenir davantage d'informations.

"verify-uselistorder" : 
	La commande "verify-uselistorder" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help verify-uselistorder", "info verify-uselistorder" ou "verify-uselistorder" pour essayer d'obtenir davantage d'informations.

"vi" : 
	NAME :
		vim - Vi IMproved, a programmer's text editor
	DESCRIPTION :
		Vim is a text editor that is upwards compatible to
		Vi.  It can be used to edit all kinds of plain text.
		It is especially useful for editing programs.

"view" : 
	NAME :
		vim - Vi IMproved, a programmer's text editor
	DESCRIPTION :
		Vim is a text editor that is upwards compatible to
		Vi.  It can be used to edit all kinds of plain text.
		It is especially useful for editing programs.

"vifs" : 
	NAME :
		vifs – safely edit fstab
	DESCRIPTION :
		The vifs utility simply locks the fstab file before
		invoking an editor on it.  This is important to
		facilitate the modification of fstab by automated
		tools and system management software.

"vim" : 
	NAME :
		vim - Vi IMproved, a programmer's text editor
	DESCRIPTION :
		Vim is a text editor that is upwards compatible to
		Vi.  It can be used to edit all kinds of plain text.
		It is especially useful for editing programs.

"vimdiff" : 
	NAME :
		vimdiff - edit between two and eight versions of a
		file with Vim and show differences
	DESCRIPTION :
		Vimdiff starts Vim on two up to eight files.  Each
		file gets its own window.  The differences between
		the files are highlighted.  This is a nice way to
		inspect changes and to move changes from one version
		to another version of the same file.

"vimtutor" : 
	NAME :
		vimtutor - the Vim tutor
	DESCRIPTION :
		Vimtutor starts the Vim tutor.  It copies the tutor
		file first, so that it can be modified without
		changing the original file.

"vipw" : 
	NAME :
		vipw – edit the password file
	DESCRIPTION :
		The vipw utility edits the password file after
		setting the appropriate locks, and does any necessary
		processing after the password file is unlocked.  If
		the password file is already locked for editing by
		another user, vipw will ask you to try again later.
		The default editor for vipw is vi(1).

"vis" : 
	NAME :
		vis – display non-printable characters in a visual
		format
	DESCRIPTION :
		vis is a filter for converting non-printable
		characters into a visual representation.  It differs
		from ‘cat -v’ in that the form is unique and
		invertible.  By default, all non-graphic characters
		except space, tab, and newline are encoded.  A
		detailed description of the various visual formats
		is given in vis(3).

"visudo" : 
	NAME :
		visudo - edit the sudoers file
	DESCRIPTION :
		visudo edits the sudoers file in a safe fashion,
		analogous to vipw(8). visudo locks the sudoers file
		against multiple simultaneous edits, performs basic
		validity checks, and checks for syntax errors before
		installing the edited file.  If the sudoers file
		is currently being edited you will receive a message
		to try again later.

"vmmap" : 
	NAME :
		vmmap – Display the virtual memory regions allocated
		in a process
	DESCRIPTION :
		vmmap displays the virtual memory regions allocated
		in a specified process, helping a programmer
		understand how memory is being used, and what the
		purposes of memory at a given address may be.

"vm_stat" : 
	NAME :
		vm_stat – show Mach virtual memory statistics
	DESCRIPTION :
		vm_stat displays Mach virtual memory statistics.
		If the optional interval is specified, then vm_stat
		will display the statistics every interval seconds.
		In this case, each line of output displays the
		change in each statistic (an interval count of 1
		displays the values per second).  However, the first
		line of output following each banner displays the
		system-wide totals for each statistic.  If a count
		is provided, the command will terminate after count
		intervals.  The following values are displayed:

"vpnd" : 
	NAME :
		vpnd – Mac OS X VPN service daemon
	DESCRIPTION :
		vpnd allows external hosts to tunnel via L2TP over
		IPSec or via PPTP from an insecure external network
		(such as the Internet) into a "secure" internal
		network, such as a corporate network.  All traffic
		through the tunnel is encrypted to provide secure
		communications, with L2TP/IPSec providing a higher
		level of security than PPTP.

"vsdbutil" : 
	NAME :
		vsdbutil – manipulates the volume status DB.
	DESCRIPTION :
		vsdbutil manipulates the volume status DB.  The
		following options are available:

"vtool" : 
	NAME :
		vtool – Mach-O version number utility
	DESCRIPTION :
		The vtool utility displays and edits build and
		source version numbers embedded in the Mach-O(5)
		file format. These version numbers are stored within
		the Mach-O load commands, as described in the
		⟨mach-o/loader.h⟩ header file and in the VERSION
		LOAD COMMANDS section below. When editing files, a
		new out_file must be specified using the -output
		flag; vtool will only ever write to a single output
		file, and input files are never modified in place.

"w" : 
	NAME :
		w – display who is logged in and what they are doing
	DESCRIPTION :
		The w utility prints a summary of the current
		activity on the system, including what each user
		is doing.  The first line displays the current time
		of day, how long the system has been running, the
		number of users logged into the system, and the
		load averages.  The load average numbers give the
		number of jobs in the run queue averaged over 1, 5
		and 15 minutes.

"wait" : 
	DESCRIPTION :
		Attend la fin d'une tâche et renvoie le code de
		retour. Attend la fin du processus identifié par
		ID, qui peut être un ID de processus ou une
		spécification de tâche, et renvoie son code de
		retour.  Si ID n'est pas donné, la commande attend
		la fin de tous les processus actifs en cours et le
		code de retour est zéro.  Si ID est une spécification
		de tâche, la commande attend tous les processus
		dans le pipeline de la tâche.

"wait4path" : 
	NAME :
		wait4path – wait for given path to show up in the
		namespace
	DESCRIPTION :
		The wait4path program simply checks to see if the
		given path exists, and if so, it exits.  Otherwise,
		it sleeps until the mount table is updated and
		checks again.  The program will loop indefinitely
		until the path shows up in the file system namespace.

"wall" : 
	NAME :
		wall – write a message to users
	DESCRIPTION :
		The wall utility displays the contents of file or,
		by default, its standard input, on the terminals
		of all currently logged in users.

"wasm-ld" : 
	La commande "wasm-ld" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help wasm-ld", "info wasm-ld" ou "wasm-ld" pour essayer d'obtenir davantage d'informations.

"wc" : 
	NAME :
		wc – word, line, character, and byte count
	DESCRIPTION :
		The wc utility displays the number of lines, words,
		and bytes contained in each input file, or standard
		input (if no file is specified) to the standard
		output.  A line is defined as a string of characters
		delimited by a ⟨newline⟩ character.  Characters
		beyond the final ⟨newline⟩ character will not be
		included in the line count.

"wdutil" : 
	NAME :
		wdutil – Wireless Diagnostics command line utility.
	DESCRIPTION :
		wdutil provides functionality of the Wireless
		Diagnostics application in command line form.

"wfsctl" : 
	NAME :
		wfsctl – WebDAV File Sharing control utility

"what" : 
	NAME :
		what – show what versions of object modules were
		used to construct a file
	DESCRIPTION :
		The what utility searches each specified file for
		sequences of the form “@(#)” as inserted by the
		SCCS source code control system.  It prints the
		remainder of the string following this marker, up
		to a NUL character, newline, double quote, ‘>’
		character, or backslash.

"whatis" : 
	NAME :
		man, apropos, whatis – display online manual
		documentation pages
	DESCRIPTION :
		The man utility finds and displays online manual
		documentation pages.  If mansect is provided, man
		restricts the search to the specific section of the
		manual.

"wheel3" : 
	La commande "wheel3" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help wheel3", "info wheel3" ou "wheel3" pour essayer d'obtenir davantage d'informations.

"wheel3.11" : 
	La commande "wheel3.11" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help wheel3.11", "info wheel3.11" ou "wheel3.11" pour essayer d'obtenir davantage d'informations.

"whereis" : 
	NAME :
		whereis – locate programs
	DESCRIPTION :
		The whereis utility checks the standard binary, and
		manual page directories for the specified programs,
		printing out the paths of any it finds.  The supplied
		program names are first stripped of leading path
		name components and any single trailing extension
		added by gzip(1), compress(1), or bzip2(1).

"which" : 
	NAME :
		which – locate a program file in the user's path
	DESCRIPTION :
		The which utility takes a list of command names and
		searches the path for each executable file that
		would be run had these commands actually been
		invoked.

"while" : 
	La commande "while" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help while", "info while" ou "while" pour essayer d'obtenir davantage d'informations.

"who" : 
	NAME :
		who – display who is on the system
	DESCRIPTION :
		The who utility displays information about currently
		logged in users.  By default, this includes the
		login name, tty name, date and time of login and
		remote hostname if not local.

"whoami" : 
	NAME :
		whoami – display effective user id
	DESCRIPTION :
		The whoami utility has been obsoleted by the id(1)
		utility, and is equivalent to “id -un”.  The command
		“id -p” is suggested for normal interactive use.

"whois" : 
	NAME :
		whois – Internet domain name and network number
		directory service
	DESCRIPTION :
		The whois utility looks up records in the databases
		maintained by several Network Information Centers
		(NICs).

"WirelessRadioManagerd" : 
	NAME :
		WirelessRadioManagerd – Wireless Radio Manager
		daemon
	DESCRIPTION :
		WirelessRadioManagerd manages multiple radio
		coexistence policy. WirelessRadioManagerd should
		not be invoked directly.

"wish" : 
	NAME :
		wish - Simple windowing shell
	DESCRIPTION :
		Wish is a simple program consisting of the Tcl
		command language, the Tk toolkit, and a main program
		that reads commands from standard input or from a
		file.  It creates a main window and then processes
		Tcl commands. If wish is invoked with arguments,
		then the first few arguments, ?-encoding name?
		?fileName? specify the name of a script file, and,
		optionally, the encoding of the text data stored
		in that script file. A value for fileName is
		recognized if the appropriate argument does not
		start with “-”.

"wish8.5" : 
	NAME :
		wish - Simple windowing shell
	DESCRIPTION :
		Wish is a simple program consisting of the Tcl
		command language, the Tk toolkit, and a main program
		that reads commands from standard input or from a
		file.  It creates a main window and then processes
		Tcl commands. If wish is invoked with arguments,
		then the first few arguments, ?-encoding name?
		?fileName? specify the name of a script file, and,
		optionally, the encoding of the text data stored
		in that script file. A value for fileName is
		recognized if the appropriate argument does not
		start with “-”.

"write" : 
	NAME :
		write – send a message to another user
	DESCRIPTION :
		The write utility allows you to communicate with
		other users, by copying lines from your terminal
		to theirs.

"wrjpgcom" : 
	NAME :
		wrjpgcom - insert text comments into a JPEG file
	DESCRIPTION :
		wrjpgcom reads the named JPEG/JFIF file, or the
		standard input if no file is named, and generates
		a new JPEG/JFIF file on standard output. A comment
		block is added to the file.

"wsgen" : 
	La commande "wsgen" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help wsgen", "info wsgen" ou "wsgen" pour essayer d'obtenir davantage d'informations.

"wsimport" : 
	La commande "wsimport" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help wsimport", "info wsimport" ou "wsimport" pour essayer d'obtenir davantage d'informations.

"xar" : 
	NAME :
		xar - eXtensible ARchiver
	DESCRIPTION :
		The XAR project aims to provide an easily extensible
		archive format. Important design decisions include
		an easily extensible XML table of contents (TOC)
		for random access to archived files, storing the
		TOC at the beginning of the archive to allow for
		efficient handling of streamed archives, the ability
		to handle files of arbitrarily large sizes, the
		ability to choose independent encodings for individual
		files in the archive, the ability to store checksums
		for individual files in both compressed and
		uncompressed form, and the ability to query the
		table of content's rich meta-data.

"xargs" : 
	NAME :
		xargs – construct argument list(s) and execute
		utility
	DESCRIPTION :
		The xargs utility reads space, tab, newline and
		end-of-file delimited strings from the standard
		input and executes utility with the strings as
		arguments.

"xartutil" : 
	La commande "xartutil" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xartutil", "info xartutil" ou "xartutil" pour essayer d'obtenir davantage d'informations.

"xattr" : 
	NAME :
		xattr – display and manipulate extended attributes
	DESCRIPTION :
		The xattr command can be used to display, modify
		or remove the extended attributes of one or more
		files, including directories and symbolic links.
		Extended attributes are arbitrary metadata stored
		with a file, but separate from the filesystem
		attributes (such as modification time or file size).
		The metadata is often a null-terminated UTF-8 string,
		but can also be arbitrary binary data.

"xcdebug" : 
	La commande "xcdebug" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xcdebug", "info xcdebug" ou "xcdebug" pour essayer d'obtenir davantage d'informations.

"xcode-select" : 
	NAME :
		xcode-select - Manages the active developer directory
		for Xcode and BSD tools.
	DESCRIPTION :
		xcode-select controls the location of the developer
		directory used by xcrun(1), xcodebuild(1), cc(1),
		and other Xcode and BSD development tools. This
		also controls the locations that are searched for
		by man(1) for developer tool manpages.

"xcodebuild" : 
	La commande "xcodebuild" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xcodebuild", "info xcodebuild" ou "xcodebuild" pour essayer d'obtenir davantage d'informations.

"xcrun" : 
	NAME :
		xcrun - Run or locate development tools and properties.
	DESCRIPTION :
		xcrun provides a means to locate or invoke developer
		tools from the command-line, without requiring users
		to modify Makefiles or otherwise take inconvenient
		measures to support multiple Xcode tool chains.

"xcscontrol" : 
	La commande "xcscontrol" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xcscontrol", "info xcscontrol" ou "xcscontrol" pour essayer d'obtenir davantage d'informations.

"xcsdiagnose" : 
	La commande "xcsdiagnose" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xcsdiagnose", "info xcsdiagnose" ou "xcsdiagnose" pour essayer d'obtenir davantage d'informations.

"xctrace" : 
	La commande "xctrace" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xctrace", "info xctrace" ou "xctrace" pour essayer d'obtenir davantage d'informations.

"xed" : 
	La commande "xed" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xed", "info xed" ou "xed" pour essayer d'obtenir davantage d'informations.

"xgettext" : 
	NAME :
		xgettext - extract gettext strings from source
	DESCRIPTION :
		Extract translatable strings from given input files.

"xgettext.pl" : 
	NAME :
		xgettext.pl - Extract translatable strings from
		source
	DESCRIPTION :
		This program extracts translatable strings from
		given input files, or from STDIN if none are given.

"xgettext5.30.pl" : 
	La commande "xgettext5.30.pl" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xgettext5.30.pl", "info xgettext5.30.pl" ou "xgettext5.30.pl" pour essayer d'obtenir davantage d'informations.

"xip" : 
	NAME :
		xip – Create or expand a secure archive for secure
		distribution.
	DESCRIPTION :
		The xip tool is used to create a digitally signed
		archive. As of macOS Sierra, only archives that are
		signed by Apple are trusted, and the format is
		deprecated for third party use.

"xjc" : 
	La commande "xjc" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xjc", "info xjc" ou "xjc" pour essayer d'obtenir davantage d'informations.

"xml2-config" : 
	NAME :
		xml2-config - script to get information about the
		installed version of GNOME-XML
	DESCRIPTION :
		xml2-config is a tool that is used to determine the
		compile and linker flags that should be used to
		compile and link programs that use GNOME- XML.

"xml2man" : 
	NAME :
		xml2man – MPGL to mdoc (man page) translator

"xmlcatalog" : 
	NAME :
		xmlcatalog - Command line tool to parse and manipulate
		XML or SGML catalog files.  --no-super-update | [-v
		| --verbose]] {CATALOGFILE} {ENTITIES...}
	DESCRIPTION :
		xmlcatalog is a command line application allowing
		users to monitor and manipulate XML and SGML catalogs.
		It is included in libxml(3).

"xmllint" : 
	NAME :
		xmllint - command line XML tool
	DESCRIPTION :
		The xmllint program parses one or more XML files,
		specified on the command line as XML-FILE (or the
		standard input if the filename provided is - ). It
		prints various types of output, depending upon the
		options selected. It is useful for detecting errors
		both in XML code and in the XML parser itself.

"xpath" : 
	NAME :
		xpath - a script to query XPath statements in XML
		documents.
	DESCRIPTION :
		xpath uses the XML::XPath perl module to make XPath
		queries to any XML document.  The XML::XPath module
		aims to comply exactly to the XPath specification
		at "http://www.w3.org/TR/xpath" and yet allows
		extensions to be added in the form of functions.

"xpath5.30" : 
	NAME :
		xpath - a script to query XPath statements in XML
		documents.
	DESCRIPTION :
		xpath uses the XML::XPath perl module to make XPath
		queries to any XML document.  The XML::XPath module
		aims to comply exactly to the XPath specification
		at "http://www.w3.org/TR/xpath" and yet allows
		extensions to be added in the form of functions.

"xsanctl" : 
	NAME :
		xsanctl – Xsan file system control utility
	DESCRIPTION :
		xsanctl provides a basic control facility for the
		Xsan file system.  It operates by sending commands
		to the Xsan file system management daemon xsand.
		xsanctl must be run by the superuser.

"xscertadmin" : 
	NAME :
		xscertadmin -- process Certificate Revocation Lists
		in an OD environment
	DESCRIPTION :
		Manage Certificate Revocation Lists (CRLs)  in an
		Open Directory Environment.

"xslt-config" : 
	La commande "xslt-config" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help xslt-config", "info xslt-config" ou "xslt-config" pour essayer d'obtenir davantage d'informations.

"xsltproc" : 
	NAME :
		xsltproc - command line XSLT processor  --stringparam
		PARAMNAME PARAMVALUE  | --nonet | --path "PATH(S)"
		| --load-trace | --catalogs | --xinclude |
	DESCRIPTION :
		xsltproc is a command line tool for applying XSLT
		stylesheets to XML documents. It is part of libxslt(3),
		the XSLT C library for GNOME. While it was developed
		as part of the GNOME project, it can operate
		independently of the GNOME desktop.

"xsubpp" : 
	NAME :
		xsubpp - compiler to convert Perl XS code into C
		code
	DESCRIPTION :
		This compiler is typically run by the makefiles
		created by ExtUtils::MakeMaker or by Module::Build
		or other Perl module build tools.

"xsubpp5.30" : 
	NAME :
		xsubpp - compiler to convert Perl XS code into C
		code
	DESCRIPTION :
		This compiler is typically run by the makefiles
		created by ExtUtils::MakeMaker or by Module::Build
		or other Perl module build tools.

"xsubpp5.34" : 
	NAME :
		xsubpp - compiler to convert Perl XS code into C
		code
	DESCRIPTION :
		This compiler is typically run by the makefiles
		created by ExtUtils::MakeMaker or by Module::Build
		or other Perl module build tools.

"xxd" : 
	NAME :
		xxd - make a hexdump or do the reverse.
	DESCRIPTION :
		xxd creates a hex dump of a given file or standard
		input.  It can also convert a hex dump back to its
		original binary form.  Like uuencode(1) and uudecode(1)
		it allows the transmission of binary data in a
		`mail- safe' ASCII representation, but has the
		advantage of decoding to standard output.  Moreover,
		it can be used to perform binary file patching.

"xz" : 
	NAME :
		xz, unxz, xzcat, lzma, unlzma, lzcat - Compress or
		decompress .xz and .lzma files
	DESCRIPTION :
		xz is a general-purpose data compression tool with
		command line syntax similar to gzip(1) and bzip2(1).
		The native file format is the .xz format, but the
		legacy .lzma format used by LZMA Utils and raw
		compressed streams with no container format headers
		are also supported. In addition, decompression of
		the .lz format used by lzip is supported.

"xzcat" : 
	NAME :
		xz, unxz, xzcat, lzma, unlzma, lzcat - Compress or
		decompress .xz and .lzma files
	DESCRIPTION :
		xz is a general-purpose data compression tool with
		command line syntax similar to gzip(1) and bzip2(1).
		The native file format is the .xz format, but the
		legacy .lzma format used by LZMA Utils and raw
		compressed streams with no container format headers
		are also supported. In addition, decompression of
		the .lz format used by lzip is supported.

"xzcmp" : 
	NAME :
		xzcmp, xzdiff, lzcmp, lzdiff - compare compressed
		files
	DESCRIPTION :
		xzcmp and xzdiff invoke cmp(1) or diff(1) on files
		compressed with xz(1), lzma(1), gzip(1), bzip2(1),
		lzop(1), or zstd(1).  All options specified are
		passed directly to cmp(1) or diff(1).  If only one
		file is specified, then the files compared are file1
		(which must have a suffix of a supported compression
		format) and file1 from which the compression format
		suffix has been stripped.  If two files are specified,
		then they are uncompressed if necessary and fed to
		cmp(1) or diff(1).  The exit status from cmp(1) or
		diff(1) is preserved unless a decompression error
		occurs; then exit status is 2.

"xzdec" : 
	NAME :
		xzdec, lzmadec - Small .xz and .lzma decompressors
	DESCRIPTION :
		xzdec is a liblzma-based decompression-only tool
		for .xz (and only .xz) files.  xzdec is intended
		to work as a drop-in replacement for xz(1) in the
		most common situations where a script has been
		written to use xz --decompress --stdout (and possibly
		a few other commonly used options) to decompress
		.xz files.  lzmadec is identical to xzdec except
		that lzmadec supports .lzma files instead of .xz
		files.

"xzdiff" : 
	NAME :
		xzcmp, xzdiff, lzcmp, lzdiff - compare compressed
		files
	DESCRIPTION :
		xzcmp and xzdiff invoke cmp(1) or diff(1) on files
		compressed with xz(1), lzma(1), gzip(1), bzip2(1),
		lzop(1), or zstd(1).  All options specified are
		passed directly to cmp(1) or diff(1).  If only one
		file is specified, then the files compared are file1
		(which must have a suffix of a supported compression
		format) and file1 from which the compression format
		suffix has been stripped.  If two files are specified,
		then they are uncompressed if necessary and fed to
		cmp(1) or diff(1).  The exit status from cmp(1) or
		diff(1) is preserved unless a decompression error
		occurs; then exit status is 2.

"xzegrep" : 
	NAME :
		xzgrep - search compressed files for a regular
		expression
	DESCRIPTION :
		xzgrep invokes grep(1) on files which may be either
		uncompressed or compressed with xz(1), lzma(1),
		gzip(1), bzip2(1), lzop(1), or zstd(1). All options
		specified are passed directly to grep(1).

"xzfgrep" : 
	NAME :
		xzgrep - search compressed files for a regular
		expression
	DESCRIPTION :
		xzgrep invokes grep(1) on files which may be either
		uncompressed or compressed with xz(1), lzma(1),
		gzip(1), bzip2(1), lzop(1), or zstd(1). All options
		specified are passed directly to grep(1).

"xzgrep" : 
	NAME :
		xzgrep - search compressed files for a regular
		expression
	DESCRIPTION :
		xzgrep invokes grep(1) on files which may be either
		uncompressed or compressed with xz(1), lzma(1),
		gzip(1), bzip2(1), lzop(1), or zstd(1). All options
		specified are passed directly to grep(1).

"xzless" : 
	NAME :
		xzless, lzless - view xz or lzma compressed (text)
		files
	DESCRIPTION :
		xzless is a filter that displays text from compressed
		files to a terminal.  It works on files compressed
		with xz(1) or lzma(1).  If no files are given,
		xzless reads from standard input.

"xzmore" : 
	NAME :
		xzmore, lzmore - view xz or lzma compressed (text)
		files
	DESCRIPTION :
		xzmore is a filter which allows examination of xz(1)
		or lzma(1) compressed text files one screenful at
		a time on a soft-copy terminal.

"yaa" : 
	NAME :
		aa - Manipulate Apple Archives
	DESCRIPTION :
		aa creates and manipulates Apple Archives

"yacc" : 
	NAME :
		yacc – parser generator
	DESCRIPTION :
		On Darwin, yacc is a wrapper for bison(1), equivalent
		to bison -y.

"yaml-bench" : 
	La commande "yaml-bench" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yaml-bench", "info yaml-bench" ou "yaml-bench" pour essayer d'obtenir davantage d'informations.

"yaml2obj" : 
	La commande "yaml2obj" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yaml2obj", "info yaml2obj" ou "yaml2obj" pour essayer d'obtenir davantage d'informations.

"yamlpp-events" : 
	La commande "yamlpp-events" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-events", "info yamlpp-events" ou "yamlpp-events" pour essayer d'obtenir davantage d'informations.

"yamlpp-events5.30" : 
	La commande "yamlpp-events5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-events5.30", "info yamlpp-events5.30" ou "yamlpp-events5.30" pour essayer d'obtenir davantage d'informations.

"yamlpp-highlight" : 
	La commande "yamlpp-highlight" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-highlight", "info yamlpp-highlight" ou "yamlpp-highlight" pour essayer d'obtenir davantage d'informations.

"yamlpp-highlight5.30" : 
	La commande "yamlpp-highlight5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-highlight5.30", "info yamlpp-highlight5.30" ou "yamlpp-highlight5.30" pour essayer d'obtenir davantage d'informations.

"yamlpp-load" : 
	La commande "yamlpp-load" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-load", "info yamlpp-load" ou "yamlpp-load" pour essayer d'obtenir davantage d'informations.

"yamlpp-load-dump" : 
	La commande "yamlpp-load-dump" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-load-dump", "info yamlpp-load-dump" ou "yamlpp-load-dump" pour essayer d'obtenir davantage d'informations.

"yamlpp-load-dump5.30" : 
	La commande "yamlpp-load-dump5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-load-dump5.30", "info yamlpp-load-dump5.30" ou "yamlpp-load-dump5.30" pour essayer d'obtenir davantage d'informations.

"yamlpp-load5.30" : 
	La commande "yamlpp-load5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-load5.30", "info yamlpp-load5.30" ou "yamlpp-load5.30" pour essayer d'obtenir davantage d'informations.

"yamlpp-parse-emit" : 
	La commande "yamlpp-parse-emit" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-parse-emit", "info yamlpp-parse-emit" ou "yamlpp-parse-emit" pour essayer d'obtenir davantage d'informations.

"yamlpp-parse-emit5.30" : 
	La commande "yamlpp-parse-emit5.30" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help yamlpp-parse-emit5.30", "info yamlpp-parse-emit5.30" ou "yamlpp-parse-emit5.30" pour essayer d'obtenir davantage d'informations.

"yapp" : 
	NAME :
		yapp - A perl frontend to the Parse::Yapp module
	DESCRIPTION :
		yapp is a frontend to the Parse::Yapp module, which
		lets you compile Parse::Yapp grammar input files
		into Perl LALR(1) OO parser modules.

"yapp5.30" : 
	NAME :
		yapp - A perl frontend to the Parse::Yapp module
	DESCRIPTION :
		yapp is a frontend to the Parse::Yapp module, which
		lets you compile Parse::Yapp grammar input files
		into Perl LALR(1) OO parser modules.

"yes" : 
	NAME :
		yes – be repetitively affirmative
	DESCRIPTION :
		The yes utility outputs expletive, or, by default,
		“y”, forever.

"z3" : 
	La commande "z3" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help z3", "info z3" ou "z3" pour essayer d'obtenir davantage d'informations.

"zcat" : 
	NAME :
		gzip, gunzip, zcat – compression/decompression tool
		using Lempel-Ziv coding (LZ77)
	DESCRIPTION :
		The gzip program compresses and decompresses files
		using Lempel-Ziv coding (LZ77).  If no files are
		specified, gzip will compress from standard input,
		or decompress to standard output.  When in compression
		mode, each file will be replaced with another file
		with the suffix, set by the -S suffix option, added,
		if possible.

"zcmp" : 
	NAME :
		zcmp, zdiff – compare compressed files
	DESCRIPTION :
		zcmp and zdiff are filters that invoke cmp(1) or
		diff(1) respectively to compare compressed files.
		Any options that are specified are passed to cmp(1)
		or diff(1).

"zdiff" : 
	NAME :
		zcmp, zdiff – compare compressed files
	DESCRIPTION :
		zcmp and zdiff are filters that invoke cmp(1) or
		diff(1) respectively to compare compressed files.
		Any options that are specified are passed to cmp(1)
		or diff(1).

"zdump" : 
	NAME :
		zdump – timezone dumper
	DESCRIPTION :
		The zdump utility prints the current time in each
		zonename named on the command line.

"zegrep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"zfgrep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"zforce" : 
	NAME :
		zforce – force gzip files to have a .gz suffix
	DESCRIPTION :
		The zforce utility renames gzip(1) files to have a
		‘.gz’ suffix, so that gzip(1) will not compress
		them twice.  This can be useful if file names were
		truncated during a file transfer.  Files that have
		an existing ‘.gz’, ‘-gz’, ‘_gz’, ‘.tgz’ or ‘.taz’
		suffix, or that have not been compressed by gzip(1),
		are ignored.

"zgrep" : 
	NAME :
		grep, egrep, fgrep, rgrep, bzgrep, bzegrep, bzfgrep,
		zgrep, zegrep, zfgrep – file pattern searcher
	DESCRIPTION :
		The grep utility searches any given input files,
		selecting lines that match one or more patterns.
		By default, a pattern matches an input line if the
		regular expression (RE) in the pattern matches the
		input line without its trailing newline.  An empty
		expression matches every line. Each input line that
		matches at least one of the patterns is written to
		the standard output.

"zic" : 
	NAME :
		zic – timezone compiler
	DESCRIPTION :
		The zic utility reads text from the file(s) named
		on the command line and creates the time conversion
		information files specified in this input. If a
		filename is -, the standard input is read.

"zip" : 
	NAME :
		zip - package and compress (archive) files
	DESCRIPTION :
		zip is a compression and file packaging utility for
		Unix, VMS, MSDOS, OS/2, Windows 9x/NT/XP, Minix,
		Atari, Macintosh, Amiga, and Acorn RISC OS.  It is
		analogous to a combination of the Unix commands
		tar(1) and compress(1) and is compatible with PKZIP
		(Phil Katz's ZIP for MSDOS systems).

"zipcloak" : 
	NAME :
		zipcloak - encrypt entries in a zipfile
	DESCRIPTION :
		zipcloak encrypts all unencrypted entries in the
		zipfile.  This is the default action.

"zipdetails" : 
	NAME :
		zipdetails - display the internal structure of zip
		files
	DESCRIPTION :
		Zipdetails displays information about the internal
		record structure of the zip file. It is not concerned
		with displaying any details of the compressed data
		stored in the zip file.

"zipdetails5.30" : 
	NAME :
		zipdetails - display the internal structure of zip
		files
	DESCRIPTION :
		Zipdetails displays information about the internal
		record structure of the zip file. It is not concerned
		with displaying any details of the compressed data
		stored in the zip file.

"zipdetails5.34" : 
	NAME :
		zipdetails - display the internal structure of zip
		files
	DESCRIPTION :
		Zipdetails displays information about the internal
		record structure of zip files. It is not concerned
		with displaying any details of the compressed data
		stored in the zip file.

"zipgrep" : 
	NAME :
		zipgrep - search files in a ZIP archive for lines
		matching a pattern
	DESCRIPTION :
		zipgrep will search files within a ZIP archive for
		lines matching the given string or pattern.  zipgrep
		is a shell script and requires egrep(1) and unzip(1L)
		to function.  Its output is identical to that of
		egrep(1).

"zipinfo" : 
	NAME :
		zipinfo - list detailed information about a ZIP
		archive
	DESCRIPTION :
		zipinfo lists technical information about files in
		a ZIP archive, most commonly found on MS-DOS systems.
		Such information includes file access permissions,
		encryption status, type of compression, version and
		operating system or file system of compressing
		program, and the like. The default behavior (with
		no options) is to list single-line entries for each
		file in the archive, with header and trailer lines
		providing summary information for the entire archive.
		The format is a cross between Unix ``ls -l'' and
		``unzip -v'' output.  See DETAILED (under Unix, a
		link to it); on some systems, however, zipinfo
		support

"zipnote" : 
	NAME :
		zipnote - write the comments in zipfile to stdout,
		edit comments and rename files in zipfile
	DESCRIPTION :
		zipnote writes the comments in a zipfile to stdout.
		This is the default mode.  A second mode allows
		updating the comments in a zipfile as well as allows
		changing the names of the files in the zipfile.
		These modes are described below.

"zipsplit" : 
	NAME :
		zipsplit - split a zipfile into smaller zipfiles
	DESCRIPTION :
		zipsplit reads a zipfile and splits it into smaller
		zipfiles.

"zless" : 
	NAME :
		zmore, zless – view compressed files
	DESCRIPTION :
		zmore is a filter that allows the viewing of files
		compressed with Lempel-Ziv encoding.  Such files
		generally have a “Z” or “gz” extension (both the
		compress(1) and gzip(1) formats are supported).
		Any flags that are specified are passed to the
		user's preferred PAGER (which is /usr/bin/more by
		default).

"zlib_decompress" : 
	NAME :
		zlib_decompress - decompress mysqlpump ZLIB-compressed
		output
	DESCRIPTION :
		The zlib_decompress utility decompresses mysqlpump
		output that was created using ZLIB compression.

"zmore" : 
	NAME :
		zmore, zless – view compressed files
	DESCRIPTION :
		zmore is a filter that allows the viewing of files
		compressed with Lempel-Ziv encoding.  Such files
		generally have a “Z” or “gz” extension (both the
		compress(1) and gzip(1) formats are supported).
		Any flags that are specified are passed to the
		user's preferred PAGER (which is /usr/bin/more by
		default).

"znew" : 
	NAME :
		znew – convert compressed files to gzipped files
	DESCRIPTION :
		The znew utility uncompresses files compressed by
		compress(1) and recompresses them with gzip(1).

"zprint" : 
	NAME :
		zprint – show information about kernel zones
	DESCRIPTION :
		zprint displays data about Mach zones (allocation
		buckets).  By default, zprint will print out
		information about all Mach zones.  If the optional
		name is specified, zprint will print information
		about each zone for which name is a substring of
		the zone's name.

"zsh" : 
	NAME :
		zsh - the Z shell
	DESCRIPTION :
		Zsh is a UNIX command interpreter (shell) usable
		as an interactive login shell and as a shell script
		command processor.  Of the standard shells, zsh
		most closely resembles ksh but includes many
		enhancements. It does not provide compatibility
		with POSIX or other shells in its default operating
		mode:  see the section `Compatibility' below.

"zstd" : 
	NAME :
		zstd - zstd, zstdmt, unzstd, zstdcat - Compress or
		decompress .zst files
	DESCRIPTION :
		zstd is a fast lossless compression algorithm and
		data compression tool, with command line syntax
		similar to gzip(1) and xz(1). It is based on the
		LZ77 family, with further FSE & huff0 entropy stages.
		zstd offers highly configurable compression speed,
		from fast modes at > 200 MB/s per core, to strong
		modes with excellent compression ratios. It also
		features a very fast decoder, with speeds > 500
		MB/s per core.

"zstdcat" : 
	NAME :
		zstd - zstd, zstdmt, unzstd, zstdcat - Compress or
		decompress .zst files
	DESCRIPTION :
		zstd is a fast lossless compression algorithm and
		data compression tool, with command line syntax
		similar to gzip(1) and xz(1). It is based on the
		LZ77 family, with further FSE & huff0 entropy stages.
		zstd offers highly configurable compression speed,
		from fast modes at > 200 MB/s per core, to strong
		modes with excellent compression ratios. It also
		features a very fast decoder, with speeds > 500
		MB/s per core.

"zstdgrep" : 
	NAME :
		zstdgrep - print lines matching a pattern in
		zstandard-compressed files
	DESCRIPTION :
		zstdgrep runs grep(1) on files, or stdin if no files
		argument is given, after decompressing them with
		zstdcat(1).

"zstdless" : 
	NAME :
		zstdless - view zstandard-compressed files
	DESCRIPTION :
		zstdless runs less(1) on files or stdin, if no file
		argument is given, after decompressing them with
		zstdcat(1).

"zstdmt" : 
	La commande "zstdmt" de type "file" ne possède pas de page manuel dédiée.
	Tapez "help zstdmt", "info zstdmt" ou "zstdmt" pour essayer d'obtenir davantage d'informations.

"[" : 
	DESCRIPTION :


"[[" : 
	La commande "[[" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help [[", "info [[" ou "[[" pour essayer d'obtenir davantage d'informations.

"]]" : 
	La commande "]]" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help ]]", "info ]]" ou "]]" pour essayer d'obtenir davantage d'informations.

"{" : 
	La commande "{" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help {", "info {" ou "{" pour essayer d'obtenir davantage d'informations.

"}" : 
	La commande "}" de type "keyword" ne possède pas de page manuel dédiée.
	Tapez "help }", "info }" ou "}" pour essayer d'obtenir davantage d'informations.

